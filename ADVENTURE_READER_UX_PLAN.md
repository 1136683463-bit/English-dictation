# 冒险阅读页（AdventurePlayPage）一屏化布局方案

> 目标：整页内容一屏显示完，消灭反复上下滚动
> 状态：待确认（确认后再动代码）

---

## 一、现状诊断：为什么总要滚

当前页面是 5 段式纵向堆叠，整页高度轻松超过 1200px：

| 区块 | 约占高度 | 问题 |
|------|---------|------|
| 标题区（kicker + 大标题 + 中文摘要段落） | ~120px | 摘要占 2-3 行，信息密度低 |
| 阅读工具栏（进度/模式/上一句下一句/播放） | ~64px | 独立一行，sticky 但只覆盖自身 |
| 正文卡（提示行 + 逐句/对照正文） | 300–600px | 对照模式下翻倍；整页滚动 |
| 「接下来怎么做」选择区 + 自定义输入 | ~200px | 藏在正文最底部，读完才能看到 |
| 「这章想带走什么」词汇区 | ~260px | 完整第二屏，收词必须滚下去 |
| 路线图（右栏） | — | 随页面滚动，长路线时自身也滚 |

**核心矛盾**：阅读时想盯着正文，但选择区、词汇区、进度都要靠滚动才能看到/操作。

## 二、方案：100vh 固定框架 + 分区内部滚动

### A. 标题区 + 工具栏合并为一行顶栏（~56px）

- 左：返回按钮 + 标题（单行截断）+「Chapter 1 · AI 剧情」徽章
- 中文摘要不再独立成段：标题旁「ⓘ 摘要」，hover/点击弹出气泡
- 中：句进度（3/15）压缩成一条细进度条
- 右：模式切换（逐句/对照）、上一句/下一句（图标化）、连续播放、播放/暂停（合并为一个播放控件组）、**Aa 字号调节**、「⋯」菜单（收纳自动预加载开关）、路线图按钮

### B. 正文区 = 剩余视口高度的内部滚动容器

- 顶栏和动作坞之外的全部高度给正文；正文过长时**只有这一块内部滚动**，页面级滚动为 0
- 逐句/播放时的自动跟随改为容器内 `scrollIntoView`，句子永远滚到可视位置
- 阅读提示行（"点击句子打开译文…"）改为可关闭，记住选择不再显示
- 字号三档（A- / 标准 / A+），localStorage 记忆，配合对照模式也能塞进一屏

### C. 底部固定动作坞（关键改动）

「接下来怎么做」从正文末尾**上移为常驻底部动作坞**（**纵向布局**，约 150px）：

- **选择按钮纵向堆叠**（沿用现有 choice 行样式：label + description 单行截断），紧凑行高；超过 3 个选择时坞内自身滚动
- 自定义行动输入独立一行，放在选择列表下方
- 预加载状态徽章（已预加载 ✓ / 转圈中）保留在每个选择行右侧
- 状态提示（status / 预加载说明）改为动作坞顶部一行小字或轻量 toast

→ 用户读完即可直接选择，永远不需要为"继续故事"滚动。

### C2. 「本章生词」入口移到顶栏（不与选项混放）

- 顶栏右侧新增 **星标图标按钮 + 数量徽章**（「本章生词 · N」），点击唤起词汇抽屉
- 动作坞只保留选择与自定义行动，不再放词汇入口

### D. 词汇区改为抽屉（消除整个第二屏）

- vocab sheet → 右侧滑出抽屉，由**顶栏的「本章生词 · N」按钮**触发（见 C2）
- 抽屉内：词汇卡纵向紧凑排列（复用现有卡片），底部固定「保存所选词汇」
- 保存后自动关闭 + toast 反馈；正文中点词收藏的链路不变

### E. 路线图右栏

- 桌面端（≥1080px）保持常驻，但高度锁定为视口内、列表内部滚动
- 窄屏维持现有 toggle + 抽屉行为

### F. 断点策略

| 宽度 | 布局 |
|------|------|
| ≥1080px | 顶栏 + [正文内部滚动 | 路线图] + 底部动作坞，一屏完成 |
| 768–1079px | 路线图默认收起（按钮唤起），动作坞选择行保持纵向紧凑 |
| <768px | 保持纵向流（移动端不强制一屏），动作坞仍固定底部 |

## 三、不改什么

- 全部业务逻辑：TTS/逐句播放/连续播放、AI 续章与预加载、词典 popover、收藏生词、路线回看
- 逐句/对照两种阅读模式的交互本体，只动它们的容器
- 视觉风格（暖米底 + 橙主色），只做密度收紧

## 四、实施范围

| 文件 | 改动 |
|------|------|
| `src/pages/AdventurePlayPage.tsx` | header 合并、正文容器化、choices 移入动作坞、vocab 改抽屉、字号状态 |
| `src/styles.css` | 新增一屏框架布局（`height: 100vh` 减法 + `overflow` 分区），约 300 行；清理旧 sticky 规则 |
| 其余 | 无新依赖；`speechService` / `adventureService` 等不动 |

预估 ~400 行改动，风险集中在布局层，逻辑零改动。

## 五、验收标准

1. 1080p（1920×1080）及 900px 高的窗口下：标题、进度、正文、选择动作坞、生词入口全部一屏可见，页面级滚动为 0
2. 正文超长（或对照模式）时仅正文区内部滚动，选择坞永远可见
3. 收词全程不离开当前视口：顶栏生词按钮唤起抽屉 → 勾选 → 保存 → 关闭
4. 播放/逐句导航时句子自动滚动跟随在正文容器内正确工作
5. 现有功能（预加载、连续播放、路线回看、词典弹窗）无一丢失
6. 三个断点下均无横向滚动

---

**一个取舍需要你确认**：一屏化后正文的可视高度会缩小（约让出 130px 给顶栏+动作坞）。超长章节或对照模式下，正文需要内部滚动——这是"永远不用滚页面"和"正文一屏全显示"之间的必然取舍。如果你更希望正文也尽量不滚，可以搭配字号调节 + 紧凑密度来缓解，但无法保证极端情况下零滚动。
