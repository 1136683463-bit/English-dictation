# 冒险学习页面 · UI 设计方案（1:1 还原）

> 设计稿来源：Clipboard_Screenshot.png（2026-09-12）
> 高保真实现：`design/adventure-ui/index.html`（浏览器直接打开即可预览，含轻交互）
> 目标：像素级还原设计稿，并可直接映射到 `src/pages/AdventurePage.tsx` + `src/styles.css`。

## 1. 页面结构（自上而下）

| 区块 | 说明 |
| --- | --- |
| 左侧图标导航 | 76px 白底窄栏：品牌 Logo（橙色渐变圆角方块 + 声波图标）+ 8 个「图标+文字」导航项（首页/探索/学习/路线/收藏/社区/帮助/设置），底部下载按钮；激活项为浅橙底胶囊 + 橙色图标文字 |
| 英雄横幅 | 150px 圆角图卡，复用 `src/assets/adventure-hero.jpg`，左侧白色渐变蒙版保证文字可读；内容为 `ADVENTURE` 眉标 / `冒险学习` 大标题 / 副文案；右上角手写体 "Better You Every Day"（白色，-2° 旋转） |
| 继续阅读卡 | 白色圆角横条：54px 封面缩略图（左下角橙色 `A3` 角标）+ 标题 + 橙色进度条（46%）+ 灰色元信息；右侧橙色胶囊按钮「继续阅读 →」 |
| 新冒险区（2026-09-12 第二版截图） | 区块头（`NEW ROUTE` 眉标 + 「开始一段新冒险」+ 右侧「恢复默认 / 随机推荐」幽灵按钮）；路线卡**直接置于页面背景上**（无白色外层面板），5 列布局：沙漠古城/星际小邮差/海底秘境/老宅钟声/自定义冒险；图标为**圆形**（40px，语义色浅底 + 深前景）；「自定义冒险」选中态：橙色描边 + 浅橙底 + **标题保持深色墨蓝、描述为橙色**、橙色圆形实底星芒图标 |
| 表单区 | 三行内联布局（与路线卡同为扁平化，直接置于页面背景上）：阅读难度 chips（A1–C1，A2 橙色选中）→ 线索输入框（白底、右侧内嵌星芒图标）→ 灵感推荐 chips（点击回填输入框）；底栏左侧自动保存提示、右侧「开始冒险 +」主按钮 |
| 我的路线 | 区块头（`SAVED ROUTES` 眉标 + 「我的路线」+ 右侧「5 段 ›」）；3 列已存路线卡：缩略图 + 标题 + 章节元信息 + `A2` 等级标签 + 书签图标（收藏态为橙色实心）+ 相对时间 + 右箭头 |

## 2. 设计令牌（Design Tokens）

> **配色说明：全部沿用项目现有配色体系（`src/styles.css` `:root`），未引入新色。**

### 颜色
| Token | 值 | 对应项目变量 | 用途 |
| --- | --- | --- | --- |
| `--c-bg` | `#f4f6f8` | `--bg` | 页面冷灰背景 |
| `--c-surface` | `#ffffff` | `--surface` | 卡片/侧栏表面 |
| `--c-border` | `#e8edf1` | `--line` | 默认描边 |
| `--c-border-strong` | `#d7dfe6` | `--line-strong` | 输入框/按钮描边 |
| `--c-text` | `#1f2b3a` | `--ink` | 主文字（墨蓝黑） |
| `--c-text-2` | `#5f6b7a` | `--muted` | 次级文字 |
| `--c-text-3` | `#98a4b3` | muted 浅一档 | 弱文字/占位/箭头图标 |
| `--c-primary` | `#f26a1b` | `--accent` | 品牌橙（按钮/选中/眉标） |
| `--c-primary-deep` | `#d95a12` | `--accent-strong` | 主按钮 hover |
| `--c-primary-soft` | `#fff1e8` | `--accent-soft` | 侧栏激活底色 |
| `--c-primary-softer` | `#fff6ef` | accent-soft 浅一档 | 选中卡底色 |

模板图标五色（全部映射项目语义色，图标底/前景）：
- 蓝 `--blue-soft #eef3ff / --blue #3b6ef5`（沙漠古城·指南针）
- 红 `--danger-soft #fdeeee / --danger #d64545`（星际小邮差·邮件）
- 绿 `--green-soft #e9f8ef / #2f9e63`（海底秘境·波浪；前景为 `--secondary #48b878` 加深档，保证图标可读性）
- 琥珀 `--amber-soft #fdf3d8 / #d99e00`（老宅钟声·铃铛；前景为 `--amber #f5b301` 加深档）
- 橙 `--accent #f26a1b` 实底（自定义冒险·星芒，白图标）

### 圆角 / 阴影 / 间距
- 圆角：面板 `20px`、路线卡 `14px`、输入框 `10px`、按钮与 chips 全胶囊 `99px`（项目半径体系 12/16/24 的近似取值，落地时可对齐）
- 阴影：卡片 `0 1px 2px + 0 4px 14px rgba(31,43,58,.06)`（对应 `--shadow-sm/--shadow-md`）；主按钮 `rgba(242,106,27,.22)`（与 `.brand-icon` 一致）
- 间距基准 4px；主区内边距 `22px 28px`；卡片间隙 `12px`

### 字体
- 与项目字体栈一致：`Inter, "HarmonyOS Sans SC", "PingFang SC", ui-sans-serif, system-ui`
- 手写体（英雄区右上）：Snell Roundhand / Segoe Script，白色 + 轻投影，`-2°` 旋转
- 层级：页面标题 32px/800 → 区块标题 21px/800 → 卡片标题 14.5–16.5px/700 → 正文 13px → 辅助 11.5–12px → 眉标 10.5–11px/800 字距 2.5–3px

## 3. 交互与状态（已在实现中内置）

- 路线卡：hover 上浮 2px + 投影；点击切换选中（橙色描边 + 浅橙底 + 橙色文字/箭头）
- 难度 chips：单选，选中橙色实底白字
- 灵感 chips：点击回填线索输入框
- 书签图标：点击切换收藏态（灰色 ↔ 橙色实心）
- 输入框：focus 橙色描边 + 3px 橙色外圈
- 主按钮：hover 提亮、active `scale(0.97)` 触感反馈
- 可访问性：保留 `prefers-reduced-motion` 降级；导航/卡片均为可聚焦按钮；≤1080px 时路线卡降为 3 列、≤720px 降为 2 列并隐藏手写体

## 4. 落地到现有代码的映射建议

1. `App.tsx` 侧边栏如需与设计稿一致，将 navItems 替换为「首页/探索/学习/路线/收藏/社区/帮助/设置」图标导航（复用 `.side-item` 样式）。
2. `AdventurePage.tsx` 中现有 `adv-tone-*` 色调替换为本文档四色令牌；`自定义冒险` 卡对应 `Selection { kind: "custom" }` 的选中态。
3. 进度条宽度 = 当前章/总章比例（实现里示例为 46%）。
4. 封面缩略图与城市缩略图可先用内联 SVG（已内置），后续可替换为真实封面图。
