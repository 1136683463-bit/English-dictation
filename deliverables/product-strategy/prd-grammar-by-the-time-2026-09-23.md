# PRD · `by the time` 交付（B 档清单最后一项）

**批次**：第五十七批 · 2026-09-23
**主题**：补交上一份权威 B 档文档点名、但**从未落地**的顺位 2 课程
**状态**：已落地（L205 + 案件 #214 + 里程碑 + 全套守门通过）

---

## 一、一句话

`competitive-analysis-b-tier-closure-2026-09-21.md`（权威 B 档收口文档）在 §3.5 点名了 3 课，
其中 `had to` → 已交付 L188、`so...that ＋ such a` → 已交付 L193/L194，
**唯独顺位 2 的 `by the time` 从未落地**（全库实测 0 命中）。本批把它补齐。

---

## 二、为什么现在做这一课，而不是继续查缺陷

批五十三至五十六连续四批在补数据缺陷，每批都得出同一条结论：
「**清零只在守门覆盖的范围内成立**」。但**长期目标是完成 B 档课程**。

本批回到权威文档核对**目标本身的完成度**，而不是继续找缺陷：

| 文档点名（§3.5 步骤①）| 档位 | 现状 |
|---|---|---|
| `had to` | B＋ 顺位 1 | ✅ 已交付 L188 |
| **`by the time`** | **B 顺位 2** | ❌ **从未交付（本批补上）** |
| `so...that ＋ such a` | B 顺位 3 | ✅ 已交付 L193 / L194 |

**⇒ 这是「已登记但从未执行」的第二例幻影项**（第一例是批五十五的 `loud`/`careful`，
但方向相反：那项**其实做了**，这项**其实没做**）。

---

## 三、交付内容：`lesson-205-by-the-time`

### 3.1 完全按权威文档的规格实现

文档 §4.2 给了目标句候选、4 条跨源支撑、3 处带标记错。本批逐条落实：

| 项 | 文档规格 | 落地 |
|---|---|---|
| **targetSentence** | 首选 `By the time I got home, it was dark.` | ✅ 采用（纯过去、不涉 had，与 L178 形成干净切分）|
| **examples 进阶句** | 备选 1 `By the time we got to the station, the train had left.` | ✅ 采用（引向 L178 的 had）|
| **错 1** | `By the time you will wake up…` → 前半句别请 will | ✅ 落地（Cambridge 逐字 `Not: By the time you will wake up …`）|
| **错 2/3** | 该用 `had` 的地方写成一般过去 | ✅ 落地（`the train left` → `had left`）|
| **造词成本** | 文档算「0 个新词」| ✅ 实测确认：`by`(L52) `time`(L6) `got`(L108) `home`(L9) `was`(L10) `dark`(L48) 全部已教 |

### 3.2 三处切分（课程标准要求「每课要有明确的旧课接口」）

| 切分对象 | 本课怎么讲 |
|---|---|
| **L48（if 的老规矩）** | 同一条纪律的**第四次应用**：「前面那半句说的是『什么时候』，用平常的版本，不请 will 出场」|
| **L178（had ＋ 做过版）** | 「更早那件」穿 had 版（`the train had left`）——与首选句形成「要不要分先后」的对照 |
| **L188（had to 不得不）** | 形近义不同：`had to` 后面跟动作、`had` 后面跟做过版 |

### 3.3 案件 #214 `hunt-by-the-time`

4 处植错，2 处本课新知识点 ＋ 2 处旧课回流（`has` / `went`），与库内其它 205 课后段课的配案密度一致。

---

## 四、新守门与既有守门的实测反馈

新增一课触发了**整个测试套件的规模耦合**——这**不是缺陷，是正确的耦合**。逐条记录：

| 守门 | 反馈 | 处置 |
|---|---|---|
| **季节覆盖** | 「第 205 课不在任何季区间内——会被路径页静默过滤」 | season-28 区间 204 → 205，并补 hint 尾项 ✅ |
| **里程碑覆盖** | 「里程碑最大 afterLesson=204，但全库最大课号=205」 | 补 `can-do-m52` ✅ |
| **薄讲解守门** | guided[2] 的 explain 太薄且本课无素材可借 | 讲解加厚（说清「为什么用不上 By the time」）✅ |
| **可抄率 C 层** | 「5 题全是课内已出现的句子」 | 换 1 题为新句（`By the time she got to school, the door was open.`）✅ |
| **E0 选项不变量** | `spot` 的 `wrongToken`「will get」不在题干词块里 | 改单词 `will`（全库 204 处 `spot` **全是单词**，我这处是唯一例外）✅ |
| **拖拽基建** | practice 带干扰项数 1052 → 1048 | 补 4 道 `distractors` ✅ |
| **RV11 editOp 自洽性** | `left` → `had left` 标成 `replace`，字面推断是 `insert` | 改 `insert`（**批五十三建的闸抓到了我自己的错标**）✅ |
| **H1 对位校验** | `idx=24 token="two" original="have"` | 两处下标偏移修正 ✅ |
| **h4 / p1 / p3 / gq2 / r7 计数** | 写死的 213 案 / 204 课 | 全部 → 214 案 / 205 课（16 处 + 3 处）✅ |

**注**：所有这些守门**都是我或前批建的**，它们在我犯错时逐个报错——这正是它们该做的。

---

## 五、验收

| 项 | 结果 |
|---|---|
| `tsc --noEmit` | **0 错误** |
| `vite build` | ✓ 通过（2.80s）|
| 全量测试 | **2504 / 2506 通过**（237 文件）|
| `grammarLessons.test.ts` | **46 项全绿**（含本批新增的课内一致性 + variants 结构）|
| 课程 / 案件 | **204 → 205 课** / **213 → 214 案** |
| **走查**（jsdom 真实渲染）| ① 路径页显示「到家的时候」与「by the time」✓<br>② 课程页可打开、讲解段含 `By the time` ✓<br>③ **6 道 guided 全部可作答（6/6）** ✓ |

### 5.1 两条非我引入的失败（已核实）

| 失败 | 原因 |
|---|---|
| `kb4-arrange-keyboard` | 并发进程改了 `GrammarLessonPage.tsx` 的操作提示文案（`git status` 显示该测试与其断言目标均未被我修改）|
| `bo4-correction-observability` | 并发进程改了 `GrammarBoostPage.tsx`：把「你这句：」的**冒号删掉**了（`git diff` 逐字可见 `-<p>你这句：<em>` → `+<span>你这句</span>`），而 `bo4` 断言 `toContain("你这句：")`。**该文件于 19:09 被修改，我从本批开始（18:30）到结束从未触碰它。** |

---

## 六、B 档目标完成度（本批的实质进展）

### 6.1 权威文档点名的部分：**已 100% 完成**

| 来源 | 项数 | 状态 |
|---|---|---|
| §3.5 步骤①（3 课）| 3 | ✅ **全部交付**（L188 / L193-194 / **L205 本批**）|
| 批三十一登记的 10 项 B 档候选 | 10 | ✅ 文档自身已确认 100% 收口 |

### 6.2 仍开放的部分（诚实登记）

文档 §3 明确写过「**不能宣告完成所有 B 档**」，三条理由至今成立：

1. **历批的 B 档从来不是穷举**（文档原话）——本批也证实了：我在核对时又发现
   `by the time` 这一项「已评估但未交付」；
2. **§5 登记的未核实项仍有 2 项实质开放**：`such`（未做独立档位评估，虽已在 L194 用上）、
   `even though`（我方全库 0，与 `even if` 同页一个 H2）；
3. **文档建议的「同口径穷举」从未执行**——即「遍历 Cambridge 语法页索引 + BC 68 课全目 +
   中文侧两站 sitemap 做成对照表」。文档估计工作量是批三十一批普查的 **2–3 倍**。

**§1 的 9 项维持判决**（`several` / `even if` / `as if` / `as though` / `ought to` / `dare` /
`provided` / `in case` 等）本批复核**仍然全库 0 命中**，与文档判决一致，无需改动。

---

## 七、建议的下一步（按价值排序）

| 顺位 | 事项 | 说明 |
|---|---|---|
| **1** | **执行「同口径穷举」** | 这是文档 §3.5 步骤② 明确建议、且**从未做过**的事。它是宣告「完成所有 B 档」的**唯一前置条件**。产出是一份**可复跑的缺口清单**（对照表），而不是又一次「凭语感列举」 |
| 2 | `even though` 判档 | 与 `even if` 合并判（同页一个 H2，我方全库 0）|
| 3 | `such` 独立判档 | 它已在 L194 被用上，但未单独判「本义（`such + 不可数/复数`）」那一格 |
| 4 | 词汇线是否单独立项 | 仍**待产品负责人拍板**（含 `quietly` 缺口）|
