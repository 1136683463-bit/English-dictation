# 路线图 · 第四十批（`such a` + 三项修复）

- 日期：2026-09-21
- 交付：L194 / 案 #203 / season-28 扩至 194 / can-do-m41
- 性质：**质量批**——三项修复是主体

---

## 1. 交付清单

| 文件 | 变更 |
|---|---|
| `src/data/grammarLessons.ts` | +L194 `lesson-194-such-a`（193 → **194 课**）；**修 L89 的 4 处「a 位置说反」** |
| `src/services/huntService.ts` | **export `correctedSentenceOf`**（错词本例句修复的前置）|
| `src/pages/GrammarHuntPage.tsx` | 错词本例句改用 `correctedSentenceOf(activeCase)`（不再存题面错句）|
| `src/data/huntCases.ts` | +案 #203 `hunt-such-a`（202 → **203 案**）；**修 `hunt-call-mother` 的插入点**（`very is` → `is very`）|
| `src/data/grammarSeasons.ts` | `season-28` 扩至 `min: 182, max: 194`（**未新建 season-29**——≤3 课小季已顶格）|
| `src/pages/GrammarPathPage.tsx` | +`can-do-m41` |
| `src/edge/**`（13 文件）| 计数更新；**`pr1` 两条断言反转**（从「要求含红线词」改为「不得含」）；`gq1` 基线 543→546（附原因）|
| `deliverables/` | 本 PRD + 本路线图 + 瑞思/竞析两份研究报告 |

---

## 2. 验证证据

| 项 | 结果 |
|---|---|
| **全量测试** | **1562 / 1562 全绿**（133 文件）——**本会话首次全绿** |
| `tsc --noEmit` | 我的文件 **0 错误** |
| `vite build` | ✓ 通过（2.76s）|
| 独立核验 | **13 / 13** |
| 浏览器走查 | L194 首屏 ✓；路径页 `7 / 194 课` + 末三课含 L194 ✓；找错页 `3 / 203` + 第 28 季 13 案 + 案 203 解锁指向 ✓；L194 零术语 0 命中 ✓ |

---

## 3. 三项修复的量化结果

| 缺陷 | 修前 | 修后 |
|---|---|---|
| L89「a 站在…后面」说反 | **4 处**（一课内 4 错 1 对，自相矛盾）| **0** |
| 错词本例句是含错句 | **202 / 202 案** | **0** |
| 修正句含错序（`She very is glad`）| 1 | **0** |
| `pr1` 把缺陷当契约的断言 | 2 条（要求 plain 含术语）| 已反转为「不得含」|

---

## 4. 并发进程状态

本批结束时，**全量测试首次全绿**（1562/1562）——此前 28 处失败中，绝大部分已由并发进程自己修完。本批观察：

| 现象 | 说明 |
|---|---|
| 测试文件 130 → 133 | 仍在小幅增长 |
| `GRAMMAR_ERROR_TAG_PLAIN` 已零术语 | 并发进程完成了「2026-09-21 术语清理」，但**它自己的断言没跟上**（本批已代为反转）|
| `gq1` 基线漂移 543 → 546 | 我的数据增长所致（附原因更新）|

**建议**：并发进程的套件已可用（全绿），但**基线值需在每次数据增长后同步**——目前它用绝对数的地方仍有 3 处（`huntDuplicateCards` / `huntCardCjkInFront` / `distractorNotRealWord`），长期看应像 `clozeFewerThanFourOptionsRatio` 那样改用比例。

---

## 5. 携带项

| # | 项 | 优先级 | 说明 |
|---|---|---|---|
| 1 | ~~L89 的 a 位置说反~~ | **✅ 结案** | §3，全库归零 |
| 2 | ~~错词本例句存错句~~ | **✅ 结案** | §3，202/202 → 0 |
| 3 | **语义层守门缺失** | **⚠️ 高·新** | 零术语红线抓不到「说反了」。L89 那 4 处存活很久无人发现。**建议补「同课内不得出现互相矛盾表述」类检查** |
| 4 | **断言不得把缺陷当契约** | **⚠️ 中·新** | `pr1` 的「不少于 N 个缺陷」写法已修。登记为审查项：全库扫描同类写法 |
| 5 | **`its`（所属）独立课** | **中·新** | 核实：真缺口（正面用法为零，11 处全是 `It's` 缩写坑）。但与 `such a` 不同族，**待独立排期** |
| 6 | `several` 并入 L114 | 待办 | 与 `a few` 同格 |
| 7 | `a bit` | 登记 | 语域不搭（英式口语），待程度轴收口时考虑 |
| 8 | 词汇线是否单独立项（约 316 项 A0/A1 词缺席）| 待产品负责人决策 | 延续多批 |
| 9 | `try to` vs `try doing` | 登记 | 会是第 6 个「-ing 门卫」|
| 10 | `decide` / `hope` 已被 gate/runes 消费却从未教 | 观测项 | 延续 |
| 11 | Murphy 双册原件全失 | 延续 | 本批竞析确认仍不可得 |
| 12 | `deepDive` 术语豁免 | 待产品负责人决策 | 全字段遍历守门唯一豁免字段 |
| 13 | 并发套件基线用绝对数的 3 处 | 低 | 建议改比例 |
| 14 | 封面池消耗 | 延续 | 本批取 cover77 |

---

## 6. 下批建议

1. **优先：语义层守门**（§5 携带项 3）——本批最有价值的发现。L89 的错误能存活说明「事实正确性」完全靠人读，这在 194 课的规模上不可持续。可先做一条最小的：扫描同一课内对「位置/顺序/方向」的表述，检查是否互相矛盾。
2. **其次：`its` 独立课**（§5 携带项 5）——真缺口已核实（零正面用法），且它是**唯一的**「只被告知是错的、从未教过对的」词。
3. 教学内容候选：
   - `several` 并入 L114（补员，不是新课）
   - 词汇线立项决策
4. **「完成所有 B 档」的宣告判据**（延续）：本批又发现新缺口（`such a` 此前为零）。建议采用「**连续两次调研零新增**」作为收口判据。
