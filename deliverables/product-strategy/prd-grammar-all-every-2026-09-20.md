# PRD：第二十六批 · 全都（`all` / `every` · 2 课 L151–L152）

**日期**：2026-09-20 ｜ **类型**：产品需求规格书（PRD）｜ **主理人自撰**（两研究结论一致）
**上游**：批二十五路线图 §6 携带项 4（「中文『都』的第三格」）＋ 本批两研究（瑞思／竞析，2026-09-20）＋ **L148 深挖卡埋下的钩子**

| 版本 | 日期 | 说明 |
|---|---|---|
| v1 | 2026-09-20 | 初稿：**2 课**（L151–L152）＋2 案（#160–#161），开 season-26、m28；单拱「书桌 → 教室」；**档位 B−**（必造 1 词 `all`）；**摘钩子**（L148 已埋）；**轴 A 连押三批** |

---

## §1 批次定位与选题裁决

### 1.1 本批的决定性依据：L148 已经埋好钩子

批二十五交付的 L148（`both` 立岗）深挖卡末尾**逐字写着**：

> 「顺带记一句：这个 both 只管「两个」。**要是三个以上一起都好，英语有另一个词，今天先不碰。**」

**（主理人已逐字复核 `grammarLessons.ts` 中 L148 的 `deepDive.paragraphs[3]`，确认成立。）**

**→ 本批是去摘这个钩子，不是开新话题**——这是本批最强的「连续性」论证，也是两研究一致推荐它的核心原因。

### 1.2 两研究结论一致，课量取 2

| 研究 | 推荐 | 课量 |
|---|---|---|
| 瑞思 | **轴 B（`all`）** | **2 课**（`all` 立岗 → `every` 切开） |
| 竞析 | **轴 B（`all`）** | **2 课** |

**两研究独立得出一致结论，无分歧。** 竞析另给了一条强课程位证据：**Murphy 初级 U80 标题逐字 `80everyandall`**（题目本身就是「every and all」）＋ 中级 **U90 逐字 `90alleverywhole`**（与 U89 零间隔）。

**⚠️ 为什么只有 2 课（不设收口课）**：瑞思判——**`all` 没有 `neither` 那样的换词伙伴**。实测 `none`／`no one`／`nobody` **三词全零**，是独立缺口须另行评估；**故本批不做否定侧**，没有「两张脸」可收口。

### 1.3 轴 A 连押三批（竞析的方案经主理人否决）

竞析提出一个「`unless` ＋ `in case`」的 2 课方案，声称两课各 6 条对比卡全有跨源依据。**主理人复核后不采纳，理由一条**：

**该方案的 6 条卡里有 2 条是 ❌will 规则**（「unless 从句里不用 will」）。**实测该规则已在 7 课教过**（L47／L48／L49／L109／L142／L143／L144）——**若本批再用，是同一规矩的第八次应用**，正面违反「一课一增量」。

**→ 裁决：轴 A 继续押后（连押三批）。** 批二十七重新评估时须先回答：**去掉 ❌will 之后，`unless` 还剩几条对比卡？**

### 1.4 批次定位（诚实标注）

**档位 B−**：

| 项 | 本批 | 对照 |
|---|---|---|
| **新造词** | **1 个**：`all`（量词义真零） | 批二十五 2 个；批二十 3 个（历史最高） |
| **机制复用** | **2 处**（「带上 s」回流 L11／L148；「搭档用 are」回流 L7／L148） | 批二十五：1 处 |
| **须自建规则表述** | **1 处**（`all` 后面可以站 `the`） | — |
| 认读升格 | **1 处**：`every`（词已会，**用法**是新的——从 `every day` 到 `every student`） | — |
| 场景侧造词 | **0 个** | — |

> **对外口径**：写「**B−：上游双册双格（Murphy 初级 U80 ＋ 中级 U90）＋ 中文侧专文（含 `all` vs `both` 对举）＋ 必造 1 词**」。**不得**写「A 档」或「B 档」。

---

## §2 逐课规格

**L151 立岗（`all` 也站最前面）**

| 项 | 内容 |
|---|---|
| 课注 id | `lesson-151-all-three`；`number: 151`；episode「小美的一天 一百五十一」；scene `mansion`；cover `cover34` |
| title / grammarLabel | 「全都好」/「三个以上都 · all 也站最前面」 |
| targetSentence | `All the books are good.`（5 词 ✅；**cloze 友好，比 4 词版更优**) |
| 场景 | 同一张书桌——这回不是两本，是四本摊成一排 |
| 新知识点 | **只有一件**：`all` 管三个以上、也站最前面，**后面可以站 the** |
| oneLineRule | 「说「全都」：all 也站最前面，后面可以站 the——All the books are good（这几本全都好）。它管的是三个以上，一个都不落下。」 |
| 对比卡 6 条方向 | ① `All student is here.` ❌（**`student`**，plural——一群要带 s）② `All of books are good.` ❌（**`of`**，word_order——**all 后面直接接，中间不加 of**）③ `All the books is good.` ❌（**`is`**，sv_agreement——搭档用 are）④ 双正解（L148）：`Both books are good.` ✅ ⑤ 双正解：`All my books are new.` ✅ ⑥ 双正解（L11）：`I ate two sandwiches.` ✅ |
| 变体三态 | 肯定 `All the books are good.`／否定 `I don't like coffee.`（**借老句**——`all` 管肯定侧，否定侧本轮不做）／疑问 `Are all the books good?` |
| 复现题设计 | 第 1 题 target＋变体逐字题；第 2 题复现 L148；第 3 题复现 L11；第 4 题复现 L115 `I have a new bag.`（**余 5**） |
| 案件规划 | `hunt-all-the-books`（#160） |

**L152 切开（`every` 一个一个来）**

| 项 | 内容 |
|---|---|
| 课注 id | `lesson-152-every-student`；`number: 152`；episode「小美的一天 一百五十二」；scene `campus`；cover `cover35` |
| title / grammarLabel | 「每个都到了」/「差在哪儿 · 好多个一起／一个一个来」 |
| targetSentence | `Every student is here.`（4 词 ✅） |
| 场景 | 早读课前，教室里人还没坐齐；组长数了数——一个都不少 |
| 新知识点 | **只有一件**：`every` 后面只说一个（**与 `all` 的直接对照**） |
| oneLineRule | 「说「一个一个都」：every 后面只说一个——Every student is here（每个学生都到了）。上一课那个 all 后面是好几个，这个后面只站一个。」 |
| 对比卡 6 条方向 | ① `Every students are here.` ❌（**`students`**，plural——不加 s）② `Every are here.` ❌（**`Every`**，fragment——不能自己单站）③ `All student is here.` ❌（**`All`**，word_order——**本轴最关键的一条**：后面是单的，最前面要用 Every）④ 双正解（L151）：`All the students are here.` ✅ ⑤ 双正解（L9）：`I go to school every day.` ✅ ⑥ 双正解（L148）：`Both books are good.` ✅ |
| 变体三态 | 肯定 `Every student is here.`／否定 `I don't like coffee.`／疑问 `Is every student here?` |
| 复现题设计 | 第 1 题 target＋变体逐字题；第 2 题复现 L151；第 3 题复现 L9 `I go to school every day.`（**余 5**）；第 4 题复现 L7 `They are students.`（**满 6**） |
| 案件规划 | `hunt-every-student`（#161） |

**⚠️ 场景切换**：L145–L151 连续 7 课都在 `mansion`，故 **L152 改 `campus`**（既换空气，又配合「一群同学」的语义）。

---

## §3 与已教内容的切分专章

**中文一个「都」字，英语走到第四个词了**：

| 课 | 词 | 管什么 | 位置 |
|---|---|---|---|
| L148／L149 | `both`／`neither` | **两个** | 最前面 |
| **L151（本批）** | **`all`** | **三个以上／数不清** | **最前面** |
| **L152（本批）** | **`every`** | **一个一个来** | 最前面 |

**切分点＝「管几个」＋「后面带不带 s」**。四处必须切开（瑞思逐条核过）：
1. **「书」在 L148–L150 连续三课是主角** → 本批**必须把「两本」改成「四本」**，并在 `sceneSetupZh` 明写数量变化；
2. **「三个苹果」是 L26 的名句** → **本批不用苹果**；
3. **「茶」在 L145–L147 连续三课是主角** → **本批不用茶**；
4. **`There is a book on the desk.` 已顶死 6 课**（实测 L26／37／55／60／114／148）→ **本批禁用**（改用 `I have a new bag.`）。

---

## §4 中文负迁移处理专章

| 课 | 典型中式错句 | 设计含义 | 归入 |
|---|---|---|---|
| L151 | `*All of books are good.` | 中文「所有的书」连着说，学生可能照搬「of」 | 对比卡 ②（word_order） |
| L151 | `*All student is here.` | 中文「学生」不分多少个 | 对比卡 ①（plural） |
| L152 | `*Every students are here.` | 「每个学生」在中文里仍是复数概念 | 对比卡 ①（plural）**主靶** |
| L152 | `*Every are here.` | 中文「每个都到了」可以省略名词 | 对比卡 ②（fragment） |
| L152 | `*All student is here.`（想说「每个」） | **中文「都」一个字，两个词都能翻** | 对比卡 ③（**本轴核心**） |

---

## §5 复现取材与红线头寸（实测）

| 句 | 当前课数 | 剩 | 本批 |
|---|---|---|---|
| `Yesterday I went to the park.` | 6（顶死） | 0 | ❌ **禁** |
| **`There is a book on the desk.`** | **6（顶死）** | **0** | ❌ **禁**（瑞思已发现） |
| `Both books are good.` | 3 | 3 | ✅ |
| `I ate two sandwiches.` | 3 | 3 | ✅ |
| `I go to school every day.` | 1 | 5 | ✅ |
| `I have a new bag.` | 1 | 5 | ✅ |
| `They are students.` | 0 | 6 | ✅ |
| `We are happy.` | 2 | 4 | ✅ |
| `All the books are good.` | 0 | 6 | ✅ |
| `Every student is here.` | 0 | 6 | ✅ |

**生产纪律**：逐句脚本核验（G14）。

---

## §6 验收标准

### 6.1 检查清单（G1–G15）

- [ ] **G1** practice ≥4 题且含变体逐字题
- [ ] **G2** tokens/answer 词集一致；distractors 不与答案词重复
- [ ] **G3** recall 三字段非空
- [ ] **G4** 目标句 ≤8 词（5／4）
- [ ] **G5** 罪名 10 枚举内（不碰 `comparison`）
- [ ] **G6** 案件：4 错、单 token 可修、≥1 净词、`reviewed: true`、tokenIndex 对齐
- [ ] **G7** **`contrast` 每课恰好 6 条**
- [ ] **G8** 展示层：`season-26` `{min:151,max:152}`（**`max` ≥152**）＋ `can-do-m28`（afterLesson 152）
- [ ] **G9** orphans：2 案全被引用；episode「一百五十一」…「一百五十二」
- [ ] **G10** cloze 逐课实跑登记
- [ ] **G11** 摘钩子：L148 `deepDive` 埋的那句必须在本批兑现
- [ ] **G12** **`every` 不得被当作错项**（首次出现即在正确位置）
- [ ] **G13** **四处切开**（书的数量／不用苹果／不用茶／禁用顶死句）
- [ ] **G14** 复现取材逐句脚本核验
- [ ] **G15** 零术语 ＋ `guided.explain` ≥20 字（**批二十五立的软建议**）

### 6.2 G-boost 硬护栏

每课 contrast 6 条中 **≥2 条带 `wrongMark`**（本批每课 3 条）；`wrongMark` 不得为纯标点；`guided.spot` 的 `wrongToken` 与 tokens **逐字相等**。

---

## §7 案件规划（2 案 · #160–#161）

| 案件 id | # | 配课 | 错误构成 |
|---|---|---|---|
| `hunt-all-the-books` | 160 | L151 | ① `student`→`students`（plural，旧 L11）② `of` 去掉（word_order，新）③ `is`→`are`（sv_agreement，旧 L7）④ `pen.`→`pens.`（plural，旧 L11） |
| `hunt-every-student` | 161 | L152 | ① `students`→`student`（plural，新——**反向**）② `Every`→`Every student`（fragment，新）③ `like`→`likes`（sv_agreement，旧 L25）④ `watch`→`watched`（tense，旧 L10） |

**案件纪律**：**旧错句全部重选**（批二十五用过的四句现已占用）——实测零复用：`We have two pen.`／`My sister like music.`／`Last night I watch TV.`／（L7 的 `is`→`are` 用 `All the books is good.` 本身）。

---

## §8 Non-goals

- **不做轴 A**（`unless`／`in case`）——**连押三批**；竞析方案的 ❌will 卡与已教 7 课重复（§1.3）
- **不做否定侧**（`none`／`no one`／`nobody`）——三词双文件全零，独立缺口
- **不做 `all of`／`none of`**（中级 U88 那一层）
- **不做 `all`＋人称**（`All of us`／`We are all here.`）——须先有 `of` 概念
- **不做 `each`**（`each` vs `every` 的「个体 vs 群体」在零术语下极难讲）
- **不做 `whole`**（GL 0）
- **不做 `seem`／`appear`／`would rather`**（连押）
- **不改 L1–L150**；**不引番外 5 案**；**不新增封面**；**零引擎改动**

---

## §9 开放问题

1. **轴 A 的剩余考点**：去掉 ❌will 后 `unless` 还剩几条对比卡？（批二十七前置）
2. **否定侧（`none`／`no one`／`nobody`）**：三词全零，是一个独立缺口——**中文「都」的否定侧目前仍是空白**。
3. **`each`**：中文侧有专文，但零术语下难讲——**是否永远不做**待评估。
4. **cloze 借句漂移**（§10 生产实录 E3）：借老句作否定变体时，boost cloze 会落在**老句**上而非本课考点——**跨批问题，须决定是否调整变体写法**。
5. **`mansion` 连续 7 课的观感**：本批 L152 已改 `campus`，**后续批次是否要系统性轮换场景**待评估。

---

> 本规格书由产品战略团队 AI 协作生成（主理人自撰），重要决策请由产品负责人审定。
