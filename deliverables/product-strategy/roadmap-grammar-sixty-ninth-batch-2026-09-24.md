# 路线图 · 第六十九批（真实使用数据审计）

- 日期：2026-09-24
- 交付：**#4 行为日志自审**——读本机 WebKit localStorage 得到**真实使用数据**
- 结论：**语法线 205 课 / 找错 214 案 / 语言之门 50 关，真实使用次数全部为 0**

---

## 1. 怎么做的（方法可复核）

本项目单人自用、无服务端遥测，但 **Tauri 桌面端把数据写在本机 WebKit localStorage**，
可以直接读。找到两份：

| 来源 | 修改时间 |
|---|---|
| `~/Library/WebKit/cn.personal.vocab/.../localstorage.sqlite3` | 2026-09-22 |
| `~/Library/WebKit/personal-vocab-desktop/.../localstorage.sqlite3` | 2026-09-23 |

**读取方法（记录，供复用）**：WebKit 的 localStorage 是 SQLite 单表 `ItemTable`，
value 是 **UTF-16LE blob**——要 `select hex(value)`，再
`Buffer.from(hex,"hex").toString("utf16le")`。直接 `select value` 拿到的是二进制。

---

## 2. 核心发现

两份数据**结论完全一致**：

| 模块 | 已建 | 真实使用 |
|---|---|---|
| 语法课 | **205 课** | **0** |
| 语法三关卡 / 趁热练 | 205×3 / 三档 | **0 / 0** |
| 找错案件 | **214 案** | **0** |
| 语言之门 | **50 关** | **0** |
| 日记批改 | 121 题 | **0** |
| 复习记录 | — | **0** |
| 导入材料 | — | **0** |

**被真正用过的**：`cards` 115 张（内置核心 100 词）、`units` 6-7 本、`adventures` 6 个
（自建，节点 1-8，**完成 0**）。

**遥测侧独立佐证**（同为本机实取）：
- `adventure-telemetry`：**15 条**，时间跨度 2026-09-16 14:45 → 15:34（**49 分钟内**）
- `settings-telemetry`：**9 条**
- **`grammar-telemetry-events-v1` 键根本不存在**——语法线遥测一次都没写过

---

## 3. 对本会话已做工作的冲击

我连续九批在给语法线**加内容与可见性**（L205、`quietly`、今日页主 CTA、
统计页语法区块、随课词桥）。真实数据说明：**这条线从第一天起就没被打开过**。

| 路线图项 | 原判断 | 修正 |
|---|---|---|
| #1 首页主 CTA（已交付 `cd92417`）| RICE 8.4 最高 | ⚠️ **前提被推翻**：不是「藏得深」，是「没进动线」|
| #2 今日页/统计页入口（已交付）| RICE 6.3 | 同上 |
| #5 输入回路 / #6 随课词桥 UI | 待做 | ⚠️ **先别做**——整条线没被使用 |
| ~~B1 扩容~~ | 已撤销 | ✅ 撤销理由更强 |

**469 个内容单元（205+214+50），真实消费 0。**

---

## 4. ⚠️ 三个局限（诚实登记）

> **2026-09-24 复核更正**（`data-audit-real-usage-verification-2026-09-24.md`）：
> 第 1 条「浏览器 profile 都没找到」**不准确**——Chrome **`Profile 1`** 里有
> `127.0.0.1:1420` origin 的 `personal-vocab-app-data-v1`，
> 值在 **snappy 压缩块**里所以 `strings` 看不到内容。
> 解压后：`schemaVersion 6`、**09-10 21:58→22:07**、**零语法键**
> ⇒ 三份里最旧、**早于语法模块（09-12）**，反而排除了「用户在 dev server 学语法」。
> **本批结论不变，证据更完整。**

1. **时间截止**：数据最新时间戳 **2026-09-16 / 09-22**。之后用户可能改用
   dev server（本机确实在跑 `vite --host 127.0.0.1:1420`）——~~但我在
   Chrome / Edge / Brave / Safari 的 profile 里都没找到对应数据~~
   （**已更正**：找到了，但它是 09-10 / schema 6 的旧数据，无语法键）。
   ⇒ 只能说「Tauri 端两份数据里没有」，**不能断言「此后也没用」**。
2. **可能是早期快照**：最符合证据的解释是「装了、导了词、搁置了」，
   但我**没有用户本人确认**。
3. **样本 n=1**：单人项目，结论只对这一个用户成立。

---

## 5. 我的建议：**先确认，别写代码**

**从证据看最可能的解释是「整个 app 还没进入使用阶段」**——
因为 `reviews: 0` 说明**连词汇复习都没跑起来**。
若是如此，「语法线没人用」不是语法线的问题，而是**卡在更前面**。

三种情况三种做法：

| 若… | 则 |
|---|---|
| **代表现状** | 该解决的不是功能，而是**第一个动作卡在哪**：`cards 115 / reviews 0` ⇒ 导入了词但没开始复习 |
| **数据过时** | 请给当前数据，我重审 |
| **用户在用别的工具** | 定位需要重新讨论 |

⇒ **本批只出报告，不改代码**——所有判断都依赖「数据是否代表现状」这一个未确认前提。

> ### ✅ 2026-09-24 复核：这一栏**已用证据回答，不必再问用户**
>
> 独立复核（`data-audit-real-usage-verification-2026-09-24.md`）把三种可能逐一判掉：
>
> | 可能 | 判定 | 依据 |
> |---|---|---|
> | B. 入口不在日常路径（用 dev server）| ❌ **排除** | 找到 Chrome `Profile 1` 的 dev-server origin 数据，但它是 **09-10 / schema 6**、**零语法键**，比 Tauri 侧更旧 |
> | C. 数据是早期快照 | ⚠️ 部分成立 | 三份数据都始于 **09-10**，最后写入 **09-23 02:29**；此后无学习写入 |
> | A. 还没学到那一步 | ✅ **唯一剩下** | `reviews: 0` ⇒ 连词汇复习都没开始 |
>
> **⇒ 判为 A：「数据代表现状」成立。**「语法线零使用」是硬结论。
>
> ### ⚠️ 但有一件事这份数据**答不了**（别混为一谈）
>
> **今日页语法首屏入口（`cd92417`）上线于 09-23 22:47，
> 而数据截止在 09-23 02:29——早 20 小时。**
>
> | 问题 | 能答吗 |
> |---|---|
> | 「语法线此前被用过吗？」 | ✅ 能：零使用 |
> | 「语法首屏入口有效吗？」 | ❌ **不能**：入口上线时数据已停 20 小时 |
>
> **⇒ 别把「零使用」当成「首屏入口无效」的证据**——那是用旧数据指导新结论。
>
> ### 建议的下一步
>
> 不是加功能，也不是改入口（后者没有数据支持任何判断），而是：
> **查清「导入 115 张卡之后，为什么没开始第一次复习」**（`cards 115 / reviews 0`）。
> 这一条**只能由用户本人回答**——数据能证明「没做」，不能说明「为什么」。
>
> ⚠️ 仍存局限：若用户用别的机器/其他浏览器 profile，本复核看不到。
> 但**能找到的三处口径一致**，且比原报告多覆盖一处。

---

## 6. 路线图进度（本批后）

| # | 项 | 状态 |
|---|---|---|
| 1 | 首页主 CTA | ✅ 已交付（**前提被本批推翻**）|
| 2 | 今日/统计页语法入口 | ✅ 已交付（同上）|
| 3 | 忆段补 `openWhyWrong` | ⚠️ 丢失（并发截断）|
| 4 | **行为日志自审** | ✅ **本批完成**——发现真实使用为 0 |
| 5 | 输入回路 | ⏸ **暂停**（整条线未被使用）|
| 6 | 随课词桥 | ✅ 服务层已交；⏸ **UI 暂停** |
| 7 | ~~冠词专项~~ | ❌ 撤销 |
| 8 | 数据寿命出口 | ✅ 归档半边已完成（批六十八核实）|

**⇒ 路线图实质冻结，等用户确认数据是否代表现状。**

---

## 7. 本批追加的精确发现（读数据后补齐）

| 项 | 实测 |
|---|---|
| 115 张卡的状态 | **全部 `status:"new"`、`intervalDays:0`、`reviewCount:0`** |
| `reviews` | **0 条** |
| 卡的 `sourceId` | 115/115 **空**（内置卡，非自建）|
| ⇒ 结论 | **导入了内置核心 100 词，但一次复习都没做过** |

**一个看似矛盾、实为设计正确的点**：115 张卡的 `nextReviewAt` 早已过期，
但 `dueTotal = 0`、`availableNewWords = 115`。这不是缺陷——
`reviewService.ts:107-109` 明确写了这是 R1 有意设计（新卡走每日训练队列，
不进口「到期」桶）。复刻该状态实测 `getNewCardsForToday` 返回 **10 张/天**，
115 张约 **12 天**走完。**机制是通的，是没人走。**

**冒险那 49 分钟的真实轨迹**（唯一的行为数据，15 条遥测还原）：

| 时段 | 时长 | 行为 | 推进 |
|---|---|---|---|
| 14:45:04–14:45:41 | **32.3 秒** | 新建冒险 → 进入 → 看节点 → 选一次 → 退出 | `nodesAdvanced: 0` |
| 15:34:02–15:34:22 | **20.4 秒** | 再进入 → 选选项 → 完成 2 节点 → 退出 | `nodesAdvanced: 2` |

⇒ 合计 **53 秒**、中间隔 49 分钟——这是**「试了一下」**，不是「学习」。

**与开发时间线的决定性对照**：

| 时间 | 事件 |
|---|---|
| 09-10 ~ 09-16 | 数据活动期 |
| **09-16 15:34** | **数据活动停止** |
| 09-17 ~ 09-24 | **开发继续 8 天、数十次提交** |

⇒ 数据在**开发中期就停了**，之后 8 天 product 大量改动、用户数据零新增。
最符合「那批数据来自构建/验收阶段的试用」的解释。

---

## 8. 核实为**不成立**的两项担忧（记录以防重查）

| 担忧 | 核实结果 |
|---|---|
| 「115 张卡全到期却不显示为 due」 | ❌ **非缺陷**——R1 有意设计，见上；走新卡队列是通的 |
| 「导出备份会带出 API key」 | ❌ **非缺陷**——`exportService.ts:21-30` 已有 `withoutSecrets`，实测导出 JSON 既不含 `apiKey` 也不含 `dataSync.token` |

**仍成立的一条事实**：`apiKey` 在 localStorage 里是明文。对本地单机可接受
（数据不出本机），若将来做多端同步需单独评估。
