# PRD：第三十一批 · 需要／大多数（`need to` / `most of` · 2 课 L161–L162）

**日期**：2026-09-20 ｜ **类型**：产品需求规格书（PRD）｜ **主理人自撰**
**上游**：`b-tier-frontier-survey-2026-09-20.md`（B 档前沿普查，顺位 1 与 3）

| 版本 | 日期 | 说明 |
|---|---|---|
| v1 | 2026-09-20 | 初稿：**2 课**（L161–L162）＋2 案（#170–#171），开 season-31、m33；**档位 B＋**；**普查的第一个执行批次** |

---

## §1 选题裁决

### 1.1 本批的由来

批三十的路线图 §9 审计发现：**历批候选池来自「上一批的候补清单」，按需产出、不是全库普查**——**真正的 B 档总量从未被穷举**。

**批三十一前段完成普查**（26 项跨源判定 → 10 项 B 档候选）。**本批执行顺位 1 与顺位 3。**

### 1.2 逐项裁决

| 顺位 | 候选 | 档位 | 取/不取 | 理由 |
|---|---|---|---|---|
| **1** | **`need to`** | **B＋** | ✅ **取** | 规则页最厚（**3 h2＋7 h3＋2 Warning＋7 禁用**）；**CEFR A1**（本组最低）；`need` 词已入库 |
| 2 | `be able to` | B＋ | ❌ | **课位压在 B1-B2（跨级）**；BC 参考层 beginner 段明文不收它 |
| **3** | **`most`（量词义）** | **B** | ✅ **取** | **零造词**；与 L31 形成「同一个词形两件活」的干净对照 |

### 1.3 批次定位：档位 B＋

| 项 | 本批 | 对照 |
|---|---|---|
| **新造词** | **1 个**：`need`（4 处 NPC 台词，**从未立岗**） | 批三十 1 个 |
| **`most` 造词成本** | **0**（词已入库，只是新增义项） | — |
| **机制复用** | **1 处**（`to` 垫板——L15 的第 **3** 次应用） | — |
| **配对设计** | **2 处**（L161 与 L16「必须」对照；L162 与 L157 `none of` 配对） | — |

---

## §2 逐课规格

**L161 立岗（`need to`：需要）**

| 项 | 内容 |
|---|---|
| 课注 id | `lesson-161-need-to`；`number: 161`；episode「小美的一天 一百六十一」；scene `city`；cover `cover44` |
| title / grammarLabel | 「我需要买牛奶」/「需要 · need 后面也跟 to」 |
| targetSentence | `I need to buy some milk.`（6 词 ✅） |
| 场景 | 放学路上经过小卖部，想起家里牛奶喝完了 |
| 新知识点 | **只有一件**：说「需要」用 `need`，**后面请 `to` 垫一下**；与 L16「必须」的语义切分 |
| oneLineRule | 「说「需要」用 need，后面请 to 垫一下——I need to buy some milk（我需要买点牛奶）。它和第 16 课那个「必须」不一样：must 是别人要求你，need 是这件事本身要办。」 |
| 对比卡 6 条方向 | ① `I need buy some milk.` ❌（**`need`**，word_order——**少了 to 连不上**）② `I need to buys some milk.` ❌（**`buys`**，verb_form——**to 后面穿原样**）③ `I needs to buy some milk.` ❌（**`needs`**，sv_agreement——**句首是 I，不带 s**）④ 双正解（L16）：`I must finish my homework today.` ✅（**「必须」vs「需要」**）⑤ 双正解（L15）：`I want to go.` ✅ ⑥ 双正解（L160）：`He seems to know you.` ✅（**同一个 to 垫板**） |
| 变体三态 | 肯定 `I need to buy some milk.`／否定 `I don't need to buy milk.`／疑问 `Do you need to buy milk?` |
| 复现题设计 | 第 1 题 target＋变体逐字题；第 2 题复现 L16；第 3 题复现 L15；第 4 题复现 L159 |
| 案件规划 | `hunt-need-to-buy`（#170） |

**L162 立岗（`most of`：大多数）**

| 项 | 内容 |
|---|---|
| 课注 id | `lesson-162-most-of`；`number: 162`；episode「小美的一天 一百六十二」；scene `campus`；cover `cover45` |
| title / grammarLabel | 「大多数都喜欢」/「大多数 · most 后面也要 of」 |
| targetSentence | `Most of the students like it.`（6 词 ✅） |
| 场景 | 课间投票，班长数完举手的人 |
| 新知识点 | **只有一件**：说「大多数」用 `most`，**后面拴 `of`**；与 L31「最」的切分 |
| oneLineRule | 「说「大多数」用 most，后面要拴一个 of——Most of the students like it（大多数学生都喜欢它）。第 157 课那个 none 后面也拴 of，这两个正好一对。」 |
| 对比卡 6 条方向 | ① `Most the students like it.` ❌（**`Most`**，word_order——**少了 of**）② `Most of the students likes it.` ❌（**`likes`**，sv_agreement——**一群人配原样**）③ `Most of student like it.` ❌（**`student`**，plural——**一群要带 s**）④ 双正解（L157）：`None of the cups are mine.` ✅（**两头配对**）⑤ 双正解（L31）：`This is the most beautiful flower.` ✅（**同一个词形两件活**）⑥ 双正解（L151）：`All the books are good.` ✅ |
| 变体三态 | 肯定 `Most of the students like it.`／否定 `Most of the students don't like it.`／疑问 `Do most of the students like it?` |
| 复现题设计 | 第 1 题 target＋变体逐字题；第 2 题复现 L157；第 3 题复现 L31；第 4 题复现 L161 |
| 案件规划 | `hunt-most-of-students`（#171） |

---

## §3 与已教内容的切分专章

**`need to` 的三分**：

| 课 | 词 | 语义 |
|---|---|---|
| L16（已教） | `must`／`have to` | **必须**——外面有人要求你 |
| L47（已教） | `should` | **应该**——给建议 |
| **L161（本批）** | **`need to`** | **需要**——这件事本身要办 |

**`most` 的两件活**：

| 课 | 词形 | 语义 | 位置特征 |
|---|---|---|---|
| L31（已教） | `the most + 形容词` | **最** | 前面站 `the` |
| **L162（本批）** | **`most of + 那东西`** | **大多数** | 后面拴 `of` |

---

## §4 中文负迁移处理专章

| 课 | 典型中式错句 | 设计含义 | 归入 |
|---|---|---|---|
| L161 | `*I need buy some milk.` | 中文「我需要买」中间没有介词 | 对比卡 ①（word_order）**主靶** |
| L161 | `*I need to buys some milk.` | 学生以为「买」要跟着某种变化 | 对比卡 ②（verb_form） |
| L162 | `*Most the students like it.` | 中文「大多数学生」直接连着说 | 对比卡 ①（word_order）**主靶** |
| L162 | `*Most of the students likes it.` | 中文「大多数」在语感上像单数 | 对比卡 ②（sv_agreement） |

---

## §5 复现取材与红线头寸（实测）

| 句 | 课数 | 剩 | 本批 |
|---|---|---|---|
| `Yesterday I went to the park.` | 6 | 0 | ❌ **禁** |
| `There is a book on the desk.` | 6 | 0 | ❌ **禁** |
| `The bag is light enough to carry.`（L71） | 1 | 5 | ✅ |
| `I want to go.`（L15） | 2 | 4 | ✅ |
| `He seems to know you.`（L160） | 1 | 5 | ✅ |
| `It looks like a boat.`（L159） | 2 | 4 | ✅ |
| `I need to buy some milk.` | 0 | 6 | ✅ |
| `Most of the students like it.` | 0 | 6 | ✅ |

---

## §6 验收标准

### 6.1 检查清单（G1–G15）

- [ ] **G1** practice ≥4 题且含变体逐字题
- [ ] **G2** tokens/answer 词集一致；distractors 不重复答案词
- [ ] **G3** recall 三字段非空
- [ ] **G4** 目标句 ≤8 词（6／6）
- [ ] **G5** 罪名 10 枚举内
- [ ] **G6** 案件：4 错、单 token 可修、≥1 净词、`reviewed: true`、tokenIndex 对齐
- [ ] **G7** **`contrast` 每课恰好 6 条**（3 带标记 ＋ 3 双正解）
- [ ] **G8** 展示层：`season-31` `{min:161,max:162}` ＋ `can-do-m33`
- [ ] **G9** orphans：2 案全被引用
- [ ] **G10** cloze 逐课实跑（**借句漂移不复现**）
- [ ] **G11** `scene` 合法
- [ ] **G12** **`need` 的语义与 L16「必须」切开**（不得只做同义换词）
- [ ] **G13** **`most` 的量词义与 L31 的最高级义切开**
- [ ] **G14** 零术语 ＋ **`guided.explain` ≥20 字**（**本批踩过一次**）
- [ ] **G15** 旧线零回归：L1–L160 不动

### 6.2 G-boost 硬护栏

每课 contrast 6 条中 **≥2 条带 `wrongMark`**（本批 3 条）；`wrongMark` 不得为纯标点；`guided.spot` 的 `wrongToken` 与 tokens **逐字相等**。

---

## §7 案件规划（2 案 · #170–#171）

| 案件 id | # | 配课 | 错误构成 |
|---|---|---|---|
| `hunt-need-to-buy` | 170 | L161 | ① `need`→`need to`（word_order，新）② `buys`→`buy`（verb_form，新）③ `read`→`reads`（sv_agreement，旧 L25）④ `watch`→`watched`（tense，旧 L10） |
| `hunt-most-of-students` | 171 | L162 | ① `Most`→`Most of`（word_order，新）② `likes`→`like`（sv_agreement，新）③ `chair.`→`chairs.`（plural，旧 L11）④ `is`→`are`（sv_agreement，旧 L7） |

---

## §8 Non-goals

- **不做 `be able to`**——课位压在 B1-B2（跨级）
- **不做 `ought to`／`as though`**——同义换词
- **不做 `dare`／`provided`**——无规则页或跨源自述罕用
- **不做动词短语整组**（7 项）——普查判**词汇点，不进语法线**
- **不做 `a lot of`**——与 `much`/`many` 同义
- **不改 L1–L160**；**不引番外 5 案**；**零引擎改动**

---

## §9 开放问题

1. **`explain` 长度断言**（第 3 次出现该类缺陷）——建议批三十二落地。
2. **`be able to` 是否永远不做**（跨级问题）。
3. **动词短语是否该开词汇线**（普查判不进语法线，但学习需求可能仍在）。
4. **`as if` 的缓排何时解除**（与 L159/L160 槽位冲突）。
5. **普查的覆盖完整性**：26 项凭语感列举，**仍非数学穷举**。

---

> 本规格书由产品战略团队 AI 协作生成（主理人自撰），重要决策请由产品负责人审定。
