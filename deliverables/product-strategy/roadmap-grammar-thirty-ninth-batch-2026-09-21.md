# 路线图 · 第三十九批（两项工程修复 + `so ... that`）

- 日期：2026-09-21
- 交付：L193 / 案 #202 / season-28 扩至 193 / can-do-m40
- 性质：**质量批**——两项长期缺陷修复是本批主体

---

## 1. 交付清单

| 文件 | 变更 |
|---|---|
| `src/services/huntService.ts` | **修复 `pickCorrectionWord`**：先挡括注式、再剥尾部标点（错词本垃圾 115 → 0）|
| `src/data/grammarLessons.ts` | +L193 `lesson-193-so-that-result`（192 → **193 课**）；**23 处展示句替换**（重放题 23 → 1）|
| `src/data/huntCases.ts` | +案 #202 `hunt-so-that-result`（201 → **202 案**）|
| `src/data/grammarSeasons.ts` | `season-28` 扩至 `min: 182, max: 193` |
| `src/pages/GrammarPathPage.tsx` | +`can-do-m40` |
| `src/edge/**`（11 文件） | 硬编码计数更新 192→193、201→202 |
| `deliverables/` | 本 PRD + 本路线图 + 瑞思/竞析两份研究报告 |

---

## 2. 验证证据

| 项 | 结果 |
|---|---|
| 交付相关 7 个测试文件 | **182 / 182 通过** |
| `tsc --noEmit` | 我的文件 **0 错误**（其余错误在 `src/edge/`，并发进程）|
| `vite build` | ✓ 通过（2.97s）|
| 独立核验 | **12 / 12**（字段完整、对照卡 3+3、结构量、scene 合法、零术语、无星号、spot 对齐、案件结构、互引唯一、季覆盖、难度闸门、修复终值）|
| 浏览器走查 | L193 首屏 ✓；路径页 `7 / 193 课` + 末三课含 L193 ✓；找错页 `3 / 202` + 第 28 季 12 案 + 案 202 解锁指向 ✓；L193 零术语 0 命中 ✓ |
| 全量测试 | 28 失败——**全部在并发进程新增的 49 个诊断套件内**（见 §4）|

---

## 3. 两项修复的量化结果

| 缺陷 | 修前 | 修后 |
|---|---|---|
| 错词本垃圾词 | **115 处** | **0** |
| 重放题 / 余量 | 23 / **余量 3** | 1 / **余量 25** |

**两项都是「一个函数/一类槽位统一修」的性质**，不是逐条打补丁：
- 修复一改的是 `pickCorrectionWord` 一个函数，全库 748 个植错点同时受益；
- 修复二改的是「被练习答案逐字复制的展示句」这一类槽位，23 处同批处理。

---

## 4. 并发进程状态（需产品负责人知悉）

本批验证期间并发进程活动**加剧**：

| 现象 | 数据 | 是否影响本批 |
|---|---|---|
| 测试文件从 91 涨到 **140**（新增 49 个诊断套件）| 全部 `?? 未跟踪` | 否 |
| 全量测试 28 项失败 | 断言标题带「已确认缺陷／当前行为」，失败样本从 `lesson-01-am` 起（全库共性）| 否 |
| `gq1` 基线漂移 | 该套件自行从绝对数改为**比例**（656/3609 = 18.2%），吸收了我的数据增长 | 否——已自动解决 |
| `GrammarHuntPage.tsx` 的 `addableCorrectionWords` 未定义 | 上批报过，本批仍在 | 否 |

**判别方法（本批新增，建议登记）**：并发套件的失败若满足「断言标题含『已确认缺陷/当前行为』」+「失败样本覆盖全库第一课」，即为它们的登记项，不是本批回归。

---

## 5. 携带项

| # | 项 | 优先级 | 说明 |
|---|---|---|---|
| 1 | ~~重放题余量只剩 3~~ | **✅ 结案** | §3，余量已拉到 25 |
| 2 | ~~`correction` 写法不统一~~ | **✅ 结案** | §3，垃圾归零 |
| 3 | **`correction` 三类写法仍并存**（`（去掉 X）` 9 / 括注其他 4 / `去掉 X` 64）| 低 | 函数侧已兼容三者，**数据侧统一非必要**；若统一只为一处可读性 |
| 4 | **「入库词 == 原词」的语义空卡 60 处**（`happy`/`is happy`、`cake`/`a cake`）| **新·中** | 竞析本批发现：不是垃圾字符，是**语义空卡**——修完 §3 后剩下的真问题 |
| 5 | 词汇线是否单独立项（约 316 项 A0/A1 词缺席）| 待产品负责人决策 | 延续多批 |
| 6 | `such a` | **降级登记** | 本批拒绝（与 L89 撞车 + 牛津 A2），建议**与冠词收口课合并**，不单独立课 |
| 7 | `its` 正例 / `several` 刻度卡 | 待办 | 小补丁 |
| 8 | `try to` vs `try doing` | 登记 | 会是第 6 个「-ing 门卫」|
| 9 | `decide` / `hope` 已被 gate/runes 消费却从未教 | 观测项 | 延续 |
| 10 | Murphy 双册原件全失 | 延续 | 竞析本批确认仍不可得 |
| 11 | `deepDive` 术语豁免 | 待产品负责人决策 | 全字段遍历守门唯一豁免字段 |
| 12 | 封面池消耗 | 延续 | 本批取 cover76 |

---

## 6. 下批建议

1. **优先：`correction` 的语义空卡 60 处**（§5 携带项 4）——它是修复一之后剩下的真问题，且有明确判据（入库词与原词相同/包含关系）。
2. **其次：`such a` 并入冠词收口课**（§5 携带项 6）——本批已论证它不该单独成课，但仍应补上。
3. 教学内容候选：
   - `its` 正例卡 + `several` 刻度卡（小补丁，可并入任意课）
   - 词汇线立项决策（若立项，`along`/`around` 等可归入该线）
4. **「完成所有 B 档」的宣告判据**（延续）：本批又发现新缺口（`so ... that` 全库为零）。建议采用「**连续两次调研零新增**」作为收口判据。
5. **方法学登记**（本批新增）：批量替换必须带上下文限定，禁止 `.replace(/toBe\(N\)/g, …)` 这类无差别全局替换——本批踩过一次，虽未造成损害，但手法本身不安全。
