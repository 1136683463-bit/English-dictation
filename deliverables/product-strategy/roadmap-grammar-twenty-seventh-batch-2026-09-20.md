# 路线图更新：第二十七批课程（L153–L154 还没、已经、还 · **2 课小章 · 单拱 · B 档**）

**日期**：2026-09-20 ｜ **类型**：路线图（语法线第二十七批）｜ **主理人自撰**
**上游**：`prd-grammar-yet-already-2026-09-20.md` ＋ 本批两研究（瑞思／竞析）＋ 批二十六路线图 §6 携带项 1／2

---

## 📌 TL;DR

- **选题**：**`yet`（还没）／`already`（已经）／`still`（还在）**。
- **本批最大动作**：**轴 A 判不成立（0 课）、正式关闭**——两研究独立复核一致（去掉 ❌will 后 `unless` 只剩 3 条卡，距 6 条差 3 条）。
- **形态**：**2 课**（立岗／切开）。
- **档位**：**B**（跨源课程位本批最厚：BC 独立正课 ＋ Murphy 三格 ＋ 中文三篇专文）。
- **交付物**：L153／L154 ＋ 案 #162／#163 ＋ `season-27` ＋ `can-do-m29`。
- **零引擎改动**。
- **一处研究引文的行号指认有偏差，已更正**（§1.1：钩子在 L151 `:28490`，非 L152）。

---

## 🎯 核心结论卡片

| 项 | 结论 |
|---|---|
| 课程 | L153 `lesson-153-yet-already`／L154 `lesson-154-still` |
| 目标句 | `She hasn't come yet.`（4）／`She is still waiting.`（4） |
| 案件 | `hunt-yet-already`(#162)／`hunt-still-waiting`(#163) |
| 展示 | `season-27` `{min:153,max:154}`；`can-do-m29` `afterLesson: 154` |
| 封面 | `cover36`／`cover37`（实测各用 1 次） |
| 新造词 | **2 个**：`yet`＋`already`（`still` 属第 2 课） |
| 总量 | **154 课／163 案／27 季** |

---

## §1 选题裁决

### 1.1 瑞思推荐轴 B 的依据：**钩子确实存在，但行号指错了**（主理人判定更正）

**⚠️ 本节为 2026-09-20 事后更正**（批二十八研究复核时发现）。

瑞思 §0 逐字称：「轴 B 是**欠账不是新选题**——**L152 `:28684` 逐字「all 管肯定侧，否定侧下一批再看」**」。

**批二十七主理人当时只查了 L152，未查到该句，遂判「引文失实」。批二十八复核后确认：判定有误。**

**实测结论**（`sed -n` 可复跑）：

| 课 | 行号 | 否定变体的 `noteZh` 逐字 |
|---|---|---|
| **L151** | **`:28490`** | **「这是第 5 课的老句子（all 管肯定侧，否定侧下一批再看）。」** ✅ **钩子在这里** |
| L152 | `:28684` | 「第 5 课的老句子（every 管肯定侧）。」 |

**→ 更正后的判定**：**瑞思引用的内容完全正确，只是把行号写成了 L152（实为 L151）。** 主理人当时的核查范围过窄（只查被指认的那一课），**「引文失实」这个结论不成立**。

**⚠️ 但方向裁决不受影响**：本批（批二十七）取轴 C 的三条理由中，**理由 2（跨源课程位本批最厚）与理由 3（给已教句式补词）都是独立成立的**，不依赖本节。**且批二十八两研究亦独立复核确认钩子在 L151**（竞析本轮逐字兑现了它）。

**⚠️ 机制教训（比结论本身更重要）**：**核查「逐字引用」时不能只查被指认的那一课**——引文可能真实存在但行号有偏差（尤其跨相邻课）。**正确做法：全库检索该字符串，再定位它实际在哪一课。** 本批已按此做法更正。

**⚠️ 同时保留的登记**：**研究的「逐字引用」仍无事后抽检机制**（§6 携带项 1）——本节的教训正是它的价值所在：**既要拦「不存在的引文」，也要避免「因核查范围过窄而误判」**。

### 1.2 裁决取轴 C（两研究分歧）

| 研究 | 推荐 |
|---|---|
| 瑞思 | 轴 B（`none`／`nobody`）2–3 课 |
| **竞析** | **轴 C（`yet`／`already`／`still`）2 课** |

**取轴 C**，三条理由：

1. **理由 1**：轴 B 的「摘钩子」优势**在批二十八才被竞争掉**——当时轴 C 的证据厚度是独立成立的（**本理由已按 §1.1 的更正重述**）。
2. **理由 2**：**轴 C 的跨源证据本批最厚**——**BC 有独立正课 `present-perfect-just-yet-still-already`**（B1/B2，独立课程位）＋ Murphy 初级 **U16／U95** ＋ 中级 **U111**（三格）＋ 中文侧**三篇专文**。
3. **理由 3**：**轴 C 是「已教内容的空白补充」**——实测 L21–L24 的否定变体都用 `haven't done` 表达「还没」**但都没带 `yet`**；「已经」也只用 `have done`，**没用 `already`**。

### 1.3 轴 A 判不成立（0 课）——两研究独立复核一致

| 研究 | 结论 |
|---|---|
| 竞析 | 去掉 ❌will 两条后，`unless` **只剩 3 条**硬增量 |
| 瑞思 | 同样判 **3 条**（「不与 if 连用」1 条带标记 ＋「if…not 等价」1 条双正解 ＋ L48 回流 1 条） |

**→ 两研究独立得出同一结论：距 6 条差 3 条，轴 A 不成立。**

**根因（主理人补记）**：**❌will 规则实测已在 6 课教过**（L48／L49／L109／L142／L143／L144；⚠️ **批三十一普查更正**：初版含 L47，复算不成立——L47 那次 `will` 落在 L48 的注释行上）——**轴 A 的自然考点大半已被别的课吃掉**，这是它连押四批的根本原因。

**竞析追加发现**：批二十六第 6 条卡的跨源依据**挂错段落**。

**→ 裁决：轴 A 关闭候补（不再登记）。**

### 1.4 档位 B

| 项 | 本批 | 对照 |
|---|---|---|
| 新造词 | **2 个**（`yet`＋`already`） | 批二十六 1 个 |
| **跨源课程位** | **BC 独立正课 ＋ Murphy 三格 ＋ 中文三篇专文（本批最厚）** | 批二十六：Murphy 双册双格 |
| 认读升格 | **1 处**（L21–L24 的 `haven't done` 补上 `yet`） | — |

**→ 判 B（不写 B−）**：**课程位厚度是 B− 课（批二十五／二十六）的数倍**，且增量是「给已教句式补词」，不是重复讲句式。

---

## §2 拆课总表

| 课 | id | 标题 | grammarLabel | 目标句（词数） | 场景 | 封面 |
|---|---|---|---|---|---|---|
| **L153** | `lesson-153-yet-already` | 还没、已经 | 还没 · yet 站句尾／已经 · already 站中间 | `She hasn't come yet.`（4 ✅） | `mansion` | `cover36` |
| **L154** | `lesson-154-still` | 还在等 | 差在哪儿 · 还没来／还在等 | `She is still waiting.`（4 ✅） | `city` | `cover37` |

**episode**：「小美的一天 一百五十三」／「一百五十四」。
**⚠️ 场景**：L153 用 `mansion`（厨房等饭）、**L154 改 `city`**（校门口，换空气——承接 L152 `campus` 的轮换）。

---

## §3 生产节奏（D1–D6）

| 阶段 | 内容 | 门 |
|---|---|---|
| **D1** | 数据落盘：L153–L154 ＋ 案 #162–#163 ＋ `season-27` ＋ `can-do-m29` | — |
| **D2** | cloze 逐课实跑登记 | **F27-A 打样门** |
| **D3** | 验证：全量测试 ＋ `tsc` ＋ 构建 ＋ 零术语 ＋ 复现红线 | 六道门 |
| **D4** | 展示层走查 | 走查留痕 |
| **D5** | 案件走查 | — |
| **D6** | 关账：E 节回填 | **F27-B 观察门** |

---

## §4 质量门与验收（实测结果）

| # | 门 | 判据 | 结果 |
|---|---|---|---|
| **G-1** | 数据完整性 | `npm test` 全绿 | ✅ **800 通过 / 61 文件** |
| **G-2** | 类型 | `tsc --noEmit` 零错 | ✅ **0 错** |
| **G-3** | 构建 | `npm run build` | ✅ **通过（2.66s）** |
| **G-4** | 零术语 | 首屏三字段＋whyZh／explain／noteZh | ✅ **零命中** |
| **G-5** | 复现红线 | 全库 practice 答案 ≤6 课 | ✅ **最高 6** |
| **G-6** | 案件 | 2 案结构／下标／`reviewed`／罪名 | ✅ **逐项通过** |

### 4.1 逐课结构核验（脚本）

| 课 | 目标句 | contrast 配方 | guided | practice | 星号 | 零术语 | explain≥20 字 |
|---|---|---|---|---|---|---|---|
| L153 | 4 词 ✅ | 6（**3 新错 + 3 双正解**）✅ | 6 ✅ | 4 ✅ | 0 ✅ | ✅ | ✅ |
| L154 | 4 词 ✅ | 6（**3 新错 + 3 双正解**）✅ | 6 ✅ | 4 ✅ | 0 ✅ | ✅ | ✅ |

**配方与近 14 课惯例一致**（立岗／切开课＝3 新错＋3 双正解，实测确认）。

### 4.2 术语红线

**禁用**：从句／状语／时态／语序／介词／副词／三单／复数／完成时。
**本批自建说法**：「做过版」（L21 起的既有说法）／「站句尾」／「站中间」／「紧挨着」／「还没发生 vs 一直在」。

---

## §5 生产实录（D1–D4 回填）

### E1. cloze 逐课实跑（D2）

| 课 | boost 侧 | ambush 侧 |
|---|---|---|
| **L153** | **落 `already` ★**（`I have ___ eaten.`，来源 `variants:0`） | 落 `have`／`Have` |
| **L154** | 落 `waiting`（`She isn't ___.`，来源 `variants:1`） | 落 `is`／`Is` |

**判定**：**L153 的 boost 空位落在考点词 `already`，题面 `I have ___ eaten.`**——考点词在自己的正确语境里 ✅。

**⚠️ L154 落在 `waiting`（否定变体的实词）**——**批二十六登记的「cloze 借句漂移」在本批复现**（§6 携带项 3）。**严重度仍判低**：本课考点由关 2 的 rebuild（`She is still waiting.`）＋ contrast 3 条带标记卡 ＋ `guided.spot` 承载。

**ambush 侧**：结构性假友好（`yet`／`already`／`still` 不在 `GRAMMAR_WORDS` 表内）——**与批二十四–二十六同型**。

### E2. G11 定性（`yet`／`already`／`still` 的位置类标注）

脚本初查报 6 处「`wrongMark` 是 yet／already／still」，**逐条定性后全部正确**：

| 课 | wrong | 标注 | 定性 |
|---|---|---|---|
| L153 | `She hasn't come already.` | `already` | ✅ **词错**（该用 yet） |
| L153 | `She hasn't yet come.` | `yet` | ✅ **位置错**（词对、位置错——正是本课主靶） |
| L153 | `I have eaten already yet.` | `yet` | ✅ 词多余 |
| L154 | `She still is waiting.` / `She is waiting still.` / `She isn't still waiting.` | `still` | ✅ **三处都是位置／用法错**（词对、站位错） |

**→ 无真缺陷。** 说明：**「词对、位置错」是本轴的正常错型**（与批二十四 `too`／批二十五 `both` 的「站错位置」同型）。

### E3. 案件旧错句实测（连续子序列口径）

**批二十六用过的句子现已占用**，本批重选并实测零复用：

| 靶 | 最终选用 | 状态 |
|---|---|---|
| L21 `verb_form` | **`I have do my homework.`** | ✅ 零复用 |
| L10 tense | **`Yesterday we go out.`**／**`I see her yesterday.`** | ✅ 零复用 |
| L7 sv_agreement | **`They is at home.`** | ✅ 零复用 |
| L25 三单 | **`He like tea.`** | ✅ 零复用 |

### E4. 走查实录（D4）

| 项 | 结果 |
|---|---|
| 路径页 `season-27` | ✅ 「第二十七季 · 还没、已经、还 0 / 2 课」，总进度 **0 / 154 课** |
| L153 课前试一试 | ✅ 首屏 `她还没来`；选项 `yet`／`already`／`still`；判断卡 `She hasn't come already.` 判对；讲解显示自己的文案 |
| L154 课前试一试 | ✅ 首屏 `她还在等`；选项 `still`／`yet`／`already`；判断卡 `She still is waiting.` 判对 |
| 案件 #162／#163 | ✅ 解锁指向第 153／154 课正确；总案数 **0 / 163** |

**一处观察（非缺陷）**：L153 的 `choose` 选项含 `still`（L154 才教的词）。**经核为既有惯例**——L151 的选项也曾含 `Every`（L152 才教），属「用下一个词做干扰项」的设计。**保留。**

### E5. 尚未做（诚实登记）

1. **L153–L154 的完整通关走查**（只走课前试一试，**未逐题走完六段**）。
2. **F27-A 真实首玩计时**（需真人）。
3. **关 2／关 3 端到端走查**（以服务层实跑代替）。
4. **视觉终检**（未出整页截图）。

---

## §6 携带项

| # | 携带项 | 状态 | 本批处置 |
|---|---|---|---|
| **1** | **研究的逐字引用无抽检机制**（§1.1 新发现） | **新（重要）** | **建议列入研究交付验收步骤**；本批已拦下一处 |
| **2** | **轴 A 关闭** | **已关** | 连押四批后判不成立；**根因：❌will 考点已被 6 课吃掉**（批三十一更正：原记 7 课含 L47，复算不成立） |
| **3** | **cloze 借句漂移**（批二十六登记） | **已复现** | 本批 L154 复现；**严重度仍判低**；**建议批二十八决定是否调整变体写法** |
| **4** | **轴 B（`none`／`nobody`）** | **押后** | **批二十八首选**；瑞思已做逐课规格可参考 |
| **5** | **`ago`** | **新** | 竞析判 B＋（Murphy 初级 U19 `for/since/ago`），**建议与 `since` 配对立项** |
| **6** | **`for + 一段时间`** | **新** | `I have been here for three days.` 是自然表达，我方为零；**是否立项待评估** |
| **7** | **ambush 侧结构性假友好** | 延续 | **观测点**：关 2 回访正确率 |
| **8** | 封面池消耗 | 延续 | 本批取 cover36／37——**单用池 82 → 80**；「三用」策略仍未启用 |
| **9** | `can-do-m*` 零引用（无守门） | 延续 | 本批人工核 ✅（m29 已加） |
| **10** | `src/tmp-prd/` 历史 85 错 | 延续 | `tsc` 判据显式排除 |
| **11** | 干扰项池 42 个非词典串 | 延续 | 本批零新增 |
| **12** | **检查脚本判据**（批二十五／二十六累计 4 次修正） | 延续 | 建议沉淀为工具 |

---

## §7 决策记录

| 时点 | 决策 | 依据 | 可逆性 |
|---|---|---|---|
| D0 | **取轴 C，不取轴 B** | 轴 C 跨源证据更厚（§1.2 理由 2／3） | 中 |
| D0 | **轴 A 关闭候补** | 两研究一致判 0 课 | 中 |
| D0 | **判 B（非 B−）** | 跨源课程位本批最厚 | 低 |
| D0 | L154 改 `city` | 场景轮换（承接 L152 `campus`） | 低 |
| D4 | 保留 L153 选项里的 `still` | 既有惯例（L151 同款） | 低 |

---

## §8 开放问题

1. **研究逐字引用的抽检机制**（§6 携带项 1）——**本批最重要的一条**。
2. **cloze 借句漂移是否调整**（§6 携带项 3）。
3. **`ago` 与 `since` 是否配对立项**（§6 携带项 5）。
4. **`for + 一段时间`**（§6 携带项 6）。
5. **轴 A 关闭后 `unless`／`in case` 长期为零**是否影响内容完整性。

---

## 附录：核查留痕

### A. 交叉验证（本轮脚本复跑）

| 断言 | 结果 |
|---|---|
| **「否定侧下一批再看」在哪一课** | ⚠️ **在 L151 `:28490`**（非 L152 `:28684`）——**批二十七主理人原判「失实」有误，已于 §1.1 更正** ✅ |
| `yet`／`already`／`still` 真零 | `yet` 0／`already` 2／`still` 1（**且那 1 处不是「还在」义**） ✅ |
| L21–L24 的「还没」表达 | **全用 `haven't done`，都没带 `yet`** ✅ 确认为增量 |
| ❌will 规则已教课数 | ⚠️ **批三十一更正为 6 课**（L48／49／109／142／143／144）——原记 7 课含 L47，按课块切分＋剔除注释行复算不成立 ✅ |
| 全库 contrast 配方 | **立岗／切开课＝3 新错＋3 双正解** ✅ |
| 封面 cover36／37 | **各用 1 次**，资产存在 ✅ |
| 案件 8 处错误句 HC 复用 | **连续子序列口径全 0** ✅ |
| 复现红线 | 最高 **6** ✅ |

### B. 测试与实算

| 项 | 结果 |
|---|---|
| `npm test` | **800 通过 / 61 文件** |
| `npx tsc --noEmit` | **0 错**（排除 `src/tmp-prd/`） |
| `npm run build` | **通过** |
| 全库规模 | **154 课 / 163 案 / 27 季** |

---

> 本路线图由产品战略团队 AI 协作生成（主理人自撰），重要决策请由产品负责人审定。
