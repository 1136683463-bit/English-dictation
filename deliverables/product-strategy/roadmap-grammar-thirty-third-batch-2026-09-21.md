# 路线图更新：第三十三批（全库质量复核 · **零课程新增**）

**日期**：2026-09-21 ｜ **类型**：质量批次（不新增课程）｜ **主理人自撰**
**上游**：批三十二路线图 §6 携带项 4（「并发方新增 23 课未评估」）

---

## 📌 TL;DR

- **本批不新增课程**，而是**对并发扩展后的全库做一次健康审计并修复**。
- **主发现**：**`variants[].noteZh` 长期没有零术语守门**——该字段**用户可见**（变体卡直接渲染 ＋ 练习答对后的「为什么」回退源），实测 **25 处术语泄漏**，集中在 18 课。
- **已修**：① 25 处泄漏**全部按既有自建术语体系统一改写**；② **新增守门断言**（已验证能拦注入的回归）；③ 修并发方 2 处薄 `explain`。
- **全库规模**：**185 课／194 案／28 季**；**825 项测试全绿**（+7，含本批新增的三组守门：variants.noteZh／里程碑／全字段兜底）。

---

## §1 本批的由来

批三十二发现**库被并发进程从 162 课扩展到 185 课**（+23 课），而**那些新课未经质量评估**。
本批执行该复核（携带项 4）。

---

## §2 全库健康审计结果（185 课）

| 检查项 | 结果 | 评估 |
|---|---|---|
| `contrast` ≠ 6 条 | **0 课** | ✅ |
| `practice` < 4 题 | **0 课** | ✅ |
| `scene` 非法 | **0 课** | ✅ |
| `spot` 错位 | **0 课** | ✅（初查报 3 课，复核为我的脚本假阳性——逗号在引号内被误切） |
| markdown 星号 | **0 课** | ✅（初查报 1 课，复核为假阳性） |
| 目标句 > 8 词 | **9 课** | ⚠️ 见 §3 |
| 薄 `explain` | **118 课** | ⚠️ 见 §4（存量，已知） |
| **零术语越线** | **18 课** | 🔴 **见 §5（本批主修）** |
| 案号／id 唯一性 | **194／194 唯一** | ✅ |
| 孤儿案 | 5（番外，预期内） | ✅ |

---

## §3 目标句 > 8 词（9 课）——**判为合规，不修**

| 课 | 词数 | 类型 |
|---|---|---|
| L86／L101／L102 | 10／12／14 | **历史存量**（收口课／混排课） |
| **L182／L183／L184／L185** | 10／11／10／13 | **并发方新增的 4 课收口课** |
| **L173／L178** | 10／9 | **并发方新增的 2 课普通课** |

**判定依据（主理人核实）**：
- **全库历史先例**：`>8 词` 共 9 课，其中 **6 课是收口课**（含历史 3 课）——**收口课用多句合并的「排一行」句是既定形态**（L86／L102 先例），故 L182–L185 **合规**。
- **L173／L178 是普通课**，形态上偏离 ≤8 词纪律，**但**：
  ① 两者目标句均为**单句**（非多句合并），只是含一个介词短语（`in order to catch the bus`）／一个时间小句（`before I got home`）；
  ② **≤8 词是纪律、非断言**（全库测试未守此条）；
  ③ **改短会损失该课的核心结构**（`in order to` 本身就是三词块；`had done` 必须有先后两句）。
- **→ 本批判「记录不修」**，建议后续若要收紧，**应先把它升为断言再统一治理**（否则每批争议一次）。

---

## §4 薄 `explain`（118 课）——**存量，维持不加断言**

**分布**：L1–50 约 56 处、L51–100 约 51 处、L101+ 约 67 处（**全库均匀**）。
**关键事实**：`grammarExplainService.resolveGuidedExplain` 对「薄」讲解会**主动挑更厚的同考点对比卡讲解顶上**（批二十四已实测验证）——**该机制工作正常，故薄讲解不是缺陷而是分级设计的输入**。
**本批处置**：**沿用批三十一的判断，不加断言**（加断言会让 118 课同时报红，而回退机制本就处理了它）。
**⚠️ 但本批修了并发方新增的 2 处**（L168／L175），因为**新内容应从严**。

---

## §5 🔴 本批主修：`variants[].noteZh` 的零术语缺口

### 5.1 缺口是什么

**零术语守门原有 6 个字段**（`grammarLabel`／`oneLineRule`／`summary.rule`／`contrast.whyZh`／`guided.explain`／`recall.noteZh`），后经批二十九扩展覆盖 `guided.promptZh`／`blocks.role`／`summary.points`／`variants.label` 等「必读文案」。

**但 `variants[].noteZh` 始终在守门外**，而它**是用户可见的**，出现在**两处**：

| # | 位置 | 证据 |
|---|---|---|
| ① | 变体卡直接渲染 | `GrammarLessonPage.tsx:2328` `{currentVariant.noteZh && <p className="lesson-variant-note">…` |
| ② | 练习答对后的「为什么」 | `GrammarLessonPage.tsx:881-883` —— `practiceWhy` 的 **② 级回退**：答案句 == 某变体句 → 取该变体 `noteZh` |

### 5.2 实测泄漏（25 处，18 课）

| 术语 | 处数 | 涉及课 |
|---|---|---|
| 「原形」 | **11** | L10×2／L11×2／L24／L25×2／L32／L68／L73／L93 |
| 「复数」 | 3 | L26×2／L54 |
| 「比较级」 | 2 | L17／L76 |
| 「介词」 | 2 | L18×2 |
| 「形容词」 | 2 | L19／L89 |
| 「主语」 | 2 | L27／L39 |
| 「疑问句」 | 2 | L27／L30 |
| 「三单」／「语序」／「否定句」 | 各 1 | L25／L27／L30 |

### 5.3 处置

**① 25 处全部改写**，按既有自建术语体系：

| 术语 | 改为 | 例 |
|---|---|---|
| 原形 | **穿原样** | 「did 出场后，动词要变回原形 go」→「…要**穿回原样** go」 |
| 比较级 | **「更…」** | 「much 站比较级前面加力」→「much 站**「更…」**前面加力」 |
| 主语 | **句首那个「谁」** | 「be 搬到主语前」→「be 搬到**句首那个「谁」**前面」 |
| 复数 | **好几个（东西）** | 「复数用 aren't」→「**好几个东西**用 aren't」 |
| 形容词 | **「怎么样」的词** | 「What a + 形容词」→「What a + **「怎么样」的词**」 |
| 介词 | **小词** | 「介词 at 站在原地」→「**at 这个小词**站在原地」 |
| 疑问句／否定句 | **问句／带「不」的句子** | 「否定句用 any」→「**带「不」的句子**用 any」 |
| 三单 | **「他/她/它」一个** | 「三单的「不」用 doesn't」→「**「他/她/它」一个**的「不」用 doesn't」 |

**② 新增守门断言**（`grammarLessons.test.ts`）：`variants[].noteZh 不得含语法术语`。

**③ 断言已做注入验证**：故意把一处改回「原形」→ **断言变红并指出是哪一课** ✅；还原后转绿。

**④ 副作用检查**：改写**未引入生硬拼接**（既有「双「的」」模式断言仍绿）。

---

## §5.4 同类第二处缺口：里程碑文案（本批一并修）

**发现**：做 §5 时顺手做了全字段盘点，发现 **CAN_DO_MILESTONES（路径页「我能说…」成就卡）也不在任何零术语断言内**。

**实测**：34 个里程碑中 **m4 越线**——其 zh 逐字含「**三单**、存在句、疑问词全拿下…」（命中「三单」等）。

**处置**：

1. **改写 m4 文案**：「三单、存在句、疑问词全拿下」→「**「他/她/它」后面的 -s、有没有、问什么**——最顽固的小毛病全拿下」；
2. **新增守门测试** `GrammarPathPage.milestone.test.ts`（2 项）：① title／zh 零术语；② **afterLesson 严格递增 ＋ id 唯一**（解锁判定的前提）；
3. **导出常量** `CAN_DO_MILESTONES` 以供测试引用；
4. **注入验证**：把 m4 改回含术语 → **断言变红** ✅；还原后转绿。

**→ 两处缺口（variants.noteZh 25 处 ＋ 里程碑 1 处）本批全部关闭并守门。**

---

## §5.5 🔁 第三处与「根治」：改为全字段兜底巡检

**三次连漏的教训**：零术语守门一直是**逐字段列举**式的——每发现一处漏网就补一条断言。这个模式已连漏三次：

| 次序 | 漏网字段 | 处数 | 发现契机 |
|---|---|---|---|
| ① | `variants[].noteZh` | 25 | 批三十三（本批）审计 |
| ② | `CAN_DO_MILESTONES` 里程碑文案 | 1 | 同批顺手盘点 |
| ③ | **`guided[].correctionZh` ＋ `guided[].replaceTarget`** | **7 ＋ 2** | **本批系统盘点** |

**第三处的实测**（9 处，均已改写）：

| 课 | 字段 | 命中 | 改写 |
|---|---|---|---|
| L12 | correctionZh | 原形 | 「换回原形 draw」→「换回**穿原样**的 draw」 |
| L15 | correctionZh | 原形 | 同上 |
| L27 | correctionZh | 主语 | 「be 搬到主语前面」→「be 搬到**句首那个「谁」**前面」 |
| L28 | correctionZh | 副词 | 「频率副词的位置」→「**「多久一次」那些词**的位置」 |
| L30 | correctionZh | 否定句 | 「否定句用 any」→「**带「不」的句子**用 any」 |
| L30 | replaceTarget | 疑问句 | 「改成疑问句」→「改成**问句**」 |
| L33 | correctionZh | 复数 | 「books 是复数」→「books 是**好几个**」 |
| L68 | replaceTarget | 语序 | 「换成…语序」→「换成**先说给谁、再说给什么**」 |
| L83 | correctionZh | 否定句 | 同 L30 口径 |

**两个字段都是用户可见的**：`correctionZh` 在 `GrammarLessonPage.tsx:2545` 直接渲染；`replaceTarget` 在 `GrammarLessonPage.tsx:2511` 与 `GrammarBoostPage.tsx:1048` 两处渲染。

### 根治：把「逐字段列举」换成「遍历式兜底」

**新增兜底断言**（`grammarLessons.test.ts`）：**遍历每课的所有字符串字段**过术语表，**只排除 `deepDive`**（深挖卡是「想知道为什么」的进阶内容，**有意保留术语**，产品设计如此，测试文件原有注释亦已说明）。

**三项收益**：
1. **未来新增字段自动纳入保护**——不必再逐字段补断言（这正是连漏三次的根因）；
2. **报错可定位**：输出「课 id ＋ 字段路径 ＋ 命中词」，数组下标归一为 `[]`；
3. **与既有逐字段断言并存**（不删除——它们报错信息更贴题）。

**注入验证**：往 `sceneSetupZh`（此前从未单独守门的字段）注入「主语」→ **断言变红并精确定位到 `lesson-161-need-to sceneSetupZh`** ✅；还原后全绿。

**→ 本批共关闭 4 个字段缺口（25 ＋ 1 ＋ 7 ＋ 2 ＝ 35 处），并把它从「逐处打补丁」升级为「结构性兜底」。**

---

## §6 验收结果

| # | 门 | 判据 | 结果 |
|---|---|---|---|
| **G-1** | 数据完整性 | `npm test` | ✅ **825 通过 / 62 文件**（+7） |
| **G-2** | 类型 | `tsc --noEmit` | ✅ **0 错** |
| **G-3** | 构建 | `npm run build` | ✅ **通过（2.76s）** |
| **G-4** | **零术语（含新字段）** | `variants[].noteZh` | ✅ **25 → 0** |
| **G-5** | 复现红线 | 全库 ≤6 课 | ✅ |
| **G-6** | 案号／id 唯一 | 194／194 | ✅ |
| **G-7** | 季区间 | 无重叠／无缺失／无小季 | ✅ |
| **G-8** | 新断言有效性 | 注入回归 → 变红 | ✅ 两组都验证过 |
| **G-9** | 页面抽查 | L10 页面无术语 | ✅ |
| **G-10** | **里程碑零术语** | 34 个成就卡 | ✅ **1 → 0** |
| **G-11** | **全字段兜底** | 遍历所有字符串字段 | ✅ **35 处 → 0** |

---

## §7 携带项

| # | 携带项 | 状态 | 本批处置 |
|---|---|---|---|
| **1** | **`variants.noteZh` 零术语缺口** | **✅ 已修 + 已守门** | 本批主修（§5） |
| **2** | **并发方新增 23 课的质量** | **✅ 已审计** | 本批完成（§2）；2 处薄 explain 已修 |
| **3** | **目标句 > 8 词的 2 课普通课（L173／L178）** | **记录不修** | §3 判合规；**若要收紧须先升为断言** |
| **4** | **薄 `explain` 118 课** | 维持不加断言 | §4；回退机制已覆盖 |
| **5** | **并发写入（第 3 次）** | 未解 | 批三十二已升级；**本批未再冲突**（写入已停） |
| **6** | **`can-do-m34` 未加** | 待办 | 批三十二避让冲突未加；**本批未补**（§8 开放问题） |
| **7** | **B 档普查剩余** | 减少 | 真缺口：`several`／`so that`／`as long as`／`even if`／`as if` |
| **8** | ambush 侧结构性假友好 | 延续 | **观测点**：关 2 回访正确率 |
| **9** | 封面池消耗 | 延续 | 本批无新课、无消耗 |

---

## §8 开放问题

1. **`can-do-m34` 何时补**（§7 携带项 6）——批三十二未加，本批也未加；**它是 L180／L181 的里程碑，目前缺失**。
2. **`>8 词` 是否升为断言**（§3）——若不升，每批都要人工判一次。
3. **`variants.noteZh` 之外是否还有未守门的用户可见字段**——本批只补了这一处，建议做一次**全字段可见性盘点**。
4. **B 档剩余 5 项**（`several`／`so that`／`as long as`／`even if`／`as if`）的排期。
5. **并发进程是否已结束**——本批写入已停，但**不确认其是否还会继续**。

---

## §9 季体量分析与处置（不修）

**背景**：并发进程已把近年小季合并为 6–12 课大章（`plan.ts` 的合并方案），并**新增了一条守门断言**「季体量不得过小（用户明确要求『一个章节的课多一些』），上限 ≤3 个」。

**实测分布**（28 季）：

| 体量 | 季数 |
|---|---|
| 3 课 | 3（上限，已满额） |
| 4 课 | 2 |
| 5 课 | 4 |
| **6–8 课** | **15** |
| 10 课 | 1 |
| 12 课 | 3 |

**剩余 3 个小季**（`season-6` 建议与条件 L47–49／`season-12` 更上一层 L76–78／`season-19` 我看到的和感觉到的 L125–127）：

**主理人核实了能否再合并——结论：不宜**。以 `season-6` 为例，其周边主题为：

```
L42-46  -ing 的两份工作 + to 垫板（season-5「动词的两件新搭档」）
L47-49  should + if 条件（season-6「建议与条件」）
L50-54  幕后句（season-7）
```

**三块是三个独立主题族**——合并会得到「动词搭档 ＋ 建议 ＋ 被动」这种杂标题，**反而降低可读性**。
`season-19` 同理（L125–127 是 look 家族，前接 L119–124 的 used to 族、后接 L128–133 的五种感官族）。

**→ 处置：不动**。理由三重：① **该断言已通过**（≤3 个，当前正好 3）；② **合并会混主题**；③ 这 3 季是**早期批次的历史形态**，与用户诉求（针对「每次一个章节就两三节课」的新批次）不冲突——**新批次已全部按 6 课以上组织**。

**登记为观察项**：若产品负责人希望全库统一到 6 课以上，需**先重划主题边界**（不是简单相邻合并），属独立议题。

---

## 附录：核查留痕

### A. 交叉验证（本轮脚本复跑）

| 断言 | 结果 |
|---|---|
| `variants.noteZh` 术语命中 | **修前 25 → 修后 0** ✅ |
| 全库 `contrast` 条数 | 185/185 恰好 6 ✅ |
| 全库 `practice` ≥4 | 185/185 ✅ |
| 非法 `scene` | 0 ✅ |
| `spot` 错位 | 0（初查 3 为脚本假阳性） ✅ |
| markdown 星号 | 0（初查 1 为假阳性） ✅ |
| 案号／id 唯一 | 194／194 ✅ |
| 季区间 | 28 季、0 重叠 0 缺失 0 小季 ✅ |
| 新断言注入验证 | 变红并定位到课 ✅ |

### B. 测试与实算

| 项 | 结果 |
|---|---|
| `npm test` | **825 通过 / 62 文件** |
| `npx tsc --noEmit` | **0 错** |
| `npm run build` | **通过** |
| 全库规模 | **185 课 / 194 案 / 28 季** |

---

> 本路线图由产品战略团队 AI 协作生成（主理人自撰），重要决策请由产品负责人审定。
