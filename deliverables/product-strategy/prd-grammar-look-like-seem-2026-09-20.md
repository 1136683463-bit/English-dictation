# PRD：第三十批 · 像什么／好像（`look like` / `seem to` · 2 课 L159–L160）

**日期**：2026-09-20 ｜ **类型**：产品需求规格书（PRD）｜ **主理人自撰**（两研究结论一致）
**上游**：批二十九路线图 §6 携带项 ＋ 本批两研究（瑞思／竞析，2026-09-20）＋ **L127 `:24406` 的库内许诺**

| 版本 | 日期 | 说明 |
|---|---|---|
| v1 | 2026-09-20 | 初稿：**2 课**（L159–L160）＋2 案（#168–#169），开 season-30、m32；**档位 B＋**；**摘钩子**（L127 埋的「以后再说它」）；**本批另修一处连续推迟四次的技术债**（§5） |

---

## §1 批次定位

### 1.1 本批的决定性依据：L127 埋的「以后」

**L127 `:24406` 逐字**（收口课深挖卡）：

> 「还有一句 It looks like rain.（看着要下雨）——那是这张脸再往后走一步说「像什么」。**今天只认脸，不学新花样——以后再说它。**」

**→ 库里已明文许诺「以后再说它」，本批就是那个「以后」。**

### 1.2 瑞思的关键切分判定（本批的选题基础）

**瑞思本轮推翻了自己之前的推荐方向**，给出一个精确切分：

| 形态 | 判定 | 理由 |
|---|---|---|
| `seem + 形容词` | ❌ **不可做** | 中文必然译成「看起来」，而 **`看起来` 68 处／`看着` 86 处已被 L125–L133 九课占满**；L125 `:23981` 逐字把「看起来」钉死在 `look` 上（「It 【looks】 nice 是「我看到的它」」）——**再做就是自相矛盾** |
| **`look like`** | ✅ **可做** | **零造词**（`look` 247／`looks` 261／`like` 537 全在库）；**且 L127 已埋许诺** |
| **`seem to + 动词`** | ✅ **可做** | `look` 结构上做不到；中文入口「好像」是三口径 **0** 的唯一干净入口 |

**竞析独立复核同意该切分**，并给出更硬的跨源证据：**Cambridge `Seem` 页的 HTML 结构为 `h1 Seem` ＋ 两个平级 `h2`**（`Seem as a linking verb`／`Seem + to-infinitive`）——**上游自己就把这两个形态当两个教学点**。

**→ 裁决：L159 做 `look like`（像什么），L160 做 `seem to`（好像）。**

### 1.3 批次定位：档位 B＋

| 项 | 本批 | 对照 |
|---|---|---|
| **新造词** | **1 个**：`seem`（`look like` 零造词） | 批二十九 2 个 |
| **跨源档位** | **`look like` B（零造词）／`seem to` B＋** | 本批取 B＋ |
| **机制复用** | **1 处**（`to` 垫板——L15 `want to go` 的**第 2 次应用**） | — |
| **库内许诺** | **1 处**（L127 `:24406`） | — |
| 场景侧造词 | **0 个** | — |

---

## §2 逐课规格

**L159 立岗（`look like`：像什么要多一个 like）**

| 项 | 内容 |
|---|---|
| 课注 id | `lesson-159-look-like`；`number: 159`；episode「小美的一天 一百五十九」；scene `ocean`；cover `cover42` |
| title / grammarLabel | 「像一条船」/「看起来像 · look 后面跟 like」 |
| targetSentence | `It looks like a boat.`（5 词 ✅） |
| 场景 | 海边的防波堤，两个人抬头看云——「你看，像不像一条船？」 |
| 新知识点 | **只有一件**：说「像什么」要在 `look` 后面**多请一个 `like`** |
| oneLineRule | 「说「看起来像」：look 后面要请 like 出场——It looks like a boat（它看起来像一条船）。第 125 课那个 look 后面跟的是「怎么样」（looks nice）；今天跟的是「像什么」，中间多一个 like。」 |
| 对比卡 6 条方向 | ① `It looks a boat.` ❌（**`looks`**，word_order——**少了 like 说不通**）② `It like a boat.` ❌（**`like`**，word_order——**look 自己也要在**）③ `It looks like boat.` ❌（**`boat`**，article——**a 不能丢**）④ 双正解（L125）：`The sky looks dark.` ✅ ⑤ 双正解（L126）：`You look tired.` ✅ ⑥ 双正解（L15）：`I want to go.` ✅（**同一个「小垫板」位置**） |
| 变体三态 | 肯定 `It looks like a boat.`／否定 `It doesn't look like a boat.`／疑问 `Does it look like a boat?` |
| 复现题设计 | 第 1 题 target＋变体逐字题；第 2 题复现 L126 `You look tired.`；第 3 题复现 L15 `I want to go.`；第 4 题复现 L158 |
| 案件规划 | `hunt-looks-like-boat`（#168） |

**L160 立岗（`seem to`：好像要垫个 to）**

| 项 | 内容 |
|---|---|
| 课注 id | `lesson-160-seem-to`；`number: 160`；episode「小美的一天 一百六十」；scene `lighthouse`；cover `cover43` |
| title / grammarLabel | 「他好像认识你」/「好像 · seem 后面跟 to」 |
| targetSentence | `He seems to know you.`（5 词 ✅） |
| 场景 | 灯塔下的石阶，等摆渡船；远处走来一个人朝这边招了招手 |
| 新知识点 | **只有一件**：说「好像」用 `seem`，**后面请 `to` 垫一下**；`to` 后面穿原样 |
| oneLineRule | 「说「好像」用 seem，后面请 to 垫一下——He seems to know you（他好像认识你）。to 后面那个动作穿原样：to know，不换形状。」 |
| 对比卡 6 条方向 | ① `He seems know you.` ❌（**`seems`**，word_order——**少了 to 连不上**）② `He seems to knows you.` ❌（**`knows`**，verb_form——**to 后面穿原样**）③ `He seem to know you.` ❌（**`seem`**，sv_agreement——**「他」一个要带 s**）④ 双正解（L159）：`It looks like a boat.` ✅ ⑤ 双正解（L15）：`I want to go.` ✅（**同一个 to 垫板**）⑥ 双正解（L126）：`You look tired.` ✅（**眼睛看到的 vs 心里猜的**） |
| 变体三态 | 肯定 `He seems to know you.`／否定 `He doesn't seem to know you.`／疑问 `Does he seem to know you?` |
| 复现题设计 | 第 1 题 target＋变体逐字题；第 2 题复现 L159；第 3 题复现 L15；第 4 题复现 L157 |
| 案件规划 | `hunt-seems-to-know`（#169） |

**⚠️ 场景选择**：**L159 用 `ocean`、L160 用 `lighthouse`**——**这两个场景在全库此前都是 0 课**（实测：`ocean`／`desert`／`space`／`lighthouse` 各 0 课）。既换空气，又用上了从未用过的插画。

---

## §3 与已教内容的切分专章

| 最近 | 切开点 |
|---|---|
| **L125 `It looks nice.`** | `look` 后面跟**「怎么样」**（形容词）；本批 L159 跟**「像什么」**（`like + 东西`） |
| **L126 `You look tired.`** | 是**眼睛看到的**（说出来像报事实）；L160 的 `seem` 是**心里猜的** |
| **L127 收口** | 明写「以后再说它」——**本批就是那个以后** |
| **L15 `I want to go.`** | `want` 后面的 `to` 是本课 L160 的**同一个垫板** |

---

## §4 中文负迁移处理专章

| 课 | 典型中式错句 | 设计含义 | 归入 |
|---|---|---|---|
| L159 | `*It looks a boat.` | 中文「像一条船」的「像」容易被省掉 | 对比卡 ①（word_order）**主靶** |
| L159 | `*It looks like boat.` | 中文不说「一条」，`a` 容易漏 | 对比卡 ③（article） |
| L160 | `*He seems know you.` | 中文「他好像认识你」中间没有介词 | 对比卡 ①（word_order）**主靶** |
| L160 | `*He seems to knows you.` | 学生以为「他」是三单，后面也要加 s | 对比卡 ②（verb_form） |
| L160 | `*He seem to know you.` | 学生被 `to` 分散注意，忘了主词要带 s | 对比卡 ③（sv_agreement） |

---

## §5 **本批另修一处连续推迟四次的技术债**（生产实录见路线图 §5）

### cloze 借句漂移（批二十六首报 → 批二十七／二十八／二十九连续登记，四次未决）

**问题**：tier-1 的「选词填空」从 `variants` 抽句出题。**实测 23 课**的肯定变体**不是本课目标句，而是别的课的老句子**（这是有意的教学对照，如 L146 的肯定态用 L145 的 `I like tea too.` 来摆「两张脸」）。抽中它时就变成**在考上一课的词**——实测 L157 抽中 `All the books are good.` 的 `All`、L158 抽中 `Someone is at home.` 的 `home`，**本课考点落空**。

**处置（本批执行）**：把这类句子从 cloze 候选池剔除。

**验证（全库）**：
- **0 课**因此丢失 cloze；
- **0 课**题型跌到 4 类以下；
- **0 处**残留借句落点。

**→ 四次登记的债，本批关闭。**

---

## §6 验收标准

### 6.1 检查清单（G1–G16）

- [ ] **G1** practice ≥4 题且含变体逐字题
- [ ] **G2** tokens/answer 词集一致；distractors 不重复答案词
- [ ] **G3** recall 三字段非空
- [ ] **G4** 目标句 ≤8 词（5／5）
- [ ] **G5** 罪名 10 枚举内
- [ ] **G6** 案件：4 错、单 token 可修、≥1 净词、`reviewed: true`、tokenIndex 对齐
- [ ] **G7** **`contrast` 每课恰好 6 条**（3 带标记 ＋ 3 双正解）
- [ ] **G8** 展示层：`season-30` `{min:159,max:160}` ＋ `can-do-m32`（afterLesson 160）
- [ ] **G9** orphans：2 案全被引用；episode「一百五十九」…「一百六十」
- [ ] **G10** cloze 逐课实跑登记
- [ ] **G11** `scene` 合法（全库 0 违规，本批用 `ocean`／`lighthouse`）
- [ ] **G12** 摘钩子：L127 `:24406` 埋的那句必须在本批兑现
- [ ] **G13** 复现取材逐句脚本核验（**`It looks nice.` 只剩 1 课头寸，本批不进 practice**）
- [ ] **G14** 零术语 ＋ `guided.explain` ≥20 字
- [ ] **G15** 旧线零回归：L1–L158 不动
- [ ] **G16** **cloze 借句漂移已关闭**（全库 0 残留）

### 6.2 G-boost 硬护栏

每课 contrast 6 条中 **≥2 条带 `wrongMark`**（本批 3 条）；`wrongMark` 不得为纯标点；`guided.spot` 的 `wrongToken` 与 tokens **逐字相等**。

---

## §7 案件规划（2 案 · #168–#169）

| 案件 id | # | 配课 | 错误构成 |
|---|---|---|---|
| `hunt-looks-like-boat` | 168 | L159 | ① `boat.`→`a boat.`（article，新）② `looks`→`looks like`（word_order，新）③ `want`→`wants`（sv_agreement，旧 L25）④ `go`→`went`（tense，旧 L10） |
| `hunt-seems-to-know` | 169 | L160 | ① `seems`→`seems to`（word_order，新）② `knows`→`know`（verb_form，新）③ `bag.`→`bags.`（plural，旧 L11）④ `is`→`are`（sv_agreement，旧 L7） |

**案件纪律**：旧错句全部实测零复用（连续子序列口径）。

---

## §8 Non-goals

- **不做 `seem + 形容词`**——中文「看起来」已被 L125–L133 九课占满（§1.2）
- **不做 `appear`**——竞析判 B 但**课量 0**（跨源逐字写成可与 `seem` 互换），**只作认读位**
- **不做 `would rather`**——B−，本批不做（竞析三条跨源理由：真切分口是「偏好 vs 建议」，而我方 L47 `should` 已占建议轴）
- **不做 `each`**——**判关闭本体**（理由：容量账，不是无价值）
- **不做 `It seems that…`／`there seems to be`**——超出零基础承载
- **不改 L1–L158**（除 §5 的引擎修复）；**不引番外 5 案**；**不新增封面**

---

## §9 开放问题

1. **`appear` 的认读位**：本批未设；**是否要像 L109 `as` 那样埋一个认读种子**待定。
2. **研究报告的「逐字引用」抽检**：批二十七已登记为机制项，**仍未落地为工具**。
3. **`would rather`／`each` 是否正式移出候补池**——两研究本轮都判不做，**建议正式关闭**。
4. **配方的表述更正**：批二十九文档曾写「全库 3 标记+3 双正解」，**实测严格符合的只有 59 课**（另有 `6m+0b` 41 课／`2m+4b` 36 课等）——**已在路线图更正**。
5. **cloze 修复的观察**：本批关闭该债后，**关 3 的题面质量是否提升**待观察。

---

> 本规格书由产品战略团队 AI 协作生成（主理人自撰），重要决策请由产品负责人审定。
