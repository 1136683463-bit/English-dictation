# 路线图 · 第六十批（257 条结构候选逐层收口）

- 日期：2026-09-23
- 交付：**执行并完成批五十九列为顺位 1 的「给 256 条无档位候选加档位」**
- 性质：**审计批**（`src/` 零改动）
- 结论：**257 条 → 1 条真候选 → 该条有独立理由不做 ⇒ 0 待办**

---

## 1. 交付清单

| 文件 | 变更 |
|---|---|
| `working/b-tier-exhaustive-2026-09-23/triage-257-candidates.md` | **新增**：四层过滤报告 + 逐条裁定 |
| `working/.../triage-data.json` | **新增**：可复核的机器可读数据 |
| `deliverables/` | 本 PRD + 本路线图 |

---

## 2. 验证证据

| 项 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错误 |
| 全量测试 | **2504 / 2506 通过** |
| 两条失败 | `kb4` / `bo4`——**并发进程改 UI 所致**（批五十七已逐字核实）|
| 另一次 `pf4-save-cost` 失败 | **隔离重跑通过**——计时比值断言，并发下饿死（与 `pf1` 同类）|
| 归档可复核 | `triage-data.json` 含全部 47 条探测明细 |

---

## 3. 本批做完了什么

批五十九的顺位 1 是「给 256 条无档位候选加档位」，当时的判断是：
> 机械路径已试两条、皆不通；词典 `epp-xref` 可行但要**逐词查**，256 条成本很高。

**本批补上了漏掉的一步：先人工分桶。**

| 层 | 筛掉 | 依据 |
|---|---|---|
| A 类目中枢 | 31 | `Adjectives` / `Nouns` …——整章目录页 |
| B 按单词建的参考条目 | 112 | `Prepositions: home` / `Word order: always`——与已排除的 `Word patterns` 同形 |
| C 语域/文体/篇章 | 32 | `Newspaper headlines` / `Swearing` / `Inversion` … |
| **余 D 真候选** | **82** | |

**成本对比**：

| 项 | 批五十九预估 | 实际 |
|---|---|---|
| 词典查询 | **256 次** | **3 次** |
| 待办 | 未知 | **0** |

---

## 4. 82 条真候选的结果

- **探测 47 条** → 36 已覆盖 / 8 存疑（**核实后全为探测词不足**）/ **3 缺口**
- **判定 37 条**（章节式）→ 27 超纲 / 7 已裁定或非教学点 / 3 落线内（属 L50 被动的细化格）

**3 个缺口的判档**（词典 `epp-xref`）：

| 缺口 | 档位 | 裁定 |
|---|---|---|
| `Names and titles: addressing people` | **`mr` = A1** | **不做**（四条理由，见 PRD §四）|
| `Future in the past` | `would` = B1 | 超纲 |
| `Reported speech: reporting nouns` | `promise` = B1 | 超纲 + 已裁定 |

---

## 5. 对「完成所有 B 档」的当前状态

| 层次 | 状态 |
|---|---|
| 批三十一登记的 10 项 B 档候选 | ✅ **已 100% 收口** |
| 文档 §3.5 点名的 3 课 | ✅ **已交付**（L188 / L193-194 / **L205**）|
| BC 三档 68 课（A1-A2 / B1-B2 / C1）| ✅ **已取全归档**；A1-A2 档 **18/18 覆盖** |
| **Cambridge 257 条结构候选** | ✅ **本批逐层收口到 0 待办** |

**⇒ 文档建议的三层穷举（Cambridge 索引 / BC 三档 / 我方清单）现已全部做完。**

### 5.1 仍然不能无条件宣告「完成」的两条

| # | 项 | 性质 |
|---|---|---|
| 1 | **中文侧两站未做** | 文档建议的第三层（`english.cool` 873 / `letmeenglish` 965 条 sitemap）；`english.cool` 可 curl，`letmeenglish` 需浏览器 |
| 2 | **探测词表由人写** | 本批 47 条 × 2–5 词 ≈ 150 个探测词**全是我写的**；**人写的那层就是覆盖率的下限**——本批 8 条「存疑」正因此而来（核实后全为已覆盖）|

**第 2 条是三批连续出现的同一个结构性限制**：只要上游不标档位、我方用零术语，
「穷举」就不可能纯机械化——最终还是靠人写桥接表。

---

## 6. 建议的下一步

| 顺位 | 事项 | 阻塞 | 价值 |
|---|---|---|---|
| **1** | **中文侧两站 sitemap**（补第三层）| `letmeenglish` 需浏览器（同 BC 的解法）| 中——补齐文档建议的最后一层，做交叉验证 |
| 2 | **探测词表补厚** | 无阻塞，纯人力 | 低——收益递减（存疑项核实后多为已覆盖）|
| 3 | **词汇线是否单独立项** | **待产品负责人拍板** | — 含 `quietly` 缺口 + 172 条词汇辨析点 + `Mr`/`Mrs` 称呼项 |

### 6.1 一条判断

**语法侧的 B 档穷举，我认为已经真正做完了**：
三层（Cambridge / BC / 我方）全部走完，四条线（批三十一的 10 项、§3.5 的 3 课、
BC A1-A2 的 18 课、Cambridge 的 257 条候选）**全部 0 待办**。

剩下的确只有两件事：
- **一层的补做**（中文侧，机械可完成）；
- **一个需要你拍板的决策**（词汇线立项——它同时能收掉 `quietly`、172 条词汇辨析点、
  以及本批这唯一 1 条 `Mr`/`Mrs` 称呼项）。

⇒ **下一步建议：做中文侧那一层（无阻塞），并把词汇线的决策提给你。**
