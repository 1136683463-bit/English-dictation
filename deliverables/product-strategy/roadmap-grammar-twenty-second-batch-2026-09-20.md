# 路线图更新：第二十二批课程（L139–L141 虽然下雨了，我还是要出去 · **3 课小章 · 单拱 · B 档**）

| 元信息 | 内容 |
|---|---|
| 标题 | 路线图更新：第二十二批 L139–L141 排期、F22-A 打样门与 F22-B 上线观察门（**`although` 让步与转折 · 3 课小章 · 单拱 · 开 season-22 ＋ m24 · B 档结清后的第一个新选题**） |
| 日期 / 执笔 | 2026-09-20 ｜ 路径（路线图规划师） |
| 类型 | 路线图更新（衔接主路线图与第五–二十一批路线图；**不动其锁定项与 F6-B–F21-B 判据**） |
| 关键输入 | **`prd-grammar-contrast-2026-09-20.md`（析客 PRD，已定稿 §1–§11／823 行，本批排期唯一依据）**；三研究 2026-09-20（`user-research-grammar-twenty-second-batch-2026-09-20.md` 瑞思 §4 主骨架 / `data-audit-grammar-twenty-second-batch-2026-09-20.md` 数析 §1 缺口盘点＋§5 cloze / `competitive-analysis-grammar-twenty-second-batch-2026-09-20.md` 竞析 §4.1／§5 复现型章／§7 口径校正）；`roadmap-grammar-twenty-first-batch-2026-09-20.md`（格式基准）；**主理人裁决：批二十二＝「让步与转折」3 课 L139–L141、单拱、B 档；三研究分歧处取瑞思（`although`）、不取竞析（`as soon as`），`as soon as` 顺位为批二十三首选** |
| 主交付物 | L139–L141 三课 ＋ 案 #148–#150 ＋ season-22 / m24（展示层，~13 行） |
| 本批特殊点 | ①**B 档系列结清后的第一个新选题**（批二十一路线图 §6.2 原话：「候补池里已无 B 档及以上项目……须由新一轮选题研究产生」）；②**跨源课程位最硬**（BC B1-B2 第 6 课 `Contrasting ideas: 'although', 'despite' and others` ＋ Murphy 中级 U113 标题逐字 `although though even though in spite of despite`）；③**我方真零**（`although` 0/0；`though` GL 0／HC 2 且 2 处全在案 #7 错句里）；④**先考后教倒挂转正**（案 #7 的 `Though + but` 判例挂 L12，全库 138 课没教过）；⑤**与 L19 `but` 切开**（本批脊柱二）；⑥**中文「虽然…但是…」成对负迁移**是全库唯一「已被案件显式植错却无课教过」的结构；⑦**cloze 假友好**（关 2 回马枪侧考点词成空位率 0%，21/21 全落 `is`／`was`／`will`）；⑧**零术语头号陷阱＝「从句」**（在 29 词表内，而 `although` 的语言学描述正是「引导让步从句」）；⑨**`run_on` 三案连用是本批破例项**（全库最薄档 14 处）；⑩**复现取材红线头寸只剩 1–4 次**（瑞思本轮方法学发现）；⑪**L1–L138 一字不动**（硬红线，认领 ≠ 编辑） |

---

## 📌 TL;DR

1. **裁决：即刻开工**——无 P0 阻断项；**3 课＋3 案＋展示层（~13 行）＋打样＋验收 ≈2.0 人日**（D1–D6，含滑移吸收位）；两段式（段一 L139–L140／段二 L141）。**五道防御＋合并关账**：**F22-A 打样门**（L139 首玩）→ **中段自走查点（本批定在 L140：与 L19 `but` 的切开）** → **收口复点（L141：两个说法排一行）** → **展示层闭环**（D5）→ **全量验收**（D6）→ **F22-B 上线观察门**（1–2 周；**F9-B–F22-B 十四批一次读齐**，**含批二十三候补清单**）。
2. **3 课单拱「虽然下雨了，我还是要出去」**：`although` 立岗（L139 `Although it is raining, I will go out.`）→ **与 L19 `but` 切开**（L140 `It is raining, but I will go out.`）→ 零新知收口（L141 两个说法排一行）。开 **season-22 `{139,141}`＋m24（afterLesson 141）**，批量 **141 课、150 案**。
3. **本批是 B 档系列结清后的第一个新选题**——**旧候补清单一条不引**，全部由三研究重新论证。**「B 档结清」指的是旧清单结清，不是档位消失**（批二十一路线图原话「须由新一轮选题研究产生」正是本批的动作）。**同时采纳**：**`as soon as` 列为批二十三首选**（瑞思 §3.1 逐字「时间家族第六格不会跑掉；而『先考后教』倒挂每多挂一批就多一次教学债」），并把与 L109 的形式冲突登记为**携带项**（§6.3 序 1）。
4. **主理人裁决：取瑞思的 `although` 3 课，不取竞析的 `as soon as` 3 课**。**三条理由**：① **瑞思的「先考后教」论据已核实成立**（案 #7 `hunt-because-so` 挂 L12，第 2 处错 `but`／`correction: "去掉 but"`／explanation 逐字「同样的道理：Though 和 but 不能同时出现，留一个就够」；而 L12 是 `will 将来时`——**学生在第 12 课就判过这道题，全库 138 课没教过**）；② **竞析自己也指出 `as soon as` 的硬伤**——我方时间家族已有五格（`when`／`after`／`before`／`until`／`while`），第六格的语义差**只有一刻度**，增量真实但小于 `although`（整个让步结构族都没有）；③ **`as soon as` 有跨课形式冲突**——L109 `:20328` 把 `I waited until the rain stops.` 明设为错项，而 `as soon as` 的正确用法恰是 `stops`（现在版表将来），需额外切开设计。
5. **档位＝B（诚实标注，不得写成 A 档）**：**须新造 `although`（GL 0／HC 0）＋`though`（GL 0／HC 2，2 处全在案 #7 错句里、不构成「已教」）两个词**；**场景侧零造词**（`raining` 98／`rain` 103／`go out` 35／`stay at home` 21／`cold` 267／`happy` 124／`tired` 155／`snow` 17 全在库）。→ **对外一律写「B 档：跨源有课程位级专课（BC B1-B2 第 6 课）＋ 有专单元（Murphy 中级 U113），但我方两文件真零、须造两个词」**；**不得**写「A 档」、**不得**写「两文件全 0」（会让生产单漏掉 `though` 的 2 处占用的对撞风险）、**不得**写「库内有现成句子」。
6. **本批的脊柱一＝先考后教倒挂转正**（照 L78→L92 `:17175`／L133→L134 `:25220` 的「认读升格」成例句式）；**脊柱二＝与 L19 `but` 切开**（L140 整课承担）。**⚠️ 本批与前两次转正的差异：种子位是「已判错」不是「认读」**——**L139 开场话术必须把「错的是多出来的 `but`，不是 `Though` 本身」说清**（生产期不得省此半句）。
7. **零术语红线：本批头号高危词＝「从句」**（在 `grammarZeroTerms.ts:17-27` 的 29 词表内，逐字实读）——而 `although` 的语言学描述正是「引导让步**从句**」。→ **全批改写成「Although 后面跟一整句」「Although 站最前面」**；**「连词」不在 29 词表内**（文件注释逐字：「不包含『动词 / 名词 / 连词 / be 动词』」）**且 L19／L20 已在用**，**但本批定稿不用它**（用「站最前面」把注意力引向站位而非词性）；**「让步」「主句」「转折」一并禁**（分析用语，非机检覆盖，人工核）。
8. **cloze「假友好」是设计前提**（PRD 独立复刻 `pickClozeWord` 实跑，与数析交叉验证）：**`although`／`though`／`but`／`so`／`raining`／`rain` 六词全部不在 `GRAMMAR_WORDS` 表内**（实读 `:160-186`；`but` 另在 `CLOZE_STOP_WORDS` 内），故**关 2 回马枪侧考点词成空位率 0%（21/21 全落 `is`／`was`／`will`）**。→ **考点承载一律压在 `contrast`（每课 6 条、3 条带 `wrongMark`）与 `guided.spot`（每课 1 道）**；**boost 侧三课课课有保障句（全批 4 处落考点词 `Although`），是「种子运气」不是引擎保证，落盘后须复跑**（§4 G12）。**⚠️ 另一条紧约束（瑞思本轮方法学发现）**：`grammarLessons.test.ts:163-178` 要求**同一练习句最多出现在 6 课**，**当前头寸只剩 1–4 次**（本路线图独立复算：`yesterday i went to the park` **已用满 6 课**／`i like reading` 5／`there is a book on the desk` 5／`i am happy` 5／`i am used to getting up early` 5／`it looks nice` 5／`he drinks milk every day` 4）——**本批涉及的三句头寸充足**（`The snow is cold but fun.` 1／`It was cold, so I stayed at home.` 0／`It was windy, but we were happy.` 0），**但生产期必须逐句核验，不得超限**（§5 风险 1／§4 G11 第 ⑥ 项）。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|---|---|
| 推荐方案 | 即刻开工：L139 打样（D1–D2，含 F22-A 门）→ L140 切开（D3，含**中段自走查点**）→ L141 收口＋展示层（D4–D5）→ 预检（D5）→ 验收（D6）→ F22-B 观察 |
| 优先级 | P1（内容生产批次）；**无 P0 阻断项** |
| 预期影响 | **B 档结清后的第一批**；本项目**第一次把「反着说」的结构正面立起来**（此前只有 L19 的 `but` 一条路）；**还清全库唯一一笔「先考后教」教学债**（案 #7 → L139）；**中文「虽然…但是…」这条最高频的成对负迁移第一次被正面拦截**；批量 141 课、150 案 |
| 资源需求 | ≈2.0 人日（内容 1.6 ＋ 展示层 0.2 ＋ 走查 0.2）；两段式；上线后 1–2 周观察 |
| 风险等级 | 中（① **复现取材红线头寸只剩 1–4 次**（本批三句头寸充足，但机制上是全库级紧约束）；② **cloze 假友好**——考点词成空位率 0%；③ **与 L19 `but` 的混淆**——化解＝L140 整课切开；④ **零术语头号陷阱＝「从句」**；⑤ **`run_on` 三案连用的观感**；⑥ **`although` 造词成本**（新词无搭配位）；⑦ 封面图像语义未二次目视；⑧ 测试基线漂移；⑨ **八批未提交的单点风险**） |
| 硬性范围红线 | 课量 **3 不扩不缩**（**第 4 课只能靠 `despite`／`in spite of` 换词，不是新结构**）；一课一增量；**L141 零新知**；六段式；每课 1 案（4 错＝新 2＋旧 2、单 token 可修）；对比卡 6 条（**≥2 条带 `wrongMark`，本批 3 条**）；零术语词表 29 逐课自查（**机检**）＋**本批头号红线八词**：**「从句」「状语」「语序」在 29 词表内**，**「让步」「主句」「转折」「连词」「门牌」为人工核的额外禁用**（§4.5 另列 12 词自查表）；comparison 不进案件；tagStats=10 不动；**L1–L138 一字不动**（含案 #7 一字不改）；番外 5 案冻结；covers 池内取（cover22–cover24，**解唯一**）；**禁用 `even`／`despite`／`in spite of`／`though`（全批零出现）**；**不得写 `although` 引导「从句」**；**不得写「第 12 课已经教过」** |

---

## §1 批次定位与形态决策

### 1.1 形态裁决：单拱 · 3 课小章（B 档结清后的第一个新选题）

**批二十二要回答的是**：B 档没了之后，接下来开什么。

**答案（主理人已定，不重新论证）**：**开一条新的高价值结构线——「让步与转折」，档位 B，3 课单拱。**

`批十八（`be used to`，6 课）→ 批十九（`look + 那个「怎么样」的词`，3 课）→ 批二十（五种感官，6 课）→ 批二十一（`look forward to`，5 课）→ **B 档系列结清**（批二十一 PRD §1.1 逐字：「本批交付后批二十二的候补清单里将不再有 B 档及以上项目」）。`

**「B 档结清后为什么还能开出 B」——须明写的一条口径**：批二十一的「结清」指**旧候补清单结清**，不是「B 档这个档位不存在了」。批二十一路线图 §6.2 原话逐字：「**候补池里已无 B 档及以上项目……批二十二的选题不能再沿用本清单，须由新一轮选题研究产生**」——**「须由新一轮研究产生」正是本批的动作**。本批三研究**不引用任何旧候补清单**，从上游四源重新取数：

| 项 | 上游（逐条，实读／未核实分标） | 我方现状 | 档 |
|---|---|---|---|
| **`although`／`though`（本批）** | **BC B1-B2 第 6 课逐字＝`Contrasting ideas: 'although', 'despite' and others`**〔竞析 §1.1 BC-2 本轮直连实取〕＋**Murphy 中级 U113 标题逐字＝`113 although though even though in spite of despite`**〔瑞思 §1.7 从本机 PDF 原件逐单元抽出〕＋**中文侧四个 slug**（`/although-despite/`／`/although/`／`/despite/`／`/even-though/`）〔瑞思 §1.7④，站点地图层面，**未读正文**〕 | **`although` GL 0／HC 0；`though` GL 0／HC 2（2 处都在案 #7 错句里）；`even though` 0/0；`despite` 0/0；`in spite of` 0/0；`even` 单词本身也 0/0** **〔本路线图独立复验，§附录 B〕** | **B** |

> **一句话（生产单可抄）**：「**B 档结清指的是旧清单结清——本批是新一轮选题研究产生的新项目：`although` 跨源有课程位级专课（BC B1-B2 第 6 课）＋ 有专单元（Murphy 中级 U113），但我方两文件真零、须造 `although`／`though` 两个词，故对外一律写 B 档，不得写成 A 档。**」

**为什么是 3 课（三条理由，逐条）**：

| # | 理由 | 实读依据 |
|---|---|---|
| ① | **增量恰好 3 格，每格都是真增量**：**立岗**（`although` 上线 ＋ 「Although 站最前面领一整句」的位置感）／**切开**（`Although` 站最前面 对 `but` 站中间——**这一格是 L19 从未讲过的轴**）／**收口**（零新知，两个说法排一行） | PRD §1.1／§2.1；瑞思 §4.4 |
| ② | **不取 2 课**：**切开位是本章唯一的独立价值**——若不切开，`although` 与 L19 的 `but` 在学生眼里就是**同一件事的两种写法**，L139 会退化成「L19 的换词课」，**这正是「一课一增量」被破的形态** | 瑞思 §4.4 第 2 条逐字 |
| ③ | **不取 4 课**：第 4 课的唯一可能来源是 **`despite`／`in spite of`**（本路线图实读：两者 GL 0／HC 0；BC 第 6 课标题含 `'despite'`；Murphy U113 含 `in spite of despite`）——**但它们是 `although` 的同义换挡，不是新结构**；**且 `despite` 后面跟名词、不跟小句子**（`despite the rain` 对 `although it is raining`），教它等于在同一批里再开一个「后面跟什么」的新格，**会把一课变两课** | PRD §2.1；瑞思 §4.4 第 3 条 |

**课量红线**：**3 不扩不缩**。**与近期批次对照**：批十八 6 课／批十九 3 课／批二十 6 课／批二十一 5 课。**本批 3 课＝与批十九同量，是本项目「小章」的既有形态**（本路线图实读 `grammarSeasons.ts` 21 季的章长清单＝ `12,12,10,7,5,3,5,6,6,5,4,3,8,8,8,8,8,6,3,6,5`，**第 22 季＝本批，长度 3**；此前 3 课季＝ season-6 `{47,49}`／season-12 `{76,78}`／season-19 `{125,127}`）。

**单拱声明**：全批**一条「虽然下雨了，我还是要出去」线**，**不折入他章、不并他项**；**不取双拱**（批十四破例一次后，**批十五至二十二连续第九次单拱**）。

### 1.2 三研究分歧与主理人裁决记录（取瑞思、不取竞析）

| 方案 | 三研究结论 | 主理人裁决 |
|---|---|---|
| **甲·「让步与转折」3 课 L139–L141（`although`）** | **瑞思 §0 推荐（首选）**；数析 §1.1 逐词盘点确认**真零成立**；竞析 §8 判据表亦承认它是「跨源四层全有 ＋ 课程位最硬」的项 | **✅ 拍板：甲 3 课单拱** |
| **乙·「雨一停我就出去」3 课（`as soon as`）** | **竞析 §4.1 首选**（判 B）；瑞思 §0 列为**备选 1**（判 B） | ❌ **本批不做**（**顺位为批二十三首选**，§6.2） |
| 丙·`seem`／`appear` 3 课 | 瑞思 §1.3 判 **C 档、不成章**；竞析 §4.3 列备选 A（判 C＋） | ❌ 不做 |
| 丁·`neither/either/both` | 瑞思 §1.6 判 **B−**（造词 4 个超先例上限）；竞析 §4.4 列备选 B（判 B−，成本四候选最高） | ❌ 不做 |
| `would rather` 2 课 | 瑞思 §1.4 判 **B−**（2 课封顶、无收口位、中文侧零专文） | ❌ 不做 |
| 纯复现型大章（方向 1） | 瑞思 §2.3／竞析 §5.3 一致判 **不做**（跨源无先例 ＋ 无数据字段承载 ＋ 我方已有 7 条复现通道） | ❌ 不做 |
| 换轴（语法+读写／听力） | 瑞思 §2／竞析 §6.3 一致判 **不做混合课**（BC 六栏目分离；我方语法线与冒险阅读零耦合） | ❌ 不做 |

#### 「取瑞思不取竞析」的三条理由（主理人独立核实，逐条写清）

**① 瑞思的「先考后教」论据已核实成立（本批最硬的一条）。**

- **事实链（本路线图逐行实读 `huntCases.ts` ＋ `grammarLessons.ts` 复核，行号精确）**：案 #7 `hunt-because-so` 在 `:328`；第 2 处错在 `:361-367`，逐字为 `tokenIndex: 14`／`tag: "run_on"`／`original: "but"`／`correction: "去掉 but"`／`explanation: "同样的道理：Though 和 but 不能同时出现，留一个就够。"`（`:366`）；该案 `tokens` 第 11 项逐字是 `"Though"`（`:343`）。
- **该案挂在 L12**（`grammarLessons.ts:2307` `huntCaseIds: ["hunt-because-so", "hunt-word-order"]`），**而 L12 是 `will 将来时`**（`:2131`）——**L20 才是 `because/so`**（`:3598`）。
- **词架实读〔本路线图独立复验〕**：`although` **GL 0／HC 0**；`though` **GL 0／HC 2**（**2 处都在该案错句里：`:343` 的 token 与 `:366` 的 explanation**）；`even though` 0/0；`despite` 0/0；`in spite of` 0/0；**连 `even` 这个单词本身也是 0/0（真零底座）**。
- → **学生在第 12 课就要判一道「虽然…但是…不能并存」的题，而这个结构全库 138 课没有一课教过**。
- **同型先例**：批十七把 `-ed/-ing` 形容词判进课的理由之一就是「全库唯一的『先考后教』倒挂」。**本轮这条比它更硬**：`-ed/-ing` 倒挂是**案件里出现同型错**，本项是**案件里直接植入了这个结构本身的错**（`Though` 与 `but` 同句）。

**② 竞析自己也指出了 `as soon as` 的硬伤。**

- 竞析 §0 第 1 条与 §4.1 都明写：**`as soon as` 与 `when` 语义最近**——「只差一个语义刻度」（竞析 §4.1 逐字）。
- **我方时间家族现状〔本路线图实读〕**：`when`←L92／`after`←L90／`before`←L91／`until`←L109／`while`←L98——**五格各有专属课与专属标签，地板已经铺满**（数析 §1.3 实读五格合计 GL 663 处／HC 50 处）。
- → **第六格的增量真实，但只有一刻度**；**而 `although` 是整个让步结构族都没有**（0/0 真零）。**两者增量大小不在一个量级**。

**③ `as soon as` 有跨课形式冲突（本批新核，登记为携带项）。**

- **L109 `:20328` 逐字**：`wrong: "I waited until the rain stops."`／`wrongMark: "stops"`／`correct: "I waited until the rain stopped."`／whyZh `:20331` 逐字「前面用了昨天版 waited，后面也得跟昨天版：until the rain 【stopped】。」〔**本路线图实读原文确认**〕
- 而 **`as soon as` 的正确用法恰是 `stops`**（现在版表将来——Cambridge `conjunctions-time` 逐字 `I will call you as soon as I get to the office.` 对 `as soon as I will get to the office.` 明标错；竞析 §1.2 CAM-1）。
- → **同一个短语 `the rain stops` 在 L109 是错的、在 `as soon as` 课里是对的**——**差别只在主句版本**。**对零基础用户这是「同一形式、两种命运」的硬坑**（竞析 §4.2 已开三条处置，推荐「换场景」）。
- → **裁决：本批不做 `as soon as`，该冲突登记为携带项**（§6.3 序 1），**批二十三开工前必须先裁处置方案**。

**同时采纳的一条（不丢）**：**`as soon as` ＝ 批二十三首选**（瑞思 §3.1 第 1 句逐字）。

### 1.3 档位诚实标注：B（造词 ＋ 先考后教倒挂转正）

**场景线（单拱一句话）**：**「虽然下雨了，我还是要出去」——雨天出门、天冷但开心，两个说法排一行。**

`L139 早上出门遇雨（city）` → `L140 屋檐下/课间换个说法（campus）` → `L141 书桌前本子最后一页（mansion）`。**scene 只用库内既有值**（本路线图实读在用 11 种：`mansion` 48／`campus` 37／`city` 23／`sparkle` 9／`school` 5／`island` 5／`train` 3／`mystery` 3／`magic` 2／`forest` 2／`snow` 1）——**本批取 `city` 1／`campus` 1／`mansion` 1，不引入新场景 id**。

> **⚠️ 场景闭环（本批须明写的一条设计说明）**：**L139 的雨天场景是 L109 的延续**——L109 `:20304` `sceneSetupZh` 逐字「放学时下起了雨，小美在屋檐下一直等，等到雨停。」／`:20309` `targetSentence: "I waited until the rain stopped."`／`scene` 逐字 `"city"`。**本批 L139 接着那场雨往下走**：雨还在下，「虽然下雨了，我还是要出去」——**同一个雨天，一个是「等到雨停」，一个是「不等了，撑着伞就出去」**。
> **⚠️ 一条必须遵守的纪律（防与 L109 串台）**：**L139 不得复用 `until`**（那是 L109 的身份词），**也不得把 `I waited until the rain stopped.` 写进任何练习答案**；**本批 3 课的任何字段不得出现 `until`／`waited`**（G11 已锁）。**L109 的字一字不改**（G14）。

**逐词成本实读（GL ＋ HC 两文件，本路线图独立复验）**：

| 词 | GL | HC | 判定 | 本批处置 |
|---|---|---|---|---|
| **`although`** | **0** | **0** | 🔴 **真零，须新造**（本族身份词，不可替代） | **L139 立岗** |
| **`though`** | **0** | **2**（`:343` token ＋ `:366` explanation，**全在案 #7 错句里**） | 🔴 **须新造**——**2 次面熟不构成「已教」**（0 处进练习、0 处进 `guided`） | **⚠️ 最终定稿：全批零出现**（§4 G11；案 #7 是它唯一出处） |
| `even though`／`even` | 0／0 | 0／0 | 🔴 **本批禁用**（真零底座，留给后续批次） | 全批 0 次 |
| `despite`／`in spite of` | 0／0 | 0／0 | 🔴 **本批禁用**（同义换挡，不是新结构） | 全批 0 次 |
| `raining` | **98** | 12 | ✅ 零造词（L96 `:17866` `It is raining.` 的现成垫子） | 直接使用 |
| `rain` | **103** | 20 | ✅ 零造词 | 直接使用 |
| `go out` | **35** | 0 | ✅ 零造词 | 直接使用 |
| `stay at home` | **21** | 0 | ✅ 零造词 | 直接使用 |
| `cold`／`happy`／`tired` | **267**／**124**／**155** | 16／38／18 | ✅ 厚词 | 邻句使用 |
| `snow` | **17** | 4 | ✅ 在库（L19 一门 10） | 邻句使用 |

**结论**：**本批的造词成本 ＝ 必造 2（`although` ＋ `though`）**；**场景侧零造词** → **对外一律写「B 档：须新造 `although` 与 `though` 两个词」**；**不得**写「库内有垫子」、**不得**写「零成本」、**不得**写「两文件全 0」。

| 项 | 本批（批二十二） | 对照：批二十一 | 对照：批十九 |
|---|---|---|---|
| 新造词 | **2 个**（`although` 双 0 ＋ `though` GL 0／HC 2 且在案 #7 错句里） | 2 个（`forward`＋`seeing`） | 0 个（升格课） |
| **先考后教倒挂转正** | **1 处**（案 #7 的 `Though + but` 判例，挂 L12 → **L139 正课**） | — | — |
| 场景侧造词 | **0 个**（雨天场景每一个零件都在库内） | 0 个 | 0 个 |
| 名词／动词侧造词 | **0 个** | 0 个 | 0 个 |
| 结论 | **B｜造词 ＋ 先考后教倒挂转正**（本项目第一次「还教学债」形态） | 「B＋｜造词 ＋ 认读升格」 | 「升格课：给已有句子发身份」 |

> **折扣与增项并列（不得只写好话）**：折扣＝① **`although`／`though` 两词都不可替代**（换掉就不是让步）；② **cloze 假友好**（§4 G12）；③ **与 L19 `but` 的混淆风险**（§5 风险 3）；④ **`though` 的 2 处既有占用**（须核「案 #7 一字不改」＋「L139 不重复它的场景词 `cold`／`went out`」）；⑤ **`run_on` 三案连用**（§5 风险 5）。增项＝① **场景侧全零造词**；② **接口最厚**（L19 `:3429`／`:3493` ＋ L20 `:3612`／`:3629` ＋ 案 #7 回流，**三条旧线同时接上**）；③ **脚本次数零冲突**（`although`／`though` 在两文件除案 #7 外零命中）；④ **`fragment`／`run_on` 两个薄档有天然落点**（各 14 处，全库最薄）。

### 1.4 逐课增量账（为什么第 4 课不存在）

| 账 | 数 |
|---|---|
| **内容自然容量** | **3 格**：① 立岗 ② 切开 ③ 零新知收口 |
| **增量数** | **2 项真增量 ＋ 收口 1 次** |
| **为什么不是 2 课** | **切开位是本章唯一的独立价值**（不切开则 L139 退化为「L19 的换词课」，破「一课一增量」） |
| **为什么不是 4 课** | 第 4 课唯一来源 `despite`／`in spite of` 是**同义换挡**，且**后面跟名词不跟小句子**——会再开一个「后面跟什么」的新格，把一课变两课 |
| **一课一增量检查** | L139（立岗）→ L140（切开）→ L141（零新知）。**三课三个落点，无一重复。** |
| **对标先例** | 3 课章在本系列已有三个先例：season-6 `{47,49}`／season-12 `{76,78}`／season-19 `{125,127}`（本路线图实读 `grammarSeasons.ts` 21 季） |

**对内纪律与对外口径（本批必须写成文的一条）**：**课量由内容容量决定，不由节奏决定**——3 课不是「缩量」，是**小章的自然长度**。**对外口径**：「**『虽然』这件事有两件真实的新东西要学（怎么说、怎么说才不错），加一次把两个说法排一行的收口，所以 3 课。我们不为了整齐凑课数（不做 `despite` 的换词课），也不为了好看砍内容（不砍 L141）。**」

---

## §2 拆课总表（3 课 · 一课一增量 · 课注 id 已冻结）

> **通用说明**：六段＝① 看（情景讲解）→ ② 跟（guided：choose／arrange／spot／replace）→ ③ 忆（recall）→ ④ 练（practice ≥4 题）→ ⑤ 破（侦探挑战＝huntCaseIds）；本批 3 课**全部配 recall**（`grammarLessons.test.ts:66-76` 对 `number >= 13` 硬断言三字段非空）。字段名与形态沿用 `types.ts:478-618`。**结构性参数沿用批十八–二十一逐课同规格**（实读 L119–L138 二十课全部一致）：`examples` 4 条／`contrast` 6 条（**其中 3 条带 `wrongMark`**）／`variants` 3 条／`practice` 4 条（**L141 为 6 条**，沿用 L124／L133／L138 收口课六题形态）／`guided` 6 条／`sceneSwings` 3 条／`dialogue` 3 行（2 npc + 1 me）／`huntCaseIds` 1 个。
> **课注 id 已冻结，三个 id 在本路线图落盘前已实读确认全库无冲突**（`grep -c 'lesson-139\|lesson-140\|lesson-141' src/data/grammarLessons.ts` ＝ **0**，本路线图复跑）。
> **全批话术纪律（本批最重要的一条）**：三字段（`grammarLabel`／`oneLineRule`／`summary.rule`）一律改写为「**站最前面**」「**后面跟一整句**」「**只留一个**」「**谁 ＋ 怎么样**」「**先说哪半句**」「**反着说**」；**「从句」「状语」「让步」「主句」「语序」「转折」「连词」「门牌」八词全批禁**（**前六词是本批头号红线**，§4.5）。
> **全批 cloze 纪律**：**ambush 侧空位一律落 `is`／`was`／`will`（考点词成空位率 0%）**；**考点承载一律压在 `contrast`（3 条 mark）与 `guided.spot`（1 道）**；**boost 侧另有 4 处「保障句」落考点词 `Although`**（**种子运气，非引擎保证**）。**本批不改任何 cloze 引擎**。
> **全批场景纪律**：**L139 是 L109 雨天场景的延续**，**但三课不得复用 `until`，也不得把 `I waited until the rain stopped.` 写进任何字段**（**只把「同一场雨」当背景**）。

| 课 | id | 主题 | 增量（只此一点） | 目标句 | cloze 落点保障句 | 案 | 封面 |
|---|---|---|---|---|---|---|---|
| **L139** | `lesson-139-although` | 虽然下雨了 | **立岗**：`Although` 后面跟一整句（`although` 本身是新词 ＋ **先考后教倒挂转正**） | `Although it is raining, I will go out.`（8 词） | **`Although it is raining, I am happy.`（`sceneSwings[2]` → 落 `Although` ✅）** | `hunt-although-rain` **#148** | `cover22` |
| **L140** | `lesson-140-but-vs-although` | 可是 vs 虽然 | **切开**：与 L19 `but` 的语义差（**同义两说，先说哪半句哪半句是重点**） | `It is raining, but I will go out.`（8 词） | **`Although it is raining, I will go out.`（`examples[1]` → 落 `Although` ✅）** | `hunt-but-vs-although` **#149** | `cover23` |
| **L141** | `lesson-141-close-22` | 反着说，排一行（收口） | **零新知**：两个说法排一行 | 复现混排（**逐字复用 L139 target** ＋ 四句既有） | **`Although it was raining, we were happy.`（`sceneSwings[1]` ✅）＋ `Although it is raining, we will go out.`（`practice[5]` ✅）** | `hunt-close-22` **#150** | `cover24` |

**逐课增量硬纪律（写入生产规格）**：L139 **只做「Although 站最前面 ＋ 后面跟一整句」这一条**（**不碰 `but` 的对照轴**——那是 L140 的切开位，本课只在 `contrast` ④ 并排一次、不讲轴；**不碰 `though`／`even though`／`despite`／`in spite of`**，`though` 全批零出现）；L140 **只做「位置差」这一条**（**不重讲「怎么用 `but`」**——L19 已教，只做一次点名；**不做否疑的位置变化**）；L141 **零新知**（**四句全部逐字取既有、一字不改**；**变体只做一组三态**，不得给四句各做一套）。

**变体三态（逐课，cloze 逐字复跑已登记）**：L139 肯定 `Although it is raining, I will go out.`（落 **`is`**，假友好）／否定 `Although it is raining, I will not go out.`（noteZh：「不」站在 `will` 后面——**`not` 跟 `will` 走**；复跑落 `is`）／疑问 `Will you go out although it is raining?`（noteZh：**`Will` 搬句首**，`Although` 那一整句留在后面不动；复跑落 `is`）；L140 肯定 `It is raining, but I will go out.`（落 **`is`**）／否定 `It is raining, but I will not go out.`（落 `is`）／**疑问 `It is raining, but will you go out?`**（⚠️ **本批唯一一次「半句变问」**——**`but` 不搬到句首**（搬到句首就成了 `But` 开头，那是另一件事），**生产期须逐字核 `but` 的位子不动**）；L141 肯定（＝L139 target，落 `is`）／否定 `Although it is raining, I will not go out.`（落 `is`）／疑问 `Will you go out although it is raining?`（落 `is`）。**⚠️ 逐课须核「variants 至少 1 句让主考点落空」**——本批三课全部满足（三态全落 `is`，是「假友好」的必然形态，已登记）。

**复现取材（跨批钩子兑现·第九次，逐课列出 — ⚠️ 本批的红线头寸已实算，见下）**：L139 复现 L19 `:3432` `The snow is cold but fun.`／L20 `:3615` `It was cold, so I stayed at home.`；L140 同上两句；L141 **六题**：变体题 ＋ `It is raining, but I will go out.`（保障）＋ `Although it is raining, we will go out.`（保障）＋ L19 两句复现。
**⚠️ 生产红线（复现取材 · 本批头号机制风险）**：`grammarLessons.test.ts:163-178` 断言「**同一句练习答案最多出现在 6 课**」。**本路线图独立复算全库 138 课的 practice 答案**（§附录 C）——**当前已用满 6 课的：`yesterday i went to the park`（L21/24/93/95/100/104，头寸 0）**；**只剩 1 次头寸的 5 句**：`i like reading`（5）／`there is a book on the desk`（5）／`i am happy`（5）／`i am used to getting up early`（5）／`it looks nice`（5）；**`he drinks milk every day` 4 课（头寸 2）**。
- **本批涉及的三句头寸实算**：`The snow is cold but fun.` **当前 1 课（L19）→ 头寸 5 ✓**；`It was windy, but we were happy.` **0 课 → 头寸 6 ✓**；`It was cold, so I stayed at home.` **0 课 → 头寸 6 ✓**；`It is raining hard.` **0 ✓**；本批两句新句子（`Although it is raining, I will go out.`／`It is raining, but I will go out.`）**当前 0 课，各只 +1 课 ✓**。
- **→ 本批本身安全；但红线是全库级紧约束，生产期必须逐句核验，不得超限**（沿用批次：批二十一 L138 的六题形态曾是最近一次触线压力点）。

**案件规划（3 案 · #148–#150，PRD §9 定稿）**：

| 案 id | # | 配课 | 场景 | 定稿错误构成（原 → 修正（tag，新/旧）） |
|---|---|---|---|---|
| `hunt-although-rain` | **148** | L139 | 早上出门遇雨：楼道口看天，本子上写了两句 | ① `but` → 去掉 `but`（`Although it is raining, but I will go out.`，**`run_on`**，**新**——**中文成对负迁移主靶**；**逐字同型先例** 案 #7 `:361-367`）② `raining,` → `it is raining,`（`Although raining, I will go out.`，**`fragment`**，**新**——**Although 后面跟的是一整句，那个「谁」不能丢**；补词型 `correction`）③ `go` → `went`（`Yesterday I go to the park.`，**`tense`**，**旧错 L10 `:1782`**）④ `was` → `were`（`They was happy.`，**`sv_agreement`**，**旧错 L19 `:3423`**） |
| `hunt-but-vs-although` | **149** | L140 | 课间：同学看见鞋上的水，问还出不出去 | ① `so` → 去掉 `so`（`Although it is raining, so I will go out.`，**`run_on`**，**新**——**`so` 说结果、`Although` 说让步，两个不能同台**；**同源先例** 案 #7 `:353-360`）② `raining` → 补 `be`（`It raining, but I will go out.`，**`missing_be`**，**新**；**薄档补位：全库 24 处**）③ `drink` → `drinks`（`He drink milk every day.`，**`sv_agreement`**，**旧错 L25 `:4528`**）④ `was` → `were`（`We was happy.`，**`sv_agreement`**，**旧错 L19**；**与 #148④ 换主语**避免逐字重复） |
| `hunt-close-22` | **150** | L141（**收口案**） | 书桌前：本子最后一页，两句排成两行 | ① `but` → 去掉 `but`（**`run_on`**，**回流 #148①**）② `raining,` → `it is raining,`（**`fragment`**，**回流 #148②**）③ `was` → `were`（`We was happy.`，**`sv_agreement`**，**回流 #149④**）④ `egg.` → `eggs.`（`We have two egg.`，**`plural`**，**旧错 L11 `:1964`**）——**收口案＝三点锚本批 ＋ 一点锚旧课**（照 #136／#142／#147 收口案式） |

**本批罪名分布（3 案 × 4 错 ＝ 12 处）**：`run_on` **3**（#148①／#149①／#150①）／`fragment` **2**（#148②／#150②）／`sv_agreement` **4**（#148④／#149③／#149④／#150③）／`missing_be` **1**（#149②）／`tense` **1**（#148③）／`plural` **1**（#150④）。**校验：3＋2＋4＋1＋1＋1 ＝ 12 ✓**。**`comparison` 0 处**（不进案件）。
> **注 1（`run_on`／`fragment` 连续同型的破例——本批必须明写的一条）**：**先例纪律是「避开 `fragment`／`run_on` 连续同型」**（批十八／十九／二十 PRD 逐字；批二十一实读 **0 处** `fragment`／`run_on`）。**本批破这条纪律**：**`run_on` 是本章的语法主靶本身**（案 #7 的判例就是 `tag: "run_on"`），**「虽然…但是…」并存正是 `run_on` 的教科书案例**（两句连写、真凶是多余连接词）——**不写 `run_on` 等于不考本课**。**账**：`fragment` **14 处（全库最薄）**／`run_on` **14 处（全库最薄）**，**本批各补 2／3 处是最薄档的真实补血**。**三条处置（缺一不可）**：① **三处 `run_on` 必须不同句、不同词**——#148① 是 `but`／#149① 是 `so`／#150① 是回流；② **两处 `fragment` 的 `correction` 型不同**（#148② 补「谁＋be」／#150② 为其回流）；③ **`run_on` 三案不得都用 `but`**——**#149① 用 `so`，这就是本批的差异化**。**携带项**：**若主理人认为「三案连用 `run_on`」不可接受，唯一可换的是 #149①**——**但本批句子没有需要 `the` 的名词位（§注 2），实际不可换** → **建议在走查里单列观察本批 `run_on` 题的判对率**（§4.3 F22-B）。
> **注 2（`article` 位的调整说明）**：**本批三案 12 处错里不含 `article`**——**理由**：本批核心句没有需要 `the` 的名词位（`raining`／`go out`／`stay at home`／`snow`／`cold`／`happy` 全是不可数或形容词位），**硬塞 `article` 会造出不通的句子**。→ **调整动作**：#149② 由「`article`（`summer` → `the summer`）」改为 **`missing_be`**——**这是薄档补位（`missing_be` 24 处比 `article` 29 处更薄），且它是本批句子的天然错型**（**中文说天气没有 be**）。**登记：批二十一的 `article` 三连用（L134③／L135③／L137③）在本批不延续**。
> **注 3（同案同句的避免重复处置）**：#148④ 与 #149④ **都涉及 `was/were`**——**#148④ 用 `They was happy.`／#149④ 用 `We was happy.`**（两案只能有一案用 `They`）；**#150③ 再用 `We was happy.`（回流 #149④）**——**⚠️ `We was happy.` 在本批出现 2 次，这是「回流」的合法形态**（收口案回流位就是逐字复用前案错句，先例 #147④）。
> **注 4（tokenIndex 定稿）**：生产时逐 token 核 `tokenIndex` 与 `tokens` 下标（`huntCases.ts` 头注释 `:15`：标点跟在前一个词后面）。**短句排一行的下标规律**（照 #143–#147）：第 1 句从 0 起、第 2 句从「第 1 句词数」起、依次累加。**本批逐案下标**：

| 案 | tokens（逐字） | 错点下标 |
|---|---|---|
| **#148** | `["Although","it","is","raining,","but","I","will","go","out.","Although","raining,","I","will","go","out.","Yesterday","I","go","to","the","park.","They","was","happy."]` | ① **4**（`but`）② **10**（`raining,`）③ **17**（`go`）④ **22**（`was`） |
| **#149** | `["Although","it","is","raining,","so","I","will","go","out.","It","raining,","but","I","will","go","out.","He","drink","milk","every","day.","We","was","happy."]` | ① **4**（`so`）② **9**（`raining,`）③ **16**（`drink`）④ **21**（`was`） |
| **#150** | `["Although","it","is","raining,","but","I","will","go","out.","Although","raining,","I","will","go","out.","We","was","happy.","We","have","two","egg."]` | ① **4**（`but`）② **10**（`raining,`）③ **14**（`was`）④ **20**（`egg.`） |

> **⚠️ 净词校验（逐案）**：#148 `tokens 24 > errors 4` ✓｜#149 `tokens 23 > errors 4` ✓｜#150 `tokens 22 > errors 4` ✓。
> **⚠️ 标点口径（逐案须核）**：**四处带尾标点的原文（`raining,`／`go.`／`happy.`／`egg.`）必须与 `tokens` 逐字一致**（先例 #134④／#135④／#147④ 均带尾标点）。
> **⚠️ 三处新错 `explanation` 的硬要求**：**#148①（`but` → 去掉 `but`）必须含「第 12 课你判过这一句，当时只是找错——今天它转正了」**（**先考后教转正**，§3）；**#149①（`so` → 去掉 `so`）必须含「第 20 课学过 `so` 说结果」**（**同源家规点名，不重讲**）；**#149②（补 `is`）必须含「说天气那个「谁」后面那个词不能丢」**（**L6 的老座位**）。
> **⚠️ 逐案 `explanation` 的零术语自查（`huntCases` 侧无守门测试，纯纪律）**：本批 12 条 `explanation` **全部不得出现 29 词 ＋ 本批额外禁词**——**特别是「从句」（#148②／#150② 讲 `Although` 后面那半句时最易写）／「状语」「语序」**；**统一写「一整句」「站最前面」「两个只留一个」**。

**封面（数析 §7.3 唯一解 · 池内复用 · 本路线图独立复算）**：`L139←cover22`｜`L140←cover23`｜`L141←cover24`——**min-gap 117 为理论上限且解唯一**。
- **实算依据（本路线图复跑）**：工作区现有 **117 张图、138 次使用**（单次池 **96 张 ＝ cover22–cover117**／二用池 **21 张 ＝ cover1–cover21**／三用 0；本路线图 `grep -o 'cover: cover[0-9]*' | sort | uniq -c` 复算分布 **96 张计 1 ＋ 21 张计 2**，**与三研究逐字一致**）。**按「距上次使用 ≥ 35」筛（对 L139 起）的可用池 ＝ 83 张：cover22–cover104**（`cover104` 距 L139 ＝ 35 压线过；**`cover105` ＝ 34 出局**；**本批三张到各自被指派课处的间距全 ＝ 117**——`cover22`：L22→L139／`cover23`：L23→L140／`cover24`：L24→L141——**「从 cover22 起连续取」即最优解**）。
- **本批已实读确认三张资产齐全**：`ls src/assets/lessons/ | grep -E "lesson-(22|23|24)\.jpg"` → **三张全在**；`ls | wc -l` ＝ **117**；`grammarLessons.ts` 的 `import cover*` 计数 ＝ **117**；并已实读确认三张的当前唯一占用（`:3968` `cover: cover22,`〔L22〕／`:4150` `cover: cover23,`〔L23〕／`:4333` `cover: cover24,`〔L24〕——**三者均只在 L22–L24 用过一次**）。
- **本批禁用**：❌ `cover1`–`cover21`（二用张，段距 1–21）；❌ `cover105`+（L139 的 gap ≤ 34）；❌ **`cover118`+ 资产不存在**（实读共 117 张）。
- **⚠️ 唯一允许的池内换法（写死，不留活口）**：**三张内部互换（集合不变）**，须登记且须同时登记 min-gap 变化（**任何错位会把 min-gap 从 117 降到 116**）。**默认按数析的唯一最优解顺次指派**。`cover` 在 `types.ts` 为可选字段，缺省回退 scene SVG，**语义不匹配不阻断上线**。
- **⚠️ 携带项（须主理人注意）**：**本批取 3 张后，二用池将从 21 张扩到 24 张、单次张从 96 降为 93 张**（数析 §7.3 明写这是「封面二用池长期耗尽」决策的第二个触发点，批二十一路线图 §6.3 携带项 1 已登记）——**本路线图按唯一解执行，不自行改池政策**。

**episode 写法**：`小美的一天 一百三十九`／`一百四十`／`一百四十一`。**先例充足**：末项实读 `:25918` ＝「小美的一天 一百三十八」，且 L131→L138 连续八课**无跳号**（数析 §6.3 实读）。

---

## §3 生产节奏与阶段划分（D1–D6 · 两段式）

**段一（D1–D3）＝立岗与切开段（L139–L140）**——打样 → 切开，含 **F22-A 打样门** 与 **中段自走查点（L140）**。
**段二（D4–D5）＝收口与展示层段（L141）**——零新知收口 ＋ 两说法排一行 ＋ season-22／m24。
**预检与验收（D5–D6）**——全量预检 → 验收放行；**F22-B 上线后 1–2 周**。

| 阶段 | 任务 | 产出 | 门/决策 |
|---|---|---|---|
| **D1** | L139 `lesson-139-although` + `hunt-although-rain`（立岗；**先考后教倒挂转正开场**）＋**封面三张拍板**（逐张目视核图像语义）＋**cloze 逐课预演**（ambush 落 `is`／boost 保障句落 `Although`）＋**三字段零术语替换表逐条自查**（首查「从句」「状语」） | L139 + 案件 `reviewed` | 封面拍板；cloze 预演；术语自查 |
| **D2** | L139 首玩走查（清 localStorage、非开发会话）→ **F22-A 门** → 放行本批 | 走查记录 | **F22-A 门**（放行） |
| **D3** | L140 `lesson-140-but-vs-although` + `hunt-but-vs-although`（**切开课：与 L19 `but`**）＋**中段自走查点（L140）**＋ **段一联检（前 2 课）** | L140 + 案件；走查记录 | **中段自走查：与 L19 `but` 的切开是否被读懂** |
| **D4** | L141 `lesson-141-close-22` + `hunt-close-22`（零新知收口；两说法排一行；**四句逐字取既有**）＋**收口复点走查** | L141 + 案件；走查记录 | **收口复点：两句会不会串台** |
| **D5** | **season-22 ＋ m24（~13 行）**（展示层闭环）＋全量预检（**G12 逐课 cloze 实跑登记**、G-boost 四项自查、**`wrongToken` 逐字核对（含尾标点）**、封面逐张复核、**G14 `L1–L138` 零改动 diff 核**、**G11 复现句头寸逐句核验**） | 路径页分组/里程碑；预检记录 | **展示层闭环**（G8／G8-b）；验收前置 |
| **D6** | 全量验收（checklist G1–G15 + 抽测剧本 + 路径页 141 课 + m24 触发 + **测试实跑基线** + `tsc --noEmit` + build）；滑移吸收位／放行缓冲（未用即提前交） | 验收记录 | **上线放行** |
| 上线后 1–2 周 | 遥测（首过率、`Although…but` 并存错改对率、两句串台率、`Although` 新词接受度、m24 触发） | 观察数据 | **F22-B；与 F9-B–F21-B 合并裁决** |

**纪律**：F22-A 首玩须在**非开发会话**产生（清 localStorage；HMR 污染场次作废重采，沿用批五–二十一纪律）。负载：D1–D6 依次 ≈0.5/0.3/0.4/0.4/0.25/0.15 人日 ＝ **≈2.0 人日**（PRD 记 ≈2.0，含展示层 0.2＋走查 0.2）——**单日 ≤1、每周 ≤3 封顶**；**滑移先砍 deepDive 深度与复现条数、不砍课**（不做 3→2 缩量，亦不砍 L141 收口）。

**中段自走查点（本批定在 L140：与 L19 `but` 的切开）**——D3：

> **为什么设在 L140（而不是 L141）**：**本批与 L19 的接口就是这一课**——L140 是「同一个意思、两个说法」的第一次正式切开，**若这一课被讲成「新学一条规矩」或「`but` 换词课」，后面 L141 的排一行必然退化成重复课**。**L141 的两句串台另设收口复点（D4）**，两处各有分工。

| 观察点 | 判据 | 异常处置 |
|---|---|---|
| **「位置差」是否被接住**（**本批头号教学读数**） | 走查问「`Although it is raining, I will go out.` 和 `It is raining, but I will go out.` 哪句先说「下着雨」」——**正确反应是「`Although` 那句」** | 答不出集中 → **L140 `oneLineRule` 首句改成「先说哪半句，哪半句就是重点」并把 `contrast` ① 提位为第 1 条**；仍不接 → D6 缓冲转 0.5 日小修（**不砍课**） |
| **「二次切开」是否生效** | 走查问「这两句意思一样吗」——**正确反应是「一样，就是先说哪半句不同」** | 答「不一样」集中 → L140 `contrast` ① whyZh 增半句「**两句都对，意思也一样**」（**兜底成本为零**） |
| **反向风险：学生是否以为两句有强弱之分** | 走查问「哪句更好」——**正确反应是「都好，看你想先说哪个」** | 答「有强弱」→ L140 `contrast` ① whyZh 增「**没有哪句更好**」半句 |
| **与 L19 的接口是否被读懂**（防退化成换词课） | 走查问「`It was windy, but we were happy.` 这句对不对」——**正确反应是「对——`but` 自己也能站中间」** | 答「错／要改成 Although」→ **L140 `contrast` ⑤ 提位为第 1 条并加 `deepDive` 一段**（**本批最易失守处**） |

**收口复点（D4，L141 生产后）**：

| 观察点 | 判据 | 异常处置 |
|---|---|---|
| **两句会不会串台** | 走查问「`Although` 那句和 `but` 那句，哪句把「下着雨」放前面」——**能一次分清** | 串台集中 → L141 `contrast` ④（脊柱卡）提位为第 1 条；仍不接 → **登记进 F22-B，不硬改本批** |
| **「排一行」的收口是否成立** | 走查问「这一章学了几个说法」——**正确反应是「两个」** | 答「三个／四个」→ L141 `summary.rule` 按 PRD §2.2 定稿逐字重写（**已是定稿，只须落地**） |
| **零新知红线**（最易失守处） | 生产稿是否引入新句／新词；`grammarLabel` 是否写成新句型标签 | 引入即**就地重写该课**；`grammarLabel` 须写「**收口 · 零新知（两个说法排一行）**」——**不写「让步大章收口」**（含红线词「让步」） |
| **变体是否只做一组三态** | 是否给四句各做一套三态（**四套 ＝ 把一课变四课，破零新知红线**） | 出现四套 → **就地删至一组** |

**段一联检（D3 末，排期纪律，不设独立门）**：① 前两课话术是否同线（「站最前面／后面跟一整句」→「站中间／先说哪半句」）；② **术语红线三字段是否 0 命中 29 词＋本批额外八词**；③ 封面逐张语义复核（D1 拍板结果落地）；④ **课号连续性 139–140 人工核对**（缺号永久锁死且无报错，`lessonService.ts:157` 用 `number - 1`）；⑤ **L140 的 `guided.spot` 与 L139 逐字相同一事是否已按设计登记**（**本批唯一一处「同题两课」，有意的二次切开**，§6.3 序 6）。

## §4 质量门与验收

### 4.1 门序列（六道）

| 门 | 时点 | 性质 | 判据摘要 |
|---|---|---|---|
| **门 0 · 生产期逐课自查** | D1–D4 每课落盘即查 | 硬纪律（无独立走查） | G1–G7／G10／G11＋G-boost 四项逐课过；**课号连续性**逐课核；**不依赖测试兜底** |
| **门 1 · F22-A 打样门** | D2（L139 首玩 + 走查后） | 决策门（继续/调整/止损） | 见 §4.2 |
| **门 2 · 中段自走查** | **D3（L140 切开课）** | 排期纪律（生产即查） | 见 §3 中段自走查表（**本批定在 L140**：与 L19 的接口在这一课才可测）；**收口复点在 D4（L141）** |
| **门 3 · 展示层闭环** | D5 | 硬需求 | G8（season-22＋m24 上线）＋G8-b（守门测试 4 项全绿）＋路径页 141 课走查 |
| **门 4 · 全量验收** | D6 | 放行门 | G12 逐课 cloze 实跑登记＋G13 时长抽查＋G14（**L1–L138 零改动**、测试实跑基线、build、`tsc --noEmit`）＋**G11 复现句头寸逐句核验** |
| **门 5 · F22-B 上线观察门** | 上线后 1–2 周 | 观察门（与 F9-B–F21-B 合并） | 见 §4.3 |

### 4.2 F22-A 打样门（L139 真实首玩 + 走查后；D2）

| 档 | 判据 |
|---|---|
| **继续 → L140–L141 放行** | 试玩者能复述「**说「虽然」用 `Although`——它站句子最前面，后面跟一整句（谁 ＋ 怎么样）**」并说出「\*`Although it is raining, but I will go out.` 不行」；**cloze 落 `is` 的落点**不产生「这题考什么」的困惑（与批十四 L87 落 `It's`／批十五 L95 落 `was`／批十七 L111 落 `is`／批十八 L119 落 `am`／**批二十一 L134 落 `am`** 同型）；**「先考后教转正」被读懂**（**本批第一读数**：走查问「第 12 课那句 `Though…but…` 你判它哪儿错」——**正确反应是「多了一个 `but`，不是 `Though` 本身错」**）；**「只留一个」被接住**（走查问「`Although it is raining, but I will go out.` 对不对」——**正确反应是「不对——两个只留一个」**）；**「一整句」被理解**（走查问「`Although raining, I will go out.` 少什么」——**正确反应是「少「谁」——说天气要 `it`」**）；场景锚可懂（早上出门遇雨）；时长 12–15 min（**L139 首玩略长，含 `Although` 新词的认读**） |
| **调整** | 时长超标 → 收紧 deepDive（**不砍课**）；**「先考后教」答「这句是错的」集中**（**本批最危险的反向风险**：学生把「这句错」记成「整个结构错」）→ **L139 开场话术按 PRD §3.2 定稿增补「错的是多出来的 `but`——`Though` 本身没错，它站对了地方」**（**兜底成本为零**）；`Although raining, …` 答不出「少谁」→ L139 `contrast` ② whyZh 增半句「**说天气那个「谁」永远是 `it`（第 6 课的老座位）**」；术语越线（「从句」「状语」「让步」「主句」「语序」「转折」「连词」「门牌」漏进三字段）→ **就地重写该字段**（硬红线，不设容忍） |
| **止损** | >18min 或话术改进后仍不可达 → 批次暂停、退回析客（**不推翻 3 课拆分**）；**G-boost 护栏被生产反复突破（某课 contrast 带 `wrongMark` 错卡 <3 条进入验收）→ 就地暂停并把「boost 护栏是否升级为生成期硬校验」提为跨工作流 P0**；**若「造词 ＋ 先考后教转正」被实测证明负荷过高 → 退回「砍 L140 并入 L139」的 2 课方案须主理人裁，不自行缩量** |

### 4.3 F22-B 上线观察门（批次上线后 1–2 周）

| 档 | 判据 |
|---|---|
| **继续** | L139–L141 **首过率**与全批窗口基线相当（±5pp）；**「`Although…, but…` 并存」的改对率上行**（**＝#148①／#149①／#150① 的选错率——这是本批中文成对负迁移拦截效果的唯一读数**）；**两句串台率**低位（`Although it is raining, I will go out.` 对 `It is raining, but I will go out.` 的判对率）；**位置差判对率**（走查问「哪句先说「下着雨」」）；**`Although raining, I will go out.` 型错（漏「谁」）的改对率**（**新错型方向是否被接住**）；**`Although` 作为新词的接受度**（走查问「`Although` 难不难」——**正确反应是「不难／就是「虽然」」**）；**`run_on` 三案连用的判对率**（**与全库均值对照，本批破例项单列**）；**m24 触发**与 season-22 分组可见正常 → **与 F9-B–F21-B 合并裁决**（含批二十三候补清单） |
| **调整** | 任一课首过率 <70% → 该课 deepDive 收紧或复现题加练（**不砍课**）；`Although…but…` 并存错集中 → L139 `contrast` ① 提位（**兜底成本为零**）；**位置差答不出集中 → L140 `contrast` ① 提位为第 1 条**；`but` 类错（学生要求把 `It was windy, but we were happy.` 改成 `Although`）集中 → **L140 `contrast` ⑤ whyZh 增一句「`but` 自己也能站中间」**（照抄既有话术，成本为零）；`Although` 认读负担集中 → **登记为教学法级问题，提为批二十三选题的输入**（**不为它硬改本批课序**） |
| **止损** | 任一课/案违反红线（**术语进三字段**、comparison 进案件、新增枚举、多 token 错不可修、引番外 5 案、**课号不连续**、**每课 contrast 带 `wrongMark` 错卡 <3 条**、**改动了 L1–L138 任一字**（含案 #7 `huntCases.ts:327-370` 与 L19／L20／L109／L96 被认领的原句）、**L139–L141 出现 `though`／`even`／`despite`／`in spite of`／`until`／`waited`**、**L139 写成「第 12 课已经教过」**、**`although` 被写成引导「从句」**、**L141 引入新句／新词**）→ 当日下线该内容；**连续两课完成率显著下滑且走查＋遥测双证** → 3 课小章形态退回「2 课」重设计（season-22 分组暂留） |

### 4.4 G1–G15 的路线图口径（PRD 逐条 → 阶段门）

| PRD 项 | 要求（压缩） | 路线图口径：在哪道门验 | 放行判据 |
|---|---|---|---|
| **G1** | practice ≥4 题且含一道与 variants **逐字一致**的非肯定卡题；**G1-b 每课 ≥1 复现题** | 门 0 逐课；门 4 抽测 | 三课逐课登记变体题（L139 `Will you go out although it is raining?`／L140 `It is raining, but will you go out?`／L141 `Will you go out although it is raining?`）；**L141 六题**；**`practice` 与 `examples` 重合 <4**（`grammarLessons.test.ts:149-157`） |
| **G2** | tokens／answer 词集一致＋distractors 不重复答案词；arrange／practice 展示序 ≠ 答案序 | 门 0 | `grammarLessons.test.ts:31-50`／`:52-67` 全绿；**逐题 `tokens` 逐字见 PRD §2.2 三张表**（**标点跟在前一个词后面**） |
| **G2-b** | **零术语红线**：29 词禁于 `grammarLabel`／`oneLineRule`／`summary.rule`；另核 AI 引用源三字段（`contrast.whyZh`／`guided.explain`／`recall.noteZh`） | 门 0 逐课自查 | **本批特别核「从句」「状语」「语序」「时态」「形容词」「副词」「介词」「原形」「第三人称」「疑问句」「否定句」十一词的 0 命中**；**＋本批额外禁「让步」「主句」「转折」「连词」「门牌」五词**（**非测试覆盖，人工核**）；另核 `/的的/` 等生硬拼接四模式 |
| **G3** | recall 三字段非空（`promptZh`／`intentZh`／`answer`） | 门 0 | `:66-76` 对 `number >= 13` 硬断言全绿（三课全配 recall） |
| **G4** | 目标句 ≤8 词；一课一增量 | 门 0；门 4 | L139 **8**／L140 **8**；**L141 复用 L139 句（8 词）**；**L141 是零新知**（`grammarLabel` 写作「收口 · 零新知（两个说法排一行）」，**不写新句型标签**） |
| **G5** | 3 案 tag 全落 **10 枚举**（`huntService.ts:331-342`）；不碰 comparison；**`run_on` 3 处为有意破例** | 门 0；门 4 | 分布：`run_on` 3／`fragment` 2／`sv_agreement` 4／`missing_be` 1／`tense` 1／`plural` 1；**逐案核「三处 `run_on` 不同句不同词」**（`but`／`so`／回流） |
| **G6** | 每案 4 错＝新 2＋旧 2（**#150 例外：三点锚本批＋一点锚旧课**）、单 token 可修、≥1 净词、`reviewed: true`、`tokenIndex` 对齐、`number` 连续 | 门 0 逐案；门 4 | 旧错**只取 L10 `:1782`／L11 `:1964`／L19 `:3423`／L25 `:4528`**，禁引入未教材料；**#148–#150 连续**（接 #147 `:8140` 之后）；**两处补词型写法**（#148② `"it is raining,"`／#149② `"It is raining,"`）**照既有先例**（`huntCases.ts:7379`） |
| **G7** | tagStats=10 不动；番外 5 案冻结 | 门 4 | `huntService.test.ts:109`（`toHaveLength(10)`）／`:215`（逐名单断言 `hunt-white-cat`／`hunt-sports-day`／`hunt-pen-pal-letter`／`hunt-fridge-note`／`hunt-term-review`）全绿；**3 新案全被 L139–L141 引用**（引号案数 147→150） |
| **G8** | season-22 `{139,141}`＋m24（afterLesson 141）随批上线 | **门 3（展示层闭环）** | 路径页 141 课可见、m24 触发正常 |
| **G8-b** | `grammarSeasons.test.ts` **4 项全绿**（`:12` 区间覆盖／`:22` 互不重叠且 `min ≤ max`／`:36` `label`·`hint` 非空／`:43` 最高课号被覆盖） | 门 3 | **`min:139 > 138`、`max:141 ≥ 141`**；**不落 season-22 则第 ①④ 项先红**（不会静默过滤）；**`can-do-m24` 全仓无测试引用——纯纪律项，漏加不会红，须人工核** |
| **G9** | 3 新案全被引用、不新增未引用案 | 门 0；门 4 | 引号案数 147→150、`reviewed` 全 true |
| **G10** | episode 汉字数字（「一百三十九」…「一百四十一」） | 门 0 | **>100 已有 36 例先例（L103–L138）**，**末项 `:25918`「一百三十八」无跳号** |
| **G11** | **语料锁闭集（本批最硬的一条，含「复现句头寸」新项）** | 门 0 逐课自查；门 4 | ① **`although` 纪律**：**全批 `although` 只出现在本批 3 课**（L1–L138 命中 ＝ 0、一字不动）；② **`though` 全批零出现**——**它只在案 #7 的 2 处（`:343`／`:366`），案 #7 一字不改**（`correction: "去掉 but"` 与 explanation 逐字冻结）；③ **L19／L20 冻结**：本批引用三句逐字照抄（`The snow is cold but fun.`（L19 `:3432`）／`It was windy, but we were happy.`（L19 `:3434`）／`It was cold, so I stayed at home.`（L20 `:3615`））；④ **L109／L96 冻结**：**本批任何字段零出现 `until`／`waited`**；`It is raining.`（L96 `:17866`）／`It was raining.`（L96 `:17853`）逐字冻结；⑤ **本批不引入**：`even`／`even though`／`despite`／`in spite of`／`unless`／`in case`／`as soon as`（**留给批二十三**）／`although` 的过去完成形式；⑥ **⚠️ 复现句头寸（本批新增项）**：**生产期逐句核 `grammarLessons.test.ts:163-178` 的「同一句最多 6 课」红线**——**本批三句头寸已实算充足（1／0／0），但红线是全库级紧约束，须逐句复核**（§2／§附录 C） |
| **G12** | **cloze 逐课实跑**（按生效词表）＋保障句＋「**variants 至少 1 句让主考点落空**」 | 门 4（D5 预检 + D6 复核） | **ambush 侧**：`although`／`though`／`but`／`so`／`raining`／`rain` 六词**全不在 `GRAMMAR_WORDS` 表内**（实读 `:160-186`；`but` 另在 `CLOZE_STOP_WORDS` 内）→ **考点词成空位率 0%（21/21 全落 `is`／`was`／`will`）**；**boost 侧保障句**：L139 `sceneSwings[2]`→**`Although`**／L140 `examples[1]`→**`Although`**／L141 `sceneSwings[1]`＋`practice[5]`→**`Although`×2**。**⚠️ 生产期须按生效词表重跑并逐题登记**（PRD 实跑是设计态，不是落盘态；**种子由 `lessonId` ＋ `sourceRef` 决定，一旦 `lessonId` 或句序改动落点会变**）；**若复跑发现保障句落点变了，唯一允许的动作是调整该句的槽位（`examples` 第几条／`sceneSwings` 第几条），不得改引擎** |
| **G13** | 时长 12–15 min（三课逐课走查；**L141 收口课最短，12 min，零新知**） | 门 4 | 逐课记录；**L139 首玩略长（含 `Although` 新词的认读）**；**L141 须按分段计时抽查** |
| **G14** | **L1–L138 一字不动**（**特别声明：不改案 #7 `huntCases.ts:327-370`（本批的转正来源）／L19 `:3415`–`:3502`／L20 `:3596`–`:3760` 一带／L109 `:20299`–`:20365` 一带／L96 `:17847`–`:18030` 一带 等被「认领」的句子——**认领 ＝ 在 L139–L141 里引用，不是编辑原课**）；**不改 `grammarAmbushService.ts`／`grammarBoostService.ts`／`grammarZeroTerms.ts` 三个文件**（**⚠️ 唯一例外：`grammarReviewService.test.ts` 的 banned 名单可扩**）；`npx vitest run` 全绿（**以生产时实跑为准**）；`npx tsc --noEmit` 0 错（**须排除 `src/tmp-prd/` 临时脚本目录**，见附录 C 口径说明）；build 通过 | 门 4 | **基线口径见附录 C** |
| **G15** | 封面 `L139←cover22`…`L141←cover24`（min-gap **117**，**唯一解**） | 门 0（D1 拍板）；门 4 | 不符者**只允许三张内部互换**（集合不变）且须登记 min-gap 变化；`cover1`–`cover21` 禁用、`cover105`+ 禁用、`cover118`+ 不存在；缺省回落 scene SVG 不阻断上线 |
| **G-boost** | 每课 contrast **6 条中 3 条带 `wrongMark` 的真实错卡**（**禁止贴线**）；`wrongMark` **不得为纯标点**；`guided.spot` 的 `wrongToken` 与 tokens **逐字相等**；**双正解卡 diffScore < 90** | 门 0 逐课；门 4 | `grammarBoostService.test.ts:161`（全库 `thin === []`）／`:185`／`:199` 全绿；**本批 9 个 `wrongMark` 全部含字母**：L139 `but`／`raining`／`rain`｜L140 `but`／`so`／`was`｜L141 `but`／`was`／`raining`；**每课池深 ＝ 3 mark ＋ 1 spot ＝ 4 道**；**3 处 spot 逐字核对**：L139 `["Although","it","is","raining,","but","I","will","go","out."]`/`"but"`｜L140 **同 L139（有意的二次切开）**｜L141 `["Although","raining,","I","will","go","out."]`/**`"raining,"`（带尾逗号）**——**`tokens.includes(wrongToken)` 必须为 true**；**18 组双正解 diffScore 全部实跑留档（最高 78，最低 11，全部 < 90 ✓）**；**已登记高分**：**L139 ① 与 L140 ② 的错卡 ＝ 89**（**是错卡、非 `bothRight`——门只对 `bothRight` 生效**），**L141 ⑤ ＝ 75（本课最高，须重跑）** |

### 4.5 术语红线与替换体系（生产禁出现）

**零术语红线**（`grammarLessons.test.ts:105-130` 逐字断言，作用域＝`grammarLabel`／`oneLineRule`／`summary.rule`；共享常量 `src/data/grammarZeroTerms.ts:17-27`，**本路线图逐字实读＝29 词**）：主语、谓语、宾语、表语、定语、状语、单数、复数、三单、原形、时态、一般过去时、一般现在时、现在进行时、过去进行时、现在完成时、情态动词、比较级、最高级、**从句**、**语序**、可数、疑问句、否定句、被动语态、第三人称、形容词、副词、介词。

**本批额外禁用**（非测试覆盖，同属零术语纪律，生产自查）：**让步**（**本批头号自律项——`although` 的教科书描述正是「让步状语从句」，用「让步」必然引出「从句」「状语」**）、**主句**（「从句」的配套词）、**转折**（**L19 `:3479` 的 `summary.rule` 就用了它——那是历史文本，本批不引、不改、不用**）、**连词**（**可用但本批不用**——本批核心是「站位」不是「词性」）、**门牌**（防与 L67 `:12363` 一带的 `at` 门牌串台，沿用批二十一纪律）、**系动词**、**感官动词**、**助动词**、**分词**、**词性**、**主谓一致**、**状语从句**。

| 禁写（会被机检命中） | 改写成 | 依据 |
|---|---|---|
| ❌「although 引导**从句**」 | ✅「**Although 后面跟一整句**」 | **「从句」在 29 词表内**；「一整句」是 **L90 `:16727` 逐字已有的话术**（「after 后面要跟一个完整的小句子（谁 + 做什么）」） |
| ❌「**让步**状语」「让步结构」 | ✅「**先摆出「虽然」的那半句**」 | **「状语」在 29 词表内**；**「让步」本批一并禁**（分析用语，会引出上面那套词） |
| ❌「**主句**在前／在后」 | ✅「**后面跟的那半句**」 | **「主句」不在表内，但它是「从句」的配套词**——用了必然引出「从句」，故一并禁 |
| ❌「换一下**语序**」 | ✅「**谁站最前面，谁就是这次要讲的重点**」 | **「语序」在 29 词表内** |
| ❌「**转折**连词」「表示**转折**」 | ✅「**两个都在说「反着」**」「**反着说**」 | **⚠️ 自律项**——「转折」不在 29 词表内（L19 `:3479` 就用了它），**但它是分析用语**；本批定稿一律写「反着说」 |
| ❌ 用「**时态**」解释 `was`／`is` | ✅「**昨天的事，用昨天版**」「**今天的事，用今天版**」 | **「时态」在表内**（L10 `:1782`／L96 `:17863` 的逐字话术） |
| ❌「**连词** · although」 | ✅「**虽然… · Although 站最前面（后面跟一整句）**」 | 「连词」**可用**（不在 29 词表内，L19 `:3415` 逐字就是「连词 · and / but」），**但本批为统一口径不用它** |

### 4.6 验收清单（D6 逐条可勾）

- [ ] **G1**：3 课 practice ≥4 题（**L141 六题**），且各含 1 道与 variants 逐字一致的非肯定卡变体题（L139 `Will you go out although it is raining?`／L140 `It is raining, but will you go out?`／L141 `Will you go out although it is raining?`）
- [ ] **G1-b**：**每课 ≥1 复现题**（L139 复现 L19／L20 各一；L140 同；**L141 三道复现**）；**`practice` 与 `examples` 重合 <4**；**⚠️ 逐句核「同一句最多出现在 6 课」红线（本批三句头寸 1／0／0）**
- [ ] **G2**：tokens／answer 词集一致＋distractors 不与答案词重复（`:31-50`／`:52-67`）；**逐题 `tokens` 逐字照抄 PRD 定稿**（**标点跟在前词后**）
- [ ] **G2-b**：零术语红线 29 词**全课三字段零出现**＋本批额外 12 词自查；**「从句」「状语」0 命中（头号两词）**；**「让步」「主句」「转折」「连词」「门牌」0 命中**；AI 引用源三字段（`contrast.whyZh`／`guided.explain`／`recall.noteZh`）同样 0 命中
- [ ] **G3**：3 课 recall 三字段（`promptZh`／`intentZh`／`answer`）非空
- [ ] **G4**：目标句逐句 ≤8 词（L139 8／L140 8／**L141 复用 L139 句**）；一课一增量（**L141 零新知已写明，`grammarLabel` 不写新句型标签**）
- [ ] **G5**：3 案 tag 全落 10 枚举；**不碰 comparison**；**`run_on` 三处已逐案核「不同句、不同词」（`but`／`so`／回流）**
- [ ] **G6**：每案 4 错＝新 2＋旧 2（**#150＝三点锚本批＋一点锚旧课**）、单 token 可修、≥1 净词、`reviewed: true`、`tokenIndex` 对齐、**#148–#150 连续**；旧错只取 L10／L11／L19／L25
- [ ] **G7**：`tagStats=10`（`huntService.test.ts:109`）；番外 5 案名单未动（`:215`）；3 新案**全被 L139–L141 引用**（引号案数 147→150）
- [ ] **G8**：season-22 `{139,141}`＋m24（afterLesson 141）已上线；路径页 141 课可见、m24 触发正常
- [ ] **G8-b**：`grammarSeasons.test.ts` **4 项全绿**；**m24 人工核**（无守门）
- [ ] **G9**：不新增未引用案；`reviewed` 全 true
- [ ] **G10**：3 课 episode 全汉字数字（「一百三十九」…「一百四十一」）
- [ ] **G11**：语料锁闭集逐条——**`although` 全批只在 3 课**；**`though` 全批零出现**（案 #7 2 处一字不改）；**L19／L20 引用三句逐字**；**本批零出现 `until`／`waited`**；**禁用词全 0**（`even`／`even though`／`despite`／`in spite of`／`as soon as`／`unless`／`in case`）；**复现句头寸逐句核验已登记**
- [ ] **G12**：3 课 cloze 按生效词表**实跑登记**；**ambush 侧 21 句全落 `is`／`was`／`will`（考点词 0%）**；**boost 保障句 4 处落 `Although`**（**落盘后即刻复跑**，设计态 → 落盘态）；**逐课核「variants 至少 1 句让主考点落空」**
- [ ] **G13**：3 课时长 12–15 min（**L139／L141 重点抽查，分段计时**）
- [ ] **G14**：**L1–L138 一字不动**（含案 #7 全段 ＋ L19／L20／L109／L96 被认领的原句）；`npx vitest run` 全绿（**以生产时实跑为准**）；`tsc --noEmit` 0 错（**排除 `src/tmp-prd/` 临时脚本**）；build 通过
- [ ] **G15**：封面 3 张已逐张目视核对语义；`cover1`–`cover21`／`cover105`+ 未被使用
- [ ] **G-boost ①**：每课 contrast 6 条中 **3 条带 `wrongMark` 的真实错卡**（禁止贴线；池深 4 道）
- [ ] **G-boost ②**：`wrongMark` **全部含字母**（本批 9 个，无一处为纯标点）
- [ ] **G-boost ③**：3 处 `guided.spot` 的 `wrongToken` 与 tokens 元素**逐字相等**（**含尾标点写法一致**；**L141 是唯一一处元素自带尾逗号的 spot**）
- [ ] **G-boost ④**：每课 contrast ≥3 条、`kind` 覆盖 choose／arrange／spot／replace；**18 组双正解 diffScore 上限 78 < 90 ✓**；**三处待重跑登记**（L139 ① 与 L140 ② 的 89 错卡、L141 ⑤ 的 75）
- [ ] **课号连续性**：139–141 逐一人工核对（缺号永久锁死且无报错）
- [ ] **立项前置**：**八批未提交改动**（`git status --porcelain` **162 条：29 文件 M ＋ 133 条未跟踪**，本路线图实读）**已提交或打 tag**
- [ ] **工程前置（可并行）**：**`although` 的 boost 侧变形干扰项实测**（照 `grammarReviewService.test.ts:298-328` 的 banned 正则做法，把 `Althoughs`／`Althoughed`／`Althoughing` 三条加进名单并实测；**若不产出则不动引擎**）——**本批唯一可能需要动引擎的地方**，须主理人裁（§6.3 序 4）

## §5 风险与缓解

| # | 风险 | 等级（概率/影响） | 缓解 | 触发信号 |
|---|---|---|---|---|
| 1 | **复现取材红线的头寸只剩 1–4 次**（**本批头号机制风险**）——`grammarLessons.test.ts:163-178` 断言「**同一句练习答案最多出现在 6 课**」；**本路线图独立复算：`yesterday i went to the park` 已用满 6 课（头寸 0）**；**只剩 1 次头寸的 5 句**（`i like reading`／`there is a book on the desk`／`i am happy`／`i am used to getting up early`／`it looks nice` 各 5 课）；`he drinks milk every day` 4 课（头寸 2）。**本批三句头寸实算充足**（`The snow is cold but fun.` 1／`It was cold, so I stayed at home.` 0／`It was windy, but we were happy.` 0），**但这是全库级紧约束——每多一批就少一格** | **中／高** | **逐句核验（本批新增为 G11 第 ⑥ 项）**：**生产期用 `practice` 答案逐句比对全库计数**，不得超限；**本批的复现取材只取上述三句 ＋ 本批新句**（**新句当前 0 课，各只 +1**）；**L141 六题里三道复现全部走已实算头寸的三句**；**登记：若未来某句触线，须改走「同结构新句」而不是硬塞**（本批的复现句全部是库内既有原句，**不得改写一个字**） | 生产期某题触线（测试项 `练习答案不得跨课高频复现` 变红）；某句在两批内被第三次取用；`i am happy`／`it looks nice` 被本批顺手取用（**本批明确不用这两句**） |
| 2 | **cloze「假友好」的考点承载**——关 2 回马枪侧考点词成空位率 0%（21/21 全落 `is`／`was`／`will`），**考点在 cloze 通道 100% 逃逸** | **中／中** | **考点承载 100% 压在 `contrast`（3 条 mark）与 `guided.spot`（1 道）**（**两条通道都不走 cloze**）；**boost 侧另有 4 处保障句落 `Although`**（**种子运气，非引擎保证**）；**F22-B 单列「改错题触达率」读数** | 走查者说「这题考的不是今天学的」；改错题触达率 <50%；一课内所有 cloze 空位都是同一词（**本批三课确实全部落 `is`**——**这是设计态，不是异常，须在 F22-A 确认不产生困惑**） |
| 3 | **与 L19 `but` 的混淆**（**本批头号结构性风险**）——L19 `:3429` 逐字只到「反着来用 but（但是）」、`:3493` 逐字只到「两边反着来，用 but」；**两处都没讲站位与重点**。生产期极易把 L140 写成「`but` 的换词课」，或让学生以为「`but` 只能跟 `Although` 一起用」 | **高／高** | **L140 整课承担切开**，只用一条判据：「**先说哪半句**」（`Although` 站最前面 对 `but` 站中间）；**L139 只在 `contrast` ④ 并排一次、不讲轴**（不提前消耗 L140 的增量）；**L140 `contrast` ⑤ 保留「`but` 自己也能站中间」的正解**；**⚠️ 不得把切面写成「句子长短」**——**L19 `contrast` ⑤ 逐字就是「两个半句」的长句**（`I am tired, but I don't want to stop.`），**若写成「长短」会被 L19 既有内容直接打脸**；**中段自走查专列此项**（§3） | L140 讲成「新学一条规矩」；学生要求把 `It was windy, but we were happy.` 改成 `Although` 句；走查问「`but` 自己能不能用」答不出；生产稿只在 L140 讲「怎么用 but」 |
| 4 | **中文「虽然…但是…」负迁移的拦截效果**——这是本批的主靶（`*Although it is raining, but I will go out.`），**全库唯一已被案件显式植错却无课教过的结构**；**但拦截效果无法在写稿期验证**，只能靠走查与遥测 | **中／高** | **三课各 1 条 `run_on` 错卡**（L139 ①／L140 ②／L141 ①）＋**L139／L140 各 1 道 `spot`**；**L140 ② 与 L139 ① 是同句同词的有意「二次切开」**——**但两课 `whyZh` 必须逐字不同**（L139 讲「中文成对负迁移」，L140 讲「第 139 课那条家规 ＋ 位置差」，**不得逐字复制**）；**同一条家规 L20 已教过**（`:3612` 逐字「英语只用其中一个，不成对出现」），**本批是第二次应用，须显式点名不重讲** | 走查问「`Although it is raining, but I will go out.` 对不对」答「对」；L140 该题反应时显著低于 L139；#148①／#149①／#150① 的选错率高居不下（**F22-B 主读数**） |
| 5 | **`Although` 与 `but` 的语义差是否讲清**（＝位置差）——**这是 L140 的全部价值**，也是最容易被降级为「两个字都对，随你挑」的地方 | **中／中** | **L140 `oneLineRule` 定稿**：「同一个意思，两句话都能说：`Although` 站**最前面**，先摆出让步的那半句；`but` 站**中间**，两半句手拉手。**先说哪半句，哪半句就是这次要讲的重点。**」；**`contrast` ① 是本课脊柱卡**（双正解 `bothRight`，`diffScore` 实跑 **78 < 90 ✓**）；**中段自走查专列此项**（§3：三个判据） | 走查问「哪句先说「下着雨」」答不出；走查问「哪句更好」答「有强弱」；`contrast` ① 被写成两条独立规则 |
| 6 | **造词成本（`although` 是新词，且不可替代）**——`although` 双 0，**是本族的身份词，换掉就不是让步**；**`though` 也有 2 处既有占用**（案 #7），**L139 极易顺手把案 #7 的场景词（`cold`／`went out`）也带进来** | **中／中** | **`although` 单独占第 1 课、不藏在长句里**；**`blocks` 把两句拆成可见部件**（`Although it is raining` ／ `I will go out`）；**新旧比例硬纪律：每课 `targetSentence` 里新词 ≤1**（L139＝`although`，**L140／L141＝0 个**）；**`though` 全批零出现**（G11 ②）；**L139 不重复 `cold`／`went out` 作为主场景词**（**但要引 `The snow is cold but fun.` 作 L19 回流——那是明确标注的引用句，不是场景词**）；**档位诚实标注 B，不得写成 A** | F22-A 走查问「`Although` 难不难」答「难」集中；L139 时长超标；走查者说「一直在学新词」；生产稿在 L139 引入 `though` 或 `cold` 作主场景 |
| 7 | **封面图像语义（`cover22`–`cover24` 未二次目视）**——min-gap 117 的解唯一，任何重排都让 min-gap 下降 | **中／低** | **默认按数析 §7.3 唯一最优解顺次指派**（`cover22`→L139 起）；**唯一允许的池内换法＝三张内部互换（集合不变）**，须登记且须同时登记 min-gap 变化；**兜底**：`cover` 为可选字段，缺省回落 scene SVG，**语义不匹配不阻断上线** | 画面与场景不符（早上楼道口／课间／书桌前配错）／试玩者指出「见过这张」／某课无图回落 SVG |
| 8 | **测试基线漂移**——本路线图实跑 **61 文件 / 799 项全绿**（2026-09-20 23:25，6.53s）；数析 §A4 记 **61／799**（＋`tsc --noEmit` 记录）；瑞思 §方法记 **61／799**；批二十一 PRD 记 **61／798**、批二十一路线图记 **61／798**；**工作区仍在被并行改动**（29 文件 M） | **低／高** | **路线图基线统一写「以生产时实跑为准」**；G14 登记实跑值；**D5 预检复跑**；差值为**同期另一路未提交改动**；**以 61／799 为本路线图定稿时刻的实跑值** | `npx vitest run` 文件数/项数与登记值不符／新增失败项；`grammarBoostService.test.ts` 由 50 项变动 |
| 9 | **八批未提交的单点风险**——`git log` 止 `7ad13f5`（Sep 19 06:33）；**本路线图实读 `git status --porcelain` ＝ 162 条（29 文件 M ＋ 133 条未跟踪**，含批十六–批二十一的全部课案资产与多份未跟踪交付物、未跟踪封面资产、`src/tmp-prd/` 临时脚本目录）；`git checkout .` 类操作会一次性丢失八批成果 | **中／极高** | **立项前置动作：先提交或打 tag**（D1 开始前完成）；本批按「**只做新增**」纪律不动前批任何字节；交付后立即提交；**⚠️ 顺带登记：`src/tmp-prd/` 是并行研究遗留的临时脚本目录（16 个 `.ts`，`?? ` 未跟踪），本路线图未创建亦未删除——**建议生产期一并处理（删除或移出 `src/`），否则 `tsc --noEmit` 会因它报 85 条错**（**见附录 C 口径说明**） | `git status` 出现非本批文件被改动／误执行回滚命令／`grammarLessons.ts` md5 与本路线图登记（`2e5f2935…`）不符 |
| 10 | **`run_on` 三案连用的观感（本批破例项）**——全库 `run_on` 只有 **14 处**（最薄档），**本批一次补 3 处**（＋`fragment` 2 处）；先例纪律是「避开连续同型」 | **中／低** | **账已算清**（`run_on`／`fragment` 各 14 处＝全库最薄，**本批是最薄档的真实补血，不是凑型**）；**三条处置缺一不可**（三处不同句不同词／两处 `fragment` 的 correction 型不同／`run_on` 不得都用 `but`——**#149① 用 `so` 就是差异化**）；**F22-B 单列「`run_on` 题判对率 vs 全库均值」** | 走查者说「怎么老是同一种错」；三案 `run_on` 题的判对率显著低于全库均值；#149① 的 `so` 被写成 `but` 型 |
| 11 | **L141 被写成「新知识课」或「L139 复习课」**（零新知红线最易失守处）——两个说法排一行时天然会想补一条新规则，或退化成重复 | **中／中** | **`grammarLabel` 不得写成新句型标签**（写「**收口 · 零新知（两个说法排一行）**」——**不写「让步大章收口」**，含红线词「让步」）；**四句全部逐字取既有、一字不改**；**变体只做一组三态**（**不得给四句各做一套——四套＝把一课变四课**）；**收口复点走查专列此项**（§3） | 生产稿在 L141 引入新句／新词；四句里有一句非逐字取；L141 对比卡超过 6 条 |
| 12 | **「先考后教」转正的反向风险**——**本批的种子位是「已判错」不是「认读」**（学生见过 `Though it was cold, but we went out.` 还亲手改过它）：**学生可能把「这句是错的」记成「整个结构是错的」** | **中／中** | **L139 开场话术定稿必含**：「那一句里错的是**多出来的 `but`**——`Though` 本身没错，它站对了地方；今天就把这个「虽然」正式学一遍。」（**⚠️ 不得省此半句；若生产期删简，须保留「不是 Though 本身错」**）；**`examples` 不得收录 `Though`**（种子位不转正为教材句——**只转正结构本身**）；**三条护栏（PRD §3.2）**：不得写「第 12 课已经教过」／不得改动案 #7 一个字／不得让「判过」变成「已学」的证据 | F22-A 走查问「案 #7 里那句错在哪儿」答「这句整个不对」；走查者说「第 12 课好像学过」（**它是「判过」不是「教过」**）；生产稿在 L139 `examples` 里收录 `Though` 句 |
| 13 | **`GRAMMAR_WORDS` 表不含连接词类（本批是系统的第二次暴露）**——`although`／`though`／`but`／`so` 全不在表内，**关 2 回马枪侧这类词永远抽不中** | **低／中** | **本批不改引擎**（数析 §5.3／瑞思 §6.4③ 一致判「关 2 的定位是『回马枪』而非『本课考点复练』」）；**登记为下一批研究期的事项**（§6.3 序 3）；**若未来做「连词章」，这类词的 cloze 落点会系统性偏离考点**——**须在批二十三研究期一并裁** | 走查者说「回马枪怎么老考 `is`」；两批以上出现同一现象（**批二十一的 `forward` 是第一次**） |
| 14 | **`can-do-m24` 无守门**——全仓 `can-do-m*` 测试引用数 ＝ 0（数析 §6.2 实读）；漏加不会红 | **低／中** | **D5 展示层闭环时人工核**（G8-b 已列）；**并入批二十三的一次性补齐检查** | 路径页里程碑缺一格（不影响功能，但影响体验）；走查者问「学完这一章有什么徽章」 |
| 15 | **封面二用池的长期耗尽（本批是第二个触发点）**——本批取 3 张后**二用池 21→24 张、单次张 96→93 张**（数析 §7.3） | **低／低** | **本批池内复用、零新资产**；**建议主理人评估「三用」策略或新增封面**（**产品决策，非数据结论**）——见 §6.4 携带项 1；**机制缺口（「课有 `cover` 但图缺失」不报红）沿用登记，未补** | 可用池（≥35 线）缩到 80 张以下；某课被迫用 gap <35 的封面 |

## §6 合并关账

### 6.1 观察窗合并机制（沿用批十–批二十一）

**F9-B / F10-B / … / F21-B 照旧合并关账**——一次读齐**十三批**遥测，**本批 F22-B 并入同一窗口**（判据不改，1–2 周），**一次提交批二十三候补清单并归档**。

- **为什么合并不是拖延**：单个批次的遥测样本量本就偏小，逐批关账会把噪声读成信号；十三批一次读齐可看跨批趋势。
- **本批新增观察项（并入同一读数表）**：
  - **「B 档结清后第一批」的形态回收（本批最重要的一行）**：批二十一（造词＋升格·2 词·5 课）／**批二十二（造词＋先考后教转正·2 词·3 课）**两批并读，**与批十九（升格·0 造词·3 课）三线并读**——**同为 3 课小章的是 season-19 `{125,127}`，是最可比的一条线**（PRD §11 序 13 逐字）。
  - **中文成对负迁移的拦截率**（`Although…, but…` 并存错的改对率 ＝ #148①／#149①／#150① 的选错率）——**这是本批设计的全部目的所在，也是「先考后教」这条路是否值得复制的唯一判据**。
  - **「位置差」的判对率**（`Although it is raining, I will go out.` 对 `It is raining, but I will go out.`——哪句先说「下着雨」）。
  - **「`but` 自己也能站中间」的反向风险率**（学生是否以为 `but` 必须跟 `Although` 一起用）。
  - **`run_on` 三案连用的判对率 vs 全库均值**（**本批破例项，单列**）。
  - **`Although` 作为新词的接受度**（走查问「难不难」）**与 `although` 的 cloze 空位被察觉率**（若学生反映「怎么老是抽 `is`」，则 L139 的考点承载须整体挪到 `spot`）。
  - **「先考后教转正」的复述正确率**（L139 开场后学生对「错的是多出来的 `but`，不是 `Though` 本身」的复述）。
  - **m24 触发**与 season-22 分组可见性；**3 课小章的完成率曲线 vs 批十九 3 课／批二十一 5 课**（三线并读）。
- **未关账项的携带**：批十七 **F17-A／F17-B**、批十八 **F18-A／F18-B**、批十九 **F19-A／F19-B**、批二十 **F20-A／F20-B**、批二十一 **F21-A／F21-B** 十道门**全部未回**——本批开工**不等待**（写内容 vs 读数据互不锁）。**本批的实测回收点＝F22-A（L139–L141 三课的完成率曲线）**，须与批十九 3 课／批二十一 5 课两线并读。

### 6.2 批二十三候补清单（**一次提交；三研究口径＋形态问题的结论**）

> **⚠️ 前置说明（本批的新变化）**：批二十一交付后**旧候补池已见底**（所有 B 档及以上项目在其候补清单里已列完）；**本批的候选全部由批二十二三研究重新论证产生**，**且已包含「复现型大章／换轴」两个形态问题的结论**（**均判不做，有跨源依据**）。

| 序 | 候选 | 档位／依据 | 建议形态 | 前置条件 |
|---|---|---|---|---|
| **1** | **`as soon as`（一…就…）——批二十三首选** | **B 档**：① **Cambridge `conjunctions-time` 明文把 `when, once, as soon as` 放在**同一小节**（逐字 "We use when, once and as soon as to talk about a specific point in time when something happened or will happen."）＋**❌will 明文**（`I will call you as soon as I get to the office.` 对 `as soon as I will get to the office.` 明标错）；② **BC 参考层同规则**（"We do not normally use will in time clauses and conditional clauses."＋❌NOT will finish work）；③ **词典 `as soon as` 独立标位 B1**（`soon` 本词 A2）；④ **中文侧 `conjunctions` 专文「時間關係」七项并列**（`when/while/before/after/since/as soon as/until`） | **3 课小章**（立岗／**与 `when` 切开**／六格收口）——**切开必须独立成课**（这是它唯一的独立价值，也是化解「只差一刻度」的唯一办法） | **⚠️ 必须先裁与 L109 的形式冲突**（**本批携带项 1**）：竞析 §4.2 开三条处置——① **换场景（竞析推荐）**：不用 `rain`，改 `The phone rings`／`I get the letter`／`The movie ends`，**把「雨停」留在 L109 专属**；② **保留 `rain` 但显式指认**（`oneLineRule`／`deepDive` 须逐字引用 L109 的「两边都得用昨天版」并补「今天前面说的是将来，所以后面用现在」）；③ **换词性**（`It stops raining`，实测全库 0 处）。**本 PRD 建议采纳 ①**；**须主理人裁**。**另需核**：`as` 的第三义（L65 `as tall as` 占 `as` 的 61/70＝87%）＋ **须造 `soon` 1 词**（GL 0／HC 0，本路线图复验） |
| **2** | **`seem`／`appear`（看起来好像）** | **C＋（由批二十 D 升）**：Cambridge 词典 **`seem` B1**／**`appear` 的 SEEM 义 B1** ＋ **专门 Grammar 小节 `Appear or seem?`**；**BC `Stative verbs` 表内并列**（`appear, be, feel, hear, look, see, seem, smell, taste`——**`sound` 缺席**）；**中文侧 `seem` 专文七句式 ＋ 一条硬禁用**（"Appear 後面不能加 like/ as if/ as though"）；**Murphy 双册三通道全 0**（**课程位仍零**） | **3 课小章**（备选 A） | **须回答「与 L125 `It looks nice.` 的语义距离」**（「看起来」vs「好像」对零基础几乎同义，**第 1 课有与 L125 重复的风险**）；**须造 2 词**（`seem`＋`appear`，本路线图复验双 0）；**BC 的 `Stative verbs` 规则不可教**（"aren't usually used in the present continuous form" 会撞零术语红线的「时态」，**只能作深挖卡认读**） |
| **3** | **`would rather`（宁愿）** | **B−**：**Murphy 中级 U59 标题逐字 `prefer and would rather`**〔瑞思 §1.4 本机 PDF 原件逐单元抽出〕；**词典 headword `would rather` 独立标 B1**（注 `(also 'd rather)`）；**Cambridge 专页给两条轴**（同人＋原形／换人用过去式）＋**两条典型错误逐字** | ⚠️ **2 课封顶，无收口位** | **中文侧零专文**是唯一硬伤（sitemap `rather` 只命中 `rather-than`＝「而不是」义）；**须造 `rather` 1 词**（本路线图复验 `would rather` 与 `rather` 双 0；`would` 本身已在库） |
| **4** | **`neither`／`either`／`both`（三连体）** | **B−**：**词典三段位齐**（`both` A1／`either` B1 两义／`neither` B2）；**Murphy 初 U82 ＋ 中 U89**（＋初 U42 附和轴，中级无）；**Cambridge 两页硬轴**（"We don't normally use both（of）+ not…"／"We use neither, not none…"）；**中文侧两篇专文＋10 条 ❌**（本批候选里最多） | ⚠️ **3 课封顶，且第三课必须是「否定位置」这条轴**（不能是换词） | **造词 4 个超先例上限**（`both`＋`neither`＋`either`＋`nor`，**批二十 3 个已是历史最高**；本路线图复验四词双 0）；**连接词家族只有 L19／L20 两课可复现**（数析 §3.1 G5 判「薄」） |
| **5** | **`the same as`** | **C**：词典 EXACTLY LIKE 义项 **A2**；**BC 无落点**（`Comparative adjectives` 页 `the same as` ＝ 0／`as … as` ＝ 0）；**Murphy 三通道 0**；**中文侧 sitemap `same` 零命中** | ❌ **不单开** | **形态是「比较结构的附属品」**（要两个可比对象），**与「小美的一天」单人视角不合**；且我方 `same` 的 2 处（L112 `They look the same!`／L138 `Are they the same?`）**已在用「一样不一样」这个语义**，做正课会与 L138 抢话 |
| **6** | **`shall we`／`why not`／提议家族** | **`shall we` D**（**上游 `suggestions` 专页收五种提议、不收 `shall we`**；Murphy `shall` 四处全在 will 将来义）；**`why not` C**（B1 ＋ 语法页 `suggestions` 明文收它） | ❌ **不排期／可折入** | `shall we`：**提议场我方已由 L75（`Let's`＋`How about`）占满**，再开一格只能靠「第一人称 vs `Let's`」一条细线；`why not`：**与 L75 `How about` 是同一小节的两种问法**（**「一课一增量」的反面**），**可作 L75 的补课或折卡** |
| **7** | **感官 `like` 扩展**（`sound/smell/taste/feel like`） | **D**（批二十已判） | ❌ **不排期** | **它的第 1 课必然与 L127 的认读种子 `It looks like rain.` 重复**；且 `X + like` 是「同一骨架换搭配」，**「一课一增量」只能靠「like ＝ 像」这一条轴撑一次**；**⚠️ 数据补充（数析 §A6-5）**：`look` 家族在 L125–L138 **连续 14 课在场**（`looking` GL 215 处里 L134–L138 占 199 处 ＝ 92.6%）——**再碰感官族须计入「看腻率」风险** |
| **8** | **`will have`（将来完成）** | **C（理由已改写）**：**课程位这次是「有」**——**BC B1-B2 第 8 课 `Future continuous and future perfect`**（逐字 `will/won't have + past participle` ＋ `by the time` ＋ 七条例句）＋**Murphy 中级 U24 `will be doing and will have done`**；**中文侧 `future-perfect-tense` 专文** | ❌ **不单开** | **判 C 的理由改为「课程位在但场景不在」**（将来完成要求「到那时已经做完」的完成视点，**与单日线叙事不合**）＋ **U24／U46 属中级册（跨级）**＋ **须并造 `by the time`**（本路线图复验 `will have` 双 0）；**⚠️ 「跨源无课程位」这条理由不再引用**（竞析 §7 口径校正 6） |
| **9** | **C 档 `have sth done`** | **C（维持撤出，口径零变化）**：BC 三档 68 课零课位；Murphy 仅中级 **U46**（跨级）；Cambridge `have-something-done` 专页含 Warning「not the same as perfect tenses」；**我方词架本轮复算仍近零**（`will have` 0／`cut` 0／`fixed` 0／`haircut` 0／`had my` 0／`checked` 0／`repaired` 0；**唯一差异：`have my` GL 1**） | ❌ **不单开** | **⚠️ 本批新增两条否决理由**：① **`'ll` 缩写全库 0 处**（`grep -o "'ll"` ＝ 0——**将来时的缩写形式从未在库里出现过**，而 `I'll have my hair cut.` 是最自然的招牌句，**等于在同一课里连开两个新区**）；② **对撞面比批二十一记的更深**——**L21 `I have done my homework.` 是「词形完全同」的真对撞**（`have` ＋ 过去分词），**L107 `The teacher had me come early.` 是「同一个 `had`」的近邻**（批二十一未记） |
| **10** | **形态问题一：A 档复现型大章** | **❌ 不做（跨源＋数据双依据）**：① **上游四源没有一家做「把已教结构重新编成新章」**（Murphy 用书末 Study guide ＋ Additional exercises，**不占正课单元号**；BC 只有课内嵌 Grammar test；Duolingo 用路径内嵌 practice／Legendary／老节点重玩——**共同点是「内容不变，变的是入口、排程、访问」**）；② **我方数据结构无承载字段**（`GrammarLesson` 无 `isRecap`／`newWordCount`／`revisitRatio`；**「收口课」的唯一可用结构性判据是「季末位置」，而它只能识别「1 课收口」，识别不了「整章复现」**——**「一整季零新知」在本项目零先例**）；③ **会与我方四个复现装置抢职能**（Review SM-2／Revisit 关 2／Reaudit 关 3／Boost 趁热练——**关 3「旧案重审」做的正是「旧结构新案情」，与复现型章职能重叠最直接**）；④ **错点侧会立刻暴露为「全回流」**——全库 562 条错点里只有 103 条含「回流」（18.3%），**「连续两案全回流」从未出现**（#142 与 #147 之间隔了 4 案），且回流池很浅（103 次里 L10／L11／L19／L25 四课占 62 次 ＝ 60%） | **替代动作＝「复现入口显性化」（三条，可独立于内容先行）**：① 路径页把「到期复习卡数」显性化；② **把「本章的复现地图」做成章末收口课的一屏**（**不新增课，只新增展示**——**这正是 L141／L133 型收口课已经在做的事**）；③ **把「跨批复现」做成里程碑**（**照 m23 的形态，`can-do-m24` 已用「回头看」的口吻列跨批句子**）。**本轮不做，非本批内容** |
| **11** | **形态问题二：换轴（语法+读写／听力）** | **❌ 不做混合课（跨源依据）**：**BC 的 `free-resources` 把 Listening／Reading／Writing／Speaking／Grammar／Vocabulary 分成六个独立栏目**（**Grammar 与 Vocabulary 用合并带「A1-A2 grammar」，技能按单档「A1 listening」——上游把两条轴用不同的分档粒度隔开**）；**我方冒险阅读与语法线零耦合**（`AdventurePlayPage.tsx`／`adventureService.ts` 的 `grammar` 命中 ＝ 0／0）；Duolingo 虽有但**内嵌在路径里** | **若试水，只做「复现通道」**：**听力轴＝最低成本**（`listen` **138/138 课可用**，素材现成 **528 组**非 `bothRight` 的 `contrast` 句对，**0 新词**）；**日记轴＝次低**（`GrammarDiaryPage`＋`diaryService` 已按同一套 10 罪名归因，**只需改问题池**）；**阅读轴＝不做**（全库零阅读理解题型，须新引擎，**且在零术语下不能用体裁词**）。**不新增「读写课」课型；不把冒险阅读文本搬进语法课；不改 `GrammarLesson` 接口** |

**⚠️ 批二十三的候选池结构（一句话）**：**批二十二交付后，候补池里最高只到 B 档（`as soon as`）**——**且三研究一致指出「十项里九项零课程位」是 B 档结清后的常态**（竞析 §3 小结逐字：「候选池整体下移到 B／B−／C，且全部落在『跨源有规则页、无课程位』这一层」）。→ **建议主理人在 F22-B 关账时一并裁「造词上限政策」**（`neither/either/both` 的 4 词成本仍悬置）**与「`as soon as` 的 L109 冲突处置」**两件。

### 6.3 本批携带项（不关闭，随关账一并复核）

| # | 携带项 | 状态 | 本批处置 |
|---|---|---|---|
| **1** | **`as soon as` 与 L109 的形式冲突**（**批二十三开工前必须先裁**） | L109 `:20328` 把 `the rain stops` 明设为错项；而 `as soon as` 的正确用法恰是 `stops`（现在版表将来）——**同一短语两种命运，差别只在主句版本** | **本批不做 `as soon as`，冲突登记为携带项**；**竞析 §4.2 三条处置须择一**（**建议 ① 换场景**）；**本批与 L109 零交集（G11 已锁）**，**不阻塞本批** |
| **2** | **`though` 的立岗顺位** | 本批判「只作对照、**且最终连对照位也不写**」（**`though` 全批零出现，案 #7 是它唯一出处**） | **顺位为批二十三之后的候补**（**与 `even though`／`in case`／`unless` 三条一起评估**——**Murphy 中级 U113／U114／U115 是连续三个单元，可能是一条撑 2–3 课的连续课位链**〔瑞思 §7 序 3〕）。**观察点**：走查里学生对「同一个意思还有别的说法吗」的回答——**若高频出现「还有 though」，说明它已是活跃缺口** |
| **3** | **`GRAMMAR_WORDS` 是否扩容以覆盖「连接词类」** | 当前表内不含 `although`／`though`／`but`／`so`／`because` 等连词类（**本批实读 `:160-186` 逐字确认**）——**若未来做「连词章」，这类词的 cloze 落点会系统性偏离考点** | **本批不动**（**数析 §5.3／瑞思 §6.4③ 一致判不改**）；**登记为下一批研究期的事项**（**代价：影响全库 138 课的关 2 回访问卷，回归面远超单批收益**——批十七数析对 `neither/either/both` 提过同样建议，当时判不改） |
| **4** | **boost 侧 `Although` 的变形干扰项**（**本批最有可能在落盘后爆的一个点**） | 答案 `Although` 作为「普通实词」会进入 `-s/-ed/-ing` 变形分支（`grammarBoostService.ts:286-330` 实读），**理论上会产出 `Althoughs`／`Althoughed`／`Althoughing`** | **默认处置：先按 `grammarReviewService.test.ts:298-328` 的 banned 正则做法加三条断言并实测**——**若实测产出，则在变形分支加「连接词不做变形」白名单**（**该白名单是本批唯一可能需要动引擎的地方，须主理人裁**）；**若不产出，不动引擎** |
| **5** | **L139 与 L140 的 `guided.spot` 逐字相同**（**本批唯一一处「同题两课」**） | 两课 `tokens` 都是 `["Although","it","is","raining,","but","I","will","go","out."]`、`wrongToken` 都是 `"but"`——**这是有意的二次切开**（L140 的 `correctionZh` 与 `explain` 不同） | **保留**（L140 的 `spot` 是「二次切开」的教学动作）；**观察点**：L140 该题的反应时与跳过率；**备选**：L140 的 `spot` 换成 `It was raining, but we was happy.`（`wrongToken: "was"`）——**代价：丢掉 `run_on` 的二次切开** |
| **6** | **`run_on` 三案连用的观感**（本批破例项） | 本批 `run_on` 3 处 ＋ `fragment` 2 处，**是全库最薄两档的集中补血** | **不改**（**`run_on` 是本章主靶本身**）；**备选不可行**（#150① 改 `article` 型——**但本批句子没有需要 `the` 的名词位**）；**F22-B 单列判对率** |
| **7** | **L141 的 `practice` 六题是否超载** | 本批 L141 配 **6 题**（沿用 L124／L133／L138 收口课六题形态，先例三次） | **保留 6 题**；**观察点**：L141 的完成时长是否显著高于 L139／L140（**G13 的分段计时抽查点**）；**备选**：砍到 4 题（**代价：丢掉 2 道复现位**） |
| **8** | **封面二用池长期耗尽** | 本批交付后二用池 **21→24 张**、单次张 **96→93** | **本批零新资产；建议主理人评估「三用」策略或新增封面**（**产品决策，非数据结论**） |
| **9** | **`can-do-m24` 无守门** | 全仓引用数 0（数析 §6.2 实读） | 本批人工核（G8-b）；**建议并入批二十三的一次性补齐** |
| **10** | **`comparison` 死枚举未核归属** | `types.ts:419-430` 存在、`GRAMMAR_ERROR_TAGS` 10 元不含它、**HC 实读 0 处** | **本批继续不用**（沿用批十八起口径） |
| **11** | **封面资产「课有 `cover` 但图缺失」不报红** | 机制缺口（数析 §A5-3 登记） | 本批封面三张已实读存在（`ls \| wc -l` ＝ 117）；**机制缺口未补** |
| **12** | **L19 标号口径** | 本批 #148④／#149④／#150③ 沿用 L19 | 若裁改标 L51，**本批三案须同改**并回溯登记前批（**沿用批十七–二十一口径**） |
| **13** | **`src/tmp-prd/` 临时脚本目录**（**本路线图新发现**） | `?? src/tmp-prd/`（16 个 `.ts`，未跟踪，**属并行研究遗留**）——**`tsc --noEmit` 因它报 85 条错（全仓其他文件 0 错）** | **本路线图未创建亦未删除**；**建议生产期一并处理（删除或移出 `src/`）**，否则 G14 的 `tsc --noEmit` 判据须按「排除该目录」口径执行（**不得静默**） |
| **14** | **`grammarBoostService.ts:768` 注释漂移**（数析 §A6-5 发现） | 源码注释写「全库 660 组」`contrast` 句对，**实读现态 828 组**（138 课 × 6；非 `bothRight` 的 528 组才是 listen 素材） | **本批不改代码**；**建议生产期顺手改注释**（**纯注释，无行为影响**） |

### 6.4 与批二十一的接口对照（本批须明写的两条差异）

| 项 | 批二十一（L134–L138） | 本批（L139–L141） | 说明 |
|---|---|---|---|
| **批型** | 造词 ＋ 认读升格（2 词 ＋ 1 句种子位） | **造词 ＋ 先考后教倒挂转正**（2 词 ＋ 1 处已判错的种子位） | **本批的种子位不是「认读」，是「已判错」**——**教学含义更深（学生已有一次操作经验），风险也更高**（可能把「这句错」记成「结构错」） |
| **cloze 保障句** | 5 课里只有 3 课配得出（`looks`／`look`） | **3 课课课配得出，L141 有两处**（共 4 处落 `Although`） | **原因是 `Although` 是句首词，而 boost 侧候选位是全句实词（不排除首词）**——**只要候选位数够少、种子合适就能抽中**；**⚠️ 但这是「种子运气」不是「引擎保证」，落盘后须复跑** |
| **复现取材压力** | L138 六题形态曾是最近一次触线压力点 | **本批三句头寸实算充足（1／0／0）** | **但红线是全库级紧约束**——本批新增为 G11 第 ⑥ 项，**逐句核验** |

---

## §7 决策记录（产品负责人视角）

**本批的战略意义：B 档结清后的第一格——旧清单结清，新一轮选题研究开出「让步与转折」。**

批十七用 BC A1-A2 官方 18 课全目逐条点目证实 A2 段关账；批十八用 `be/get used to` 6 课单拱开了 B1 第一格；批十九用 `look + 那个「怎么样」的词` 3 课小章开了 B1 段的词法-句法接口；批二十用五种感官 6 课把同一骨架铺满四格并第一次造三个动词；批二十一用 `look forward to` 5 课结清了 B 档系列并**第一次明写「候补池已见底、批二十二的选题不能再沿用本清单，须由新一轮选题研究产生」**；**本批回答的是「新一轮研究开出了什么」**——答案是：**开一条新的高价值结构线「让步与转折」，档位 B，3 课单拱。**

**本批的性质与前四批的对照（五种形态各有先例，本批是第五种）**：

| 批 | 形态 | 新造词 | 种子位 | 上游档位 |
|---|---|---|---|---|
| 批十八 | **升格课**：给已有句子发身份 | 0（`used` 有 L93／L100 垫子） | — | B1（Murphy 单元级 U61） |
| 批十九 | **升格课**：认领沉睡的 NPC 对白 | 0（16/16 是既有台词） | — | B（零冲突但无课程位） |
| 批二十 | **造词课**：先造出三个动词，再造句子 | **3 个动词**（全 0）＋1 个半垫子（`feel`） | — | B−（诚实标注） |
| 批二十一 | **造词 ＋ 认读升格**（第一次双成本结构） | **2 个**（`forward`／`seeing`） | **认读**（L133 两处，见过但没考过） | **B＋** |
| **批二十二（本批）** | **造词 ＋ 先考后教倒挂转正**（本项目第一次「还教学债」） | **2 个**（`although`／`though`） | **已判错**（案 #7 的 `Though + but`，学生亲手改过） | **B**（BC 课程位级专课 ＋ Murphy 专单元，**但我方真零**） |

**本批同时兑现五笔账**：

1. **B 档结清后的第一格（本批最大特征）**——**旧清单一条不引，全部由新一轮研究产生**；档位回到 B（**比批二十一的 B＋ 低一档，比批二十的 B− 高一档**），**且是「跨源课程位最硬、我方底座最薄」的组合**。
2. **还清全库唯一一笔「先考后教」教学债**——案 #7（挂 L12）里那句 `Though it was cold, but we went out.` **从第 12 课起就挂在那里，138 课没有一课教过**；本批第一次把它转正。**同型先例是批十七的 `-ed/-ing` 形容词，但本轮这条更硬**（那批是「案件里出现同型错」，本项是「案件里直接植入了这个结构本身的错」）。
3. **「反着说」这条线第一次正面立起来**——此前只有 L19 的 `but` 一条路（`:3429` 逐字「反着来用 but（但是）」、`:3493` 逐字 `cold but fun（虽然冷但好玩）`——**L19 的 deepDive 已经把中文「虽然」写出来了，但只给了 `but` 这一条路**）。本批 L140 把**「同一个意思、两个说法」的位置差**立成一课（**L19 从未讲过的轴**）。
4. **中文「虽然……但是……」这条最高频的成对负迁移第一次被正面拦截**——**且它是「同一条家规的第二次应用」**（L20 `:3612` 逐字「英语只用其中一个，不成对出现」＋`:3629` 逐字「中文「因为……所以……」成对出现，英语 because 和 so 只能来一个」）——**本批只点名、不重讲**。
5. **本批第一次把「复现取材头寸」写成可执行的核验项**——瑞思本轮的方法学发现（「同一句最多出现在 6 课」红线头寸只剩 1–4 次）**在本批第一次进入验收清单（G11 第 ⑥ 项）**，并给出逐句实算（§附录 C）。

**决策点列表**：

| # | 决策点 | 时点 | 输入 |
|---|---|---|---|
| 1 | 封面 3 张图像语义核对与最终指派（**唯一允许三张内部互换**） | D1 | 逐张人工核对（**本路线图未二次目视**，见附录 D-1）；`cover22`–`cover24` 为唯一最优解 |
| 2 | **F22-A 打样门**（放行本批） | D2 | L139 首玩走查记录（含**「先考后教转正是否被读懂」本批第一读数**） |
| 3 | **中段自走查（L140）**——位置差是否被接住／二次切开是否生效／与 L19 的接口（`but` 自己也能站中间） | D3 | L140 生产即查 |
| 4 | **收口复点（L141）**——两句串台／排一行收口是否成立／零新知红线（不得引入新句新词、不得四套变体） | D4 | L141 生产即查 |
| 5 | 展示层闭环（season-22＋m24） | D5 | 路径页 141 课走查 |
| 6 | **术语越线就地重写**（若三字段命中「从句」「状语」「让步」「主句」「语序」「转折」「连词」「门牌」） | **D1–D4 生产即查** | 硬红线，**不设容忍度**；不进入验收 |
| 7 | **`Although` 的 boost 侧变形干扰项实测**（**本批唯一可能需要动引擎的地方**） | **D5（预检）** | 先加 banned 断言实测；**产出才动引擎，须主理人裁** |
| 8 | **「造词 ＋ 先考后教转正」负荷若被实测证明过高**（本批独有决策） | **F22-A（D2）＋F22-B** | **退回「砍 L140 并入 L139」的 2 课方案须主理人裁，不自行缩量** |
| 9 | 全量验收放行 | D6 | G1–G15＋G-boost＋**实跑基线**＋`tsc`＋build |
| 10 | **F22-B 合并裁决**（含批二十三候补清单） | 上线后 1–2 周 | F9-B–F22-B 十四批一次读齐 |
| 11 | **`as soon as` 的 L109 冲突处置择一**（**批二十三开工前的必须前置**） | **F22-B 之后／批二十三立项前** | 竞析 §4.2 三条处置（**建议 ① 换场景**）；**不裁则批二十三无法开工** |
| 12 | **造词上限政策**（`neither/either/both` 的 4 词成本仍悬置） | **F22-B 关账时** | 批二十 3 词已是历史最高、批二十一／二十二各 2 词；**4 词是否获批须主理人裁** |

**硬纪律（本批不可破）**：课量 **3 不扩不缩**（**第 4 课无真实增量**）；**一课一增量**（**L141 零新知**）；**每课 contrast 6 条中 3 条带 `wrongMark` 错卡**（禁止贴线）；`wrongMark` 不得为纯标点；`wrongToken` 与 tokens **逐字相等**（含尾标点）；课号 139–141 连续；**L1–L138 一字不动**（**认领 ≠ 编辑**——含不改案 #7 `huntCases.ts:327-370`、不改 L19／L20／L109／L96 被认领的原句）；tagStats=10 不动、不新增枚举、不改 schema；**comparison 绝不进案件**；**本批 `run_on` 3 处／`fragment` 2 处为有意破例，须逐案核「不同句不同词」**；不引番外 5 案；**零术语红线 29 词＋本批额外 12 词全禁**（**头号红线＝「从句」「状语」不入三字段 ＋「让步」「主句」「转折」「连词」「门牌」不入任何字段**）；**`although` 不得写成引导「从句」**；**不得写「第 12 课已经教过」**；**全批零出现 `though`／`even`／`even though`／`despite`／`in spite of`／`until`／`waited`／`as soon as`**；**禁用 `despite` 换词课**；**池外零新资产**（`cover1`–`cover21` 禁用、`cover105`+ 本批禁用）；**双正解卡 diffScore 必 <90**；**复现句头寸逐句核验**；**生产时逐条实跑并登记**。

---

## §8 开放问题日程

| # | 开放问题 | 验的时点／门 | 默认方案 | 兜底／升级条件 |
|---|---|---|---|---|
| 1 | **「先考后教转正」的教学效果**（**本批头号观察项**）——**种子位是「已判错」不是「认读」** | **F22-A（D2）＋F22-B** | **不改话术**（PRD §3.2 定稿已含「不是 `Though` 本身错」半句）；**`examples` 不得收录 `Though`**（种子位不转正为教材句） | **反向风险＝学生把整个结构记成错的**：若走查问「案 #7 里那句错在哪儿」答「这句整个不对」集中 → **开场补一句「那一句里错的是多出来的 `but`」**（**兜底成本为零**）；**若仍不接 → 登记进 F22-B，不硬改本批** |
| 2 | **中文成对负迁移的拦截效果**（`Although…, but…`） | **D2／D3 走查 ＋ F22-B** | **三课各 1 条 `run_on` 错卡 ＋ 两课各 1 道 `spot`**；**L140 ② 与 L139 ① 是同句同词的有意二次切开，但两课 `whyZh` 必须逐字不同** | **若 #148①／#149①／#150① 的选错率无下行** → **L139 `contrast` ① 提位为第 1 条，并在 L141 加一处回流**（**不新增课**）；**若三批仍无改善 → 提为教学法级问题，交批二十三研究期** |
| 3 | **`run_on` 三案连用的观感**（本批破例项） | **F22-B** | **不改**（`run_on` 是本章主靶本身）；**#149① 用 `so` 作差异化** | **备选不可行**（#150① 改 `article` 型——本批句子没有需要 `the` 的名词位）；**若判对率显著低于全库均值 → 登记并把「同一批最多几处 `run_on`」提为跨批纪律** |
| 4 | **`as soon as` 的 L109 形式冲突**（**批二十三开工前必须先裁**） | **F22-B 之后／批二十三立项前** | **竞析 ① 换场景**（L139 不用 `rain`，把「雨停」留在 L109 专属） | **三条处置须择一，不裁则批二十三无法开工**；**② 保留 `rain` 但显式指认**为次选（须逐字引用 L109 的「两边都得用昨天版」）；**③ 换词性**（`It stops raining`）为末选 |
| 5 | **`Although` 的 boost 侧变形干扰项** | **D5（预检）** | **先加 banned 断言实测**；**若不产出则不动引擎** | **若实测产出 `Althoughs`／`Althoughed`／`Althoughing` → 在变形分支加「连接词不做变形」白名单，须主理人裁**（**这是本批唯一可能要动引擎的地方**） |
| 6 | **`GRAMMAR_WORDS` 是否扩容以覆盖「连接词类」** | **F22-B 之后（下一批研究期）** | **本批不动**（数析／瑞思一致判不改；**代价：影响全库 138 课的关 2 回访问卷**） | **若未来做「连词章」，这类词的 cloze 落点会系统性偏离考点** → 届时须与「造词上限政策」一并裁 |
| 7 | **L139 与 L140 的 `guided.spot` 逐字相同**（本批唯一「同题两课」） | **D3 走查 ＋ F22-B** | **保留**（L140 的 `spot` 是「二次切开」的教学动作） | **若 L140 该题反应时显著偏低／跳过率高 → 备选＝L140 的 `spot` 换成 `It was raining, but we was happy.`（`wrongToken: "was"`）**；**代价：丢掉 `run_on` 的二次切开** |
| 8 | **`though` 的立岗顺位** | **F22-B 之后** | **顺位为批二十三之后的候补**（**与 `even though`／`in case`／`unless` 一起评估**——**Murphy 中级 U113／U114／U115 是连续三个单元**） | **观察点**：走查里学生对「同一个意思还有别的说法吗」的回答——**若高频出现「还有 though」，说明它已是活跃缺口** |
| 9 | **封面二用池的长期耗尽**（本批是第二个触发点） | **D1 拍板 ＋ 关账携带** | 本批池内复用、零新资产；**二用池 21→24 张、单次张 96→93** | **建议评估「三用」策略或新增封面**（**产品决策，非数据结论**） |
| 10 | **`can-do-m24` 无守门** | **D5（展示层闭环）＋D6（人工核）** | 全仓 `can-do-m*` 测试引用数 ＝ **0**（数析 §6.2 复算）；**漏加不会红，须人工核** | 若漏加 → 路径页里程碑缺一格（不影响功能，但影响体验）；**建议并入批二十三的一次性补齐** |
| 11 | **批十七–二十一的十道门均未回**（F17-A／F17-B／F18-A／F18-B／F19-A／F19-B／F20-A／F20-B／F21-A／F21-B） | **F22-B 合并裁决** | 本批**不等待**（沿用批二十起口径：门未回不阻断开工）；**F22-A 三课完成率曲线须与批十九 3 课／批二十一 5 课并读** | **F21-B 若显示「造词＋升格」成本结构偏高 → 本批的「造词＋转正」设计已给出对照**（不是回避，是对照）；**F19-B／F20-B 若显示形态感知问题 → 批二十三形态决策重估**（**不改本批已交付内容**） |
| 12 | **测试与类型基线口径** | **D5 预检＋D6 验收** | **以生产时实跑为准**；**本路线图实跑：61 文件 / 799 项全绿（6.53s）**；`tsc --noEmit` 全仓其他文件 0 错、**`src/tmp-prd/` 报 85 条（临时脚本，须排除或清理）** | 生产期再变以实跑登记；D5 复跑；`grammarBoostService.test.ts` 由 50 项起核 |
| 13 | **造词上限政策**（`neither/either/both` 的 4 词成本悬置） | **F22-B 关账时** | 批二十 3 词为历史最高；批二十一／二十二各 2 词 | **若批准 4 词 → 批二十三之后的候选池打开一格；若不批 → `neither/either/both` 出局**，候补池收窄到 `as soon as`／`seem`·`appear`／`would rather` 三项 |
| 14 | **`src/tmp-prd/` 临时脚本目录**（本路线图新发现） | **D1（立项前置）** | **本路线图未创建亦未删除**；**建议删除或移出 `src/`** | **若保留 → G14 的 `tsc --noEmit` 判据须按「排除该目录」口径执行，且须登记（不得静默）** |

到点未拍板 → 自动落默认方案并记入决策记录。

---

## 附录：核查留痕

### A. 实读源文件清单（本轮）

**上游交付物（5 份，全部实读）**：`deliverables/product-strategy/prd-grammar-contrast-2026-09-20.md`（析客 PRD，**823 行，§1–§11 齐全；逐课规格 3 课／§3 先考后教转正专章／§4 与 L19 切开专章／§5 中文负迁移＋零术语替换表／§6 cloze 与考点处置／§7 叙事设计／§9 案件 #148–#150／§10 Non-goals／§11 开放问题 13 项**，逐节实读）·`user-research-grammar-twenty-second-batch-2026-09-20.md`（瑞思，**§4 是主骨架；§2 收口课先例 15 课；§6.3 复现设计；§6.4 cloze 保障句；§7.1 口径声明**）·`data-audit-grammar-twenty-second-batch-2026-09-20.md`（数析，**§1 缺口盘点／§1.5 方向 2 词架／§3 复现型章可行性／§5 cloze 落点／§6 基建护栏／§7 封面池／§8 G-boost**）·`competitive-analysis-grammar-twenty-second-batch-2026-09-20.md`（竞析，**§1.1 BC-2 课程位／§4.1 `as soon as` 首选／§4.2 L109 冲突三条处置／§5 复现型章专节／§6 换轴专节／§7 口径校正 9 条／附录 B 原文速查**）·`roadmap-grammar-twenty-first-batch-2026-09-20.md`（格式基准，**543 行，§1.2 三研究结论／§6.2 候补清单／§6.3 携带项／§7 决策记录**）。

**代码／数据（本轮独立复核，实读＋复跑）**：

| 文件 | 本轮 md5 | 本轮实读值 |
|---|---|---|
| `src/data/grammarLessons.ts` | **`2e5f2935f1ce4dea82e8f73e7a4acade`** | **26,108 行／138 课**（末课 L138；episode 末项「一百三十八」在 `:25918`）；**与数析 §A1／竞析「我方快照」逐字一致** |
| `src/data/huntCases.ts` | **`a380680f4a8f93a8df38055d00b40b2c`** | **8,181 行／147 案**（尾案 `number: 147` 在 `:8140`）；**与两源一致** |
| `src/data/grammarSeasons.ts` | **`981646310cb325848c9e590a8f9071f1`** | **21 季**；`:60` 末项 season-21 `{min:134,max:138}` |
| `src/pages/GrammarPathPage.tsx` | **`1dfbe02f79d3ab60b0bb149c65c395dd`** | **23 个里程碑**；`:276` m23 `afterLesson: 138` |
| `src/data/grammarZeroTerms.ts` | **`e767bfea6f2e0c7b4ad83ec530eccbab`** | `:17-27` **逐字实读＝29 词**（含「从句」「语序」「状语」「时态」；**不含「连词」**——文件注释逐字「不包含『动词 / 名词 / 连词 / be 动词』」） |
| `src/services/grammarAmbushService.ts` | — | `:160-186` `GRAMMAR_WORDS` 表**不含 `although`／`though`／`but`／`so`／`raining`／`rain`**；`:190-193` `CLOZE_STOP_WORDS` 含 `but`；`:195-213` `pickClozeWord` 三级回退**逐字实读**（① 语法承载词 → ② 长度 ≥3 且非停用词 → ③ 回退第 2 词） |
| `src/services/grammarBoostService.ts` | — | `:747-751` 双正解 `diffScore >= 90` 静默丢弃（`if (!contrast.bothRight) return;` 在前）；`:477-489` `poolOf` 槽位来源（target／variants／sceneSwings／practice／examples／dialogue／recall／blocks） |
| `src/services/grammarReviewService.test.ts` | — | `:298-328` **banned 正则做法在位**（含 `forwardes`／`lookinged`／`coldes`／`weekendes`／`seeings`），**本批的护栏扩面模板** |
| `src/data/grammarLessons.test.ts` | — | `:11-29` 每课 practice ≥4 且含变体题；`:31-50`／`:52-67` tokens／distractors；`:66-76` recall 三字段；`:149-157` 练习不得整组复用例句；**`:163-178` 同一句最多出现在 6 课（本批头号机制红线）** |
| `src/services/huntService.ts` | — | `:331-342` **10 枚举**（不含 `comparison`） |
| `src/services/huntService.test.ts` | — | `:109` `tagStats` `toHaveLength(10)`；`:215` **番外 5 案名单逐字** |
| `src/data/grammarSeasons.test.ts` | — | **4 项**（`:12` 区间覆盖／`:22` 互不重叠且 `min ≤ max`／`:36` `label`·`hint` 非空／`:43` 最高课号被覆盖）**逐字实读** |
| `src/services/lessonService.ts` | — | `:157` `number - 1` 解锁（**缺号永久锁死且无报错**） |
| `src/assets/lessons/` | — | **117 张 jpg**（本轮 `ls \| wc -l` 复验 ＝ 117；`import cover*` 计数 ＝ 117；**`lesson-22/23/24.jpg` 三张全在**；`cover118`+ 不存在） |

> **md5 对比结论**：**五个数据/展示层文件的 md5 与三研究逐字一致**（`2e5f2935…`／`a380680f…`／`98164631…`／`1dfbe02f…`／`e767bfea…`）——**盘面无漂移，行号引用在本路线图定稿时刻有效**。**工作区仍有 29 文件 M 未提交（并行改动），生产期须重读一次行号**（沿用批十八–二十一纪律）。

### B. raw 交叉验证（`grep` 复跑，本轮）

| 口径 | 命令 | 本轮实读 | 与上游对齐 |
|---|---|---|---|
| 课数 | `grep -c '^    number:' grammarLessons.ts` | **138** | ✅ 三源一致 |
| 案数 | `grep -c '^    number:' huntCases.ts` | **147**（尾案 `:8140`） | ✅ 三源一致 |
| season 数 | `grep -c 'id: "season-' grammarSeasons.ts` | **21**（末项 `:60` season-21） | ✅ |
| 里程碑数 | `grep -c 'id: "can-do-m' GrammarPathPage.tsx` | **23**（末项 `:276` m23） | ✅ |
| **`although`** | `grep -ow -i` GL／HC | **GL 0／HC 0** | ✅ 与 PRD §3.1／瑞思 §1.7①／数析 §1.1 一致 |
| **`though`** | `grep -ow -i` GL／HC | **GL 0／HC 2**——逐行实读命中 **`huntCases.ts:343`（token `"Though"`）＋`:366`（explanation「同样的道理：Though 和 but 不能同时出现，留一个就够。」）** | ✅ 与三源逐字一致（**2 处全在案 #7 错句里**） |
| **`even though`／`despite`／`in spite of`／`even`** | `grep -ow -i` 双文件 | **四串全部 0／0** | ✅ 与三源一致（**「本批禁用」的依据**） |
| **`as soon as`／`soon`／`seem`／`appear`／`would rather`／`rather`／`neither`／`either`／`both`／`nor`／`the same as`** | `grep -ow -i` 双文件 | **十一串全部 0／0** | ✅ 与三源一致（**批二十三候补清单的词架依据**） |
| 场景词 | `grep -ow -i` GL | **`raining` 98／`rain` 103／`go out` 35／`stay at home` 21／`cold` 267／`happy` 124／`tired` 155／`snow` 17** | ✅ 与瑞思 §1.7⑤／PRD §1.3 一致（**场景侧零造词**） |
| scene 在用值 | `grep -o 'scene: "[a-z]*"' \| sort \| uniq -c` | **11 种**：`mansion` 48／`campus` 37／`city` 23／`sparkle` 9／`school` 5／`island` 5／`train` 3／`mystery` 3／`magic` 2／`forest` 2／`snow` 1 | ✅ 与 PRD §1.3 一致（**本批取 city／campus／mansion，不引入新 id**） |
| 课注 id 冲突 | `grep -c 'lesson-139\|lesson-140\|lesson-141'` | **0** | ✅ **三个新 id 全库无冲突** |
| 封面池 | `grep -o 'cover: cover[0-9]*' \| sort \| uniq -c` | **96 张计 1 ＋ 21 张计 2**；二用张恰为 `cover1`–`cover21` | ✅ 与数析 §7.1 一致 |
| 封面资产 | `ls src/assets/lessons/ \| wc -l` | **117**；`lesson-(22\|23\|24).jpg` 三张全在 | ✅ 与数析 §A5 一致 |
| `cover22/23/24` 当前占用 | `grep -n 'cover: cover2[234],'` | `:3968`〔L22〕／`:4150`〔L23〕／`:4333`〔L24〕——**各只用过一次** | ✅ 与数析 §7.3 一致（**本批指派后间距全 ＝ 117**） |
| 尾案 | `grep -n 'number: 14[5-7]' huntCases.ts` | `:8052` #145／`:8096` #146／`:8140` **#147** | ✅ 下号 **#148–#150** |
| episode 末项 | `grep -n 'episode:' \| tail -3` | `:25530`「一百三十六」／`:25724`「一百三十七」／`:25918`「**一百三十八**」 | ✅ 下写「一百三十九」起，**无跳号** |
| **案 #7 判例（本批转正来源）** | `sed -n '327,370p' huntCases.ts` | `id: "hunt-because-so"`／`number: 7`／`tokens[10] = "Though"`（`:343`）／`tokenIndex: 14`／`tag: "run_on"`／`original: "but"`／`correction: "去掉 but"`／`explanation: "同样的道理：Though 和 but 不能同时出现，留一个就够。"`（`:366`）**逐字实读** | ✅ 与 PRD §3.1／瑞思 §1.7② 逐字一致 |
| 案 #7 挂课 | `sed -n '2305,2310p' grammarLessons.ts` | `huntCaseIds: ["hunt-because-so", "hunt-word-order"]`（`:2307`）；所在课 `id: "lesson-12-will"`／`number: 12`／`grammarLabel: "will 将来时"`（`:2127-2131`） | ✅ **「学生第 12 课判过、课里从没教过」成立** |
| L19 家底 | `sed -n '3415p;3423p;3429p;3432p;3434p;3479p;3493p'` | `grammarLabel: "连词 · and / but"`／`targetSentence: "I was busy and happy."`／`oneLineRule: "…反着来用 but（但是）。"`／`The snow is cold but fun.`／`It was windy, but we were happy.`／`summary.rule: "…转折用 but（但是）…"`／`"两边反着来，用 but：cold but fun（虽然冷但好玩）——but 前后在「唱反调」。"`**全部逐字实读** | ✅ 与 PRD §4.1 逐字一致（**本批引用的两句 `:3432`／`:3434` 逐字照抄**） |
| L20 家规 | `sed -n '3598p;3606p;3612p;3615p;3629p'` | `grammarLabel: "连词 · because / so"`／`oneLineRule: "说「因为」用 because 接原因，说「所以」用 so 接结果——英语只用其中一个，不成对出现。"`／`It was cold, so I stayed at home.`／`whyZh: "中文「因为……所以……」成对出现，英语 because 和 so 只能来一个：because 开头，后面就不许再放 so。"` | ✅ 与 PRD §3.3 逐字一致 |
| L109 雨天 | `sed -n '20299,20312p;20326,20334p'` | `targetSentence: "I waited until the rain stopped."`／`scene: "city"`／`sceneSetupZh: "放学时下起了雨，小美在屋檐下一直等，等到雨停。"`／`contrast[0]`: `wrong: "I waited until the rain stops."`／`wrongMark: "stops"`／`whyZh: "前面用了昨天版 waited，后面也得跟昨天版：until the rain 【stopped】。"` | ✅ 与 PRD §1.3 场景闭环 ＋ 竞析 §4.2 冲突条逐字一致 |
| L96 天气垫子 | `sed -n '17847,17868p'` | `title: "当时正下着雨"`／`targetSentence: "It was raining."`／`oneLineRule: "天气句的昨天版：It was raining——今天版说今天（It is raining），昨天版说那天。"`／`examples`: `It was raining.`／`It is raining.`／`It was cold that night.`／`It's cold today.` | ✅ **本批 `it is raining` 有现成垫子（零造词）** |

### C. 测试实跑与关键实算（本轮）

| 命令 | 结果 |
|---|---|
| `npx vitest run`（全量，23:25:58） | ✅ **61 文件 / 799 项全绿（6.53s）**——`grammarBoostService.test.ts` **50 项**全绿（含「全库每课 ≥2 道可换」1254ms／「改错题每轮都有一道」1025ms 两条重负载用例）；`grammarSeasons.test.ts` **4 项**全绿 |
| `npx tsc --noEmit` | ⚠️ **exit 2／85 条错，全部来自 `src/tmp-prd/`（16 个并行研究遗留的临时脚本，`?? ` 未跟踪）**；**全仓其他文件 0 错**（`grep 'error TS' \| grep -v 'src/tmp-prd/'` ＝ **0 行**）→ **登记为携带项 13；G14 判据须按「排除该目录」口径执行（不得静默）** |
| `git log --oneline -1` | `7ad13f5 新增语法第四至十五批课程与 Boost 强化训练`（Sep 19 06:33） |
| `git status --porcelain` | **162 条**：**`M` 29 文件 ＋ `??` 133 条**（含未跟踪资产、`src/tmp-prd/`、多份未跟踪交付物）——**八批交付全部未提交** |
| **practice 答案跨课复用实算**（本批新增） | **已用满 6 课（头寸 0）**：`yesterday i went to the park`（L21/24/93/95/100/104）；**5 课（头寸 1）**：`i like reading`／`there is a book on the desk`／`i am happy`（L113/119/125/128/134）／`i am used to getting up early`（L120/122/124/136/138）／`it looks nice`（L125/126/128/133/134）；**4 课（头寸 2）**：`he drinks milk every day`／`they were playing football`／`could you help me`／`are you used to it`。**本批涉及三句的头寸**：`The snow is cold but fun.` **1 课 → 5**／`It was windy, but we were happy.` **0 → 6**／`It was cold, so I stayed at home.` **0 → 6**／`It is raining hard.` **0 → 6**——**本批安全 ✓（已写入 G11 第 ⑥ 项须逐句核）** |
| **封面池实算**（本批新增） | **可用池（按「距上次使用 ≥ 35」筛，对 L139 起）＝ 83 张：cover22–cover104**（`cover104` 距 L139 ＝ 35 压线过；**`cover105` ＝ 34 出局**）；**本批到各自被指派课处的间距全 ＝ 117**（`cover22`：L22→L139；`cover23`：L23→L140；`cover24`：L24→L141）→ **「从 cover22 起连续取」为唯一最优解（min-gap 117）✓** |
| **案 #7 全段实读** | 20 行逐字（含 `tokens` 18 项、两处 `errors`、`reviewed: true`）——**「转正＝引用不是编辑」，本批一字不改** |

> **基线结论**：**本路线图定稿时刻的实跑值＝61 文件 / 799 项全绿**（与瑞思 §方法／数析 §A4 一致；批二十一 PRD 记 61／798，**差值 1 项为批二十一交付期新增的干扰项断言**）。**路线图统一写「以生产时实跑为准」**，D5 预检复跑并登记。

### D. 未核实项（须生产期登记或人工确认）

1. **封面 3 张的图像语义未二次目视**——指派按**编号间距实算**（min-gap 117，数析 §7.3 二分＋穷举已证唯一解）；**D1 须逐张复核**（画面是否与「早上出门遇雨／课间／书桌前」相符）。
2. **3 案错型分布为 PRD 定稿**（#148–#150 的 4 错骨架），**生产时逐 token 核 `tokenIndex` 与 `tokens` 下标**；**带尾标点的原文（`raining,`／`go.`／`happy.`／`egg.`）须与 `tokens` 逐字一致**。
3. **cloze 落点为 PRD 复刻实跑（ambush 21 句 ＋ boost 逐槽位），但未按「生效词表」在真服务上逐题跑过**——**落盘后须即刻复跑登记**（设计态 → 落盘态）；**boost 的 4 处保障句落点是「种子运气」**——`lessonId` 或句序一改就会变，**若复跑发现落点变了，唯一允许的动作是调整该句槽位（不得改引擎）**。
4. **`diffScore` 18 组为 PRD 实跑（最高 78／最低 11，全部 < 90 ✓）**——**三处须重跑登记**：**L139 ① 与 L140 ② 的错卡 ＝ 89**（**错卡，门不生效；若误配 `bothRight` 会被静默丢弃**——批二十一 L135 ① 的前车之鉴）、**L141 ⑤ ＝ 75（本课最高）**。
5. **无真实遥测**（样本＝1）；**无「核心 500 词」机检资产**——`although`／`though` 是否超纲**无法机检**（两词为本族唯一可替代成分，无法替换）。
6. **批十七–二十一的十道门全部未回**（F17-A／F17-B／F18-A／F18-B／F19-A／F19-B／F20-A／F20-B／F21-A／F21-B）；本批设计**按最保守假设**（不引用任何未回的读数）。
7. **`can-do-m24` 无任何测试守门**（全仓测试引用数 ＝ 0）——漏加不会红，**须人工核**。
8. **`Although` 的 boost 侧变形干扰项未实测**（理论上会产出 `Althoughs`／`Althoughed`／`Althoughing`）——**D5 预检须实测**；**这是本批唯一可能要动引擎的地方**。
9. **上游中文侧四个 slug（`/although-despite/`／`/although/`／`/despite/`／`/even-though/`）只证存在、未读正文**（站点地图层面，瑞思 §7.1⑤ 标记）——**本批的 §10 Non-goals 已明写不引中文侧术语直抄**。
10. **Murphy 双册只有 TOC 抽文，没有正文**——**U113 的正文例句、页码、练习量均未核实**；**引用时只写「标题逐字」**（**不得**写「U113 的例句是…」）。
11. **`src/tmp-prd/` 目录的归属未知**（16 个 `.ts`，属并行研究遗留；本路线图未创建亦未删除）——**D1 须确认：删除 → 登记「已清理」；保留 → G14 的 `tsc` 判据按「排除该目录」口径执行并登记**（**不得静默**）。
12. **本路线图未做任何改仓动作**——只读取与统计；工作区 `M` 列表均为**审计前已存在的未提交改动**，非本次引入。

---

> 本报告由产品战略团队 AI 协作生成，重要决策请由产品负责人审定。
