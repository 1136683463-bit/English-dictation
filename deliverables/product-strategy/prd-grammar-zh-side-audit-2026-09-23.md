# PRD · 中文侧对照：三层穷举的最后一层

**批次**：第六十四批 · 2026-09-23
**主题**：补做文档建议的第三层穷举（中文教学站），完成三层交叉验证
**状态**：已完成。**三层结论一致：A1-A2 范围内无未覆盖语法点**

---

## 一、一句话

文档建议的**三层穷举**（Cambridge 索引 / BC 三档 / 中文侧两站），前两层已在批五十八、五十九完成。
本批补做第三层：实取 `english.cool` **879 条** + `letmeenglish` **965 条**（共 1844），
筛出 168 个语法候选、抽出 56 个可判定语法点，结果 **45 覆盖 / 7 存疑 / 4 缺口**——
**4 个缺口全部与前两层一致，均在 A1-A2 线之上**。

---

## 二、为什么中文侧是独立的一层

Cambridge 与 BC 是**英语母语者写的语法体系**；中文侧是**中文母语者写的**，
更贴近中国学生的实际疑难（例如「中文说『我吃了』，英文为什么分那么多种」）。
它用来**验证前两层是否漏项**——如果中文侧捞出前两层没有的东西，说明英语视角有盲区。

---

## 三、实取的 1844 条

| 站点 | sitemap | URL 数 |
|---|---|---|
| `english.cool` | `/wp-sitemap-posts-post-1.xml` | **879** |
| `letmeenglish.com` | `/post-sitemap.xml` | **965** |

⚠️ **两个取数陷阱（记入归档 README）**：
1. `english.cool` 把全部 URL **压在一行 XML 里**——`grep -c "<loc>"` 返回 **1**，
   必须用 `grep -o "<loc>" | wc -l` 才数得对（实测 879）。
2. `letmeenglish` 每页有 `zh-hans/` 简体镜像 ⇒ **去重后 483 条**。

**语法相关筛选**：`english.cool` 94 条 / `letmeenglish` 74 条。

---

## 四、覆盖结果

口径：**具体英文实例检索**（与前两层同一口径）。

| 结果 | 数量 |
|---|---|
| 已覆盖 | **45** |
| 存疑 | 7 |
| **缺口** | **4** |

### 4.1 缺口 —— 全部与前两层一致

| 缺口 | 前两层的判定 |
|---|---|
| `future-continuous-tense` | **BC 列在 B1-B2 档**（批五十九已记为缺口）|
| `future-perfect-tense` | 同上一篇文章 |
| `subjunctive-mood` | **BC 标 C1**（`Unreal time` 属 C1）|
| `phrase-clause-sentence` | **Cambridge 归「高于我方线」27 条之一** |

**⇒ 中文侧没有捞出前两层之外的新缺口。**

### 4.2 7 个「存疑」核实后多为探测词不足

| 存疑项 | 说明 |
|---|---|
| `question-tags` | 批十七已裁定 **BC 明确 B1-B2 + Murphy 整书无** |
| `conditionals` | `if it rains` 实测 **86 次**——已教，探测词只写 2 个 |
| `comparative-superlative-adverbs` | `more quickly` 属 B1+ |
| `phrasal-verb` | **上游标 B1**（词典 `phrasal-verb` = B1）|
| `perfect-tenses` / `continuous-tenses` / `s-verb-agreement` | 探测词表过窄，实为已覆盖 |

---

## 五、三层交叉验证完成

| 层 | 来源 | 结果 |
|---|---|---|
| 一 | Cambridge 全站 **554 条** | 257 结构候选 → **1 条真候选 → 裁定不做** |
| 二 | BC **三档 68 课** | A1-A2 档 **18/18 覆盖** |
| 三 | **中文侧两站 1844 条** | 56 语法点 → **45 覆盖 + 7 存疑 + 4 缺口（全部超线）** |

> **三层都没有在 A1-A2 范围内捞出未覆盖的语法点。**

---

## 六、验收

| 项 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错误 |
| `src/` 改动 | **零**（本批产出是数据抓取与审计）|
| 归档 | 4 个 URL 清单 + README（含两个取数陷阱）|
| 三层可复跑 | Cambridge `cambridge-topics.json`；BC `bc-lessons.json` + `audit-bc-coverage.mts`；中文侧 `*-urls.txt` + 本 PRD |

---

## 七、已知限制（诚实登记）

1. **slug 判定，未逐篇读正文**——本轮用 URL slug 推断文章主题，**没有抓正文**。
   中文站有 canonical 回指型假页的先例（见 `b-tier-exhaustive` README 的方法学坑 C），
   故本层可能有误差。
2. **探测词表由人写**——56 × 1–5 ≈ 120 个探测词全是我写的，
   **人写的那层就是覆盖率的下限**（7 个「存疑」正因此而来）。
3. 中文站还有大量非语法内容（发音、听力、TOEIC、文化），本层只筛了语法相关 168 条。
