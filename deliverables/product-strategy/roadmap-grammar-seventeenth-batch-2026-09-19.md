# 路线图更新：第十七批课程（L111–L118 我一直想说的那些 · A2 收尾缺口补课章 · 大章节第五度 · 单拱 · 开 season-17）

| 元信息 | 内容 |
|---|---|
| 标题 | 路线图更新：第十七批 L111–L118 排期、F17-A 打样门与 F17-B 上线观察门（**第五个 8 课大章节 · 单拱 · 开 season-17 · A2 四处官方课位关账**） |
| 日期 / 执笔 | 2026-09-19 ｜ 路径（路线图规划师） |
| 类型 | 路线图更新（衔接主路线图与第五–十六批路线图；**不动其锁定项与 F6-B–F16-B 判据**） |
| 关键输入 | 三研究 2026-09-19（`user-research` / `competitive-analysis` / `data-audit-grammar-seventeenth-batch-2026-09-19.md`）；`prd-grammar-a2-closeout-2026-09-19.md`（**已落盘，本批排期依据**）；`roadmap-grammar-sixteenth-batch-2026-09-19.md`（格式基准） |
| 主交付物 | L111–L118 八课 + 案 #120–#127 + season-17 / m19（展示层，~12 行） |
| 本批特殊点 | ①**大章节第五度**（第五个 8 课大章）；②**A2 官方课位关账**（BC A1-A2 18 课点目后余 4 处：物主 `'s`／`-ed/-ing`／`few·a few·little`／`have got`；形容词＋介词折卡不单开）；③**单拱「说说身边的人和事」**；④**首次「案件回流课程」**（HC #16 `:771`／#17 `:826` 的 `excited` 转正）；⑤**L112 增量经校正**（`mine` 已由 L33 教透，真实空白＝`'s` 与 `mine` 的选用）；⑥**封面单次池归零**（全部二用升三用，最优 min-gap 35）；⑦ 本批 **0 处 fragment／run_on**（避开批十五 #103、批十六 #111 的连续同型） |

---

## 📌 TL;DR

1. **裁决：即刻开工**——无 P0 阻断项；**8 课＋8 案＋展示层（~12 行）＋打样＋验收 ≈5 人日**（D1–D10，两段式、单日 ≤1、每周 ≤3 人日封顶 → 跨 2 周）。
2. **大章节第五度 · 单拱**：一条「**说说身边的人和事**」线——家里的人（L111–L112）→ 心里的感觉（L113）→ 桌上的东西有多少（L114）→ 手里的东西哪来的（L115–L116）→ 合体（L117）→ 收口（L118）；开 **season-17 `{111,118}`＋m19（afterLesson 118）**，批量 **118 课、127 案**。
3. **选题是「A2 关账」不是「跳 B1」**：BC 官方 A1-A2 索引 **18 课逐条点目后仍余 4 处课程位空缺**（`Possessive 's`／`Adjectives ending in '-ed' and '-ing'`／`Quantifiers: 'few', 'a few', 'little' and 'a bit of'`／`Present simple: 'have got'`），且 **Murphy 双册各有独立单元**（初 U64／中 U81；中 U98；初 U84／中 U87；初 U9／中 U17）＝**A 档（跨源官方课位）**；批十一「A2 结构收官」宣告不完整，本批正面补齐——甲 `be/get used to` 列**批十八首选**。
4. **档位诚实标注（本批纪律）**：L111／L113／L114＝**A 档**；L112＝**A 档（增量经校正）**——`mine` 已由 L33 `:5931`／`:5937`／`:6001` 教透，真实空白是「**`my brother's` 与 `mine` 的选用**」（采竞析 §⑤-1，**勿沿用瑞思 §2 T11 旧口径**）；L115／L116＝**B+ 档·说法扩充**（同义换挡，**不作章主锚**，须防与完成时混淆）；L117／L118＝**自研**。
5. **中文负迁移正面接两句**：`*I am boring.`（中文「无聊」一个词管两头）＝**真错第一名** → L113 三条对比卡全部围绕它；`*Grandma birthday is in May.`（漏撇号）＝章首攻；`*There are few apples.`（丢 a）＝L114 攻。
6. **首次「案件回流课程」**：HC #16 `:771`（`excite→excited` 单 token 修）与 #17 `:826`（缺 be）**已考未教**——L113 对比卡 ⑥ 正面认领；L118 案 #127 **全回流**（分锚 #120／#122／#123／#125）；**本批 8 案 0 处 fragment／run_on**（瑞思 §1 ⑨ 纪律）。
7. **封面＝纯二用升三用 8 张（单次池已归零）**：`L111←cover29／L112←cover42／L113←cover25／L114←cover7／L115←cover2／L116←cover3／L117←cover4／L118←cover1`（**最优最小间距 35**，二分答案＋二分图完美匹配＋穷举验证）；❌ `cover49` 永久禁用（段距 9）；批十五刚三用的 10/16/18/20/21/26/31/32 勿碰；批十六刚二用的 41/45/47/48 不做四用。
8. **三道防御＋合并关账**：**F17-A 打样门**（L111 首玩，附验 L112 校正口径）→ **段一联检**（D5）→ **中段自走查**（PRD 定为 **L113 与 L117** 两处）→ **展示层闭环**（D8）→ **全量验收**（D10）→ **F17-B 上线观察门**（1–2 周；**与 F9-B–F16-B 一次读齐，一并提交批十八候补清单**）。

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|---|---|
| 推荐方案 | 即刻开工：L111 打样（D1–D2，含 F17-A 门）→ 段一 L112–L114（D2–D4，A 档缺口段，含 L113 自走查点）→ 段一联检（D5）→ 段二 L115–L116（D6–D7，B+ 换挡段）→ L117 合体＋展示层（D8，含 L117 自走查点）→ L118 收口＋预检（D9）→ 验收（D10）→ F17-B 观察 |
| 优先级 | P1（内容生产批次）；**无 P0 阻断项** |
| 预期影响 | 第五个大章节；**A2 四处官方课位关账**（跨源 A 档）；补 `article`（24，最薄档之一）承载位（#121／#123／#124／#127 四案）；**首次「案件回流课程」**（HC `excited` 转正）；L56 `:10222`／`:10278` 孤例转正；L70 `:12871` 孤例转正；兑现 L67 `:12362`／`:12365` 门牌家族扩员（折 1 处 `in → at`）；批量 118 课、127 案 |
| 资源需求 | ≈5 人日（内容 4 + 展示层 0.25 + 打样走查 0.5 + 验收 0.25）+ 上线后 1–2 周观察；两段式跨 2 周 |
| 风险等级 | 中（`bored/boring` 话术是否接得住；`have got` 增量偏薄；`'s` 与 `of` 互扰；`a few` 的 a 丢失率未验；封面 8 张语义未核；连续第五个大章疲劳；测试基线漂移） |
| 硬性范围红线 | 课量 8 不扩不缩；一课一增量（L118 零新知）；六段式；每课 1 案（4 错＝新 2＋旧 2、单 token 可修）；对比卡 6 条（**≥2 条带 wrongMark**）；零术语词表；comparison 不进案件；tagStats=10 不动；**L1–L110 零改动**；封面池外零新资产；**课号 111–118 连续** |

---

## §1 批次定位与形态决策

### 1.1 形态裁决：单拱 · 第五个大章节（8 课不缩）

| 维度 | 方案 A（等 F16-B 关账后开工） | 方案 B（即刻开工）✅ |
|---|---|---|
| 与主路线图冲突 | 零 | 零（两段式：每段 ≈1.5–2 人日，封顶内） |
| P0 前置 | 无 | 无——三研究口径一致放行（8 课已由主理人拍板） |
| 数据依赖 | 等 F16-B（1–2 周） | 已具备（110 课 / 119 案 / **732 项全绿**；语料、cloze、封面、G-boost 护栏已核） |
| 展示层风险 | 相同 | 相同（season-17 有守门、m19 走查兜底；大章节零阻塞已由批十三–批十六四度验证） |
| 节奏 | 断档 1–2 周 | 创作势能连续（批十六工作区全绿） |

**裁决：方案 B。** 语料与护栏预检已完成＝开工无返工项；F17-B 观察窗与生产并行不互锁（写内容 vs 读数据）。

**单拱声明**：全批**一条「说说身边的人和事」线**，**不跨场景跳**——家里挂历前（L111）→ 茶几上两本书（L112）→ 雨天沙发（L113）→ 果盘端上桌（L114）→ 楼下新车棚（L115）→ 沙发上姐姐的新包（L116）→ 把这一章的话摆一行（L117）→ 同一张桌子填总表（L118）。**明确不做双拱**：丁（乙 6＋甲 2）在乙方案里**没有落点**，硬拼既要背双线代价、又要担 `to + -ing` 的话术冲突（与批十六「丁与甲实质等同却要背双线代价」同一逻辑）；批十四破例一次后，批十五／十六／十七**连续单拱**。

**8 课不缩（正面回答「批十七是否维持 8 课」）**：4 处缺口各自需要「立岗课」——L112（`'s` vs `mine`）与 L116（`has got` 换人换形）**不是复述课**（竞析 §⑤-1 校正「`mine` 已教透」的旧口径，反证真实空白在「选用关系」上，恰需一课把两张脸并排切开）；砍任一项即掉到 6 课——**不是注水，是缺口数决定课数**（瑞思 §4 八项校验：八项独立、互不重叠）。

**档位诚实标注（逐课）**：

| 课 | 增量 | 跨源档 | 依据 |
|---|---|---|---|
| L111 | 物主 `'s` | **A** | BC A1-A2 第 9 课位＋Murphy 双册各一单元（初 U64／中 U81） |
| L112 | `'s` 与 `mine` 的分工 | **A（增量经校正）** | BC 课位在册；**`mine` 本身已由 L33 `:5931`／`:5937`／`:6001` 教透**，真实空白是「`my brother's` vs `mine` 的选用」（竞析 §⑤-1） |
| L113 | `-ed/-ing` 分工 | **A** | BC A1-A2 第 2 课位（A1＋A2，10 组成对）＋Murphy 中级 U98；**跨源分级不一致须诚实标注**：BC 在 A1-A2、Murphy 在中级 U98——**不得混述为「A2 双证」** |
| L114 | `a few / few`（＋`a little`） | **A-** | BC A1-A2 第 15 课位＋Murphy 初 U84／中 U87；中文侧 `quantifiers` 专文**无明文负迁移**（对比卡须自造） |
| L115–L116 | `have got`／`has got` | **B+·说法扩充** | BC A1-A2 第 13 课位＋Murphy 初 U9／中 U17＋Cambridge `have-got` 专页；**中文侧四度零专文，增量＝同义换挡而非新能力**——**不作章主锚** |
| L117 | 四点合体 | **自研** | 收口先例 L54 `:9823`／L78 `:14362`／L94 `:17400`／L102 `:18919`／L110 `:20418` |
| L118 | 零新知收口 | **自研** | 同上 |

> **口令一句**：本批**不新造语言能力**，只是把 A2 官方课位上的四处空格填满；L115／L116 连「新结构」都算不上（同一个「有」，口气更轻）。

**季课数政策**：维持 6–8 课/季（本季 8 课，与批十三–批十六一致）；`grammarSeasons.test.ts` **无「一季课数」断言**（大章节零阻塞，唯一隐性硬约束＝**课号连续**）。

### 1.2 定位裁决：A2 关账 vs B1 开局（不进本批，列批十八首选）

| 方案 | 三研究结论 | 裁决 |
|---|---|---|
| **乙·A2 收尾缺口补课章 8 课** | 瑞思 §0 推荐；竞析 §④ 首选（「6 个增量点全部有 BC A1-A2 官方课位＋Murphy 双册独立单元，是跨源硬位」）；数析给 6/7/8 三档封面方案并证 8 课可行 | **✅ 拍板：乙 8 课** |
| **甲·B1 开局章 6 课**（`be/get used to` 3＋`have sth done` 2＋收口 1） | 竞析判 **A-（BC B1-B2 课位，一页三脸）但两个硬伤**：① 段位（B1）；② **话术冲突**——L93 `:17294` 明文「to 后面永远穿原样」，`be used to + -ing` 要求 `to` 接名字版；数析：`have sth done` 零底座（`will have` 0／`'ll` 0／40 个 PP 零覆盖） | **❌ 不进本批；列批十八首选**（`have sth done` 维持撤出） |
| 丙 散点补漏（`so that`／`neither`／`already` 等） | 无共同语义场与场景锚（瑞思 §2 T8；数析 §1.6「只能按 2–3 个语义家族组章」） | ❌ 不做 |
| 丁 混合（乙 6＋甲 2） | 甲的 2 课在乙方案里没有落点（瑞思 §3） | ❌ 不做（单拱） |

### 1.3 与批十六的衔接（前提未验须登记）

- 批十六 **F16-A 走查与 F16-B 关账均未回**（瑞思「未核实」⑥）——本路线图的「使役已补齐」前提**未经遥测验证**；F16-B 判据不改，**并入本轮同一观察窗**（§6）。
- 批十六工作区改动**全部未提交**（`git log` 止 `7ad13f5`，`git status` 21+ 文件 M）——**本批立项前置动作：先提交或打 tag**（数析 §附录 E；否则 `git checkout .` 类操作会一次性丢失批十六 8 课＋8 案＋bothRight／L96·L102·L89 三处回修）。

---

## §2 拆课总表（8 课 · 一课一增量 · 课注 id 已冻结）

> **通用说明**：六段＝① 看（情景讲解）→ ② 跟（guided：choose／arrange／spot／replace）→ ③ 忆（recall）→ ④ 练（practice ≥4 题）→ ⑤ 破（侦探挑战＝huntCaseIds）。本批 8 课**全部配 recall**（`grammarLessons.test.ts:68-79` 对 `number >= 13` 硬断言）；字段名与形态沿用 `types.ts:580-618`；**目标句逐句 ≤8 词**。

| 课 | id | 主题 | 增量（只此一点） | 目标句 | cloze 落点／保障句 | 案 | 封面 |
|---|---|---|---|---|---|---|---|
| L111 | `lesson-111-possessive-s` | 奶奶的生日 | 物主 `'s`：人后面加撇号 s ＝「谁的」，贴在东西前面（**不碰 `of`**） | `Grandma's birthday is in May.`（5 词） | 落 **is**／`My brother's car is new.`（落 is） | `hunt-grandmas-birthday` #120 | `cover29` |
| L112 | `lesson-112-this-is-mine` | 这本是我的 | **`'s` 与 `mine` 的分工**：后面有东西用短版（my／`brother's`）／后面空了用长版（mine） | `This book is mine.`（4 词） | 落 **is**／`That book is my brother's.`（落 is） | `hunt-whose-book-mine` #121 | `cover42` |
| L113 | `lesson-113-bored-boring` | 我很无聊 | `-ed/-ing` 分工：**感到版**说「我感到」／**让人版**说「它让人」 | `I am bored.`（3 词） | 落 **am**／`The book is boring.`（落 is——`bored`／`boring` **不在词表**） | `hunt-bored-boring` #122 | `cover25` |
| L114 | `lesson-114-a-few` | 只剩几个了 | **a 的有无**：a few apples＝还有几个／few apples＝几乎没了（＋`a little` 数不清版） | `There are a few apples.`（5 词） | 落 **are**／`There is a little milk.`（落 is——`few`／`little` **不在词表**） | `hunt-few-apples` #123 | `cover7` |
| L115 | `lesson-115-have-got` | 我有一辆新自行车 | `have got`＝「有」的**轻口气版**（本课只做 I／you／we／they 版） | `I have got a new bike.`（6 词） | 落 **have**／`We have got a new bike.`（落 have） | `hunt-have-got-bike` #124 | `cover2` |
| L116 | `lesson-116-has-got` | 她有一个新包 | **换人换形**：she／he／it 用 `has got`（缩起来 `'s got`） | `She has got a new bag.`（6 词） | 落 **has**／`He has got a new bike.`（落 has） | `hunt-she-has-got` #125 | `cover3` |
| L117 | `lesson-117-all-i-wanted` | 我一直想说的那些（合体） | **无新点**：四点合体（`'s`／mine／-ed·-ing／a few）＋`have got` 排一行 | 复现混排（逐句 ≤8 词） | 逐句实跑登记（**勿全落同一词**） | `hunt-all-i-wanted` #126 | `cover4` |
| L118 | `lesson-118-close-17` | 收口（零新知） | **零新知**：L111–L116 各 1 句＋总表卡 | 复现混排（逐句 ≤8 词） | 逐句实跑登记 | `hunt-close-17` #127 | `cover1` |

**一课一增量硬纪律（写入生产规格）**：L111 只做「人＋`'s`」（`of` 仅 1 张对照卡，不展开、不进 practice）；L112 只做「长短版选用」（**不重讲 L33 指示代词**）；L113 只做「感到版／让人版」（**不扩 10 组／15 组清单，只取 bored／boring 一组打透**）；L114 只做「a 的有无」（**不重讲 L30 some／any／much／many**）；L115 只做 `have got`（**不碰 `has got`**）；L116 只做换人换形（**不重讲 `have got`**）；**全批不引入 `be/get used to`、`have sth done`、`look like`、被动新面**。

**封面实算（逐张三用后间距 · 本轮逐条复跑）**：`cover29`（L29／L75 → L111）＝min(46,36)＝**36**；`cover42`（L42／L77 → L112）＝**35**；`cover25`（L25／L78 → L113）＝**35**；`cover7`（L7／L76 → L114）＝**38**；`cover2`（L2／L80 → L115）＝**35**；`cover3`（L3／L81 → L116）＝**35**；`cover4`（L4／L82 → L117）＝**35**；`cover1`（L1／L79 → L118）＝min(78,39)＝**39**。**全局最小间距 35（`cover42`／`cover25`／`cover2`／`cover3`／`cover4` 五张同时压线）**，max-sum 288；可用张**只有 9 张**（1·2·3·4·5·7·25·29·42），其余 28 张天花板 <35。❌ `cover49`（L49 `:8889`／L58 `:10586`，段距 9）**永久禁用**；批十五刚三用 10/16/18/20/21/26/31/32（升四用最优 8–31）**勿碰**；批十六刚二用 41/45/47/48（天花板 12–15）**不做四用**。**池外零新资产**（`cover` 在 `types.ts:590` 为可选，缺省回退 scene SVG——池耗尽不阻断上线）。

**episode 写法**：`小美的一天 一百一十一` … `一百一十八`（>100 已有 **11 例先例**：L103–L110 的「一百零三…一百一十」；末课现为 L110 `:20421`「一百一十」；本轮实读「一百一十一…一百一十八」**零占用**）。**scene 取值**：一律 `mansion`（家里）或 `city`（外面），**不引入新场景 id**。

---

## §3 生产节奏与阶段划分（D1–D10 · 两段式 · 跨 2 周）

**段一（D1–D5）＝A 档缺口段（L111–L114）**——`'s` 立岗 → 长短版分家 → `-ed/-ing` 转正 → `a few` 立岗，含 L111 打样门与 **L113 自走查点**；**段一联检（D5）**。
**段二（D6–D8）＝B+ 换挡段（L115–L116）＋合体（L117）**——`have got` 立岗 → 换人换形 → 四点合体，含 **L117 自走查点**与**展示层闭环**。
**收口与验收（D9–D10）**——L118 零新知＋全量预检 → 验收放行；**F17-B 上线后 1–2 周**。

| 阶段 | 任务 | 产出 | 门/决策 |
|---|---|---|---|
| D1 | L111 `lesson-111-possessive-s` + `hunt-grandmas-birthday`（`:10222`／`:10278` 孤例转正；「谁的」话术立岗）＋**封面 8 张拍板**（**图像语义逐张人工核对**）＋**cloze 逐课预演**（落 is） | L111 + 案件 reviewed | 封面拍板；cloze 预演 |
| D2 | L111 首玩走查（清 localStorage、非开发会话）→ **F17-A 门** → L112 `lesson-112-this-is-mine` + `hunt-whose-book-mine`（**校正口径落地**：`mine` 是 L33 老熟人，今天与 `brother's` 分家） | 走查记录；L112 + 案件 | **F17-A 门**（放行本批） |
| D3 | L113 `lesson-113-bored-boring` + `hunt-bored-boring`（**倒挂转正**：HC #16 `:771`／#17 `:826` 正面认领；感到版／让人版话术立岗） | L113 + 案件 | L113 生产预演（真错第一名） |
| D4 | L114 `lesson-114-a-few` + `hunt-few-apples`（`article` 承载；接 L30 `:5389` 下一格）＋**中段自走查点 ①（L113 生产即查）** | L114 + 案件；自走查记录① | **自走查点 ①：`bored/boring` 话术接住率** |
| D5 | **段一联检**（L111–L114 四课：A 档线连贯、话术统一、封面逐张复核、`of` 互扰预警） | 联检记录 | **段一收官**；滑移吸收位 |
| D6 | L115 `lesson-115-have-got` + `hunt-have-got-bike`（轻口气版；**走查观察「学完仍只用 have」**） | L115 + 案件 | L115 关（增量感知度） |
| D7 | L116 `lesson-116-has-got` + `hunt-she-has-got`（换人换形；**同形隔离**：`'s got` vs「已经得到」显式切开） | L116 + 案件 | L116 关（同形混淆） |
| D8 | L117 `lesson-117-all-i-wanted` + `hunt-all-i-wanted`（四点合体；#126 折 1 处 `in → at` 门牌回流）＋**season-17 + m19（~12 行）**＋**中段自走查点 ②（L117）** | L117 + 案件；路径页分组/里程碑；自走查记录② | **展示层闭环**；**自走查点 ②：四点是否各自认得出来** |
| D9 | L118 `lesson-118-close-17` + `hunt-close-17`（零新知；六句＋总表卡）＋全量验收准备（**G12 逐课 cloze 实跑**、G-boost 自查、**wrongToken 逐字核对**） | L118 + 案件；预检记录 | 验收前置 |
| D10 | 全量验收（checklist G1–G14 + 抽测剧本 + 路径页 118 课 + m19 触发 + **732 基线** + build） | 验收记录 | 上线放行 |
| 上线后 1–2 周 | 遥测（首过率、`I am boring.` 误答回流率、`have got` 使用率、`a few` 的 a 丢失率、`'s`／`of` 互扰率、m19 触发） | 观察数据 | **F17-B；与 F9-B–F16-B 合并裁决** |

**纪律**：F17-A 首玩须在**非开发会话**产生（清 localStorage；HMR 污染场次作废重采，沿用批五–十六纪律）。负载：D1–D10 依次 ≈0.5/0.75/0.5/0.5/0.25/0.5/0.5/0.75/0.75/0.25 人日＝**≈5.25 人日**（PRD 记 ≈5，含展示层 0.25＋走查 0.5＋验收 0.25）——**单日 ≤1、每周 ≤3 封顶**；**滑移先砍 deepDive 深度与复现条数、不砍课**（不做 8→6/7 缩量，亦不砍 L118 收口）。

**段一联检（D5，排期纪律，不设独立门）**：① 四课话术是否同线（「谁的」→「长版短版」→「感到版／让人版」→「小招牌 a」）；② `of` 互扰是否已抬头（L111 对比卡 ② 提位预案）；③ 封面逐张语义复核（D1 拍板结果落地）；④ 课号连续性 111–114 人工核对（缺号永久锁死且无报错，`lessonService.ts:156-159` 用 `number - 1`）。

**展示层（D8 随批上线，~12 行）**：
- `grammarSeasons.ts` 在 season-16（`:50` 末项 `{min:103,max:110}`）后追加：`{ id: "season-17", label: "第十七季 · 我一直想说的那些", hint: "谁的（撇号 s）、我的（mine）、我的感受（感到版）、还有几个（a few）、有（have got）——身边的事，一句话说清一件", min: 111, max: 118 }`
- `GrammarPathPage.tsx` CAN_DO_MILESTONES 在 m18（`:240-245`，**全仓 18 个里程碑**）后追加 m19：`{ id: "can-do-m19", afterLesson: 118, title: "我能说清身边的人和东西", zh: "谁的（`'s`）+ 我的（mine）+ 我的感受（bored）+ 还有几个（a few）+ 有（have got／has got）——身边的人和东西，一句话说清一件。", samples: ["Grandma's birthday is in May.", "This book is mine.", "I am bored."] }`

---

## §4 质量门与验收

### 4.1 门序列（六道）

| 门 | 时点 | 性质 | 判据摘要 |
|---|---|---|---|
| **门 0 · 生产期逐课自查** | D1–D9 每课落盘即查 | 硬纪律（无独立走查） | G1–G7／G10／G11＋G-boost 三项逐课过；**课号连续性**逐课核；**不依赖测试兜底** |
| **门 1 · F17-A 打样门** | D2（L111 首玩 + 走查后） | 决策门（继续/调整/止损） | 见 §4.2 |
| **门 2 · 段一联检** | D5 | 排期纪律 | L111–L114 四课线连贯、话术统一、封面复核、`of` 互扰预警 |
| **门 3 · 中段自走查** | D4（L113）／D8（L117） | 排期纪律（生产即查） | 见 §4.2 |
| **门 4 · 展示层闭环** | D8 | 硬需求 | G8（season-17＋m19 上线）＋G8-b（守门测试 4 项全绿）＋路径页 118 课走查 |
| **门 5 · 全量验收** | D10 | 放行门 | G12 逐课 cloze 实跑登记＋G13 时长抽查＋G14（L1–L110 零改动、732 基线、build、tsc） |
| **门 6 · F17-B 上线观察门** | 上线后 1–2 周 | 观察门（与 F9-B–F16-B 合并） | 见 §4.3 |

### 4.2 F17-A 打样门与中段自走查（判据）

**F17-A 打样门（L111 真实首玩 + 走查后；D2）**

| 档 | 判据 |
|---|---|
| 继续 → L112–L118 放行 | 试玩者能复述「**『谁的』贴在东西前面：人后面加个撇号 s**」并说出「\*Grandma birthday 不行」；**cloze 落 is 的落点**不产生「这题考什么」的困惑（与批十四 L87 落 It's／批十五 L95 落 was／批十六 L103 落 makes 同型）；**中文「一个『的』管到底」被接住**（能把「奶奶的生日」主动对应到 `'s`）；**L112 校正口径可懂**——开场「第 33 课学过 mine，今天看它跟 `brother's` 怎么分家」不产生「重复学」感；场景锚可懂（家里挂历前）；时长 6–8 min |
| 调整 | 时长超标 → 收紧 deepDive（不砍课）；「跟第 56 课重复吗」反馈集中 → L111 开场补一句分工；`of` 直译抬头 → 对比卡 ② 提位为第 2 条（≤0.25d）；L112 被感知「学过了」集中 → 对比卡 ③（`*This book is brother's.`＝本课唯一新点）提位；HMR 污染 → 剔除重采 |
| 止损 | >12min 或话术改进后仍不可达 → 批次暂停、退回析客（**不推翻 8 课拆分**）；**G-boost 护栏被生产反复突破（某课 contrast 带 wrongMark 错卡 <2 条进入验收）→ 就地暂停并把「boost 护栏是否升级为生成期硬校验」提为跨工作流 P0**（护栏为落盘资产，`grammarBoostService.test.ts:161` 全库逐课断言 `thin === []`） |

**中段自走查 ①（L113 生产后；D4）——本批最大未知**

| 观察点 | 判据 | 异常处置 |
|---|---|---|
| **「感到版／让人版」话术是否接得住「我很无聊」** | 中文一个词管两头，学生能否**一次分清** `bored`／`boring`？三条对比卡（`I am boring.` ❌／`I bored.` ❌／`The book is bored.` ❌）是否被一次打透 | 误答集中 → 对比卡 ③ 提位（与 ① 成对）＋保障句 `The book is boring.` 提前；仍不接 → 段间加 1 日缓冲（不砍课），并带至 F17-B 单列指标 |
| **`a few` 的 a 丢失率是否真高于 a 的其他用法** | L114 的全部价值押在这一点上；对比卡 ①（`There are few apples.`＝丢招牌）误答集中度 | 若走查发现 a 丢失率**不高** → 登记为批十八前重评估项（**本批不动结构**） |
| **倒挂转正的认领效果** | 学生能否说出「案件里见过的 excited，今天正面教」 | 无反应 → 开场话术加一句「你已经在案件里见过它」 |

**中段自走查 ②（L117 生产后；D8）**

| 观察点 | 判据 | 异常处置 |
|---|---|---|
| **四点是否各自认得出来**（本章最大未知） | 混排场景下四句能否逐句指认「这是第 N 课学的、说的是哪一件事」；是否互相串台（`'s` 漏撇号／`bored` 说成 `boring`／`a few` 丢 a／`has got` 漏换形） | 串台集中 → practice 第 2–4 题顺序改为「按课号回放」（≤0.25d） |
| **L117 与 L118 的重复度** | 两课均复现混排（各 6 句），L118 另加总表卡——重叠度是否过高 | 重复感集中 → **L118 总表卡改「六行＋一句话」形态**（PRD §8-7 备选；不改 L117） |
| **门牌折卡的过度泛化预警** | #126 折的 `in → at`（L67 `:12365` 门牌家族回流）是否引发 `interested at` 型反向错 | 误答率高 → L117 补 1 张 `good at`／`interested in` 并排卡（**不新增课**，PRD §8-5 备选） |

### 4.3 F17-B 上线观察门（批次上线后 1–2 周）

| 档 | 判据 |
|---|---|
| 继续 | L111–L118 **首过率**与全批窗口基线相当（±5pp）；**`I am boring.` 误答回流率**下行（真错第一名被接住）；**`have got` 使用率**（学完仍只用 `have` 的比例）低于阈值；**`a few` 的 a 丢失率**低于阈值；**`'s`／`of` 互扰率**低于阈值；**m19 触发**与 season-17 分组可见正常 → **与 F9-B–F16-B 合并裁决**（含批十八候选清单） |
| 调整 | 任一课首过率 <70% → 该课 deepDive 收紧或复现题加练（**不砍课**）；`have got` 被感知「没必要学」集中 → L115 对比卡 ④（双正解）改为显式「加了 got 是聊天口气」的偏好提示；L117/L118 重复感集中 → L118 总表卡改「六行＋一句话」；`interested at` 型泛化上行 → L117 补 1 张并排卡（不新增课） |
| 止损 | 任一课/案违反红线（**术语进话术**、comparison 进案件、新增枚举、多 token 错不可修、引番外 5 案、**课号不连续**、**课号出现 fragment／run_on**、**每课 contrast 带 wrongMark 错卡 <2 条**）→ 当日下线该内容；**连续两课完成率显著下滑且走查＋遥测双证** → 大章节形态退回「章内一拱 4–5 课」重设计，season-17 分组暂留 |

### 4.4 G1–G14 的路线图口径（PRD 逐条 → 阶段门）

| PRD 项 | 要求（压缩） | 路线图口径：在哪道门验 | 放行判据 |
|---|---|---|---|
| **G1** | practice ≥4 题且含一道与 variants **逐字一致**的否定/疑问题；**G1-b 每课 ≥1 复现题** | 门 0 逐课；门 5 抽测 | 8 课逐课登记变体题（L111 `Is Grandma's birthday in May?`…L118 `Have you got a new bike?`）；L117／L118 各 6 题复现 |
| **G2** | tokens／answer 词集一致＋distractors 不重复答案词；arrange／practice 展示序 ≠ 答案序 | 门 0 | `grammarLessons.test.ts:30`／`:51` 全绿 |
| **G2-b** | **零术语红线**：`grammarLabel`／`oneLineRule`／`summary.rule` 三字段禁 28 词（主语/谓语/宾语/…/形容词/副词/介词） | 门 0 逐课自查 | §4.5 替换体系逐条对照；`grammarLessons.test.ts:110/119/128` 全绿 |
| **G3** | recall 三字段非空（`promptZh`／`intentZh`／`answer`） | 门 0 | `:68-79` 对 `number >= 13` 硬断言全绿 |
| **G4** | 目标句 ≤8 词；一课一增量 | 门 0；门 5 | L111 5／L112 4／L113 3／L114 5／L115 6／L116 6；L117／L118 逐句 ≤8 |
| **G5** | 8 案 tag 全落 **10 枚举**；不碰 comparison；**带上 `article`**（现 24，最薄档之一）；**避开 fragment／run_on** | 门 0；门 5 | `article` 落 #121／#123／#124／#127 四案；本批 **0 处 fragment／run_on** |
| **G6** | 每案 4 错＝新 2＋旧 2、单 token 可修、≥1 净词、`reviewed: true`、`tokenIndex` 对齐、`number` 连续 | 门 0 逐案；门 5 | 旧错**只取 L10／L11／L19／L25**；#120–#127 连续 |
| **G7** | tagStats=10 不动；番外 5 案冻结 | 门 5 | `huntService.test.ts:109`（`toHaveLength(10)`）／`:215`（逐名单断言）全绿；本批 8 案**全被 L111–L118 引用** |
| **G8** | season-17 `{111,118}`＋m19（afterLesson 118）随批上线 | **门 4（展示层闭环）** | 路径页 118 课可见、m19 触发正常 |
| **G8-b** | `grammarSeasons.test.ts` **4 项全绿**（区间覆盖／互不重叠／label·hint 非空／最高课号 118 落区间） | 门 4 | **不落 season-17 则第 ①④ 项先红**（不会静默过滤）；**m19 全仓无测试引用（纯纪律项，走查兜底）** |
| **G9** | 8 新案全被引用、不新增未引用案 | 门 0；门 5 | 引号案数 119→127、`reviewed` 全 true |
| **G10** | episode 汉字数字（「一百一十一」…「一百一十八」） | 门 0 | >100 已有 11 例先例，无格式风险 |
| **G11** | 语料锁闭集（每课只做本课增量；`of` 只 1 卡；L113 只取 bored/boring 一组） | 门 0 逐课自查 | §2「一课一增量硬纪律」逐条 |
| **G12** | **cloze 逐课实跑**（按生效词表）＋保障句＋「variants ≥1 句让主考点落空」 | 门 5（D9 预检 + D10 复核） | 落点登记：L111 is／L112 is／L113 am／L114 are／L115 have／L116 has；`few`／`little`／`bored`／`boring`／`mine`／`there` 均 OUT（本轮复跑确认），靠保障句兜住 |
| **G13** | 时长 6–8 min（8 课逐课走查；L117／L118 分段计时抽查） | 门 5 | 逐课记录；L115–L118 六词句核对上限 |
| **G14** | **L1–L110 一字不动**；`npx vitest run` 全绿；`tsc --noEmit` 0 错；build 通过 | 门 5 | 本路线图基线：**58 文件 / 732 项**（2026-09-19 实跑 6.22s）；生产时以实跑为准（PRD §8-10） |
| **G-boost** | 每课 contrast 6 条中 **≥2 条带 `wrongMark` 的真实错卡**（推荐 3、禁止贴线）；`wrongMark` **不得为纯标点**；`guided.spot` 的 `wrongToken` 与 tokens **逐字相等** | 门 0 逐课；门 5 | `grammarBoostService.test.ts:161`（全库 `thin === []`）／`:199`（纯标点）／`:185`（`tokens.includes(wrongToken)`）全绿；本批 8 处 spot 逐字核对（L111 `"Grandma"`／L112 `"my."`／L113 `"boring."`／L114 `"few"`／L115 `"is."`／L116 `"have"`／L117 `"boring."`／L118 `"few"`），**不依赖 `cleanWord` 兜底** |

### 4.5 术语红线与替换体系（生产禁出现）

**零术语红线**（`grammarLessons.test.ts:110/119/128` 逐字断言，作用域＝`grammarLabel`／`oneLineRule`／`summary.rule`）：主语、谓语、宾语、表语、定语、状语、单数、复数、三单、原形、时态、一般过去时、一般现在时、现在进行时、过去进行时、现在完成时、情态动词、比较级、最高级、从句、语序、可数、疑问句、否定句、被动语态、第三人称、形容词、副词、介词——**28 个词全禁**。
**本批额外禁用**（同属零术语纪律，生产自查）：物主代词、名词所有格、分词、过去分词、现在分词、助动词、系动词、冠词、量词、限定词、主谓一致、词性。

| 禁词概念 | 本批替换说法 | 出处 |
|---|---|---|
| 名词所有格 | 「**谁的**：人后面加个撇号 s」 | 本批新增（L111） |
| 物主代词（名词性） | 「**长版**（mine）／短版（my）」 | L33 `:6001`「自己就能当主角，站句尾」升级 |
| 形容词／分词（-ed·-ing） | 「**感到版／让人版**」 | 本批新增（L113） |
| 冠词 a 的有无 | 「**小招牌 a**：挂上＝还有几个，不挂＝几乎没了」 | 本批新增（L114） |
| 可数／不可数 | 「**数得清的／数不清的**」 | L30 `:5389` 原话 |
| `have got` 的语法身份 | 「**『有』的轻口气版，一个词变两个词**」 | 本批新增（L115） |
| 第三人称单数 | 「**他/她/它加个小尾巴**」「**换人换形**」 | L25 `:4460`＋本批（L116） |
| 助动词／be 动词 | 「**帮手**」「**固定搭档**」 | L1 `:87`／L74 家族话术 |
| 疑问句／否定句 | 「**搬句首**」「**not 跟 is 走／夹在中间**」 | L27 搬法＋本批 |
| 时态／一般过去时 | 「**昨天版**」 | L10 `:1699` |

**不可抄清单（竞析 §⑥）**：一次全收（BC 六族／15 组清单／`have sth done` 四格）与越段内容（`would` 过去习惯、`not until` 倒装、双重所有格 `of`、经历型 `have + obj + -ing`）——本批一律不取。

### 4.6 验收清单（D10 逐条可勾）

- [ ] **G1**：8 课 practice ≥4 题，且各含 1 道与 variants 逐字一致的否定/疑问变体题（L111 `Is Grandma's birthday in May?`／L112 `Is this book yours?`／L113 `Are you bored?`／L114 `Are there a few apples left?`／L115 `Have you got a new bike?`／L116 `Has she got a new bag?`／L117／L118 同前）
- [ ] **G1-b**：**每课 ≥1 复现题**（L117／L118 各 6 题复现，取材 L111–L116 原句）
- [ ] **G2**：tokens／answer 词集一致＋distractors 不与答案词重复（`grammarLessons.test.ts:30`／`:51`）；arrange／practice 展示序 ≠ 答案序
- [ ] **G2-b**：零术语红线 28 词**全课三字段零出现**（`grammarLabel`／`oneLineRule`／`summary.rule`）＋本批额外 12 词自查
- [ ] **G3**：8 课 recall 三字段（`promptZh`／`intentZh`／`answer`）非空
- [ ] **G4**：目标句逐句 ≤8 词（L111 5／L112 4／L113 3／L114 5／L115 6／L116 6；L117／L118 复现逐句核）；一课一增量
- [ ] **G5**：8 案 tag 全落 10 枚举；**`article` 已落 #121／#123／#124／#127 四案**；**0 处 fragment／run_on**；不碰 comparison
- [ ] **G6**：每案 4 错＝新 2＋旧 2、单 token 可修、≥1 净词、`reviewed: true`、`tokenIndex` 对齐、#120–#127 连续；旧错只取 L10／L11／L19／L25
- [ ] **G7**：`tagStats=10`（`huntService.test.ts:109`）；番外 5 案名单未动（`:215`）；8 新案**全被 L111–L118 引用**（引号案数 119→127）
- [ ] **G8**：season-17 `{111,118}`＋m19（afterLesson 118）已上线；路径页 118 课可见、m19 触发正常
- [ ] **G8-b**：`grammarSeasons.test.ts` **4 项全绿**（区间覆盖／互不重叠／label·hint 非空／最高课号 118 落区间）
- [ ] **G9**：不新增未引用案；`reviewed` 全 true
- [ ] **G10**：8 课 episode 全汉字数字（「一百一十一」…「一百一十八」）
- [ ] **G11**：语料锁闭集逐条（L111 只做「人＋`'s`」／L112 只做长短版选用／L113 只取 bored·boring 一组／L114 只做 a 的有无／L115 只做 `have got`／L116 只做换人换形）
- [ ] **G12**：8 课 cloze 按生效词表**实跑登记**（L111 is／L112 is／L113 am／L114 are／L115 have／L116 has；L117／L118 逐句登记、勿全落同一词）；**逐课核「variants ≥1 句让主考点落空」**；保障句已就位
- [ ] **G13**：8 课时长 6–8 min（L117／L118 分段计时抽查）
- [ ] **G14**：**L1–L110 一字不动**；`npx vitest run` 全绿（本路线图基线 **58 文件 / 732 项**）；`tsc --noEmit` 0 错；build 通过
- [ ] **G-boost ①**：每课 contrast 6 条中 **≥2 条带 `wrongMark` 的真实错卡**（推荐 3、禁止贴线）
- [ ] **G-boost ②**：`wrongMark` **全部含字母**（本批清单：`Grandma`／`of`／`on`／`my`／`mine`／`brother's`／`boring`／`bored`／`few`／`apple`／`is`／`have`／`has`／`got`／`the`）
- [ ] **G-boost ③**：8 处 `guided.spot` 的 `wrongToken` 与 tokens 元素**逐字相等**（L111 `"Grandma"`／L112 `"my."`／L113 `"boring."`／L114 `"few"`／L115 `"is."`／L116 `"have"`／L117 `"boring."`／L118 `"few"`），**不依赖 `cleanWord` 兜底**
- [ ] **G-boost ④**：每课 contrast ≥3 条、`kind` 覆盖 choose／arrange／spot／replace
- [ ] **课号连续性**：111–118 逐一人工核对（缺号永久锁死且无报错）
- [ ] **立项前置**：批十六未提交改动（8 课＋8 案＋**三处回修**）**已提交或打 tag**

---

## §5 风险与缓解

| # | 风险 | 等级（概率/影响） | 缓解 | 触发信号 |
|---|---|---|---|---|
| 1 | **`bored/boring` 话术是否接得住「我很无聊」**——中文一个词管两头，无 -ed/-ing 这层区分（本批最大未知） | 高／高 | L113 三条对比卡全部围绕 `*I am boring.`（正／反／反向补枪）一次打透；保障句 `The book is boring.` 让主考点不落空；话术全禁「形容词／分词」；**中段自走查 ①（D4）＋F17-B 单列误答回流率** | `I am boring.` 误答集中／试玩者说不出两张脸／走查中 `The book is bored.` 反向错抬头 |
| 2 | **`have got` 增量偏薄**（B+·说法扩充，同义换挡非新能力）——被感知「没必要学」 | 中高／中 | 对比卡 ④ 双正解并排（`I have got a new bike.` ✅／`I have a new bike.` ✅）显式给「口气」偏好；L116 换人换形补一格实增量；**不作章主锚**；D6 走查＋F17-B 盯「学完仍只用 have」 | 学完仍只用 `have` 比例高／「这不是一样吗」反馈集中／L115 完成率异常 |
| 3 | **`'s` 与 `of` 互扰**——教了 `Grandma's` 后学生反向错出 `*the birthday of Grandma` | 中／中 | L111 对比卡 ② **只 1 张**（Murphy 中级 U81 正是把 `'s` 与 `of` 并讲，故有依据）；**不展开、不进 practice、不进案件**；段一联检（D5）专列预警 | `of` 直译抬头／L111 关 2 误选集中／`of Grandma` 出现在案件修正 |
| 4 | **`a few` 的 a 丢失率是否真高于 a 的其他用法**——L114 全部价值押在此点；中文侧无错例可引（对比卡须自造） | 中／高 | 对比卡 ① 单打丢 a；保障句 `There is a little milk.` 练「数不清版」；**中段自走查 ①（D4）**观察；**若丢失率不高 → 批十八前重评估 L114 增量**（本批不动） | 丢 a 误答率与窗口均值无差／`a few` 与 `few` 混答／试玩者说不出「a 在不在」 |
| 5 | **封面 8 张语义未核**——指派按编号间距实算（min-gap 35，多解穷举验证），**未做图像语义核对** | 中／低 | D1 逐张图形语义人工核对；不符者**池内互换**（9 张可用张内重排，**须守住 `cover42`／`cover25` 的 35 压线不自破**）；段一联检（D5）复核；**池外零新资产**（缺省回退 scene SVG 不阻断上线） | 画面与场景不符（家/外景错配）／试玩者指出「见过这张」／封面色调与课文气氛冲突 |
| 6 | **连续第五个大章节的疲劳**——F13-B–F16-B **均未关账**，判据尚无数据 | 中／中 | 两段式＋两处中段自走查（L113／L117）＋D5 段一联检；滑移先砍深挖不砍课；**F17-B 与 F9-B–F16-B 一次读齐九批遥测**；止损判据＝连续两课完成率显著下滑（走查＋遥测双证） | 段一第 3–4 课完成率下滑／时长超标集中／批十六 F16-B 若显示疲劳则本批为最后一个大章 |
| 7 | **测试基线漂移**——三研究分别记 721（瑞思／数析）与 729（竞析），本路线图实测 **58 文件 / 732 项** | 低／高 | 以上线时**实跑为准**登记（G14 写 732，若生产期再变以实跑登记）；**D9 预检复跑**；差值为同期另一路改动（bothRight 新题型等） | `npx vitest run` 文件数/项数与 732 不符／新增失败项 |
| 8 | **门牌家族扩员引发 `interested at` 型过度泛化**（L67 只教了 at 一员） | 中／中 | **不单开门牌课**，只在 #126 折 1 处 `in → at`；中段自走查 ②（D8）观察；备选：L117 补 1 张 `good at`／`interested in` 并排卡（**不新增课**） | #126 误答率高／`interested at` 出现在自由作答 |
| 9 | **`'s got` 与「已经得到」混淆**（同形两张脸；L21 `:3729` 完成时已在库） | 中／中 | L116 对比卡 ③ 显式切开（`She's got a new bag.` ✅ 并排 `She has got a new bag.` ✅＋话术「不是『已经得到』」）；L115 对比卡 ⑥（同一个 have 两张脸）；**不展开完成时全表** | `'s got` 被读成完成义／L116 关 2 误选集中 |
| 10 | **课号连续性＋`wrongToken` 逐字相等**（缺号永久锁死且无报错；错位则改错题静默消失） | 中／高 | 111–118 逐一人工核对（每课落盘即核）；8 处 spot 的 `wrongToken` 逐字核对（**含尾标点写法一致**）；门 5 含连续性与 G-boost 自查 | 关 2 无法解锁／前课找不到／某课 guided 改错题不出题 |

---
## §6 合并关账

### 6.1 观察窗合并机制（沿用批十–批十六）

**F9-B / F10-B / F11-B / F12-B / F13-B / F14-B / F15-B / F16-B 照旧合并关账**——一次读齐**八批**遥测，**本批 F17-B 并入同一窗口**（判据不改，1–2 周），**一次提交批十八候补清单并归档**。

- **为什么合并不是拖延**：单个 8 课批次的遥测样本量本就偏小，逐批关账会把噪声读成信号；九批一次读齐可看跨批趋势（首过率、误答回流率、完成率曲线）。
- **本批新增观察项（并入同一读数表）**：`I am boring.` 误答回流率（真错第一名）；`have got` 使用率（学完仍只用 `have`）；`a few` 的 a 丢失率；`'s`／`of` 互扰率；`interested at` 型泛化率；m19 触发与 season-17 分组可见性。
- **未关账项的携带**：批十六 **F16-A 走查／F16-B 关账均未回**（瑞思「未核实」⑥）——本批开工**不等待**（写内容 vs 读数据互不锁），但**F16-B 若显示使役侧异常，须在合并裁决时优先处置**（必要时插入一次小修批）。

### 6.2 批十八候补清单（一次提交，按优先级）

| 序 | 候选 | 档位／依据 | 建议形态 | 前置条件 |
|---|---|---|---|---|
| 1 | **`be/get used to`（B1 开局首选）** | **A- 档**：BC B1-B2 有课位且**一页同教三张脸**（`Different uses of 'used to'`）＋Murphy **中级 U61 独立单元**（标题即 `be/get used to`）＋Cambridge 两页（含明文 ❌×3）＋中文**两篇专文＋明文负迁移**（❌ `She used to working late.`） | 6 课单拱（`be used to` 立岗 → `get used to` → 否定/疑问 → 回收 L93/L100 的 `used to` 作显式对比 → 场景迁移 → 收口） | **话术冲突须正面处**：L93 `:17294` 明文「to 后面永远穿原样」——须写成「同一个 to，两张脸」的显式切开（**不是补充，是对位改写**）；两文件 `be used to`／`get used to` **全 0**（真空白，无认读垫子） |
| 2 | **`have sth done`** | **C 档，维持撤出**：BC **三档 68 课零课位**（A1-A2 18／B1-B2 36／C1 14 全目）；Murphy 仅**中级 U46**（跨级）；招牌句四零件（`have/had`＋`hair`＋`cut`）**全库零**、`will have` **0**、`'ll` **0**、40 个常识 PP 零覆盖 | 不排期；**若要动须先裁两件事**：① 是否接受跨级（U46）；② `'ll` 缩写是否单列一课（独立空白） | 词架不足＝硬伤（不是估的，逐词核过）；`I had my hair cut.` 合法句**作不了 ❌ 卡**（错型与难点错位） |
| 3 | **`look + 形容词` 升格评估** | **自研型候选**（非空白）：我方**实有 12 处认读**（L13/19/48/51/66/71/72/76/79/83/87/92，**逐条核查全为 NPC 对白、无一作 target**）＋BC 参考层 `where-adjectives-go-in-a-sentence`（**beginner**）有 "That film looks interesting." 落点 | 2–3 课小章或折入他章 | **与 `look like` 不得混述**：`look like` 仍是**四度三缺**（BC B1-B2／中文四度零专文／我方仅 L49 `:8909` 一句天气义）→ **维持认读** |
| 4 | **形容词＋介词（独立课评估）** | **B- 档**：BC A1-A2 课位（`Adjectives and prepositions`，六族完整清单）＋Murphy 初 **U112**／中 **U130–131**；**但我方六族只覆 1/6**（仅 `good at`，L67 `:12289`）＋中文侧**无独立专文**（`good-at`／`afraid-of` 直连 404） | **一课封顶**；或继续折卡不单开 | **「一课一增量」守不住**（搭配是清单不是结构）；本批已用 #126 折 1 处 `in → at`（L67 门牌家族回流），**扩员是否引发过度泛化先看 F17-B** |
| 5 | **机制强化重估** | 批十六路线图 `:189` 明判「无增量空间」；批十七数析复跑：boost 档 1 四题型轮转＋`spot` 固定槽＋bothRight 新题型／回马枪／复习轮换／弱点驱动**全在位**，**58 文件 732 项测试守门** | 无排期（维持不做） | 若 G-boost 护栏升级为**生成期硬校验**（F17-A 止损项）则回看 |
| 6 | **A2 段剩余盘点** | 本批关账后：BC A1-A2 18 课**我方覆盖 18/18**（14 条已覆＋本批 4 条）；**形容词＋介词（丁-5）为第 5 处课程位**（竞析 §⑤-2 校正「4 处→5 处」），本批**折卡不单开**→ 留批十八清单第 4 项 | 出一张「A2 课程位 vs 词典型证据」的**缺口表复核** | 附属盘点项（非课）：句子级 `of` 结构、双重所有格、`would` 过去习惯等**越段内容**均已在 Non-goals 冻结 |

**批十八形态建议**：若取第 1 项，**6 课单拱**（B1 开局章）；若同时取第 3／4 项，则优先第 3 项（有 12 处垫子、认知负荷低），第 4 项维持折卡。

---

## §7 决策记录（产品负责人视角）

**本批的战略意义：A2 官方课位关账。** 批十一宣告「A2 结构收官」，批十六路线图把「A2 之后走什么」列为待答三问之一；本轮用 **BC A1-A2 官方 18 课全目逐条点目**（不是抽样、不是转述）证实：**收官不完整，尚余 4 处课程位空缺**——物主 `'s`／`-ed·-ing` 形容词／`few·a few·little·a bit of`／`have got`，且这 4 处在 **Murphy 双册各有独立单元**。**正面答案是先把 A2 关账，不是跳 B1**：这 4 处是**跨源官方课位（A 档）**，与批十一型「词典型·中文需求驱动（课程位零）」有本质区别；本批 8 课落地后，**BC A1-A2 18 课我方覆盖 18/18**，A2 段才算真正闭环——之后开 B1 才有干净的起点。

**本批同时兑现三笔旧账**（都不是新造）：
1. **首次「案件回流课程」**——HC #16 `:771`（`excite→excited`）与 #17 `:826`（`We were very excited` 缺 be）**已考未教**，是全库唯一的「先考后教」倒挂 → L113 转正（判据：案件里出现过的语言点必须在课程里正面教过）。
2. **半曝孤例转正**——L56 `:10222`／`:10278`（`Grandma's`／`Grandpa's` birthday，从不作 target、不进 practice）→ L111 立岗；L70 `:12871`（`I am a little thirsty.` 对白孤例）→ L114 转正。
3. **跨批钩子兑现（第四次）**——L67 `:12362`「三块门牌」／`:12365`「门牌房里擅长只认 at」明写「只讲了一员，家族留着没开」＝L67 自己指的方向；本批**折 1 处 `in → at`（#126）**，不单开课。

**决策点列表**：

| # | 决策点 | 时点 | 输入 |
|---|---|---|---|
| 1 | 封面 8 张图像语义核对与最终指派（池内互换余量＝9 张） | D1 | 逐张人工核对；守 `cover42`／`cover25` 的 35 压线 |
| 2 | **F17-A 打样门**（放行本批） | D2 | L111 首玩走查记录 |
| 3 | L112 校正口径感知（「学过了」是否集中） | D2（附验） | L112 生产 + 走查 |
| 4 | **中段自走查 ①**（`bored/boring` 话术接住率；`a few` 丢失率） | D4 | L113／L114 生产即查 |
| 5 | 段一联检（`of` 互扰是否提位对比卡 ②） | D5 | L111–L114 联检记录 |
| 6 | L115 增量感知（学完仍只用 have？） | D6 | L115 走查 |
| 7 | **中段自走查 ②**（四点串台；L117/L118 重复度）+ 展示层闭环 | D8 | L117 走查；路径页 118 课走查 |
| 8 | 全量验收放行 | D10 | G1–G14＋G-boost＋732 基线＋build |
| 9 | **F17-B 合并裁决**（含批十八候选清单：`be/get used to` 首选） | 上线后 1–2 周 | F9-B–F17-B 九批一次读齐 |

**硬纪律（本批不可破）**：课量 8 不扩不缩；**一课一增量**（L118 零新知）；**每课 contrast ≥2 条带 `wrongMark` 错卡（推荐 3、禁止贴线）**；`wrongMark` 不得为纯标点；`wrongToken` 与 tokens **逐字相等**；课号 111–118 连续；**L1–L110 一字不动**（含不改写前批 `wrongToken`／`wrongMark`——批十六三处回修已在工作区，**本批只做新增**）；tagStats=10 不动、不新增枚举、不改 schema；**comparison 绝不进案件**；**本批 0 处 fragment／run_on**；不引番外 5 案；零术语红线 28 词＋本批额外 12 词全禁；池外零新资产；`cover49` 永久禁用。

---

## §8 开放问题日程（PRD §8 十项 → 排期）

| # | 开放问题 | 验的时点／门 | 默认方案 | 兜底／升级条件 |
|---|---|---|---|---|
| 1 | **「感到版／让人版」话术是否接得住「我很无聊」**（**本批最大未知**） | **生产期 D3 即查＋中段自走查 ①（D4）＋F17-B** | L113 三条对比卡（`I am boring.` ❌／`I bored.` ❌／`The book is bored.` ❌）一次打透；保障句 `The book is boring.` 让主考点不落空 | 误答集中 → 对比卡 ③ 提位 ＋保障句提前；仍不接 → 段间加 1 日缓冲；F17-B 单列「`I am boring.` 误答回流率」 |
| 2 | **`have got` 增量是否被感知「没必要学」** | **生产期 D6 走查＋F17-B** | 对比卡 ④ 双正解显式给「口气」偏好（`I have got a new bike.` ✅／`I have a new bike.` ✅） | 命中率高 → L115 对比卡 ④ 改为显式偏好提示（**不砍课**） |
| 3 | **`'s` 与 `of` 互扰** | **生产期 D4＋段一联检（D5）＋F17-B** | L111 对比卡 ② 单列 1 张；**不展开、不进 practice、不进案件** | `of` 直译抬头 → 对比卡 ② 提位为第 2 条（≤0.25d） |
| 4 | **`a few` 的 a 丢失率是否真高于 a 的其他用法** | **生产期 D4（自走查 ①）＋F17-B** | 对比卡 ① 单打丢招牌；保障句 `There is a little milk.` | **若丢失率不高 → L114 增量在批十八前重评估**（本批不动结构） |
| 5 | **门牌家族扩员是否引发 `interested at` 型过度泛化** | **中段自走查 ②（D8）＋F17-B** | 不单开门牌课，只在 #126 折 1 处 `in → at` | 案件误答率高 → L117 补 1 张 `good at`／`interested in` 并排卡（**不新增课**） |
| 6 | **封面 8 张语义核对** | **D1 拍板＋段一联检（D5）复核** | 9 张可用张内互换；**守住 `cover42`／`cover25` 的 35 压线不自破** | 不符者池内互换；池外零新资产（缺省回退 scene SVG） |
| 7 | **L117 取材与 L118 的重复度** | **中段自走查 ②（D8）** | L117 取 `'s`／mine／bored／a few 四句＋复现＋变体；L118 取**六句＋总表** | 重复感集中 → L118 总表卡改「六行＋一句话」形态（PRD §8-7 备选） |
| 8 | **连续第五个大章节的疲劳** | **F17-B（并入合并关账）** | **不设缓解动作，仅登记观察**（F13-B–F16-B 均未关账，判据尚无数据） | 连续两课完成率显著下滑（走查＋遥测双证）→ 大章节形态退回「章内一拱 4–5 课」重设计 |
| 9 | **L115／L116 的档位表述口径** | **对外发布前（D10 验收）** | 一律写「**B+ 档·说法扩充**」，**不得**与 L111／L113／L114 的 A 档混述 | 三研究一致要求；写进 PRD／路线图／发布文案三处 |
| 10 | **基线口径** | **D9 预检＋D10 验收** | 本路线图实测 **58 文件 / 732 项**；三研究记 721／729（**差值为同期另一路改动**） | 生产时以实跑为准（G14 写 732，若再变以实跑登记） |
| — | **（路线图追加）批十六前提未验** | **F17-B 合并裁决** | F16-A／F16-B 未回，本批不等待；若显示使役侧异常则优先处置 | 必要时插入一次小修批 |

到点未拍板 → 自动落默认方案并记入决策记录。

---

## 附录：核查留痕

### A. 实读源文件清单（本轮）

**上游交付物（6 份，全部实读）**：`deliverables/product-strategy/prd-grammar-a2-closeout-2026-09-19.md`（逐课规格 8 课／G1–G14／案件 #120–#127／Non-goals／开放问题 10 项）·`user-research-grammar-seventeenth-batch-2026-09-19.md`（瑞思）·`competitive-analysis-grammar-seventeenth-batch-2026-09-19.md`（竞析）·`data-audit-grammar-seventeenth-batch-2026-09-19.md`（数析）·`roadmap-grammar-sixteenth-batch-2026-09-19.md`（格式基准）·本文件。

**代码／数据（实读，本轮独立复核）**：`src/data/grammarLessons.ts`（20,613 行／110 课；逐课 line→number 映射脚本）·`src/data/huntCases.ts`（6,949 行／119 案）·`src/data/grammarSeasons.ts`（`:50` season-16 末项）·`src/data/grammarSeasons.test.ts`（4 项守门逐条实读）·`src/pages/GrammarPathPage.tsx`（`:233` m17／`:240-245` m18；**里程碑恰 18 个**）·`src/services/grammarAmbushService.ts`（`:160-193` 词表＋`:195-212` `pickClozeWord` 逐字复刻）·`src/services/grammarBoostService.test.ts`（`:182-200` `wrongToken` 与 `wrongMark` 断言实读）·`src/services/huntService.ts`（`:331-342` 十枚举）·`src/services/huntService.test.ts`（`:109` tagStats／`:215` 番外 5 案）·`src/services/lessonService.ts`（`number - 1` 解锁）。

### B. md5（本轮实测）

| 文件 | md5 | 与上游对比 |
|---|---|---|
| `src/data/grammarLessons.ts` | `faa827970c06a0d9512a4e4211d31eb0` | 与瑞思一致；**与数析记 `b86213af…` 不同**（未查明·见 D 未核实） |
| `src/data/huntCases.ts` | `554641ab06b9a08af610a6888ab3cedf` | 与瑞思／数析一致 |
| `src/data/grammarSeasons.ts` | `fed6e81c60746a9974c575279b4f8e5a` | 与数析一致 |
| `src/services/grammarAmbushService.ts` | `84b306b40e881c4f9b8c8d000851686c` | 与数析一致（与批十六同值＝未改动） |
| `src/services/grammarBoostService.ts` | `c8b049693b3eff9fa5392c1c8aa9387e` | **与数析记 `8fd33946…` 不同**（未查明·见 D） |
| `src/services/grammarBoostService.test.ts` | `1bb83e53f415b94cfb020c22603b1c0a` | 数析未记此文件（记 `8828d61f…` 为另一路） |
| `src/pages/GrammarPathPage.tsx` | `6b67e5f4c4a62d5c28747bd8ba47574c` | 与数析一致 |
| `src/data/grammarSeasons.test.ts` | `2c74e48900c27445c4003ea67dd921a8` | 与数析一致 |
| `src/services/huntService.test.ts` | `ad93ad15e68ea498d42568b6289776f4` | 与数析一致 |
| `src/services/lessonService.ts` | `c074a84a4ead4cf6174c4aaae9a6f014` | 与数析一致 |

### C. 测试实跑与关键实算（本轮）

| 命令／脚本 | 结果 |
|---|---|
| `npx vitest run`（全量） | ✅ **58 文件 / 732 项全绿（6.22s）**——PRD §5.1 G14 基线一致 |
| `npx tsc --noEmit` | ✅ **0 错**（exit 0） |
| `grammarAmbushService` 词表逐字复刻 | **146 条目 / 145 唯一**（重复项＝`would`）＋停用词 **30**（与 `i` 交叉 1） |
| **cloze 落点逐课实跑（本批 8 课＋保障句）** | L111 `Grandma's birthday is in May.`→**is**｜L112 `This book is mine.`→**is**｜L113 `I am bored.`→**am**｜L114 `There are a few apples.`→**are**｜L115 `I have got a new bike.`→**have**｜L116 `She has got a new bag.`→**has**；保障句 `My brother's car is new.`→is／`That book is my brother's.`→is／`The book is boring.`→is／`There is a little milk.`→is／`We have got a new bike.`→have／`He has got a new bike.`→has——**与 PRD G12 逐条一致** |
| **词表成员复核** | `few`／`little`／`bored`／`boring`／`mine`／`there`／`apple(s)`／`milk`／`book`／`birthday`／`grandma` = **OUT**；`is`／`am`／`are`／`have`／`has`／`got` = **IN** |
| 封面使用位逐条 line→lesson 映射 | `cover1`→L1·L79｜`cover2`→L2·L80｜`cover3`→L3·L81｜`cover4`→L4·L82｜`cover7`→L7·L76｜`cover25`→L25·L78｜`cover29`→L29·L75｜`cover42`→L42·L77｜`cover49`→**L49·L58（段距 9，禁用）**——与 PRD §2.1 指派逐张吻合 |
| 关键行号抽验（`grammarLessons.ts`） | `:87`（I am 固定搭档）／`:90`（I am happy.）／`:435`（I have a new bag.）／`:442`（have＋a 话术）／`:1699`（说昨天的事）／`:2816`（have to）／`:3729`（have＋做过版）／`:4460`（小尾巴 -s）／`:4652`／`:4655`（there is/are 平台）／`:5931`／`:5937`／`:6001`（L33 mine 教透）／`:10222`／`:10278`（L56 孤例）／`:12289`／`:12293`／`:12295`／`:12362`／`:12365`（L67 门牌）／`:12871`（L70 孤例）／`:15704`（L85 whose）／`:17188`（tiring 仅 distractor）／`:17294`（to 后面永远穿原样）／`:20419`／`:20421`（L110 末课）——**逐条实读吻合** |
| 关键行号抽验（`huntCases.ts`） | `:687`（`"grandma's"`）／`:771`（`excite→excited`）／`:826`（`were very excited`）／`:2636`（`"teacher's,"`）／`:4121`（`"Dad's"`）／`:6907`（尾案 `hunt-who-makes-who`）——**逐条实读吻合** |
| raw 计数 | 课 `^    number: ` **110**；案 **119**（尾 `number: 119`）；错点 `^        tag: "` **450**（verb_form 103／plural 80／sv_agreement 61／preposition 56／word_order 40／tense 36／article 24／missing_be 22／run_on 14／fragment 14）；`reviewed: true` **119**；`cover: cover` **110** 次／**49** 张；episode「一百一十一…一百一十八」**零占用**；m 里程碑 **18** 个 |

### D. 未核实项（须生产期登记或人工确认）

1. **封面 8 张未做图像语义核对**——本路线图指派按**编号间距实算**（min-gap 35，二分答案＋二分匹配＋穷举），**9 张可用张的画面内容与 L111+ 场景是否相称须逐张人工确认**（D1 拍板；沿用批十六口径）。
2. **`be/get used to` 的跨级衔接未裁**（B1 结构，与 A2 段收尾的衔接方式留批十八）；**`have sth done` 是否放行未裁**（跨级＋零底座＋错型弱三项叠加）。
3. **`look + 形容词` 是否升格未裁**（12 处认读垫子 vs `look like` 仍三缺——**两件事不得混述**）。
4. **8 案错型分布为 PRD 预判**（#120–#127 的 4 错骨架），生产时逐 token 核 `tokenIndex` 与 `tokens`。
5. **无真实遥测**（样本＝1）；**无「核心 500 词」机检资产**，`bored`／`boring`／`a few`／`interested` 是否超纲无法机检。
6. **批十六 F16-A 走查与 F16-B 关账均未回**（「使役已补齐」前提未经遥测验证）。
7. **两处 md5 与数析记录不一致未查明**：`grammarLessons.ts`（本文件 `faa82797…` vs 数析 `b86213af…`）与 `grammarBoostService.ts`（本文件 `c8b04969…` vs 数析 `8fd33946…`）——**盘面指标（110 课／119 案／450 错点／110 次封面）与两份记录一致**，推测为批十六交付后**另一路改动**所致，具体提交边界未逐项比对（数析 §附录 E 已确认工作区全部未提交）。
8. **`cloze` 词表口径三记不同**：本路线图实算 **146 条目／145 唯一＋30 停用词**；批十六数析记「147 条／146 唯一」；批十七数析记「176 条／169 唯一」；竞析记「146 条／145 唯一」。**本路线图以本轮复刻实算为准**（146／145），生产时按生效词表逐课重跑（G12 纪律）。
9. **英文侧内容瑕疵已记录**：english.cool `used-to` 专文答案区自相矛盾（「didn't used to」vs 前文「didn't use to」）——**引用只取教学段，不引答案区**（竞析 §⑤-11）。
10. **批十六的 `wrongToken`／`wrongMark` 三处回修（L96／L102／L89）仍在未提交工作区**——**立项前置动作：先提交或打 tag**；本批按「不改写前批」纪律**只做新增**。

---

> 本报告由产品战略团队 AI 协作生成，重要决策请由产品负责人审定。
