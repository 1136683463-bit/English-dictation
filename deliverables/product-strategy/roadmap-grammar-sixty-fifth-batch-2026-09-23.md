# 路线图 · 第六十五批（今日页语法入口 + 一次并发损坏事故）

- 日期：2026-09-23
- 交付：**今日页补语法线首屏入口**（已提交 `cd92417`）
- 事故：**并发进程把 `GrammarLessonPage.tsx` 截断为 0 字节**，忆段追问入口丢失

---

## 1. 交付（已提交）

**`cd92417` 今日页补语法线首屏入口**

| 改动 | 内容 |
|---|---|
| Hero 主 CTA | `继续第 N 课` / `复习 N 个语法句`——落点优先到期语法句，其次下一课 |
| 任务队列加行 | 「语法阶梯」`已完成 / 205` 进度弧 |
| 数据来源 | **复用路径页同一套服务**（`summarizeLessonProgress` / `listDueGrammarReviewCards`），不另算一套 |
| 守门 | `rv15` 5 项（含闸自检）|

**为什么做这个**：`TodayPage.tsx` 对 `grammar` 的引用数曾是 **0**——205 课 / 52 里程碑 / 214 案件全藏在导航第 4 项，移动端还不在主导航。

**验证**：tsc 0 错误；build 通过；守门双向自检（落点改成 `/training` → 4 项变红）；走查确认 CTA 在 `.ui-banner` 内（首屏第一眼），文案「继续第 1 课」→ L1。

---

## 2. ⚠️ 事故：并发进程把 3943 行的文件截断为 0 字节

**发生了什么**：

1. 我在 22:43 开始给 `GrammarLessonPage.tsx` 补「忆段追问入口」（路线图第 3 项，0.5 人日）；
2. 该文件**同时在被另一个进程编辑**（它在做 ENV3-C 输入框属性修复 + 键盘重排功能）；
3. 22:59:33，文件被**截断为 0 字节**，全库 2500+ 测试瞬间崩溃（`tsc` 报 8 处「不是模块」）；
4. 我等待 2.5 分钟确认不是瞬时写入；`HEAD` 版本完整（3943 行），**已用 `git show HEAD:` 恢复**；
5. **我的忆段改动（未提交）随截断丢失**；我写的 `rv16` 守门因此失去被测对象，已删除（不留失败测试）；
6. 恢复后并发进程又提交了它自己的版本（含键盘重排），我确认**我两块提交完好**。

**当前状态**：`tsc` 0 错误、build 通过、**2538/2539 通过**（唯一失败 `st5` 是并发进程的在途工作）。

---

## 3. 我为什么不重做忆段入口

**同一个文件被两个进程同时写，已经造成一次 0 字节事故。** 再进去改，重复的风险是实打实的。

而且恢复后的文件**已含并发进程的新代码**（键盘重排 `onKeyDown`、输入框 `lang="en"` 等），
它的版本在演进——我现在改，等于在移动靶上写代码。

**建议**：忆段入口（路线图第 3 项）**等并发进程告一段落再做**，或者由**它**来做（它更熟悉自己改后的结构）。

---

## 4. 事故后的复盘：单文件双写是根因

| 环节 | 应有做法 | 实际 |
|---|---|---|
| 开工前 | 检查目标文件是否被改过 | ✅ 我查了（22:14 改过，但看着像已停）|
| 判断并发是否停止 | 应看**修改时间是否持续推进** | ⚠️ 只看了单次时间戳，**把「35 分钟前的改动」误判为「已停」** |
| 写文件 | —— | ❌ 与另一个进程同时写入，触发截断 |

**教训**：单次时间戳不足以判断并发进程是否停止。若该文件在最近 30 分钟内有改动，
且工作区存在大量未提交改动（当时 242 项），就应当**假定它仍在工作**，不要写同一个文件。

---

## 5. 我这两批的净产出（已提交，不受事故影响）

| 提交 | 内容 |
|---|---|
| `d6c9533` | 修词典英美拼写不对称（23 组）+ 三批守门（rv11/rv12/rv13）|
| `19994fc` | L58 补 `quietly` 卡 |
| `cd92417` | 今日页语法线首屏入口（rv15）|

---

## 6. 未结项

| # | 项 | 状态 |
|---|---|---|
| 1 | 忆段追问入口（路线图 #3）| **丢失，建议延后或交由并发进程做** |
| 2 | 首页门面（路线图 #1+#2）| ✅ **已交付** |
| 3 | 遥测 `section` 硬编码 + 参照句取错 | 属忆段入口的一部分，**一并丢失** |
| 4 | 路线图 #4~#8（行为日志自审 / 输入回路 / 随课词桥 / 冠词专项 / 数据寿命）| 未开始 |
| 5 | 并发进程的在途工作 | 242 项未提交，1 项测试失败（`st5`）|
