# 听写工坊 · 全站 UI 升级设计方案

> 版本：v1.0 ｜ 日期：2026-09-12
> 风格基准：**冒险学习板块（Adventure V3）**——已精修、作为全站视觉锚点
> 目标：把冒险板块的设计语言系统化为可复用的令牌 + 组件，落地到其余 8 个板块，消除"模块之间风格割裂"

---

## 0. 为什么要做这件事（诊断结论）

对比冒险板块与其他板块的实测截图，问题不在"不好看"，而在**同一套产品里存在两套设计语言**：

| 维度 | 冒险板块（已精修） | 其他板块（现状） |
| --- | --- | --- |
| 起首区 | 通栏照片 Hero，照片底部渐隐融入底色 | 裸标题直接写在灰底上（今日/词书/错词本） |
| 区块容器 | 区块**直接置于页面背景**，无外层白卡 | 一层层白卡包裹（`panel` 套 `panel`） |
| 图标 | 40px **圆形** + 语义 tint 底 + 深前景 | 圆角方块 + 彩色底（训练/添加/统计各不相同） |
| 层级手段 | 靠**字号 + 字重 + 留白** | 靠**色块 + 边框 + 彩色徽标**（设置页黄绿橙并存） |
| 主色使用 | 橙色克制，只用于眉标/主按钮/选中态 | 高饱和色块大面积铺（词书封面橙/青满色） |
| 空状态 | 内嵌图形化引导 | 虚线框 + 居中一段灰字（错词本） |
| 控件 | 分段控件 / 胶囊 chips / ghost 按钮 | 各页自造按钮与开关样式 |

**结论**：冒险板块已经把"克制、排版主导、通栏 Hero、无框区块"这套语言验证成功。本方案不做推翻式重设计，而是**把冒险板块的做法抽象成规范，然后逐板块套用**。

---

## 1. 设计基因：从冒险板块提炼的 7 条铁律

这 7 条是全站统一的判断标准，后续任何页面改动都以此为准绳。

1. **通栏 Hero 起首**——每页第一屏由通栏视觉区承载「眉标 + 大标题 + 一句话定位 + 主行动」，Hero 底部以渐变融入页面底色，**不产生硬边**。
2. **扁平化区块**——正文区块直接置于页面背景上；白卡（Surface）只用于"必须框住的一组同类信息"（如列表、表单），**禁止卡套卡**。
3. **眉标系统**——所有区块统一 `11px / 700 / 0.14em / uppercase` 的小标签；主区块用橙色，次级区块用灰色。
4. **排版主导层级**——`32 → 22 → 17 → 15 → 13 → 12` 的字号阶梯 + 字重对比 + 留白建立层级，**不靠色块堆叠制造层级**。
5. **圆形语义图标**——40px 圆形，浅 tint 底 + 同色系**加深前景**（保证对比度达标）；**一个模块只允许出现一个语义色**。
6. **胶囊控件**——按钮与 chips 全部胶囊化；"多选一"统一用**灰底分段控件 + 白底选中块 + 橙色 inset 描边**。
7. **克制动效**——hover 上浮 2px + 投影加深，active `scale(0.97)`，统一 140–180ms ease，尊重 `prefers-reduced-motion`。

---

## 2. 设计令牌（Design Tokens）

### 2.1 颜色

沿用项目现有体系，**不引入新色相**，只补齐"文字级"色值（解决对比度问题）。

| Token | 值 | 用途 | 白底对比度 |
| --- | --- | --- | --- |
| `--bg` | `#f4f6f8` | 页面冷灰底 | — |
| `--surface` / `--surface-strong` | `#ffffff` | 卡片/面板 | — |
| `--surface-muted` | `#f8fafb` | 次级填充、分段控件容器 | — |
| `--ink` | `#1f2b3a` | 主文字（墨蓝黑） | 13.9:1 ✅ |
| `--ink-soft` | `#3e4c5e` | 次级正文 | 8.6:1 ✅ |
| `--muted` | `#5f6b7a` | 辅助/元信息 | 5.4:1 ✅ |
| `--line` / `--line-strong` | `#e8edf1` / `#d7dfe6` | 描边 | — |
| `--accent` | `#f26a1b` | **仅用于**主按钮底、选中描边、图标填充等非文字场景 | 3.04:1 ⚠️ 不可作小号文字 |
| `--accent-strong` | `#d95a12` | 主按钮 hover | 3.87:1 ⚠️ |
| **`--accent-ink`（新增）** | `#c04a0b` | **橙色小号文字**（眉标文字、链接、强调字） | **4.97:1 ✅ 达 AA** |
| `--accent-soft` | `#fff1e8` | 橙色 tint 底 | — |
| **`--green-ink`（新增）** | `#24784c` | 绿色小号文字 | 5.43:1 ✅ |
| **`--blue-ink`（新增）** | `#2f62ea` | 蓝色小号文字 | 4.62:1 ✅ |
| **`--amber-ink`（新增）** | `#8a6200` | 琥珀小号文字 | 5.01:1 ✅ |
| **`--danger-ink`（新增）** | `#b03535` | 红色小号文字 | 5.26:1 ✅ |

> **对比度校验说明**：现状眉标用 `#f26a1b` 打在 `#f4f6f8` 上仅 3.0:1，未达 WCAG AA（小号文字需 4.5:1）。这是本次升级的**可访问性硬修复**：所有 ≤16px 的橙色/绿色文字一律改用 `-ink` 系列；`--accent` 原值保留给按钮底、描边、图标等图形场景（图形仅需 3:1）。
> 语义 tint 底沿用现有 `--accent-soft / --green-soft / --blue-soft / --amber-soft / --danger-soft`。

### 2.2 字体阶梯

字体栈保持不变：`Inter, "HarmonyOS Sans SC", "PingFang SC", ui-sans-serif, system-ui`。

| 角色 | 规格 | 用途 |
| --- | --- | --- |
| `--fs-eyebrow` | 11px / 700 / `0.14em` / uppercase | 眉标 |
| `--fs-hero` | 32px / 700 / `-0.02em` / lh 1.15 | 页面主标题（Hero 内） |
| `--fs-section` | 22px / 700 / `-0.02em` | 区块标题 |
| `--fs-card` | 17px / 600 / `-0.01em` | 卡片标题 |
| `--fs-item` | 15px / 600 | 列表项标题 |
| `--fs-body` | 13.5px / 400 / lh 1.6 | 正文与描述 |
| `--fs-meta` | 12px / 500 | 元信息、辅助 |
| `--fs-num` | 30px / 650 / `-0.03em` / `tabular-nums` | 关键数字（替代 5 张大数字卡） |

### 2.3 圆角 / 阴影 / 间距 / 动效

```css
:root {
  --radius-xs: 8px;    /* 分段控件内块、小图标底 */
  --radius-sm: 12px;   /* 提示条、次级容器 */
  --radius-md: 16px;   /* 卡片、面板 */
  --radius-lg: 20px;   /* Hero 卡、大面板、弹窗 */
  --radius-pill: 99px; /* 按钮、chips */

  --shadow-sm: 0 1px 2px rgba(31,43,58,.04);
  --shadow-md: 0 8px 24px rgba(31,43,58,.08);
  --shadow-lift: 0 12px 28px rgba(31,43,58,.12);   /* hover 上浮 */
  --shadow-pop: 0 18px 44px rgba(31,43,58,.16);    /* 浮层/弹窗 */
  --shadow-focus: 0 0 0 4px rgba(242,106,27,.12);

  --sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px; --sp-4: 16px;
  --sp-6: 24px; --sp-8: 32px; --sp-12: 48px;

  --ease: 160ms cubic-bezier(.2,.7,.3,1);
}
@media (prefers-reduced-motion: reduce) { * { transition-duration: .01ms !important; animation-duration: .01ms !important; } }
```

**间距基准 4px**，常用节奏：区块间 `32px`、卡片间 `12px`、卡内边距 `16–20px`、Hero 上下内边距 `22px 内部 / 36px 页面`。

---

## 3. 组件库规范（8 类核心组件）

### 3.1 `PageHero` 页头（**最高优先级**）

现状有 4 套互不相同的实现（`page-header` / `mode-hero` / `hub-hero` / `action-hero` / `stats-brief`），统一为两种形态：

**A. 通栏图版 Hero**（仅冒险页保留，其他页可选主题图）

```
[全宽背景图 248px 高]  ← 底部 8 档渐变淡出到 --bg，无硬边
  ├ 左：眉标(橙) / H1 32px / 副文案 13.5px   最大宽度 60%
  └ 右：手写体点缀（可选，≤1 处，全站仅冒险页使用）
```

**B. 素色 Hero**（其余全部页面统一采用）

```
[无背景图，直接在 --bg 上，顶部内边距 8px]
  ┌──────────────────────────────────────────────┬──────────────┐
  │ ADVENTURE / UNITS …          ← 眉标 11px 橙   │              │
  │ 页面主标题 32px/700                           │  [主按钮]     │
  │ 一句话定位 13.5px muted                       │  [ghost 按钮] │
  └──────────────────────────────────────────────┴──────────────┘
   底部 24px 留白后接第一个区块，**不加分隔线、不加卡片底**
```
> 关键点：Heading 与右侧行动按钮**基线对齐**，行动区最多 2 个按钮（1 主 1 ghost），其余操作下沉到区块内。

### 3.2 `SectionHead` 区块头

```
NEW ROUTE / SAVED ROUTES / QUEUE …   ← 眉标 11px，主区块用 --accent-ink
区块标题 22px/700                     ← 与眉标间距 6px
                                     ← 右侧：ghost 按钮 / 计数（12.5px muted）
                                     底部 12px 留白
```
**区块本身不套白卡**，直接置于 `--bg` 上。

### 3.3 `Surface` 面板（白卡，克制使用）

```css
.surface {
  background: var(--surface);
  border: 1px solid var(--line);      /* 或 border:none + shadow-sm，二选一，禁止同时重描边+重投影 */
  border-radius: var(--radius-md);
  padding: var(--sp-6);
  box-shadow: var(--shadow-sm);
  transition: box-shadow var(--ease), transform var(--ease);
}
.surface--interactive:hover { transform: translateY(-2px); box-shadow: var(--shadow-md); }
```
**使用规则**：一张页面最多出现 **1 个** 视觉重量级面板；列表、表单可入面板；说明性文字、区块头、步骤条**不入面板**。

### 3.4 `ListRow` 列表项（替代各页形态不一的"条目卡"）

```
┌──────────────────────────────────────────────────────────┐
│ (○ 圆形图标 40px)  标题 15px/600        ← 数值/箭头 右侧   │
│                   描述 12.5px muted                       │
└──────────────────────────────────────────────────────────┘
  布局：grid-template-columns: auto minmax(0,1fr) auto; gap 14px
  分隔：同一 Surface 内用 1px --line 分隔线；独立卡片则用 --surface + radius-sm
  hover：整行底色转 --surface-muted，箭头右移 2px
  选中/进行中：左侧 3px 橙色竖条 + 底色 --accent-soft
```

### 3.5 `IconBadge` 圆形语义图标

```
直径 40px（次级 32px）；border-radius: 50%
底色：对应 --*-soft；图标色：对应 --*-ink（**不是** --*-base，保证对比度）
内部图标：lucide-react 20px / stroke 2
规则：一个模块只用一个语义色；同页多个模块可各用一个色，但**总数 ≤ 3**
```

### 3.6 按钮体系（收敛为 4 种）

| 类型 | 规格 | 用途 |
| --- | --- | --- |
| `primary` | 高 44px / 胶囊 / 底 `--accent` / 白字 13.5px 600 / hover `--accent-strong` + `translateY(-1px)` / active `scale(.97)` | 每屏**最多 1 个** |
| `ghost` | 高 36–40px / 胶囊 / 白底 + 1px `--line` / 字 `--ink-soft`；hover 描边 `--line-strong` + 底色 `--surface-muted` | 次级操作 |
| `quiet` | 无底无框，仅文字 `--muted`，hover `--accent-ink` | "查看全部 ›"、计数入口 |
| `danger` | ghost 形态但字色 `--danger-ink`，hover 底 `--danger-soft` | 删除类 |

> 移除各页自造的高饱和小按钮（设置页"立即备份"、统计页"清到期复习"等统一改成 `primary` 或 `ghost`）。

### 3.7 表单控件

- 输入框 / 下拉：高 **42px**、`--radius-xs`~10px、1px `--line`、白底；focus → 边框 `--accent` + `--shadow-focus`
- 多选一 → **分段控件**：容器 `#f2f3f6` + 3px padding + `--radius-xs`；选中块白底 + `inset 0 0 0 1px var(--accent)` + 字色 `--accent-ink`
- 开关 → 统一 44×24 胶囊，开态 `--accent`
- 复选框 → 统一 16px，选中 `--accent` 底 + 白勾
- 字段标签 → 13px/600 `--ink-soft`，与控件间距 8px；提示文字 12px `--muted`

### 3.8 空状态 / 反馈条

**空状态（统一模板）**
```
        ( 圆形 56px 浅灰底 + 线性图标 24px muted )
        主文案 15px/600 --ink
        说明 12.5px --muted，最多 2 行，居中，最大宽度 360px
        [ 主按钮 ]
   外层：1px dashed --line-strong + --radius-md + 上下 48px 内边距
```
**反馈条**（note / warn / error）
```
高 40px，--radius-sm，1px 同色系描边 + 同色系 soft 底，
字色一律用 --*-ink，左侧 16px 图标。
规则：同屏**最多 1 条**；能内联在区块里的不要做成整条横幅。
```

---

## 4. 逐板块精修方案

> 每页给出：**诊断 → 精修后结构 → 关键改动清单**

### 4.1 今日（TodayPage）· 优先级 P0

**诊断**
- 标题裸在灰底上，与下方 `action-hero` 大卡形成"两层页头"，视觉重心混乱
- `action-hero` 内左侧橙色进度环 + 右侧 3 张白卡（到期/已练/薄弱），两套层级并置
- 任务队列 4 条各带彩色底（橙/粉/紫/蓝），是全站杂色最重的一屏之一
- 页面底部有一块大面积橙色 CTA，重复了 `action-hero` 的主行动

**精修后结构（自上而下）**
```
① PageHero（素色）：NEXT BEST ACTION / 今天先清到期复习 / 一句定位 + 右侧[开始今日训练][错词本]
② SectionHead「QUEUE 今日任务队列」 + 右侧 quiet「选择模式 ›」
   └ 一个 Surface 内 4 条 ListRow（圆形图标 + 进度 + 数值），
     首条 = 进行中态（3px 橙竖条 + accent-soft 底）
③ 数据条：3 个「标签 + 30px 大数字」无边框数据列，1px 竖线分隔（替代 3 张白卡）
④ SectionHead「MISTAKES 最近错误」 + 空状态模板
⑤ 底部橙色 CTA **删除**，主行动已上提到 Hero
```

**改动清单**
| 动作 | 位置 |
| --- | --- |
| 删除现有页头块，改 `PageHero` 素色形态 | `TodayPage.tsx` 顶部 + `.page-header` |
| `action-hero` 拆解：环 → Hero 内淡化的进度信息；双按钮 → Hero 行动区 | `.action-hero*` 系列 |
| 3 张统计白卡 → 无边框数据列（`--fs-num`） | `.action-hero-stats` / `.compact-action` |
| 任务项彩色底 → 圆形图标 + 语义 tint（最多 2 色：橙=到期、红=错词） | `.quick-action-row` / `.goal-icon` |
| 底部橙色大卡删除 | `.action-hero-actions`（确认无复用后清理） |

---

### 4.2 训练（TrainingPage）· P0

**诊断**
- 已有 `eyebrow + h1` 雏形，但 Hero 与"今日训练"卡两层页头感重复
- `mode-grid` 8 张卡片全白 + 彩色浅底图标块（方块不是圆），密度高且无视觉引导
- "推荐节奏"三卡与前文风格脱节（纯白描边卡，图标又是另一种色）

**精修后**
```
① PageHero 素色：TRAINING / 选择今天的训练方式 / 定位 + 右侧[回到今日]
② 主推区：一个 Surface「今日训练」——左侧大标题 + 预计耗时；
   右侧 3 个无边框数据列 + [开始今日训练]。**去掉卡中卡**
③ SectionHead「MODES 训练方式」
   └ 8 条 ListRow（两列 grid），圆形语义图标，右侧「115 个可练单词 ›」
     语义色收敛：听力=蓝、拼写=橙、错词=红、句子=绿（只保留 4 色）
④ SectionHead「FLOW 推荐节奏」
   └ 3 条**无面板**的编号步骤（① ② ③ 数字 28px/650 橙色 + 标题 + 说明）
```

**改动清单**：`.mode-hero*` 合入 `PageHero`；`.mode-card-icon` 方 → 圆并用 `--*-ink`；`.training-flow` / `.training-note-panel` 去面板化，改编号步骤。

---

### 4.3 词书（UnitsPage）· P0

**诊断**
- **全站视觉最"重"的一屏**：封面用满色橙/青渐变，饱和度远高于全站基调，与冒险板块的克制形成断裂
- 目标进度条区域（`vocab-goal-panel`）与下方卡片密度、字号都不同步
- 分组标题的彩色竖条 chip 与眉标系统重复

**精修后**
```
① PageHero 素色：UNITS / 单元 / 定位 + 右侧[＋新建分组]
② 目标条：单行 Surface——左「0% 完成」大数字 + 细进度条；右 4 个无边框数据列
③ SectionHead「核心 100 · 5 本词书」+ quiet「编辑分组 ›」/ danger 图标
   └ 封面卡：**降低饱和度**——改为「浅色纸面 + 单色书脊 + 墨色书名」，
     等级与进度用 12px meta 行；hover 上浮 2px
④ SectionHead「冒険积累 · 1 本词书」…（同结构）
⑤ 「自定义词书 / 自定义精听」新建卡：dashed 描边 + 加号，去掉彩色双卡
```

**改动清单**：封面渐变改「低饱和纸面」方案（见 §5 预览页色板）；`vocab-goal-panel` 压成单行；分组标题色条 → 眉标；`unit-group-toolbar` 沿用现有收起逻辑。

> 备注：此页上一轮已做过密度优化（见 09-12 工作日志），本次只做**色彩与组件口径统一**，不再压缩密度。

---

### 4.4 词库（LibraryPage）· P1

**诊断**
- 顶部 5 张大数字白卡 + 4 个入口卡 + 左右主从，三层同类信息重复（数字卡与筛选条数字重复）
- 左右分栏的详情面板与浏览器面板都是白卡，中间缺少层级差

**精修后**
```
① PageHero 素色：LIBRARY / 管理你的个人语料库 / 定位 + 右侧[设置与导出]
② 数据条：5 个无边框数据列（--fs-num），替代 5 张大白卡
③ 入口行：4 个**轻量入口**（图标 + 文字 + 计数，透明底，hover 才出底）
④ 工作区：左 Surface（搜索 + 分段筛选 + ListRow 列表）/ 右 Surface（词条详情）
   —— 左右面板用同一 radius/shadow；左列 3px 橙色态条标识当前选中项
```

**改动清单**：删 `library-overview` 的大数字卡改数据列；`insight-card` 去面板化；筛选 chips → 分段控件；`library-*` 面板统一 `Surface`。

---

### 4.5 错词本（MistakeBookPage）· P1

**诊断**
- 空状态是全站最弱：虚线框内一段灰字，缺少引导与情绪
- 有数据态：`mistake-date-panel` + `mistake-book-main` 双面板，日期徽标彩色（`mistake-date-badges`）过杂

**精修后**
```
① PageHero 素色：MISTAKE BOOK / 错词本 / 定位 + 右侧[去训练]
② 空状态：套用统一空状态模板（圆形图标 + 主文案 + 说明 + 主按钮），
   并把「完成一次拼写或复习」拆成 3 步小提示
③ 有数据：左日期列表（当前日期项 3px 橙条 + accent-soft），右错词详情
   日期徽标统一为「中性灰 + 一个橙色强调」；diff 高亮沿用现有绿/红但改用 -ink 文字色
```

**改动清单**：`mistake-filter-empty` / `EmptyState` 组件升级为统一模板；`mistake-date-badges` 去彩色；`mistake-next-action` 收敛为 1 主 1 ghost。

---

### 4.6 统计（StatsPage）· P1

**诊断**
- 健康度圆环（绿）与三个彩色图标块（红/绿/蓝）+ 蓝紫渐变卡并存，色彩数量超标
- "风险分层"三卡 + "今日 10 分钟计划"步骤卡，样式与训练页各自为政

**精修后**
```
① PageHero 素色：WEEKLY REPORT / 学习周报 / 定位 + 右侧[清到期复习]
② 简报 Surface：左标题 + 定位 + [primary][ghost]；右「健康度 40」大数字
   —— **圆环改为细弧 + 大数字排版**（沿用弹窗改造中已验证的"大数字替代圆环"手法）
③ 数据条：3 个无边框数据列（今日收尾 / 错词专项 / 下周负载）
④ SectionHead「RISK LAYERS 风险分层」 → 3 条 ListRow（圆形图标，统一琥珀色）
⑤ SectionHead「TODAY PLAN 今日 10 分钟计划」→ 编号步骤（与训练页同组件）
⑥ 底部彩卡区（本周复习次数等）→ 无边框数据列栅格，**去掉渐变底**
```

**改动清单**：`.health-ring` → `.metric-figure`；`.metric-card` 去彩底；`.stats-focus-icon` 方→圆；`.combo-index` / 渐变卡统一为数据列。

---

### 4.7 设置（SettingsPage）· P0（色彩问题最严重）

**诊断**
- 同屏出现：黄色告警条 + 绿色提示条 + 橙色"已保存"按钮 + 蓝/绿/粉图标块 —— **6 种以上颜色并存**
- 设置面板左右两列都是白卡，分组标题层级弱，像后台管理页
- 危险区、云同步等区域缺少统一样式语言

**精修后**
```
① PageHero 素色：SETTINGS / 设置与导出 / 定位 + 右侧[已保存 ✓]（quiet 态，非橙色按钮）
② 备份提醒：**最多 1 条**反馈条（琥珀），右侧 [立即备份] 用 ghost
③ 分组：SectionHead + Surface，**去掉卡套卡**
   - 学习目标：3 个数字字段 + 1 条 inline 说明（说明用 muted 小字，去掉绿色底条）
   - 语音与听写：下拉 + 滑块（滑块统一橙色圆点）+ 复选框行
   - AI 中转站 / 云同步：字段 + [测试连接][保存]，按钮统一 ghost/primary
   - 危险区：Surface + 1px --danger-soft 描边，按钮 danger
④ 全页语义色**收敛到 2 种**：橙（主）+ 琥珀（提醒）；其他一律中性
```

**改动清单**：`.backup-reminder` 保留但改为标准反馈条；`.settings-impact-note`（绿底）→ 无底色 muted 文案；`.settings-status-grid` 彩色块 → 圆形 `IconBadge`；`.panel-header` 统一 `SectionHead`。

---

### 4.8 添加（AddPage）· P2

**诊断**
- `hub-hero` 整块白卡 + 右侧橙色按钮，是全站唯一把 Hero 做成大白卡的页面（与冒险的通栏 Hero 冲突）
- 4 个入口卡（`hub-card-icon` 浅底方块）+ 底部步骤条（橙色编号方块），配色尚可但组件口径不一

**精修后**
```
① PageHero 素色（不再包白卡）：ADD / 把今天遇到的材料放进来 / 定位 + 右侧[导入一段材料]
② SectionHead「CAPTURE 采集入口」→ 4 条 ListRow（圆形图标，右侧计数 + 箭头）
③ SectionHead「SUGGESTED FLOW 建议的添加顺序」
   → 编号步骤（复用训练/统计的统一组件，编号橙字 28px，去掉橙色方块底）
```

---

## 5. 实施路线（建议分 3 期）

| 期 | 范围 | 产出 | 依赖 |
| --- | --- | --- | --- |
| **第 1 期（基础）** | 令牌补齐 §2 + 组件 §3.1/3.3/3.4/3.5/3.6 | `styles.css` 新增 token 与 `.ui-*` 通用类；不动业务逻辑 | 无 |
| **第 2 期（高价值页）** | 今日、训练、设置、词书（4 页） | 视觉最割裂的 4 页归位 | 第 1 期 |
| **第 3 期（收尾）** | 词库、错词本、统计、添加（4 页） | 全站一致 | 第 1 期 |

**建议落地方式**：新增通用类前缀 `ui-`（`ui-hero` / `ui-section` / `ui-surface` / `ui-row` / `ui-icon` / `ui-btn`），与现有 `adv-*` 并行；各页逐个把旧类替换为 `ui-*`，替换完一页删一页的旧样式，**避免出现第三套命名**。

---

## 6. 验收清单（每页改完自检）

- [ ] 页面顶部为 `PageHero`，主标题 32px/700，眉标 11px uppercase，行动区 ≤ 2 个按钮
- [ ] 区块直接置于页面背景，**无卡套卡**；全页视觉重量级面板 ≤ 1 个
- [ ] 图标全部为圆形 `IconBadge`，前景色用 `-ink` 系列
- [ ] 全页语义色 ≤ 3 种；小号彩色文字全部使用 `-ink` 值（对比度 ≥ 4.5:1）
- [ ] 圆角仅出现 `--radius-xs/sm/md/lg/pill` 五档；阴影仅出现 `sm/md/lift/pop` 四档
- [ ] 多选一控件为分段控件；按钮仅 primary / ghost / quiet / danger 四型
- [ ] 空状态使用统一模板
- [ ] 键盘 Tab 可达全部交互，focus 有 `--shadow-focus` 可见环
- [ ] `prefers-reduced-motion` 下降级正常
- [ ] 1440 / 1024 / 720 / 420 四个断点无溢出、无错位

---

## 7. 风险与注意事项

1. **不要动冒险板块**——它是基准，其他页向它对齐；若要改，先改规范再同步。
2. **`styles.css` 已 14600+ 行**，存在旧入口页死代码（约 300 行）。第 1 期先建 `ui-*` 通用层，替换完成后统一清理死类，**不要边改边删**，避免误伤阅读页共用规则。
3. **弹窗类**（`unit-modal` / `custom-book-modal` / `confirm-dialog`）已单独精修过，本次只做令牌对齐，**不改结构**。
4. 词书封面降饱和是**观感变化最大**的一项，建议先做单页对比再推广，避免用户"认不出词书"。
5. 页面入场动画带 transform 时**禁止 `forwards/both` 填充**（会造成 `position: fixed` 弹层定位失效，此前已踩坑）。
