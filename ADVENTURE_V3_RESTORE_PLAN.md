# 冒险学习页 V3 视觉还原实施方案

> 目标：按方案图 1:1 还原冒险学习入口页 + 深色侧边栏
> 状态：待确认（确认后按阶段实施）
> 依据：方案图（截图）+ 现有代码（`AdventurePage.tsx` / `App.tsx` / `styles.css`）

---

## 一、方案图拆解（逐区块规格）

### 0. 全局外壳：深色侧边栏
- 底色近黑（约 `#17181c`），通栏到底，无描边
- 顶部 Logo：橙色圆角方块图标（无文字），下方导航项：图标 + 文字，当前项橙色高亮
- 底部一个白色圆角方块按钮（下载/导出图标）
- 圆角/间距：导航项高 40px、圆角 10px、左右内边距 12px，图标 18px，文字 13.5px

### 1. 顶部 Hero 背景区
- 内容区顶部约 190px 高的横版照片（山脊 + 登山者剪影，冷蓝灰晨雾 + 暖色天光）
- 照片从顶部向下渐隐融入页面底色（底部遮罩过渡）
- 右上角手写体英文 "Better You Every Day"（斜体、浅色）
- 标题区叠在照片上：eyebrow `ADVENTURE`（12px / 600 / 字距 0.12em / 橙色）→ h1 `冒险学习`（32px / 700 / 深墨色）→ 副标题（14px / 灰）

### 2. 继续阅读卡
- 白卡，圆角 14px，高约 76px，内边距 16px 20px
- 左侧 44px 深色圆角图标（内含橙色星芒图形）
- 标题 17px / 600；难度与来源徽章已去掉（方案图无）
- 中部：细进度条（高 6px，轨道浅灰，填充橙色），右侧紧跟元信息 `第 3 章 · 收了 0 个词 · 今天 08:34 保存`（12px 灰）
- 右侧：橙色主按钮 `继续阅读 →`（高 44px，圆角 12px，白字 600）

### 3. 新建冒险卡（NEW ROUTE）
白卡圆角 14px，内边距 24px，内部自上而下：
1. **头部行**：左 `NEW ROUTE`（12px 字距 0.12em 橙）+ `开始一段新冒险`（22px / 700）；右两个 ghost 按钮 `↺ 恢复默认`、`⤨ 随机推荐`（白底描边、高 36px、圆角 10px、12.5px）
2. **主题卡行**：5 张等宽卡（间距 12px），每张：
   - 左：36px 圆角 10px 彩色图标砖（蓝紫 / 紫 / 青 / 琥珀 / 橙）
   - 中：标题 14.5px / 600 + 描述 12px 浅灰（两行内）
   - 右：`›` 箭头（16px 浅灰）
   - 选中态：1.5px 橙描边 + 极浅橙底
3. **参数行**（两列）：左「阅读难度」label（13px / 600）+ 分段控件（容器浅灰圆角 10px 内衬 3px，选中项白底 + 橙描边 + 橙字，未选中灰字）；右「给故事一个线索（可选）」label + 输入框（高 42px，圆角 10px，右侧带展开小图标）
4. **灵感行**：`灵感：` + 3 个胶囊 chip（高 30px、白底描边、12px 灰字）
5. **底部行**：左 `✦ 路线会自动保存，可以随时回到旧节点。`（12.5px 灰）；右橙色胶囊按钮 `＋ 开始冒险`（高 46px）

### 4. 我的路线（SAVED ROUTES）
- 标题行：`SAVED ROUTES` + `我的路线`（22px / 700）+ 右侧 `5 段 ›`（12.5px 灰）
- 路线卡：**横向滚动**一列，卡宽约 300px（露出第 4 张的边缘暗示可滑），卡内：
  - 首行：36px 图标砖 + 标题（14px / 600 单行截断）+ 难度徽章（浅灰底 11px）+ **书签图标**（右上）
  - 进度条（高 5px，橙色填充）
  - 末行：`第 3 章 · 0 个生词` + `今天 · 08:34 保存`（11.5px 灰）+ 右缘 `›`

### 5. 配色与量感（落地 token）
| 用途 | 值 |
|------|-----|
| 页面底色 | `#f4f5f7`（冷灰，替代现有暖米 `#f5f3ef`） |
| 侧边栏底色 | `#17181c` |
| 卡片 | `#ffffff` + 描边 `rgba(16,24,40,0.07)` + 阴影 `0 1px 2px rgba(16,24,40,0.05)` |
| 主色 | 沿用 `--accent: #f06423`（与方案图橙几乎一致） |
| 圆角 | 卡片 14px / 内层元素 10–12px / 胶囊 999px |
| 文字 | 主 `#182233`、次 `#667085`、弱 `#98a2b3` |

---

## 二、与现状差异对照（要改什么）

| 区块 | 现状 | 方案图 | 处理 |
|------|------|--------|------|
| 侧边栏 | 浅米色、含分组标签、文字品牌 | 深色、无分组标签、圆角图标块 | 全局深色化（保留现有 9 项 IA，见"已知偏差"） |
| 页头 | 纯文字标题，无背景 | 背景照片 + 手写体英文 + 叠字标题 | 新增 Hero 背景区（需生成素材） |
| 继续阅读卡 | 图标 + 标题 + 徽章 + 进度条+元信息分两行 | 单行：图标 / 标题 / 进度条 / 元信息 / 橙色按钮 | 重排为单行栅格，去掉徽章 |
| 新建卡头部 | eyebrow + 标题 + 两个次按钮 | 同结构，但按钮为 ghost 风格 | 微调样式 |
| 主题卡 | 图标砖在标题上方、纵向堆叠、选中为对勾角标 | 图标砖在左、右侧 `›`、选中为橙描边 | 改横向布局 + 箭头，去掉对勾角标 |
| 难度控件 | 分段控件（深色选中块） | 浅灰容器 + 白底橙字选中 | 改选中态配色 |
| 线索输入 | 与难度同排、无尾部图标 | 同排、带尾部展开图标 | 加尾部图标 |
| 灵感 chips | 已有 | 同 | 仅样式对齐 |
| footer | 提示 + 按钮（同排） | 同排，按钮改胶囊 + `＋` | 微调 |
| 我的路线 | 3 列网格卡片、无图标砖/书签/箭头、无 `5 段 ›` | 横向滚动卡列 + 图标砖 + 书签 + 箭头 + 计数入口 | 重排为横向滚动 + 新增收藏功能 |

---

## 三、技术实现方案

### 3.1 样式命名策略（关键，避免层叠污染）
- 冒险**入口页**新样式统一使用 `adv-*` 前缀命名空间（`adv-hero`、`adv-card`、`adv-theme-card`、`adv-level`、`adv-route-card` …），追加在 `styles.css` 末尾
- 旧入口页专属规则（`.adventure-hero*` / `.adventure-create-*` / `.adventure-template*` / `.adventure-options` / `.adventure-route-grid` / `.adventure-route-card*` / `.adventure-saves-section`）**整段删除**，避免新旧规则互相覆盖
- **阅读页共用**的类（`.adventure-badge`、`.adventure-progress-track`、`.tone-*`、`.adventure-reader-*`）保持不动，只在需要时把入口页改为使用 `adv-*` 等价实现

### 3.2 侧边栏深色主题
- 文件：`src/styles.css`（`.app-shell` / `.sidebar` / `.nav-group*` / `.brand*` / `.sidebar-toggle` / `.sidebar-footer` / `.mobile-more-*`）
- 做法：新增 `:root` 深色侧栏 token（`--sidebar-bg` / `--sidebar-ink` / `--sidebar-ink-muted` / `--sidebar-hover` / `--sidebar-active`），侧边栏内所有颜色改为引用这些 token；折叠态、移动端「更多」面板、badge 一并适配
- 页面底色是否全局转冷灰：**建议一起转**（否则深色侧栏 + 暖米底会割裂），见"待确认"

### 3.3 数据结构：路线收藏（新增功能）
| 文件 | 改动 |
|------|------|
| `src/types.ts` | `Adventure` 增加 `favorite?: boolean` |
| `src/services/storage.ts` | `normalizeAdventure` 增加 `favorite: value.favorite === true`（旧数据自动补 false，无需 bump schemaVersion） |
| `src/services/adventureService.ts` | 新增 `toggleAdventureFavorite(data, adventureId)`，并新增 `getAdventureFavoriteCount(data)` 供 `5 段 ›` 入口显示 |
| `src/pages/AdventurePage.tsx` | 卡片书签按钮（点击切换、阻止冒泡、`aria-pressed`）；排序规则：**收藏优先 + updatedAt 倒序**（仅影响"我的路线"列表，Hero 仍取最近更新） |
| 测试 | `adventureService.test.ts` 补 2 条用例（切换收藏、排序） |

### 3.4 素材：Hero 背景图
- 生成规格：`2880×640`（2x，实际显示约 1440×320），横构图，主体偏右（山脊 + 登山者剪影），左侧留白给标题，色调冷蓝灰 + 暖阳，底部自然过渡到 `#f4f5f7`
- 落地方式：`src/assets/adventure-hero.jpg`，在 `AdventurePage.tsx` 中 `import heroImage from "../assets/adventure-hero.jpg"`，通过 `style={{ backgroundImage: url(heroImage) }}` 应用到 `.adv-hero`（Vite 会打包进构建产物，Tauri 离线可用）
- 融合处理：底部 `linear-gradient(180deg, transparent 55%, var(--bg-cool) 100%)` 遮罩，确保与页面底色无缝
- 手写体英文：HTML 文本 + `font-family: "Snell Roundhand", "Segoe Script", cursive`（macOS 原生，无网络字体依赖）

### 3.5 组件拆分
保持单文件为主，抽 3 个页面内子组件，控制膨胀：
- `ThemeCard`（图标砖 + 文案 + 箭头 + 选中态）
- `RouteCard`（图标砖 + 标题 + 徽章 + 书签 + 进度 + 元信息 + 箭头）
- `LevelSegmented`（难度分段控件）

预估改动量：`AdventurePage.tsx` ~+180 行（含收藏逻辑）、`styles.css` ~+420 行 / -160 行、`App.tsx` 约 0 行（仅 class 适配）、类型与存储 ~+15 行。

---

## 四、实施步骤（6 步，可逐步验证）

1. **素材 + token**：生成 Hero 背景图；新增深色侧栏 token 与冷灰底色变量
2. **侧边栏深色化**：应用到全局，检查 9 个页面 + 折叠态 + 移动端「更多」
3. **页面骨架**：Hero 背景区 + 标题区（eyebrow/h1/副标题/手写体）
4. **继续阅读卡 + 新建卡**：单行栅格、主题卡横排、难度/线索/灵感/footer
5. **我的路线**：横向滚动卡列 + 收藏功能（类型/存储/服务/交互 + 单测）
6. **响应式与还原核对**：1024 / 768 / 620 三档；截图与方案图逐项比对，输出差异清单；`tsc` + `vitest` + `vite build` 全绿

---

## 五、1:1 还原验收清单

- [ ] 侧边栏深色、当前项橙色高亮、底部白色圆角按钮
- [ ] 顶部背景图 + 底部渐隐融入底色 + 右上手写体英文
- [ ] eyebrow / h1 / 副标题 的字号、颜色、间距与方案图一致
- [ ] 继续阅读卡单行布局：图标 → 标题 → 进度条 → 元信息 → 橙色按钮
- [ ] NEW ROUTE 头部：字距 eyebrow + 两个 ghost 按钮位置与样式
- [ ] 5 张主题卡等宽横排：彩色图标砖、右侧箭头、选中橙描边
- [ ] 难度分段：浅灰容器 + 白底橙字选中态
- [ ] 线索输入带尾部图标；灵感 chips 三个
- [ ] footer：左提示文字 + 右橙色胶囊「＋ 开始冒险」
- [ ] 我的路线：`N 段 ›` + 横向滚动卡列（第 4 张部分露出）
- [ ] 路线卡元素齐全：图标砖、标题、难度徽章、书签、进度条、两段元信息、箭头
- [ ] 书签可点击切换并持久化，收藏项排在最前
- [ ] 既有功能不丢：AI 随机推荐 / 恢复默认 / 自定义冒险 / 骨架屏 / 错误提示 / 删除确认弹窗 / 空状态
- [ ] 三档断点无横向溢出，键盘可达（focus 环、`aria-pressed`）

---

## 六、已知偏差（与方案图必然不一致，需你认可）

1. **侧边栏导航项**沿用现有 9 项（今日 / 训练 / 冒险 / 错词本 / 词书 / 添加 / 词库 / 统计 / 设置）。方案图里的「探索 / 学习 / 路线 / 收藏 / 社区 / 帮助」在本应用没有对应页面，新增会做出死链；只还原视觉，不改 IA。若你想改文案（如「今日」→「首页」、「冒险」→「路线」），可以一并调整。
2. **主题卡文案**：默认仍是现有 4 个离线模板（校园谜题 / 城市寻信 / 海岸列车 / 雾林灯塔），方案图的「沙漠古城 / 星际小邮差 / 海底秘境 / 老宅钟声」属于点击「随机推荐」后 AI 产出的形态，实际内容由模型决定，不会逐字一致。
3. **背景图为 AI 生成近似素材**，非原图，山形与人物构图会有差异。
4. **`5 段 ›` 的点击行为**：方案未定义。默认实现为「滚动到列表末端」，如列表超出一屏则展开为完整列表抽屉——如需其他行为请指定。

---

## 七、需要你点头的两处决策

- **A. 页面底色**：是否随侧边栏一起全局转冷灰 `#f4f5f7`？（建议是；否则只改冒险页，其他页面仍是暖米底）
- **B. 底部动作**：`5 段 ›` 是否按第 6 节第 4 条实现？
