# 冒险板块 UI/UX 优化方案

> 角色：UI/UX 设计总监 × 产品总监
> 范围：`/adventure` 入口页（`src/pages/AdventurePage.tsx`）为主，`/adventure/:id` 阅读页为二期
> 状态：待确认（确认后再动代码）

---

## 一、现状诊断（基于截图 + 代码走查）

### 1. 信息架构与动线问题

| # | 问题 | 影响 |
|---|------|------|
| P1 | **"继续阅读"不是页面主 CTA**。回归用户最高频的动作是"接着上次读"，但它和"新建冒险"视觉权重接近，且只露出最近一条 | 老用户每次回来都要重新扫视页面 |
| P2 | **右侧橙色指南栏价值低、占地大**。三行静态文案占了约 1/4 宽度，把主表单压成 680px 左右 | 首屏信息密度低；主题卡被迫换成 2×2，"自定义冒险"孤零零掉到第二行 |
| P3 | **主题卡缺少视觉锚点**。纯文字卡扫视成本高，选中态只有细描边，一眼难以辨认当前选择 | 选择效率低，选中反馈弱 |
| P4 | **"阅读难度"用原生 select**，与整体设计语言脱节；5 个选项点开下拉才能看到，操作成本高于必要 | 表单精致感断裂，移动端体验更差 |
| P5 | **"线索"输入框与主题选择的联动不清晰**：模板模式下是可选线索，自定义模式下变必填 AI 提示，切换只有 label 文案变化，用户需要自己"悟" | 自定义模式易踩坑（写了线索才发现要配 AI） |
| P6 | **"我的路线"被压到首屏以下**，且列表条目信息贫瘠：只有标题 + 章节 + 难度，没有进度感、生词收获、来源（AI/离线） | 已有内容没有形成"成就资产"的感觉 |
| P7 | **删除用 `window.confirm`**，原生弹窗打断体验且不可撤销 | 危险操作无缓冲 |
| P8 | **AI 未配置 / 生成中 / 出错三种状态反馈弱**：生成中只有按钮文字变化，错误只在表单底部一行小字 | 用户不确定系统在干什么 |

### 2. 体验原则（本次改版的三句话）

1. **回归优先**：回来读 > 开始新的。继续阅读必须是页面第一视觉焦点。
2. **先读后选**：冒险的核心循环是"读 → 选 → 收词"，用轻量引导讲清楚，而不是靠常驻侧栏。
3. **路线是资产**：每一段冒险都是用户的进度和生词积累，要用进度可视化让它"值得炫耀"。

---

## 二、重设计方案

### A. 页头：紧凑化（反馈采纳：删除三步玩法条）

- 删掉大标题下的独立描述段落，改为「冒险学习 — 读故事 · 做选择 · 收生词」单行。
- ~~三步玩法条~~：用户反馈多余，**整体移除，不做替代**。玩法说明保留在页头一行文案里即可。

### B. 继续阅读 Hero 卡（解决 P1）

- 升级为页面第一张卡：左侧主题图标 + 标题 + **难度徽章 + 来源徽章（AI/离线）**。
- 增加 **mini 进度条 + 元信息**：第 N 章 · 已收 N 个生词 · 上次阅读时间。
- 主 CTA「继续阅读 →」保持右侧；如有多条进行中路线，Hero 只展示最近一条，其余进"我的路线"。
- 无进行中冒险时此区域隐藏，页面自然从"新冒险"开始。

### C. 主题卡：动态化 + AI 随机推荐（解决 P2/P3，反馈采纳：前 4 卡不写死）

- **移除右侧指南栏**，表单占满内容宽度，主题卡保持 **5 列等宽网格**（窄屏降为 3 列 / 2 列）。
- 每张主题卡保留 **彩色图标** 形成扫视锚点；选中态强化：主色描边 1.5px + 浅橙底 + 右上角对勾圆点。
- **前 4 张卡不再永远固定**：
  - 默认仍展示 4 个离线模板（保证不配 AI 也能玩，离线剧情始终可用）。
  - 区块标题行右侧新增 **「随机推荐」按钮**（shuffle 图标）：点击调用 AI 一次性生成 **4 个随机冒险主题**（标题 + 一句话简介 + 场景关键词），替换前 4 张卡；自定义卡固定在第 5 位不动。
  - AI 推荐卡带 **「AI 推荐」小角标**；图标按场景从内置图标池匹配（侦探/书信/列车/灯塔/海岛/魔法…）。
  - 选中推荐卡并开始时，走 AI 生成首章（与自定义模式同一链路，推荐主题作为剧情方向传入）；因无离线剧情，AI 未配置时推荐卡上显示「需配置 AI」徽章并引导设置，**提前暴露依赖**。
  - 推荐结果 **缓存在 localStorage**，刷新不丢；提供「恢复默认」小入口换回 4 个离线模板。
  - 状态设计：请求中按钮转圈 + 卡片区骨架占位；失败时内联提示「推荐没有生成，稍后再试」，默认 4 卡不受影响；随机按钮在 AI 未配置时禁用并带 tooltip。
- 卡片右上角信息位预留徽章（需配置 AI / AI 推荐）。

### D. 难度与线索：分段控件 + 灵感 chips（解决 P4/P5）

- 难度改为 **A1–C1 分段控件（segmented control）**，一屏可见、单击切换。
- 线索输入下方增加 **灵感 chips**（3 条可点击示例，一键填入），降低"不知道写什么"的启动成本；仅模板模式显示。
- 自定义模式下：输入区上移高亮为该模式的主角，label 变为「告诉 AI 你的冒险方向」，并显示 AI 说明条（现有逻辑保留）。
- 主按钮「开始冒险」紧跟表单右下，尺寸加大；自定义模式生成中显示「AI 正在构思…」+ 按钮内加载态（保留现有 spin），并在按钮上方给出氛围提示文案。

### E. 我的路线：列表升级为卡片网格（解决 P6/P7）

- 3 列卡片网格（窄屏 2/1 列），每张卡：
  - 主题图标（颜色随主题）+ 标题 + 难度徽章
  - **进度条**（按到达的最深章节估算）+「第 N 章 · N 个生词」
  - 相对时间（今天 / 3 天前 / 上周）
  - hover 显示「继续」按钮；右上角删除入口改为 **卡片内菜单 → 自定义确认弹层**（替换 window.confirm）
- 排序维持按更新时间倒序；空状态保留现有 EmptyState。

### F. 状态与无障碍

- 所有交互元素补齐 `:focus-visible` 焦点环；radiogroup/checkbox 语义沿用。
- 删除确认改为自绘 Dialog（或 Popconfirm），支持 Esc 关闭 + 焦点管理。
- 生成失败、AI 未配置等错误统一为表单内联 alert 样式，颜色语义化（红底浅红）。

### G. 响应式断点

| 宽度 | 布局 |
|------|------|
| ≥1024px | 单栏全宽：玩法条 3 列、主题卡 5 列、路线卡 3 列 |
| 768–1023px | 主题卡 3 列、路线卡 2 列 |
| <768px | 全部单列堆叠，Hero 卡 CTA 变全宽按钮 |

---

## 三、不改什么（克制清单）

- 视觉基调：暖米底 + 橙主色 + 白卡片，与全站一致，只做层级强化不做换肤。
- 业务逻辑：`adventureService` / `adventureModelService` 的创建、生成、存档逻辑全部不动。
- 阅读页（AdventurePlayPage）本轮不动，留作二期（候选优化：句子卡收词动线、路线回看的地图化展示）。

## 四、实施范围

| 文件 | 改动 |
|------|------|
| `src/pages/AdventurePage.tsx` | 结构重排：删侧栏与玩法条、Hero 强化、卡片网格/分段控件/灵感 chips/删除确认、随机推荐按钮与状态 |
| `src/services/adventureModelService.ts` | 新增 `generateAdventureRecommendationsWithModel`：一次调用生成 4 个主题（title/description/scene），复用现有 provider 调用与 JSON 解析容错模式 |
| `src/services/adventureService.ts` | 仅导出模板常量补充场景→图标映射，不改离线剧情与创建逻辑 |
| `src/styles.css` | 新增/调整 adventure 区块样式（约 150–250 行），含响应式与 focus 态 |
| `src/components/*` | 视需要新增轻量 `ConfirmDialog` 组件（约 60 行，可复用到错词本等处） |
| 图标 | 沿用 lucide-react，不新增依赖 |

预估改动量：~500 行，无路由、无数据结构变更，回归风险低。

## 五、验收标准

1. 回归用户 3 秒内能看到"继续阅读"及其进度。
2. 首屏不滚动即可完成"选主题 → 选难度 → 开始"全流程。
3. 点击「随机推荐」后出现 4 张 AI 推荐主题卡，刷新页面不丢失；「恢复默认」可一键回到离线模板。
4. 未配置 AI 时：随机按钮禁用并引导设置；选中推荐卡时提前提示，而不是提交后才报错。
5. 主题卡选中态、生成中/失败态均一眼可辨。
6. 删除路线不再出现原生 confirm，且全程键盘可达。
7. 768/1024 两档断点下无横向滚动、无孤行卡片。
