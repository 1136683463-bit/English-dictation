# 听写工坊 UI/UX 大改版执行方案

更新时间：2026-06-28

## 1. 产品设计判断

听写工坊不是内容平台，也不是泛用词典。它的核心价值是把真实英语材料沉淀成可复习的词、短语和句子，并通过听写、拼写、回忆和复盘形成长期记忆闭环。

当前改版方向应保持为“安静的学习工作台”：低噪声、强信息层级、清晰行动路径、适合每天重复使用。避免营销式大卡片、过度装饰和一屏堆满说明。

## 2. 目标体验

1. 打开后立即知道今天该做什么。
2. 开始训练后始终知道当前阶段、剩余任务和错误状态。
3. 错词、错句、材料、统计之间能自然回流，而不是散在多个管理页。
4. 移动端和桌面端都能完成核心训练，不出现横向溢出、文字挤压和按钮不可达。
5. 所有核心数据本地优先、可导出，界面要给用户稳定、可控的信任感。

## 3. 信息架构方案

主导航保留 5 个高频入口：

- 今日：每日任务、继续训练、复习负债、下一步行动。
- 训练：复习、听音拼写、错词专项、句子回译等训练模式。
- 添加：单词、句子、材料导入的统一入口。
- 词库：统一管理单词、短语、句子、材料和重点项。
- 统计：周报、健康分、错词复盘、下一步建议。

旧页面继续保留为深层管理能力：

- `/review` 复习训练
- `/spelling` 听音拼写 / 错词专项
- `/words` 单词管理
- `/sentences` 句子管理
- `/units` 单元管理
- `/import` 材料导入
- `/settings` 设置与导出

## 4. 视觉与交互系统

### 视觉方向

- 背景：温暖纸感、低对比浅色面。
- 主色：橙色用于主行动，青绿色用于学习状态和正确反馈。
- 危险色：只用于错误、复习负债和确认性风险。
- 字体层级：训练题目可以大，工具面板要紧凑。
- 卡片：只用于独立对象、工具面板、总结和列表项；页面区块不做嵌套卡片。

### 交互规则

- 每个训练页必须有“会话仪表条”：阶段、剩余、已完成、错误、下一步。
- 每个训练页必须有一个清晰主动作：开始、检查、看答案、提交或继续。
- 错误反馈必须可读，不应瞬间消失。
- 低分或错误回流要让用户看见原因和后果。
- 移动端优先保证操作可达，再压缩辅助信息。

## 5. 分阶段执行

### Phase 1：训练体验统一

目标：让 `/review` 和 `/spelling` 都变成稳定的训练工作台。

已完成：

- `/review` 新增阶段状态、会话指标、显式看答案、评分决策面板。

本轮执行：

- `/spelling` 增加会话仪表条。
- 调整听音拼写的输入、播放、提示和反馈层级。
- 错题反馈显示正确拼写和字母级 diff。
- 错题自动前进时间加长，保证用户能看见反馈。
- 移动端保持无横向溢出，评分/操作按钮不挤压。

验收：

- 构建和测试通过。
- `/spelling` 桌面和 390px 移动端无横向溢出。
- 正确/错误反馈状态可见，错误 diff 可读。

### Phase 2：材料到词句的导入闭环

目标：导入材料后，候选词、分句、材料保存和下一步训练形成一条线。

已完成：

- 导入页增加“导入会话结果”面板，显示材料保存状态、本轮收词、本轮收句。
- 保存材料后给出明确成功反馈和下一步动作。
- 从导入页可直接进入词库或复习训练。
- 候选词和句子加入后会在本轮结果里即时计数。

已完成：

- 统一候选词与句子列表的批处理状态，显示可收、已加入、已存在、未收、选中来源。
- 候选词支持批量收词，句子支持批量收句，并限制单次批处理量，避免制造过多复习负债。
- 材料详情页加入“可能已生成卡片”和“未处理片段”视图。

### Phase 3：词库管理专业化

目标：让词库从列表页升级为可筛选、可批量处理、可追踪来源的学习资产库。

已完成：

- 统一单词、短语、句子、材料的对象详情面板。
- 词库新增选择模式，支持批量标重点、取消重点、暂停、导出、加入单元和删除。
- 卡片详情新增轻量管理：重点、暂停、加入单元、删除、训练入口和完整编辑入口。
- 增加来源语境聚合：卡片可看到可能关联材料，材料可看到句段处理状态和可能生成卡片。

### Phase 4：统计与复盘增强

目标：统计页不仅展示数据，还能生成下一步训练动作。

已完成：

- 周报增加连续错误、近期错词趋势、复习负债三类风险分层。
- 周报增加“建议训练组合”，把清到期、错词拼写、句子复盘、材料补练串成可执行路径。
- 统计页可直接进入错词专项、句子复盘、材料补练和词库复盘。

### Phase 5：设置与信任感

目标：让本地优先、导出、语音设置、严格模式等关键能力更可控。

已完成：

- 设置页重组为学习目标、语音与听写、数据与导出、系统状态。
- 设置保存有明确未保存/已保存反馈。
- 导出前展示卡片、复习记录、材料、句段和最近备份时间。
- 语音设置提供即时试听状态。
- JSON 恢复和本地数据重置进入高风险区，重置需要二次确认。

## 6. 验证记录

- `npm run build` 通过；仅保留内置词典 chunk 体积提示。
- `npm test` 通过；8 个测试文件、33 个测试。
- 浏览器检查通过：`/library`、`/stats`、`/settings`、`/import` 桌面和 390px 移动端无横向溢出。
- 交互检查通过：词库批量选择、设置页重置二次确认、导入页批处理状态可见。

## 7. 长期设计原则

1. 每个页面只回答一个主问题。
2. 管理页帮助用户整理资产，训练页帮助用户专注行动。
3. 所有错误都要能回流到下一次训练。
4. 所有统计都要能转化为下一步行动。
5. 不为了“看起来丰富”增加无效 UI。
