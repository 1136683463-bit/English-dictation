# 打开网页入口

双击 `open-web.command` 即可：

1. 检查本地 `127.0.0.1:1420`
2. 如果服务已运行，直接打开浏览器
3. 如果服务没运行，自动启动 `npm run dev -- --host 127.0.0.1`
4. 等待服务可用后打开网页

第一次使用前，如果 macOS 提示无法打开脚本，可以在终端执行：

```bash
chmod +x /Users/liujun/Documents/英语听写/open-web.command
```

然后再双击。

