# 「小美的一天」深度优化 PRD · 让初学者真的学懂

| 项 | 内容 |
| --- | --- |
| 作者 | 许清楚（产品经理） |
| 日期 | 2026-09-12 |
| 触发 | 用户反馈：「这些课程，流程都太短了，而且说的太浅显，初学者未必能学懂，能不能再优化一下」 |
| 前置文档 | `GRAMMAR_PEDAGOGY_REVIEW.md`（教学评审）、`GRAMMAR_FUN_ALTERNATIVES.md`（三件套） |
| 关联代码 | `src/data/grammarLessons.ts`、`src/pages/GrammarLessonPage.tsx`、`src/data/huntCases.ts` |

---

## 1. 问题诊断：浅在哪、短在哪

### 1.1 逐课推演结论（完整推演第 1、4、10 课）

**第 1 课（I am）推演**：第①段总信息量 = 1 句场景铺垫 + 1 句对话（Who are you?）+ 3 个词块 + 1 句口诀 + 2 例句，30–40 秒即可读完。口诀「am 不能丢」只说了「要这样」，没说「为什么是 am 而不是 is/are」——而第②段的引导题干扰项恰恰就是 is/are，初学者靠猜也能过（I 后面只有一个正确项），「跟·试一试」退化成排除法游戏。学完第 1 课，用户掌握的是「I am Xiaomei」这一个句子的形状，不是「I am + 任何形容词/名词/身份」的能力。第 2 题练习 `I am a teacher.` 换了名词，但用户没机会接触到 `I am not tired`（他今天明明很累想说不累）和 `Am I right?` 这类高频变体。

**第 4 课（want + a/an）推演**：口诀说「apple、egg 这种元音开头的词前面用 an」。但本课关联的侦探案件 `hunt-new-phone` 里出现了 **an hour**——hour 根本不是元音字母开头！真正的规则是「看发音，不看字母」，课程只教了半条规则，学生一进案件就撞墙。这是「规则只给一句、没有为什么、没有边界」的实锤。同时，a / an 学完了，**the 从头到尾没讲过**，却在案件第一行就有（`the new phone` 是植入的错，但 `the library`、`the station` 这些对的 the 散落在其他案件里）——学生拿什么判断？

**第 10 课（一般过去时）推演**：口诀是「看到 yesterday，动词就要换形状：go 的昨天版是 went」。问题：① 只覆盖了**不规则动词**，初学者未来遇到的大多数动词（watch→watched、play→played、walk→walked）是规则变化加 -ed，全课 0 提及；② 例子全是肯定句，过去时的否定（I didn't go）和疑问（Did you…?）完全没出现，而本课对话原文就是问句 `What did you do yesterday?`——问句就摆在屏幕上，讲解却只回答「陈述句怎么说」；③ 动词换形状的「形状」有哪些（-ed / went 型 / was 型），没有一个全景图，学生学到的是 4 个孤立单词，不是「过去时」这个概念。

### 1.2 诊断清单（按严重程度排序）

| # | 诊断 | 证据 | 影响 |
| --- | --- | --- | --- |
| D1 | **规则只给一句，没有「为什么」，也没有边界**。oneLineRule 是口诀不是解释，遇到边界情况立刻失效 | 第 4 课「元音开头用 an」vs 案件里 an hour（发音规则）；第 9 课 go to + the，但 go to school 不加 the，未解释 | 记不住、迁移不了；进第④段案件就露馅，学用脱节 |
| D2 | **正误对比完全缺失**。12 课全只给正确形式，学生不知道「错会长什么样」 | `grammarLessons.ts` 全文无 wrong 字段；第④段侦探挑战却要求学生认错 | 学与第④段脱节：课堂上从没见过错的，破案只能靠感觉乱点 |
| D3 | **变体缺失：只练第一人称肯定陈述句**。否定、疑问、复数人称 0 覆盖 | 第 7 课对话是 `Are you happy?`、第 6 课是 `What time is it?`、第 4 课是 `What would you like?`——问句都在屏幕上，讲解不碰 | 学生学完 12 课只会「报句子」，不会「聊天」；换个人称/句式就不会 |
| D4 | **辨析缺失：相邻知识点从无对比** | am/is/are 分 3 课学（1/2/7 课），从无一张总对比表；a/an 学了，the 没讲却在案件里到处出现 | 每课孤立成立，合起来不成体系，学生无法自己选搭档 |
| D5 | **流程短 + 缺乏「我会了」的确证感**。第②段 3 题 + 第③段 2 题，全部围绕同一句式；单课 3–5 分钟 | 第 1 课 5 题全是 I am + 名词/形容词的排列组合；完课页只给一句恭喜 | 学完心里没底；「点词成句」的排除法属性让通过率虚高，成就感是假的 |
| D6 | **小剧场浅**：每课只有 1 句对话，剧情感撑不起「小美的一天」连续剧设定 | `dialogueEn` 全部为单句（Who are you? / Nice bag!） | 情景讲解的「情景」只剩背景图，故事沉浸感名存实亡 |

### 1.3 关键判断：哪些「短」其实是对的（不能加深的部分）

| 现状 | 为什么是对的 | 结论 |
| --- | --- | --- |
| 一课只教一个句型、一个「固定搭档」 | 教学评审 1.1 节 i+1 原则：内容要略高于当前水平，一次一个增量 | **保留**，变体/对比只做展示层，不新增知识点负担 |
| 不讲术语（主谓一致、冠词），用大白话「固定搭档」「小标签」 | Affective Filter 原则，术语是元语言不是能力 | **保留**，深挖层可以用术语但要自含解释 |
| 第④段挑战可选、不计时 | 与三件套设计一致 | **保留** |
| 第②③段题量少 | 刷多不如刷对，题海是用户最初否掉的「课程化」老路 | **数量基本不动**，但**题型结构要变**（加入变体题和对比题），这是「该变的不该加量的」 |
| 词汇 500 词内 | 硬规范（D4 决策） | **保留**，超纲词注释 |

**一句话总结**：用户说「短」和「浅」，真正的问题不是「学的东西太少」，而是**「每个点学得太薄」**——只有正确答案的形状，没有错误的样子、没有为什么、没有边界、没有变体。该长的（解释深度、对比、变体）没长；不该长的（题量、知识点数量、术语）已经刚好。

---

## 2. 方案对比与推荐

### 方案 A · 每课内部加深（同一框架增加层次）

四段骨架不动，第①段从「单页」改为「3 步分步」，新增正误对比、变体例句、深挖折叠；第②③段题量微调、加入变体题。

- 优点：不动课程地图和进度数据；加深和用户抱怨的「浅」正面对应；单课时长可控（6–10 分钟）。
- 缺点：第①段变长，若不做好分步交互会变成「读课文」。

### 方案 B · 每课拆上下两节（基础节 + 进阶节）

每课拆成「基础课」和「进阶课」，课程地图从 12 课变 24 课。

- 优点：每节依然短小，节奏轻。
- 缺点：① 12→24 课翻倍，初学者看到进度条的心理负担变大，与「轻松愉快」背道而驰；② 课程地图、进度存储（grammarLessonsDone）、找错解锁链都要改；③ 拆节人为割裂了「先见错、再见全貌」的自然节奏。**不推荐。**

### 方案 C · 主线快速通道 + 深入版可选展开

主线保持现在的轻量单页，深挖内容做成可选展开层。

- 优点：主线最轻，深入者有得学，分层清晰。
- 缺点：单独采用等于没改——用户的「浅」抱怨针对的正是主线本身，把答案全藏进可选层是回避问题。

### 推荐：A 为主干，吸收 C 的折叠机制作为难度分层手段

**「主线加深 + 深挖折叠」：讲解段的正误对比和变体进主线（必学），「为什么」级别的语言学解释进折叠层（选学）。**

理由：① 用户的抱怨是「学不懂」，不是「课不够多」，B 治错了病；② C 单独用是回避，但它的「可选展开」恰好解决 A 的副作用（主线变长吓退初学者）——两者结合是互补而非折中；③ 四段式骨架保留，用户已认可「讲解先行」，这是「加深」不是「推翻」。

---

## 3. 新版单课结构模板（四段式骨架保留）

总时长目标 **6–10 分钟**（现约 3–5 分钟），主线必学 5–7 分钟 + 可选深挖 1–3 分钟。

### 第①段 看 · 情景讲解（2.5–3.5 分钟，主线）——从单页改为 3 步分步

| 步 | 名称 | 内容要素 | 交互形式 | 承载数据字段 |
| --- | --- | --- | --- | --- |
| 1 | 剧场 | 场景插画 + 场景铺垫 + **2–3 句小对话**（替代现在的单句），轮到小美说的意图 | 逐句浮现，每句带朗读；「下一步」翻页 | `scene` / `sceneSetupZh` / `dialogue[]` / `intentZh` |
| 2 | 搭装 + 对错 | 词块搭装（现有）+ 一句话规则 + **1–2 组正误对比**：wrong（标出问题词）→ correct → why | 正误对比用揭示卡：先展示错句，点「看出问题了吗」或点「揭晓」后正确句浮现 + why | `blocks` / `oneLineRule` / `contrast[]` |
| 3 | 变奏 | **3–4 个例句**（现 2 个），同一句型换 2–3 个生活场景；**肯定 / 否定 / 疑问变体**分 tab 展示；「想知道为什么？」折叠卡（选学深挖：辨析、规则背后的道理） | 例句列表（现有样式）；变体用小 tab 切换；深挖卡默认收起 | `examples` / `sceneSwings` / `variants` / `deepDive` |

### 第②段 跟 · 试一试（2–3 分钟，主线）

3–4 题（现 3 题）：原有 choose/arrange 题型不变，**新增 1 题「找茬式引导」**——给一个含错的句子，让学生点出错的词块（为第④段破案做教学铺垫，直接回应 D2），或 1 题变体题（把肯定句改成否定/疑问，词块已给好）。仍然「几乎不会错」，missed 句照旧进 SM-2。

### 第③段 练 · 自己来（2–3 分钟，主线）

3 题（现 2 题）：① 1 题句型内巩固；② 1 题变体（否定或疑问）；③ 1 题场景迁移（同一句型换生活场景，如奶茶店→水果店）。错题机制不变。

### 第④段 破 · 侦探挑战（可选，不变）

不变。但正误对比（第①段步 2）和找茬式引导（第②段）已让学生在课堂上见过「错误的样子」，破案不再是无准备之战。

### 完课页 · 新增迷你小结卡

现完课页只有一句恭喜。新增小结卡：本课 1 条规则 + 1 组对比 + 2 个变体句，一屏读完，作为「我会了」的确证感收尾。

---

## 4. 第 1 课完整内容示例（JSON 草稿，可直接进 data）

```jsonc
// lesson-01-am 的增量字段（现有字段全部保留，此处只展示新增部分）
{
  "id": "lesson-01-am",

  // D6：对话扩为 2-3 句，替代单句 dialogueEn（旧字段保留做 fallback）
  "dialogue": [
    { "who": "classmate", "en": "Hi! Who are you?", "zh": "旁边的同学转过头来问你。" },
    { "who": "classmate", "en": "Are you new here?", "zh": "她又补了一句。" },
    { "who": "me", "en": "（轮到你说了——我是小美。）", "zh": "暗自想好用英语怎么回答。" }
  ],

  // D2：正误对比。学生先看到错的形状，再看到对的 + 为什么
  "contrast": [
    {
      "wrong": "I is Xiaomei.",
      "wrongMark": "is",
      "correct": "I am Xiaomei.",
      "whyZh": "is 不是 I 的搭档。英语里谁和谁是搭档是固定的：I 只跟 am 一起出场，就像搭档不能换人。"
    },
    {
      "wrong": "I Xiaomei.",
      "wrongMark": null,
      "correct": "I am Xiaomei.",
      "whyZh": "中文说「我小美」不用动词，但英语的句子必须有动词。把 am 丢了，句子就塌了。"
    }
  ],

  // D3：变体。肯定句之外，给否定和疑问的高频形状
  "variants": [
    { "label": "肯定", "en": "I am happy.", "zh": "我很开心。" },
    { "label": "否定", "en": "I am not tired.", "zh": "我不累。", "noteZh": "在 am 后面加 not，就是「不」。am 和 not 也是固定搭档。" },
    { "label": "疑问", "en": "Are you new here?", "zh": "你是新来的吗？", "noteZh": "问别人时把 Are 搬到句首。你回答时还是用 I am。" }
  ],

  // D3/D4：场景变奏——同一句型换生活场景，回应「换场景就不会」
  "sceneSwings": [
    { "sceneZh": "宿舍里，室友问你周末干嘛", "en": "I am free on Sunday.", "zh": "我周日有空。" },
    { "sceneZh": "回家后，妈妈问你今天开心吗", "en": "I am happy today.", "zh": "我今天很开心。" },
    { "sceneZh": "球场上，朋友喊你来玩", "en": "I am ready!", "zh": "我准备好了！" }
  ],

  // D1/D4：深挖折叠卡（选学，默认收起）。am/is/are 辨析放第 1 课先埋一颗种子，第 7 课收口
  "deepDive": [
    {
      "title": "为什么 am 只跟 I？is 和 are 呢？",
      "body": [
        "英语的 be 动词有三个搭档，各管一摊：I 专用 am；他、她、它和一个东西用 is；你、我们、他们和两个以上的东西用 are。",
        "下一课你会见到 is 和 are。现在只要记住：说「我」的时候，am 永远在场。"
      ]
    }
  ],

  // 完课小结卡：一屏读完，确证「我会了」
  "summary": {
    "rule": "I am 是一对固定搭档：说「我是……」它们一起出场。",
    "points": [
      "I am not tired. —— 不累：am 后面加 not",
      "Are you new here? —— 问对方：Are 搬到句首",
      "I is ❌ → I am ✅：I 的搭档不能换人"
    ]
  }
}
```

---

## 5. 数据结构增量（TypeScript 草案）

```ts
// 新增类型（追加到 src/types.ts）
export interface LessonDialogueLine {
  who: "me" | "npc";
  en: string;
  zh: string;          // 中文台词或旁白
}

export interface LessonContrast {
  wrong: string;       // 错误句（完整）
  wrongMark?: string | null; // 需要标出的问题词（null 表示整句缺词）
  correct: string;     // 正确句
  whyZh: string;       // 大白话解释，为什么
}

export interface LessonVariant {
  label: "肯定" | "否定" | "疑问";
  en: string;
  zh: string;
  noteZh?: string;     // 这个形状是怎么变出来的
}

export interface LessonSceneSwing {
  sceneZh: string;     // 换一个生活场景的一句话设定
  en: string;
  zh: string;
}

export interface LessonDeepDive {
  title: string;       // 折叠卡的标题，用提问式（「想知道为什么？」）
  body: string[];      // 2-3 段大白话，可含术语但必须自含解释
}

// GrammarLesson 只增字段，不删不改任何现有字段
export interface GrammarLesson {
  // ……现有字段保持原样……
  dialogue?: LessonDialogueLine[];   // 多句对话；渲染层 fallback 到 dialogueEn/dialogueZh
  contrast?: LessonContrast[];
  variants?: LessonVariant[];
  sceneSwings?: LessonSceneSwing[];
  deepDive?: LessonDeepDive[];
  summary?: { rule: string; points: string[] };
}
```

**向后兼容说明**：
1. 所有新字段均为可选（`?`），`grammarLessons.ts` 现有 12 课数据不做任何删除或改名，旧数据可先行上线新代码（无新字段的课自动退回现在的单页形态）；
2. `huntAttempts`、`grammarLessonsDone` 等存储结构**完全不动**——进度、SM-2 复习队列、找错解锁关系全部保持；
3. 渲染层用 `lesson.dialogue ?? [单句兼容包]` 的方式兜底，保证内容补齐前页面不空；
4. 第②段新增的「找茬式引导」复用现有 `LessonGuidedStep`，新增 `kind: "spot"`（点词找茬），`options` 字段不用，新增 `tokens`（整句词表）+ `answerIndex`——判定引擎加一个分支即可。

---

## 6. 交互增量：讲解段如何承载更多内容而不变成「读课文」

**总原则：每一屏只有一个焦点，内容靠「步进 + 揭示」获得，不靠滚动阅读获得。**

| 机制 | 具体设计 | 防读课文的关键 |
| --- | --- | --- |
| **3 步分步翻页** | 第①段拆成「剧场 → 搭装+对错 → 变奏」3 步，底部「下一步」推进，可回退；步进器显示 1/3、2/3、3/3 | 每步信息量与现在单页相当，认知负担不升 |
| **正误对比揭示卡** | 先展示错句（问题词红色波浪线，或显示「缺了一块」空洞），一行引导「看出问题在哪了吗？」，点击「揭晓」后正确句浮现 + why | 学生先猜再看，符合 Noticing 原则——先注意差异，规则才记得住 |
| **变体 tab 切换** | 肯定/否定/疑问三个小 tab，切换不翻页；每个变体一句话 + noteZh | 变体是「发现」不是「灌输」 |
| **「想知道为什么？」折叠卡** | 默认收起，只显示提问式标题；点开展开 2–3 段深挖。展开状态本地记忆，下次默认保持 | 主线长度可控：不看深挖的初学者完全感知不到它的存在 |
| **迷你小结卡** | 完课页新增，规则 1 条 + 对比 1 组 + 变体 2 句，一屏 | 收尾的「确证感」来自一屏可见的收获清单 |

红线遵守：全程不出现「错误/答错/失败」字样——对比卡的错句统一表述为「有人是这样说的，你帮他看看」；引导文案用「看出问题在哪了吗」「揭晓」。

---

## 7. 难度分层：初学者不被吓退，想深入的有的学

| 层 | 内容 | 受众 | 设计手段 |
| --- | --- | --- | --- |
| **主线必学**（5–7 分钟） | 3 步讲解（含 1–2 组对比、变体展示）+ 3–4 引导题 + 3 练习题 + 小结卡 | 所有初学者 | 这是完整闭环，学完即有确证感；对比和变体只做「展示 + 轻练习」，不是新知识点 |
| **深挖可选**（1–3 分钟） | deepDive 折叠卡（辨析、规则背后的道理、the/a/an 的完整体系预告）、第④段侦探挑战、场景变奏第 3 条 | 想深入的 | 全部折叠或可选；无进度要求、无完成度暗示——不展开的人不会看到「还有 2 项没完成」这类制造焦虑的提示 |
| **外部钩子** | 深挖卡结尾预告「第 X 课会把这个讲完」（如第 1 课埋 am/is/are 全景、第 4 课埋 the、第 10 课埋 -ed 规则变化） | 所有人 | 螺旋式课程（评审 1.2 节）的知识索引，降低单课深挖的压力 |

---

## 8. 实施优先级

| 优先级 | 内容 | 工作量 |
| --- | --- | --- |
| **P0** | ① 数据结构增量（types.ts 新类型 + 渲染层 fallback）；② 第①段 3 步分步交互 + 正误对比揭示卡；③ 12 课内容补齐：dialogue 扩句、contrast×1–2、variants、summary（内容生产约 1–1.5 人日，12 课可模板化批量写）；④ 第②段新增 1 道变体/找茬引导题、第③段增至 3 题 | **3 人日**（交互 1.5 + 内容 1.5） |
| **P1** | ① deepDive 折叠卡（含辨析内容设计：am/is/are 全景、a/an/the、-ed 规则）；② sceneSwings 场景变奏（12 课×2–3 条）；③ 完课页小结卡 | **2.5 人日** |
| **P2** | ① 场景插画增强（剧场步的多句对话配图轮播）；② 小结卡收藏进错题本/学习档案；③ 找茬式引导题的干扰项优化（同源变形成对出现，呼应评审 2.2 节） | **1.5 人日** |

上线顺序建议：P0 的数据结构 + 交互先行（旧数据兼容），内容按课号 1→12 逐课补齐逐课上线，不必 12 课齐了再发。

---

## 9. Non-goals（明确不做）

| 不做 | 理由 |
| --- | --- |
| 不推翻四段式骨架、不新增第⑤段 | 用户已认可「讲解先行」，本次是加深不是重构 |
| 不把 12 课拆成 24 课、不新增课程模块 | 方案 B 已否；课程数翻倍加重心理负担 |
| 不增加题海、不做单元测试/总测验 | 与「课程化刷题」老路划清界限；确证感靠变体题型和小结卡，不靠题量 |
| 不引入语法术语体系教学 | 深挖层可出现术语但必须自含人话解释，主线零术语 |
| 词汇仍锁核心 500 词，超纲词注释 | 硬规范不变 |
| 不做限时/排名/体力值，全程无挫败性表述 | 红线不变 |
| 不动 MistakeBookPage.tsx | 用户并行开发中，与本次无关 |

---

## 10. 决策记录（2026-09-12，用户已拍板）

| # | 决策点 | 结论 |
| --- | --- | --- |
| D1 | 单课时长 | **接受 6–10 分钟**（方案 A：每课内部加深，不拆 24 课） |
| D2 | 深挖折叠卡 | **默认收起**（主线轻，想深入的自己点开） |
| D3 | 小剧场对话扩到 2–3 句 | **进 P0，一起做** |

实施顺序：先做基础设施（类型 + 讲解页 3 步分步 + 新题型渲染）并以第 1、2 课作为模板验收，再批量完成其余 10 课内容。
