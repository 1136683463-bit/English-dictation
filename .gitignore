node_modules/
dist/
target/
src-tauri/target/
.DS_Store
*.log
.workbuddy/
src-tauri/gen/

# 临时验证/过程产物（脚本输出与浏览器会话快照，勿提交）
.playwright-cli/
scripts/smoke-*.png
/gate-v1-*.png
/theme-art-preview-*.png
/*-preview*.png
/goal-visibility-*.png
/next-gate-*.png
/settle-with-*.png

# 同步备份（含个人学习数据，勿提交）
deliverables/sync/

# 本地产物（ZCode 会话计划与封面处理临时目录）
.zcode/
.covers-tmp/

# 外部审计进程的过程产物（无障碍/拖拽/键盘/阶段报告）
.ixfind/
.rvfind/
/design-verify.html

# 审计过程中的「一次性改仓探针」（改完即删，勿提交）
# 2026-09-24：`__probe.test.ts` 这类文件靠本机绝对路径 import，提交后会成为
# 「换台机器就红」的测试；本会话已删除 1 个（src/services/__probe.test.ts）。
src/**/__probe*.test.*
src/**/__audit*_probe.test.*
